# 04-2 부록. 용어 체계 구현 개발서 — 적재·검증·보완

작성일 2026-09-06. 설계 근거는 `04-2_다섯_언어의_지도와_적용방안.md` 에 있고,
이 문서는 **무엇을 어떻게 만들었고 지금 어디가 비어 있는지**를 적는다.
코드를 읽지 않는 사람도 알아볼 수 있게 쓴다.

---

## 1. 무엇을 푸는가

같은 물건을 다섯 군데서 다르게 부른다.

```
사고보고서   "LED등기구"                     담당자가 쓰는 말 (법정어 84%)
법정 고시    "조명기기 > 일반조명기구 > LED등기구"
KC안전기준   "KC 60598-2-1"
GPC 국제분류 "전구/튜브/발광 다이오드 (10000552)"
해외 리콜    "체인 조명" · "토끼 나무 기차"      일상어 (법정어와 6%만 일치)
```

말이 안 통하면 못 찾는다. 실제로 사고보고서 71건 중 13건이 적용 기준을 못 찾아
분석이 시작조차 되지 않았고, 그중 7건이 등기구 하나였다.

---

## 2. 자료를 어떻게 들여오나 (적재)

엑셀 세 종류를 넣었다. 성격이 다르므로 표도 따로 둔다.

| 원본 | 무엇 | 들어간 곳 | 명령 |
|---|---|---|---|
| 표준 제품분류체계 DB | 법정 품목 ↔ GPC 2,148행 | `product_taxonomy` | `npm run taxonomy:load` |
| 일일동향보고 검색용 데이터 | 담당자 검색어 1,039개 · 제외어 124개 | `item_keyword`, `item_keyword_stopword` | `npm run keywords:load` |
| 안전기준목록조사 취합 | 품목→기준 558행 (아직 미적재) | — | 향후 |

### 왜 원본을 그대로 담나

행을 합치거나 다듬지 않는다. 협회가 개정판을 주면 **통째로 다시 넣는 편**이 맞고,
그러려면 원본과 같은 모양이어야 어디가 달라졌는지 볼 수 있다.

다시 넣을 때도 **사람이 만든 것만 지운다.** AI 제안과 담당자의 검수 상태는 건드리지
않는다 — 협회 파일을 다시 넣는다고 사람의 판단이 사라지면 안 된다.

### 엑셀을 라이브러리 없이 읽는다

`src/lib/xlsx.ts` 가 zip 안의 XML 을 직접 푼다. 파일 몇 개를 읽자고 의존성을 늘리면
그것도 계속 관리해야 한다. 머리글 **이름**으로 열을 찾으므로 열 순서가 바뀌어도 견딘다.

---

## 3. 맞는지 어떻게 아나 (검증)

AI 가 만든 것을 그냥 믿지 않는다. **두 겹으로 잰다.**

```
1) 사람 사전을 정답지로 쓴다
   담당자가 「전선」에 붙인 검색어 6개를 AI 에게 보여 주지 않고 만들게 한 뒤
   몇 개를 맞히는지 센다.

2) 실물로 확인한다
   지어낸 검색어가 실제 해외 리콜 품목명 1,553종 중 몇 종을 새로 덮는지 센다.
   아무도 안 쓰는 말을 만들면 이 숫자가 안 오른다.
```

둘이 함께 좋아야 채택한다. 첫째만 좋으면 사람 흉내만 낸 것이고, 둘째만 좋으면
엉뚱한 말을 마구 붙인 것일 수 있다. 명령은 `npm run alias:eval` 이다.

### 채점이 설계를 고쳤다

처음 재현율이 **9.6%** 로 처참했다. 그런데 예시를 보니 AI 가 못한 것이 아니었다.

```
「망간 건전지」
  사람: 리튬전지 · 수은전지 · 버튼셀 · 알카라인배터리 · 배터리 …22개
  AI  : 망간전지 · 아연망간전지 · 망간 배터리 …7개
```

사람 사전은 사전적 별칭이 아니라 **뉴스를 훑기 위한 검색 그물**이었다(원본이
「일일동향보고 **검색용** 데이터」다). 같은 계열 이웃 품목까지 넉넉히 묶는다.
AI 는 문자 그대로 정확한 별칭을 만들었으니 겹칠 리가 없었다. **서로 다른 일을
하고 있었다.**

목적을 프롬프트에 적고, **사람이 만든 실제 예를 보여 주자**(few-shot) 이렇게 됐다.

```
고치기 전   재현율  9.6%   새 제안 97개 중 리콜에 걸림 10개
고친 뒤     재현율 26.9%   새 제안 236개 중 리콜에 걸림 21개
```

지시문을 아무리 다듬는 것보다 **실제 예를 보여 주는 것**이 효과가 컸다.

---

## 4. 어떻게 보태나 (AI 검색어)

`npm run alias:build -- --save` 로 사람 사전이 비워 둔 409종을 채웠다.

```
검색어 5,152개 생성 · 4,943개 저장
해외 리콜 커버리지  81종 → 245종 (3배)
```

사람 것과 섞지 않는다. `source='LLM'`, `review_status='auto_unreviewed'` 로 담기고
**담당자가 확정하기 전에는 품목 확정에 쓰이지 않는다.**

---

## 5. 잘못 붙은 것을 어떻게 찾나 (부분 일치 위험)

검색어가 틀리면 엉뚱한 품목으로 인식되고, 그러면 엉뚱한 기준의 시험이 근거로 제시된다.
조용히 틀리는 종류의 고장이다. 실제로 사람 사전에도 이런 줄이 있다.

```
"고압세척기" → 진공청소기
"베개"      → 1286        품목 코드가 이름 자리에 들어간 오류
"헤어드라이어" → 모발건조기(사람) vs 모발관리기(AI)   서로 다른 품목을 가리킨다
```

### 세 가지 위험을 잰다 (`src/lib/terms/keyword-check.ts`)

| 신호 | 뜻 | 실측 |
|---|---|---|
| **충돌** | 같은 말이 여러 품목에 붙음. 어느 쪽인지 정할 수 없다 | 600개 (1,365줄) |
| **과매칭** | 다른 검색어를 통째로 품음("세척기" ⊂ "고압세척기") | "어댑터"를 품는 검색어 16개 |
| **실사용** | 실제 사고·리콜 품목명에 걸리는가 | 382개만 걸린다 |

### 검수 순서를 자료가 정한다

검색어가 5,982개다. 담당자가 하나씩 볼 수 없다. 그런데 **전부가 똑같이 급하지 않다.**

```
실제로 걸리면서 충돌하는 것   지금 결과를 흔들고 있다   ← 맨 위
실제로 걸리는데 이상한 것
충돌하지만 아직 안 걸리는 것
아직 아무것도 안 걸리는 것 약 5,600개   틀려도 지금은 해가 없다
```

`/keywords` 「손볼 곳」 탭이 이 순서로 보여 준다. 담당자 시간이 이 체계에서 가장
비싼 자원이다.

### 검색에 쓸 때의 안전 조건 셋

`src/lib/cases/resolve-scope.ts` 의 검색어 경로는 아래를 모두 만족해야 쓴다.

```
1) 정확 일치만 쓴다        부분 일치는 안 쓴다. "세척기"가 식기세척기까지 끌어온다
2) 충돌하는 말은 안 쓴다    두 품목 이상에 붙었으면 고르지 않는다
3) 확정된 것만 쓴다        AI 제안은 검수를 통과해야 한다
+ 협회가 정한 제외어 124개를 뺀다
```

---

## 5-2. 법정 품목을 기준에 잇는다 (2026-09-06 추가)

검색어 사전이 "LED등기구"를 법정 품목까지 데려다줘도, 거기서 **기준으로 넘어가는
다리가 없었다.** 담당자가 *"기준에는 적용범위가 있고 제목도 있다, 그것으로 이어야
한다"* 고 짚어 주었다.

### 글자로는 안 된다

```
법정 품목명으로 적용범위 전문검색   187종 중 7종 (4%)
부분 문자열까지 허용해도          187종 중 11종

법정 품목  "모발건조기"                        고시의 말
적용범위   "피부 또는 모발을 손질하기 위한 전기기기"    기준의 말
```

### 뜻으로 잇는다

적용범위를 임베딩해 후보를 좁히고 LLM 이 고른다. 이 저장소가 이미 쓰던 방식인데
(`scope-semantic.ts`), 사건마다 런타임으로 돌던 것을 **법정 품목 목록에 미리 한 번**
돌려 표로 담는다. 한 번 확정하면 그 품목으로 이어지는 모든 사고·리콜에 재사용된다.

적용범위 임베딩은 한 번만 계산해 저장한다(048). 전에는 호출마다 73개를 다시 임베딩했다.

**적용범위가 비어 있으면 제목을 쓴다.** 등기구 3종(KC 60598-2-1·2-2·2-4)이 그렇다.
적용범위가 0자라 후보에조차 못 들어갔는데, 제목은 "제2-1부: 고정형 등기구-개별요구사항"
처럼 분명하다. 담당자가 "제목도요"라고 한 자리가 여기다.

### 정답지로 채점했다

부속서 33종 중 32종은 품목명으로 법정 품목과 **글자로 이미 이어져 있다.** 그것을
가리고 물어 맞히는지 잰다.

```
정답이 있는 30종 중
  맞힘     26종 (86.7%)
  틀림      0종 (0.0%)
  못 고름   4종 (13.3%)
```

**정확도 하나로 뭉뚱그리지 않는다.** 이 체계에서는 틀리는 것이 안 고르는 것보다 훨씬
나쁘다 — 그럴듯한 오답은 담당자가 검수해도 걸러내기 어렵다. 그래서 틀림과 못 고름을
나눠 적는다. 뭉뚱그리면 그 차이가 사라진다.

### "이을 기준이 없다"도 담는다

전기용품 187종 중 78종이 그랬다. 전에는 콘솔에 몇 줄 찍고 버렸는데, 그러면 담당자가
"왜 이 품목엔 기준이 없지?"를 확인할 수 없다.

담으면 두 가지를 할 수 있다.

1. 담당자가 그 자리에서 바로잡는다 — 「아니다, 기준이 있다」
2. 정말 기준이 없는 품목은 **「적재해야 할 기준」 목록**이 된다.
   모델이 *"자동판매기는 KC 60335-2-75 가 필요한데 목록에 없다"* 고 짚었다.

### 사슬이 넷이라 하나라도 미검수면 통과하지 못한다

```
검색어 "LED 등기구" → 조명기기 > 일반조명기구        item_keyword   (검수 필요)
법정품목표          조명기기 > 일반조명기구 > LED등기구  product_taxonomy
대응표             일반조명기구 → KC 60598-2-4        taxonomy_standard (검수 필요)
```

결함이 아니라 설계다. 품목이 틀리면 그 품목으로 이어지는 **모든** 사고·리콜에 엉뚱한
기준의 시험이 근거로 제시된다 — 검색어 하나가 틀린 것보다 파급이 크다.

둘 다 확정한 상태를 흉내 내어 재니 **사고 71건 중 58 → 63건**이 됐다(LED등기구 5건).

---

## 6. 안전장치 모음

| 무엇 | 어떻게 |
|---|---|
| 추정이 사실이 되지 않게 | 추정 원인은 `case_tag` 에 쓰지 않고 검색 입력에만 산다 |
| AI 가 사람을 덮지 않게 | `source` 로 가르고, 재적재는 `EXPERT` 만 지운다 |
| 조용히 바뀌지 않게 | CSV 가져오기는 **미리보기 → 적용** 두 단계. 파일에 없는 줄은 안 건드린다 |
| 확정 전에 안 쓰이게 | `review_status='approved'` 인 것만 품목 확정에 쓴다 |
| 되짚을 수 있게 | `evidence` 에 어느 경로로 왔는지 남긴다. 화면이 그대로 보여 준다 |
| 개인정보 | 원본 PDF 는 저장소에 두지 않는다(`*.pdf` gitignore). 추출 직후 탐지하고 외부 전송 전에 막는다 |
| 접근 제한 | 새 표 넷 모두 RLS 켜고 `anon`·`authenticated` 권한 회수 |
| 비용 | 채점은 `--no-llm`(LLM 행 제외) · `--rows`(견줄 행만) · `--limit`(표본) 로 조절 |

---

## 7. 화면에서 하는 일

```
/keywords   품목 검색어 사전 (일상어 → 법정 품목)
   손볼 곳    실제로 걸리면서 충돌·과매칭 위험이 있는 것부터
   충돌만     같은 말이 여러 품목에 붙은 것
   검수 대기   AI 제안
   담당자 사전 / 전체
   → 개별 확정·반려·삭제 · 품목 단위 일괄 확정 · 「AI로 검색어 더 만들기」
   → 직접 넣기 · CSV 내려받기 · CSV 올리기(미리보기 후 적용)

/terms      품목 용어 사전 (품목 → 적용기준)
/codebook   위해요인 코드 · 피해유형(DT)에 따른 위해원인(HF) 추정
/analysis   사건 분석 — 절 단위 묶음 · 원인 후보 · 두 번째 목록 · CSV
```

---

## 8. 연계도 — 사고보고서에서 시험항목까지

```
PDF 올리기
  → 텍스트 추출 · 개인정보 차단 · 사진 추출
  → 담당자가 원문 확인하고 「확정」          ← 여기를 지나야 분석된다
  → 위해요인·피해유형 코드 부여(LLM)
  → 품목 확정
       ① 용어 사전(품목→기준)
       ② 검색어 사전(일상어→법정품목→기준)   ← 이번에 결선
       ③ 품목명 일치 → ④ 적용범위 검색 → ⑤ 의미 검색
  → 분석
       넓게 200건 → 절 단위로 묶어 20건
       HyDE(가상 조항)로 의미 갈래 질의를 바꿈
       리랭킹(LLM)이 순서를 다듬음
       원인 미상이면 원인 후보를 따로 제시
  → 담당자가 채택·반려 → review_log
  → 3번째 산출물(사각지대·국내외 대조·시험항목)
```

---

## 9. 시스템 점검 결과 (2026-09-06 실측)

전체를 훑었다. **돌아가지만 사람이 지나가야 할 문 앞에 대부분 멈춰 있다.**

### 자료는 갖춰져 있다

```
안전기준   76종 · 조항 13,501 · 성능요건 7,472 · 태깅 6,400 · 임베딩 6,400
사고보고서  71건 (보류 0) · 임베딩 70
해외 리콜  2,299건 · 임베딩 2,299 · 국내확인 2,299
사전       용어 169 · 검색어 5,982 · 법정품목표 2,148 · GPC 5,318 · 원인다리 175
산출물 뷰  사각지대 215행 · 국내외대조 202행 · 시험항목 524행
```

### 보완이 필요한 곳

| # | 무엇 | 실측 | 왜 문제인가 |
|---|---|---|---|
| 1 | **사고보고서 확정이 5건뿐** | 71건 중 66건 미확정 | 확정 전에는 분석이 시작되지 않는다(`SOURCE_UNCONFIRMED`). 지금 분석된 사건이 9건뿐인 이유다 |
| 2 | **전기용품 기준 43종에 품목명이 없다** | 76종 중 43종 | 검색어 사전이 법정 품목까지는 가는데 **기준까지 못 간다**. 결선했지만 지금 기여는 리콜 99건·기준 2종뿐 |
| 3 | **AI 검색어 4,943개가 전부 미검수** | 확정 1,039 / 미검수 4,943 | 확정 전에는 안 쓰인다. 검수가 시작돼야 3배 커버리지가 실제 효과가 된다 |
| 4 | **담당자 판단이 0건** | `review_log` 0 | §5.8 재현율의 다른 축이자 「시험항목 사용 현황」의 재료다. 화면을 쓰기 시작해야 쌓인다 |
| 5 | **리콜 GPC 0건** | 2,299건 전부 | 법정품목표가 들어왔으므로 이제 붙일 수 있다. 사고·리콜을 같은 품목군으로 묶는 키다 |
| 6 | **리콜 수집 배치가 자주 죽는다** | 실패 62건(504·502) | 자료는 들어왔지만 증분 수집이 불안정하다 |
| 7 | **조항 태깅이 성능요건의 86%** | 7,472 중 6,400 | 나머지 1,072개는 코드로 검색되지 않는다 |
| 8 | 문서 미반영 | `docs/스키마.md`·README | 새 표 넷(`product_taxonomy`·`item_keyword`·`item_keyword_stopword`·`cause_bridge`) |

### 다음에 할 일 — 값어치 순

```
1. 전기용품 기준 43종의 품목명을 채운다      #2 를 풀면 #3 의 효과가 실제로 나온다
     법정품목표에 전기용품 법정 품목이 187종 있다. 기준 파일명·조항 제목과 맞춰 제안하고
     담당자가 확정한다(suggest-scope 와 같은 구조)

2. 검색어 「손볼 곳」부터 검수한다            실제로 걸리는 382개가 대상이다

3. 리콜에 GPC·법정품목을 붙인다              품목별 원인 추정과 사각지대 집계가 열린다

4. 사고보고서 확정을 진행한다                 담당자 작업. 66건이 대기 중이다

5. 문서·구현이력 갱신
```

---

## 10. 명령 모음

```
자료 넣기
  npm run taxonomy:load            법정 품목 ↔ GPC
  npm run keywords:load            담당자 검색어 · 제외어
  npm run cause:build              피해유형 → 위해원인 추정표
  npm run scopes:embed             적용범위 임베딩 (없으면 제목으로)

AI 로 보태기
  npm run alias:eval               검색어 생성 품질 채점 (사람 사전이 정답지)
  npm run alias:build -- --save    비어 있는 품목에 검색어 생성
  npm run link:eval                법정 품목 → 기준 채점 (이미 이어진 것이 정답지)
  npm run taxonomy:link -- --save  법정 품목을 기준에 잇기
  npm run scope:suggest -- --save  막힌 사건에 적용 기준 제안

재기
  npm run eval -- --k 5                          정확도 비교표
  npm run eval -- --no-llm                       LLM 행 빼고 (싸게)
  npm run eval -- --rows "…"                     견줄 행만
  npm run eval -- --limit 15                     표본 줄이기 (대표성 주의)
  npm run eval -- --all                          판정 끝난 변형까지

내보내기
  /api/keywords/export             검색어 사전
  /api/terms/export                용어 사전
  /api/codebook/cause-bridge/export 원인 추정표
  /api/analysis/[caseId]/export    분석 결과
```
