# 02. 1차 보완 및 구현 설계서

작성일: 2026-09-04
대상: `docs/코드베이스_성능_로직_개선계획_2026-09-04.md`(이하 「원 계획서」)의 개정
범위: 원 계획서가 지목한 항목을 코드로 재현 검증하고, 우선순위를 다시 매기고, P0의 구현 방법을 확정한다

---

## 0. 이 문서의 위치

원 계획서는 코드베이스를 읽고 위험 항목을 뽑아 정리한 문서다. 이 문서는 그 주장들을
**실제 파일에서 재현해 본 결과**이며, 재현 결과에 따라 우선순위를 다시 매긴 개정판이다.

원 계획서는 `docs/`에 그대로 둔다. 지우지 않는 이유는 두 가지다.

- 이 저장소는 설계문서 버전이 공존하는 것을 전제로 한다(CLAUDE.md §6). 어느 한쪽이
  맞다고 임의로 판단해 덮어쓰지 않고, 무엇이 달라졌는지를 남긴다.
- 원 계획서의 **관찰**은 대부분 정확했다. 달라진 것은 관찰이 아니라 **등급**이다.
  나중에 "그때 왜 등급을 바꿨는가"를 되짚으려면 양쪽이 다 있어야 한다.

이번 라운드에서는 **코드를 고치지 않았다.** 산출물은 이 문서와 `구현이력.md` 항목뿐이다.

---

## 1. 검증 결과 — 원 계획서의 주장과 실물 대조

### 1.1 사실로 확인된 것

| 원 계획서의 주장 | 확인한 위치 | 결과 |
|---|---|---|
| 태깅 병렬 요청이 고정 오프셋 0/40/80을 쓴다 | `supabase/migrations/027_tag_chunk_parallel.sql:116` | 사실 |
| 동기화 API가 토큰 미설정 시 통과시킨다 | `src/app/api/standards/sync/route.ts:25` | 사실 |
| 검색이 반려되지 않은 태그를 모두 쓴다 | `supabase/migrations/007_clause_hybrid_search.sql:103` | 사실 |
| `loading.tsx`가 없다 | `src/app` 전체에 0개. `Suspense` 사용처도 0개 | 사실 |
| 목록 행마다 상관 서브쿼리가 붙는다 | `src/app/accidents/page.tsx:120-129` — 행당 5개 | 사실 |
| 리콜 적재가 건별 트랜잭션이 아니다 | `src/lib/recall/load.ts` 103·145·164·171·177행이 각각 별도 호출 | 사실 |
| 원본 보관 실패 후에도 진행한다 | `src/app/accidents/actions.ts:56-59` — 실패 시 `storagePath = null` 후 계속 | 사실 |
| 3번째 산출물이 없다 | `insight` 문자열이 `src`·`supabase`·`codebook` 어디에도 없음 | 사실 |
| 환경설정에 범위 검증이 없다 | `src/lib/env.ts:58` — `Number.isFinite`만 검사, 음수·과대값 통과 | 사실 |
| 작업 URL의 `limit`·`offset`에 검증이 없다 | `src/app/api/jobs/[job]/route.ts` — `Number(x) \|\| 기본값`이라 음수가 그대로 통과 | 사실 |

### 1.2 심각도를 잘못 잡은 것

#### (1) 태깅 고정 오프셋은 "누락"이 아니다

원 계획서는 "일부 조항이 건너뛰어질 가능성"을 P0으로 올렸다. 건너뛰는 것은 맞다.
세 요청이 동시에 뜨지 않으므로, 앞 요청이 태그를 저장한 만큼 뒤 요청의 목록이 당겨지고
그 차이만큼의 조항이 이번 차례에서 빠진다.

그러나 **빠진 조항은 다음 차례에 회수된다.**

- 조회 순서가 `order by c.standard_id, c.order_index`로 고정이다.
- 대상 집합(아직 태그가 없는 조항)은 줄기만 한다.
- 따라서 이번에 밀린 조항은 앞의 것들이 처리될수록 목록 앞쪽으로 당겨지고,
  결국 `offset=0` 요청의 범위 안에 들어온다.
- 자동 종료 조건이 매번 새로 세는 `v_left = 0`이므로(`027:96-111`),
  미처리분을 남긴 채 작업이 꺼질 수 없다.

중복 처리로 돈이 두 배 나가는 일도 구조적으로 일어나지 않는다. 예산 12초에 동시 6건이면
한 요청이 실제로 처리하는 건수는 10건 안팎인데(`src/app/api/jobs/[job]/route.ts`의
`TAG_TIME_BUDGET_MS`, `TAG_CONCURRENCY`), 다음 구간은 40 떨어져 있다.

**판정: P0 → P2.** 실제 피해는 "일부 조항이 몇 차례 늦게 처리됨"이다.
원 계획서가 권고한 `FOR UPDATE SKIP LOCKED` 예약 장부는 이 피해에 비해 과한 처방이다
(CLAUDE.md §2 — 문제를 푸는 최소한의 코드).

#### (2) 태그 버전 중복의 영향은 서술보다 훨씬 작다

원 계획서는 "검색 점수와 검수 근거를 부풀린다"고 썼다. 점수 부분이 코드와 맞지 않는다.

`007:183`의 코드 가산점은 다음과 같이 **불리언 판정**이다.

```sql
+ case when coalesce(cr.hit_full, 0) > 0 then p_w_code else 0 end
```

태그가 한 개든 두 개든 가산폭은 `p_w_code = 0.5`로 같다. 중복이 영향을 주는 곳은
`code_ranked`의 등수뿐이고, 그 등수가 최종 점수에 기여하는 몫은 `1/(60+등수)`,
즉 최대 `1/61 ≈ 0.016`이다. **코드 가산점의 약 1/30이다.**

실질 위험은 점수가 아니라 **후보 탈락**이다. `code_ranked`는 상위
`greatest(p_match_count*3, 60)`건만 남기므로(`007:118`), 재태깅으로 `hit_full`이 부푼
조항이 정상 조항을 60위 밖으로 밀어낼 수 있다.

또한 자동 태깅 경로는 `not exists (select 1 from clause_tag ...)` 조건이라
**재태깅을 하지 않는다**(`tag-run.ts:99, 131`). `--retag`를 명시적으로 돌린 적이
없다면 현재 중복 태그가 0건일 수 있다.

**판정: P0 → P2.** 원 계획서의 "먼저 측정한다"는 지시는 옳다. 다만 측정하기 전에
P0을 붙인 것은 앞뒤가 맞지 않는다. 아래 4.3에 분포를 세는 조회문을 싣는다.

#### (3) 동기화 API에는 이중 방어가 있다

원 계획서는 "안전기준 전체 동기화라는 쓰기 작업이 공개될 수 있다"고 썼다.
`src/middleware.ts:13`을 빠뜨렸다.

```ts
if (request.nextUrl.pathname.startsWith('/api/jobs/')) {
  return NextResponse.next();
}
```

미들웨어는 `/api/jobs/`만 통과시키고 **나머지 전 경로에 Basic 인증을 건다.**
따라서 `/api/standards/sync`가 실제로 열리는 것은 `STANDARDS_SYNC_TOKEN`과
`SITE_AUTH_PASSWORD`가 **둘 다** 비어 있을 때뿐이다.

덧붙여, 이 라우트는 `maxDuration = 300`으로 선언돼 있으나 Netlify의 HTTP 프록시는
약 30초에 끊는다(`028` 주석의 실측). 전체 동기화가 완주할 수 없는 라우트다.

**판정: P0 → P2, 그리고 수정 방향을 바꾼다.** 환경변수 분기를 더하는 것보다
**라우트를 지우는 쪽이 간단하다.** 자세한 근거는 4.2에 적는다.

#### (4) 진짜 P0이 곁가지로 밀려 있다

원 계획서는 태깅 실패 처리를 예약 방식 제안 안의 한 줄
("처리 실패 시 재시도할 수 있는 상태를 함께 남긴다")로만 다뤘다. 이것이 그 절의 본문이
되어야 한다. 자세한 것은 2장과 3.1에 적는다.

### 1.3 원 계획서에 없던 관찰

`007`의 `code_ranked`와 `keyword_ranked`는 `row_number() over (order by ...)`를 매긴 뒤
바깥 `ORDER BY` 없이 `limit`을 건다(`007:118`, `007:149`). 이 형태는 상위 60건이
반환된다는 것을 SQL 표준이 보장하지 않는다. 세 번째 갈래인 `vector_ranked`에는
명시적 `order by`가 있어서(`007:166` 직전) 이 문제가 없다.

실무상 PostgreSQL의 WindowAgg는 창 함수 정렬 순서대로 행을 내보내므로 지금 오동작하고
있을 가능성은 낮다. 다만 검색 정확성을 다루는 문서라면 짚어야 할 지점이다. **P2.**

---

## 2. 재조정한 우선순위

원 계획서는 P0이 8개였고, 그중 5개가 3번째 산출물 하나에 붙어 있었다. 전부 P0이면
우선순위가 없는 것과 같다. 여기서는 **P0을 3개로 제한한다.**

| 등급 | 항목 | 이 등급인 이유 |
|---|---|---|
| **P0-1** | 태깅 실패 조항 정체 | 같은 조항에 무한히 과금하고, 작업이 영원히 끝나지 않는다 |
| **P0-2** | 미검수 태그가 정식 코드 근거로 쓰임 | 설계 약속(검수 확정분만 근거)과 실제 동작이 다르다 |
| **P0-3** | 원본 보관 실패 후에도 진행 | 원본과 파생 결과의 감사 추적이 끊긴다 |
| P1 | 메인 화면 초기 로딩 | 담당자 체감이 나쁘지만 결과의 정확성은 바뀌지 않는다 |
| P1 | 리콜 적재 건별 트랜잭션 | 고아 데이터가 남을 수 있으나 재수집으로 회복 가능하다 |
| P1 | 사고보고서 처리 상태 분리 표시 | 오해를 줄이는 문제이지 데이터가 틀리는 문제가 아니다 |
| P1 | 리랭킹 provenance 매니페스트 | 재현성 확보. 지금도 결과는 저장되고 있다 |
| P1 | 안전기준 화면 집계 기준 통일 | 개정판이 실제로 추가되기 전에는 드러나지 않는다 |
| P1 | 입력값·환경설정 범위 검증 | 잘못 넣으면 티가 나지만 조용히 틀리지는 않는다 |
| P2 | 태깅 오프셋 정밀화 | 자기회복된다(1.2 (1)) |
| P2 | 태그 버전 단일화 | 영향이 작고 발생 건수가 0일 수 있다(1.2 (2)) |
| P2 | 동기화 라우트 정리 | 미들웨어가 막고 있다(1.2 (3)) |
| P2 | 리콜 증분 수집 | 현재 순환 42시간이 업무상 문제로 보고된 적이 없다 |
| P2 | 임베딩 차원 계약 단일화 | 아직 바꾼 적이 없다 |
| P2 | `limit` 창 함수 정렬 보장 | 현재 오동작 가능성이 낮다(1.3) |
| 분리 | 3번째 산출물(KC안전기준 개선 요인) | 개선이 아니라 신규 기능이다. 7장 참조 |

---

## 3. P0 구현 상세

### 3.1 P0-1 — 태깅 실패 조항이 큐 머리를 막는다

**현상**

태깅에 실패한 조항은 `clause_tag`에 아무것도 남기지 않는다(`tag-run.ts:220`, `283`
— 실패 시 `result.fail++` 후 그대로 넘어간다). 대상 조회 조건이
`not exists (select 1 from clause_tag ...)`이므로, 실패한 조항은 **다음 차례에도
대상 목록 맨 앞에 그대로 있다.**

그 결과 세 가지가 동시에 일어난다.

1. `offset=0` 요청이 매 차례 같은 실패 조항부터 다시 시도한다. 실패가 10건만 쌓여도
   세 갈래 중 한 갈래는 종일 같은 조항만 붙들고 AI를 호출한다.
2. `countTaggable()`이 0으로 내려가지 않는다(`tag-run.ts:85`).
3. `run_tag_chunk()`의 `v_left`도 0이 되지 않으므로(`027:96-111`),
   자동 실행이 스스로 꺼지지 않고 완료 알림도 오지 않는다. 1분마다 계속 돈다.

이것이 원 계획서가 "누락"으로 지목한 것보다 훨씬 비싼 문제다. 누락은 다음 차례에
회수되지만, 이쪽은 **회수되지 않고 비용만 쌓인다.**

**수정 방향**

실패를 조항에 기록해서 대상에서 빼는 것이 가장 단순하다. 예약 장부를 새로 만들 필요는 없다.

- 새 마이그레이션 `supabase/migrations/029_tag_failure_state.sql`
  (CLAUDE.md §8 — 이미 적용된 파일을 고치지 않고 새 번호로 쌓는다)

  `clause`에 세 컬럼을 더한다.

  | 컬럼 | 뜻 |
  |---|---|
  | `tag_fail_count int not null default 0` | 연속 실패 횟수 |
  | `tag_last_error text` | 마지막 실패 사유. 담당자가 원인을 본다 |
  | `tag_failed_at timestamptz` | 마지막 실패 시각 |

- `src/lib/standards/tag-run.ts`
  - `processOne`의 `catch`와 코드 검증 거부 경로에서 실패 횟수를 1 올리고 사유를 남긴다.
  - 성공 시 `tag_fail_count = 0`으로 되돌린다(일시적 오류였다면 이력이 남지 않게 한다).
  - 대상 조회에 `and c.tag_fail_count < 3`을 더한다. `countTaggable()`과
    `runTagging()` 양쪽에 같은 조건을 넣는다 — 두 곳의 정의가 어긋나면 화면 숫자와
    실제 처리 대상이 달라진다.
- `029`에서 `run_tag_chunk()`의 `v_left` 계산에도 같은 조건을 반영한다.
  그래야 자동 종료가 실제로 동작한다.
- 운영 화면(`src/app/ops/page.tsx`)에 "재시도 한도 초과" 건수를 별도로 표시한다.
  **0건으로 줄어든 것과 포기한 것을 같은 숫자로 보여 주면 안 된다.**
- 재시도 한도를 넘긴 조항을 담당자가 다시 열 수 있는 경로를 둔다
  (`tag_fail_count = 0`으로 되돌리는 서버 액션 하나).

**왜 한도가 3인가**

태깅은 조항당 AI를 3~4회 부른다. 일시적인 요청 한도 초과라면 다음 차례에 성공하고,
세 번 연속 실패하는 것은 조항 자체나 프롬프트의 문제일 가능성이 높다. 사람이 봐야 한다.
값은 상수로 두되 근거를 주석에 남긴다.

**검증**

- 강제로 실패하는 조항 5건을 만든다(예: 본문에 코드북에 없는 코드를 유도하는 내용).
- 3차례 실행 후 그 5건이 `countTaggable()` 대상에서 빠지는지 확인한다.
- 나머지 조항이 모두 처리된 뒤 `v_left`가 0에 도달해 자동 실행이 꺼지고
  완료 알림이 오는지 확인한다.
- 실패 5건이 운영 화면에 "재시도 한도 초과 5건"으로 따로 표시되는지 확인한다.
- 한 건을 되돌린 뒤 다음 차례에 다시 시도되는지 확인한다.

**완료 기준**

- 실패 조항이 있어도 자동 실행이 스스로 종료된다.
- 같은 조항에 대한 AI 호출이 3회를 넘지 않는다.
- 화면에서 "처리 완료"와 "포기"가 구분된다.

### 3.2 P0-2 — 미검수 태그가 정식 코드 근거로 쓰인다

**현상**

`007:103`은 반려되지 않은 태그를 모두 코드 매칭에 쓴다.

```sql
and t.review_status <> 'rejected'
```

`case_tag`·`clause_tag`의 기본값은 `auto_unreviewed`다(`004:108-109`). 즉 사람이
한 번도 보지 않은 자동 태그가 `match_path = 'CODE'`로 표시되어, 검수를 거친 태그와
같은 자격으로 담당자에게 제시된다. 설계문서가 약속한 "검수 확정 태깅을 정식 근거로"와
어긋난다.

**수정 방향**

`007`을 고치지 않고 새 마이그레이션으로 인자를 하나 더한다(CLAUDE.md §6 — 실측으로
비교할 수 있는 구조를 만들고 데이터로 판정한다).

- `supabase/migrations/030_search_approved_tags_switch.sql`
  - `clause_hybrid_search`에 `p_require_approved_tags boolean default false`를 더한다.
    **기본값을 `false`로 두어 기존 동작을 바꾸지 않는다.** 스위치를 켰을 때만
    `t.review_status = 'approved'`로 좁힌다.
  - 기존 함수와 시그니처가 달라지므로, 인자를 늘린 새 함수를 만들고 옛 시그니처는
    `drop function`으로 정리한다. 호출부는 `src/lib/search/`의 한 곳뿐이므로
    같이 고친다.
  - 함수 생성 후 `anon`·`authenticated` 실행 권한을 `revoke`한다
    (CLAUDE.md §8 — 자동으로 열리는 것을 되돌린다. 실제로 놓친 적이 있다).
- `match_run`에 `require_approved_tags boolean not null default false`를 더해
  **어떤 설정으로 돌린 결과인지 남긴다.** 갈래 스위치를 남기는 기존 관례와 같다.

**먼저 재야 할 것**

스위치를 만들기 전에 현재 코드 근거의 몇 %가 미검수인지 센다. 이 숫자가 곧 이 항목의
실제 크기다.

```sql
-- 조항 태그의 검수 상태 분포
select review_status, count(*) as n,
       round(100.0 * count(*) / sum(count(*)) over (), 1) as pct
from public.clause_tag
group by review_status
order by n desc;

-- 코드 근거로 제시된 결과 중 미검수 태그에만 기댄 비율
select
  count(*) filter (where mr.match_path in ('CODE', 'CODE-PARTIAL')) as code_results,
  count(*) filter (
    where mr.match_path in ('CODE', 'CODE-PARTIAL')
      and not exists (
        select 1 from public.clause_tag t
        where t.clause_id = mr.clause_id and t.review_status = 'approved')
  ) as unreviewed_only
from public.match_result mr;
```

**`match_path`에 상태를 더할지는 실측 후 결정한다**

`CODE-UNREVIEWED`를 새 값으로 추가하는 안이 있다. 다만 `match_result.match_path`에는
체크 제약이 걸려 있고(`005`), 화면·집계가 이 값을 읽는다. 위 조회에서 미검수 비율이
낮게 나오면 별도 표시만으로 충분할 수 있다. **숫자를 보고 정한다.**

**검증**

- 미검수 태그만 있는 조항을 만들고, 스위치를 켰을 때 코드 갈래에서 빠지는지 확인한다.
- 스위치를 껐을 때 기존 결과와 순위·건수가 동일한지 확인한다.
- 같은 사건을 두 설정으로 각각 돌리고 `match_run`에 설정값이 남는지 확인한다.

**완료 기준**

- 담당자가 결과를 볼 때 그 근거가 검수를 거친 것인지 아닌지 알 수 있다.
- 기존 분석 결과가 이 변경으로 달라지지 않는다(기본값 유지).

### 3.3 P0-3 — 원본 보관에 실패해도 진행한다

**현상**

`src/app/accidents/actions.ts:54-59`는 Storage 업로드가 실패하면 경로를 `null`로 두고
그대로 진행한다. 그 뒤 `source_file`이 만들어지고 텍스트 추출과 사건 생성까지 이어진다.
화면에는 정상 처리된 것으로 보이지만 **원본 PDF가 어디에도 없다.**

사고조사보고서는 개인정보 때문에 저장소에 두지 않는다(`api/jobs/[job]/route.ts` 주석).
즉 Storage가 유일한 원본 보관처다. 여기서 실패하면 조항 결과에서 원본 페이지로
역추적할 방법이 사라진다.

**수정 방향**

- `src/app/accidents/actions.ts`
  - `putOriginal` 실패 시 진행하지 않는다. `source_file`을 `status = 'error'`,
    `error_reason = '원본 보관 실패: …'`로 남기고 **사건을 만들지 않는다.**
  - 이렇게 하면 화면의 오류 목록에 잡히고, 담당자가 같은 파일을 다시 올리면
    `unique (kind, sha256)` 덕분에 중복이 생기지 않는다.
- 미리보기와 감사용 원문을 구분해 문서화한다. 화면은
  `left(f.extracted_text, 420)`만 읽고(`accidents/page.tsx:117`), 전체 원문은
  `source_file.extracted_text`에 있다. 두 값의 용도가 다르다는 것을 화면 문구에 남긴다.

**안전기준 JSON은 이번에 바꾸지 않는다**

원 계획서는 안전기준 JSON도 Storage에 보관하고 `source_sha256`과 1:1로 묶자고 했다.
사고보고서와 사정이 다르다 — 안전기준 JSON은 `KC안전기준/` 폴더째 저장소에 있고
git 이력으로 되짚을 수 있다. 사고보고서처럼 원본이 사라지는 구조가 아니다.

**따라서 이번 범위에서 뺀다.** 다만 협회가 폴더 밖에서 JSON을 갱신하는 운영이 생기면
그때 다시 본다. 판단 근거를 여기 남겨 둔다.

**검증**

- Storage 자격증명을 일부러 틀리게 두고 PDF를 올린다.
- 사건이 생성되지 않고, 화면에 실패 사유가 표시되는지 확인한다.
- 자격증명을 되돌리고 같은 파일을 다시 올려 정상 처리되는지, 중복 사건이 생기지 않는지
  확인한다.

**완료 기준**

- 원본이 없는 사건이 새로 생기지 않는다.
- 실패한 파일이 화면에서 "처리 대기"가 아니라 "실패"로 보인다.

---

## 4. P1·P2 구현 방향

P0보다 얕게 적는다. 각 항목의 수정 대상과 판단 기준만 둔다.

### 4.1 P1 — 메인 화면 초기 로딩

대상: `src/app/accidents/page.tsx`, `src/app/recalls/page.tsx`, `src/app/ops/page.tsx`

- **먼저 잰다.** 페이지별 TTFB, 세 쿼리(요약·전체건수·목록) 각각의 소요시간,
  반환 행 수를 기록한다. 숫자 없이 손대지 않는다.
- 요약 쿼리는 스칼라 서브쿼리 8개를 한 문장에 담아 왕복이 1회다
  (`accidents/page.tsx:80-97`). 왕복 수보다 **각 서브쿼리의 실행계획**이 문제일 수 있다.
- 세 쿼리가 순차 `await`이지만 서로 의존하지 않는다. `Promise.all`로 묶는 것은
  한 줄 변경이므로 측정 전에 해도 된다.
- `loading.tsx`를 추가하고, 요약과 목록을 별도 서버 컴포넌트로 나눠 Suspense 경계를 둔다.
- `AutoRefresh`(`src/components/AutoRefresh.tsx`)는 15초마다 `router.refresh()`로
  **페이지 전체를 다시 조회한다.** 진행 중인 화면에서는 이 비용이 반복된다.
  숫자만 갱신하는 작은 경로를 검토한다.

**측정 목표(숫자로 둔다)**

- 제목·업무 흐름 표시: 200ms 이내
- 목록 표시: 현재값 대비 개선폭을 `구현이력.md`에 기록. 목표값은 첫 측정 후 정한다.

### 4.2 P2 — 동기화 라우트는 고치지 말고 지운다

원 계획서는 "운영 환경에서 토큰이 없으면 500으로 중단"을 제안했다. 그보다 간단한 길이 있다.

- 이 라우트는 아무도 부르지 않는다. 파일 첫 주석이 그렇게 적고 있다
  ("지금은 아무도 이 라우트를 자동으로 호출하지 않는다").
- `maxDuration = 300`이지만 Netlify는 약 30초에 끊으므로 전체 동기화가 완주할 수 없다.
- 실제 자동 실행은 이미 `/api/jobs/standards-sync`가 맡고 있고, 그쪽은
  토큰이 없으면 **아무도 못 부르게 막는다**(`authorized()`가 `false`를 돌려준다).
- 미들웨어가 이 경로에 Basic 인증을 걸고 있다.

즉 이 라우트는 동작하지도 않고, 필요하지도 않고, 지금 열려 있지도 않다.
**환경변수 분기를 더하는 것은 죽은 코드에 방어를 붙이는 일이다.** 삭제를 권고한다.

나중에 관리 화면 버튼이 필요해지면 그때 `/api/jobs/` 규약에 맞춰 새로 만든다.

다만 삭제 여부는 담당자 확인이 필요하다 — 협회 측에서 이 경로를 알고 있을 수 있다.
**확인 전까지는 지우지 않는다.**

### 4.3 P2 — 태그 버전 중복은 세어 보고 정한다

삭제나 변경 전에 분포부터 센다.

```sql
-- 한 조항에 몇 가지 태깅 판이 붙어 있는가
select versions, count(*) as clauses
from (
  select clause_id, count(distinct tagging_version) as versions
  from public.clause_tag
  group by clause_id
) t
group by versions
order by versions;
```

`versions = 1`이 전부라면 이 항목은 닫는다. 2 이상이 나오면 그때
"현재 검색에 쓸 판" 개념을 도입한다.

### 4.4 P2 — 태깅 오프셋

P0-1을 적용하면 남는 문제는 "일부 조항이 몇 차례 늦게 처리됨"뿐이다.
예약 장부는 만들지 않는다. 실제로 처리 속도가 문제가 되면 그때
`clause.id > last_id` 커서로 바꾼다 — 예약 장부보다 훨씬 작은 변경이다.

### 4.5 그 밖의 P1·P2

| 항목 | 대상 | 판단 기준 |
|---|---|---|
| 리콜 건별 트랜잭션 | `src/lib/recall/load.ts` 103~177행 구간을 `db.begin`으로 묶는다. 252행은 이미 트랜잭션이다 | 단계마다 강제 오류를 넣어 고아 캐시가 남지 않는 것 |
| 사고보고서 상태 분리 | `src/lib/terms.ts`의 `FILE_STATUS_LABEL`과 화면 배지 | 추출·원문확인·코드화·검색준비·분석가능이 각각 다른 표시가 되는 것 |
| 리랭킹 provenance | `match_run`에 이미 갈래 스위치·가중치·모델이 있다. 빠진 것은 질의 원문과 프롬프트 판번호 | 같은 분석을 다시 열었을 때 저장된 결과가 그대로인 것 |
| 안전기준 집계 통일 | `src/app/standards/page.tsx` 상단 집계에 `s.is_current` 조인 조건 | 같은 표시명의 개정판을 넣고 상단 숫자와 목록 합계가 일치하는 것 |
| 입력값 범위 검증 | `src/lib/env.ts:55-59`에 최소·최대 인자를 더한다. `api/jobs/[job]/route.ts`의 `limit`·`offset`을 정수·비음수로 제한한다 | 잘못된 값이 조용히 기본값으로 바뀌지 않고 로그와 응답에 남는 것 |
| 임베딩 차원 계약 | `src/lib/env.ts`의 `embeddingDim`을 상수로 고정 | 잘못된 차원 설정이 시작 단계에서 즉시 실패하는 것 |
| `limit` 창 함수 정렬 | `007`의 `code_ranked`·`keyword_ranked`에 바깥 `order by rnk`를 더한다 | 순위 결과가 변하지 않는 것(현재 동작이 옳다는 전제의 확인) |

---

## 5. 실행 순서

2장의 우선순위와 일치시킨다. 원 계획서는 2장 등급과 3장 순서가 어긋나 있었고
(P0 메인화면이 2단계, P1 안전기준 집계가 1단계), P1 두 항목은 어느 단계에도 없었다.
그 불일치를 반복하지 않는다.

### 1단계 — P0 세 항목

1. 태깅 실패 조항 정체 해소 (3.1)
2. 미검수 태그 비율 실측 → 스위치 도입 (3.2)
3. 원본 보관 실패 시 중단 (3.3)

**완료 기준**
- 실패 조항이 있어도 자동 태깅이 스스로 종료된다
- 같은 조항에 대한 AI 호출이 3회를 넘지 않는다
- 미검수 태그 비율을 숫자로 안다
- 원본 없는 사건이 새로 생기지 않는다

### 2단계 — 담당자 체감과 실패 복구

1. 메인 화면 로딩 측정 → `loading.tsx`·Suspense (4.1)
2. 리콜 건별 트랜잭션
3. 사고보고서 처리 상태 분리 표시
4. 리랭킹 provenance 보완

**완료 기준**
- 제목 표시 200ms 이내
- 중간 실패 후 고아 데이터 0건
- 화면에서 "아직 안 함"과 "실패"가 구분된다

### 3단계 — 계약과 관측성

1. 입력값·환경설정 범위 검증
2. 임베딩 차원 계약 단일화
3. 안전기준 집계 기준 통일
4. 태그 버전 분포 실측 (4.3) → 필요하면 판 개념 도입
5. 동기화 라우트 삭제 여부 담당자 확인 (4.2)

**완료 기준**
- 잘못된 설정이 시작 단계에서 발견된다
- 운영 화면에서 실패 원인과 남은 작업을 구분해 확인할 수 있다

---

## 6. 공통 검증

코드 변경마다 다음 순서로 확인한다(원 계획서와 동일).

```powershell
$env:NODE_OPTIONS=''
npm run typecheck
$env:NODE_OPTIONS=''
npx next build
git diff --check
```

데이터 변경은 되돌릴 수 있는 샘플 행으로 먼저 검증한다. 운영 DB 적용 전 확인 사항:

- 태깅 대상·완료·실패·포기 건수의 합이 맞는지
- 현재 기준과 과거 기준의 집계가 섞이지 않는지
- 리콜 `external_ref` 중복이 없는지
- 임베딩 모델과 차원이 기존 자료와 일치하는지
- 자동 작업의 1회 실행시간과 최근 실패 응답

---

## 7. 3번째 산출물은 별도 문서로 분리한다

원 계획서 147~380행은 `KC안전기준 개선 요인` 도출 기능을 다룬다. 분량이 2장의 절반을
넘고 P0이 다섯 개 붙어 있다.

**이 문서에서는 다루지 않는다.** 이유는 두 가지다.

- 이것은 코드베이스 개선이 아니라 **신규 기능 설계**다. 성격이 다른 둘을 한 문서에
  담으면 양쪽 다 실행하기 어려워진다.
- 담당 부서·검토 절차와의 합의가 선행 조건인 항목이 섞여 있다(원 계획서의 안 3).
  코드 수정과 같은 속도로 갈 수 없다.

후속 문서(`03_…`)에서 다룰 범위만 남긴다. 원 계획서의 안 1 → 안 2 → 안 3 단계적 적용
권고와 공통 집계 규칙은 타당하므로 그대로 승계한다.

**원자료가 이미 준비돼 있다는 것은 확인했다.** 새 데이터 수집 없이 집계를 시작할 수 있다.

| 필요한 재료 | 있는 곳 | 비고 |
|---|---|---|
| 사건의 HF/DT 코드 | `case_tag` (`axis`, `code`, `review_status`) | 검수 상태로 걸러낼 수 있다 |
| 대응 조항 없음 | `match_run.result_count` | 0건 run도 보존한다고 `005`에 명시 |
| 어떤 코드로 찾았는데 없었는가 | `match_run.queried_hf_codes`, `queried_dt_codes` | 사각지대 집계의 재료라고 주석에 적혀 있다 |
| 담당자 판단 | `review_log.decision`, `reject_reason` | 반려 사유가 선택지라 집계 가능 |
| 탈락 후보 | `match_result.is_shortlisted` | 탈락분도 보존한다 |
| 시험방법 연결 | `clause_link` (`link_type = 'TEST_METHOD'`) | `to_clause_id`가 `null`이면 미해결 참조 |
| 해외 표준 번호 | `recall_cache.cited_standards` (`text[]`, GIN 색인) | |
| 품목 범위 | `case_event.product_scope_id` | 미확정은 `null` |

**없는 것**: `insight_run`·`standard_gap_insight`·`standard_level_comparison`·
`test_usage_insight` 테이블, `/insights` 화면, 집계 RPC. 저장소 전체에 `insight`라는
문자열이 하나도 없다.

---

## 8. 이번 검토에서 확인하지 않은 것

정직하게 남긴다. 아래는 **파일을 열어 보지 않았거나, 열었더라도 동작을 재현하지 않은**
영역이다.

- 안전기준 파서 본체(`src/lib/standards/` 중 파싱 부분)의 정확성
- 사고보고서 PDF 추출과 개인정보 탐지(`src/lib/cases/extract-pdf.ts`)의 동작
- HF·DT 태깅 프롬프트와 코드북 내용의 품질
- 임베딩 모델과 검색 알고리즘의 품질 판단(재현율·오탐률)
- 리랭커 응답의 타당성
- UI 문구·레이아웃

원 계획서의 P0 "원문·파싱·시험 연결의 감사 추적성 보강" 중 표 파싱 실패와 동일 marker
중복 부분은 파서를 읽어야 판단할 수 있다. 이 문서에서는 원본 보관 실패(3.3)만
떼어 P0에 두었고, 나머지는 파서 검토 후 별도로 판단한다.

---

## 9. 원 계획서와 달라진 점 요약

| 항목 | 원 계획서 | 이 문서 | 근거 |
|---|---|---|---|
| P0 개수 | 8개 | 3개 | 전부 P0이면 우선순위가 없다 |
| 태깅 오프셋 | P0, 예약 장부 도입 | P2, 보류 | 자기회복된다 (1.2 (1)) |
| 태깅 실패 처리 | 한 줄 언급 | **P0-1** | 무한 과금 + 종료 불가 (3.1) |
| 태그 버전 중복 | P0, "점수를 부풀린다" | P2, 먼저 센다 | 가산점이 불리언이라 영향이 1/30 (1.2 (2)) |
| 동기화 API | P0, 500 중단 | P2, 라우트 삭제 권고 | 미들웨어가 막고 있고 동작하지도 않는다 (1.2 (3), 4.2) |
| 안전기준 JSON 보관 | P0 범위에 포함 | 이번 범위에서 제외 | 저장소와 git 이력에 있다 (3.3) |
| 3번째 산출물 | P0 다섯 항목 | 별도 문서로 분리 | 개선이 아니라 신규 기능이다 (7장) |
| 측정 목표 | 숫자 없음 | 숫자를 넣는다 | 이 저장소는 실측으로 판단해 왔다 (CLAUDE.md §5) |
| 우선순위 ↔ 실행순서 | 어긋남 | 일치 | 원 계획서 2장과 3장의 불일치 (5장) |
| 창 함수 `limit` | 언급 없음 | P2로 추가 | 상위 N건 반환이 보장되지 않는다 (1.3) |

---

## 10. 구현 결과와 실측 판정 (2026-09-04, 구현이력 라운드 33)

이 문서를 쓴 뒤 P0 세 항목과 P1 대부분을 구현하고, 미뤄 두었던 판정을 DB에서 직접
세어 끝냈다. 상세는 `구현이력.md` 라운드 33에 있다.

### 10.1 실측으로 닫힌 항목

| 잰 것 | 결과 | 판정 |
|---|---|---|
| 조항별 태깅 판 개수 | 730개 조항 전부 1판. 2판 이상 0건 | **태그 버전 중복은 존재하지 않는다.** 4.3 항목을 닫는다 |
| 조항 태그 검수 분포 | `auto_unreviewed` 1,855건(100%), `approved` 0건 | 검수 확정 태그가 하나도 없다 |
| 코드 근거의 미검수 비율 | 분석 결과 160건 전부가 미검수 태그만으로 `CODE` 등급 | 스위치를 켜면 코드 근거가 전부 사라진다 → 기본값 끄로 확정 |
| 남은 태깅 대상 | 5,670건 | |
| 태깅 실패 조항 | 0건 | 아직 쌓이지 않았다. 지금 막는 것이 맞다 |
| `review_log` | **0건** | 7장의 전제가 흔들린다 — 아래 참조 |

### 10.2 7장(3번째 산출물)에 대한 정정

7장은 "원자료가 이미 준비돼 있으므로 새 데이터 수집 없이 집계를 시작할 수 있다"고
적었다. **틀렸다.** `review_log`가 0건이다.

`match_run`·`match_result`·`case_tag`·`clause_link`·`cited_standards`는 실제로 차 있지만,
세 산출물 중 두 개(기준 사각지대의 "후보는 있으나 모두 반려", 저활용 시험항목의
"채택·반려 횟수")는 담당자의 검토 기록을 재료로 삼는다. 그 재료가 비어 있으면 집계
화면은 만들어도 빈 표만 나온다.

따라서 후속 문서의 첫 과제는 `/insights` 화면이 아니라 **검토 기록이 실제로 쌓이는지
확인하는 것**이다. 담당자가 채택·반려를 누를 수 있는 경로가 있는지, 눌렀을 때 실제로
`review_log`에 남는지, 안 쌓이고 있다면 왜인지부터 봐야 한다.

### 10.3 구현하며 새로 찾은 것

원 계획서에도 이 문서에도 없던 항목이다.

- **리콜 임베딩이 주기마다 통째로 버려지고 있었다.** `load.ts`가 검색용 문장을 갱신할
  때 `embedding = null`을 무조건 실행했다. 리콜 수집은 약 42시간 주기로 전체 2,252건을
  다시 훑으므로, 바뀐 것이 없어도 모든 임베딩이 주기마다 날아가고 다시 만들어졌다.
  검색용 문장이 실제로 달라졌을 때만 버리도록 고쳤다.
- **원본이 갱신돼도 사건 서술이 따라가지 않았다.** `case_event` 삽입이
  `on conflict do nothing`이었다. 이제 원본에서 온 필드는 갱신하되
  `is_confirmed`·`product_scope_id`는 보존한다.

### 10.4 남은 항목 (2026-09-04 갱신 — 라운드 37 이후)

**닫은 항목**

| 항목 | 판정 |
|---|---|
| 태깅 예약 장부 (4.4) | 만들지 않는다. P0-1 로 실제 피해가 사라졌다 |
| 태그 버전 단일화 (4.3) | 실측 중복 0건. 문제가 존재하지 않는다 |
| 목록 행별 서브쿼리 정리 (4.1) | **실측으로 닫는다.** 25행에 서브쿼리 5개씩 붙여도 13ms 다. 고칠 것이 없다 |
| `/api/standards/sync` (4.2) | 삭제 대신 **잠갔다**. 토큰 미설정 시 차단으로 바꿨다 |
| 메인 화면 로딩 실측 (4.1) | 쟀다. 쿼리는 병목이 아니다(11~13ms). 병렬화는 옳다(순차 30ms → 병렬 11ms) |

**끝난 항목**: P0 셋, P1 여섯 개 전부, P2 중 임베딩 차원·입력값 검증.
그 밖에 원 계획서의 P0 「분석 가능 상태 제한」과 「태깅 검수 근거·변경 이력」,
P2 「서버 전용 경계」를 라운드 37 에서 구현했다.

**남은 항목**

| 항목 | 상태 | 판단 |
|---|---|---|
| 리콜 증분 수집 | 미착수 | 지금은 26.7시간마다 전체를 다시 훑는다. `updated_at` 커서로 바꾸면 새 리콜이 몇 분 안에 잡히고 낭비도 사라진다 |
| 창 함수 `limit` 정렬 보장 | 미착수 | 상위 N건 반환이 SQL 표준상 보장되지 않는다. `pg_get_functiondef` 로 본체를 그대로 뽑아 쓸 수 있으니 이제 비용이 작다 |
| 코드별 근거 분리 | 미착수 | 태깅 프롬프트가 코드마다 근거를 따로 돌려주게 고쳐야 한다. 검수 화면에는 "이 근거는 조항 전체에 대한 것"이라고 밝혀 두었다 |
| 3번째 산출물 | 미착수 | 별도 문서(`03_…`)로 진행 |

**3번째 산출물의 전제가 이번에 바뀌었다**

7장은 "원자료가 준비돼 있다"고 했고 10.2 에서 "`review_log` 가 0건"이라고 정정했다.
한 번 더 정정한다 — `review_log` 에 쓰는 화면은 **있었다**(`/analysis/[caseId]` 의
채택·반려 버튼). 분석을 열 번밖에 안 돌려서 기록이 안 쌓였을 뿐이다.

정말 없던 것은 **조항 태그 검수**였고, 라운드 37 에서 만들었다. 이제 세 산출물이
쓸 재료가 모두 갖춰졌다 — 다만 담당자가 실제로 검수·검토를 해야 숫자가 생긴다.
따라서 3번째 산출물은 「안 1(읽기 전용 집계)」로 시작하는 것이 여전히 맞다.
표가 비어 있는 것 자체가 "아직 판단이 쌓이지 않았다"는 정보다.
