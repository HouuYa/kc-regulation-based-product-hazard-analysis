# 03. 3번째 산출물 — KC안전기준 개선 요인 구현 설계서

작성일: 2026-09-04
범위: 컨셉 v0.4 · 구현설계 v0.6의 세 번째 산출물
단계: 안 1(읽기 전용 집계) 구현 완료. 안 2·3은 미착수

---

## 0. 이 문서의 위치

`02_1차 보완 및 구현 설계서.md` 7장이 "3번째 산출물은 개선이 아니라 신규 기능이므로
별도 문서로 분리한다"고 했다. 이 문서가 그 후속이다.

원 계획서(`docs/코드베이스_성능_로직_개선계획_2026-09-04.md`)가 제시한
**안 1 → 안 2 → 안 3** 단계적 적용을 그대로 따른다.

| 단계 | 내용 | 상태 |
|---|---|---|
| 안 1 | 원자료를 그대로 집계하는 읽기 전용 뷰 + 화면 | **구현 완료** (041, `/insights`) |
| 안 2 | 집계 실행마다 스냅샷을 저장해 수치를 고정 | 미착수 |
| 안 3 | 개선 후보를 검토 사건으로 승격 | 미착수 |

안 1부터 시작한 이유는 원 계획서가 적은 그대로다 — **집계 정의가 실제 데이터에서
성립하는지를 먼저 봐야 한다.** 표를 먼저 만들면 정의가 틀렸을 때 그 표까지 버려야 한다.

---

## 1. 이 기능이 하지 않는 것

먼저 이것부터 적는다. 숫자가 있는 화면은 결론이 있는 것처럼 읽히기 때문이다.

- **"기준이 낮다·높다"를 말하지 않는다.**
- **"강화해야 한다·완화해도 된다"를 말하지 않는다.**
- **"법적 의무가 발생한다"를 말하지 않는다.**
- 자동 보고서 문장을 만들지 않는다. 표와 근거 목록까지가 시스템의 몫이다.

시스템은 **전문가가 다시 볼 자리**를 찾아 줄 뿐이고, 판단은 담당 부서와 전문가가 한다.

---

## 2. 이 기능의 전부는 "0건의 뜻을 나누는 것"이다

v0.7 §7.8이 지적한 것이 이 산출물의 핵심 난점이다.

> 검색 0건 자체를 기준 사각지대로 집계하면 데이터 누락을 정책 신호로 오인한다.

"대응 조항이 없다"와 "아직 분석을 안 했다"와 "품목이 확정되지 않았다"는 전혀 다른
말인데, 집계하면 전부 0으로 보인다. 그래서 세 뷰 모두 **상태(status) 칸**을 갖고,
화면은 상태별로 나눠 보여 준다.

사각지대 판정은 **순서가 있다.** 앞의 것이 참이면 뒤는 보지 않는다.

```
품목 미확정 → 코드 미부여 → 분석 미실행 → 검토 대기 → 대응 조항 없음 / 후보 전부 반려
   (걸러야 할 것)                              (비로소 사각지대 후보)
```

품목이 없으면 적용할 기준을 정할 수 없으므로 "대응 조항이 없다"고 말할 자격 자체가
없다. 이 순서를 지키지 않으면 자료 미비가 정책 신호로 둔갑한다.

---

## 3. 구현 — 뷰 세 개 (마이그레이션 041)

### 3.1 `insight_standard_gap` — 기준 사각지대

사건을 `(품목, HF 주코드, DT 주코드)`로 묶고 그 묶음이 어디까지 갔는지 센다.

| 상태 | 뜻 |
|---|---|
| `SCOPE_UNRESOLVED` | 품목 미확정. 사각지대인지 말할 수 없다 |
| `NOT_CODED` | HF/DT 코드가 없다 |
| `NOT_ANALYZED` | 분석을 아직 안 돌렸다 |
| `NOT_REVIEWED` | 후보는 나왔으나 담당자가 안 봤다 |
| `NO_CANDIDATE` | 분석했는데 후보 0건 → **사각지대 후보** |
| `ALL_REJECTED` | 후보를 담당자가 모두 반려 → **더 강한 신호** |
| `COVERED` | 채택된 조항이 있다 |

### 3.2 `insight_standard_comparison` — 국내외 기준 대조

해외 리콜의 `cited_standards`와 `matched_standard_ids`를 본다.
**수준의 높낮이는 판정하지 않고**, 견줄 수 있는 자리인지까지만 말한다.

| 상태 | 뜻 |
|---|---|
| `NUMBER_MISMATCH` | 번호 체계가 달라 이어지지 않는다(EN 71·GB 4706 계열). "국내에 기준이 없다"는 뜻이 아니다 |
| `MATCHED_NO_DATA` | 번호는 이어지는데 국내 조항에 구조화된 허용치가 없다 |
| `COMPARABLE` | 번호가 이어지고 수치도 있다 → 전문가가 견줘 볼 자리 |

컨셉 8장 7번이 못박은 전제를 지킨 것이다 — 상대국이 어떤 기준을 근거로 조치했는지
알아야 비교가 된다. 문장만 보고 높낮이를 추정하면 근거 없는 결론이 된다.

### 3.3 `insight_test_usage` — 저활용 시험항목

시험방법 조항별로 제시·채택·반려 횟수를 센다.
**"채택이 적다"와 "사고·리콜이 없다"를 같은 뜻으로 보지 않는다.**

| 상태 | 뜻 |
|---|---|
| `NEVER_PROPOSED` | 후보로 나온 적이 없다. 사고가 없었는지 분석이 적었는지 지금은 구별 못 한다 |
| `NOT_REVIEWED` | 제시됐으나 판단이 없다 |
| `LOW_ADOPTION` | 제시됐는데 한 번도 채택 안 됨 → 다시 볼 후보 |
| `IN_USE` | 채택된 적이 있다 |

---

## 4. 첫 집계 결과 (2026-09-04 실측)

**이것이 안 1을 먼저 만든 이유다.** 집계를 돌리자마자 세 가지가 드러났다.

### 4.1 지금 가장 큰 병목은 사각지대가 아니라 품목 확정이다

| 상태 | 묶음 | 사건 |
|---|---|---|
| 품목 미확정 | 162 | **2,225** |
| 분석 미실행 | 25 | 38 |
| 대응 조항 없음 | 1 | 1 |

전체 2,264건 중 **품목이 확정된 것이 39건(1.7%)뿐이다.** 사각지대 후보로 올라온 것은
단 1건이고, 나머지는 전부 그 앞 단계에서 걸린다.

즉 지금 이 산출물이 말하는 것은 "기준에 구멍이 있다"가 아니라
**"품목 확정률을 올리기 전에는 기준 개선을 논할 수 없다"**이다.
이것이 안 1이 준 첫 번째 답이고, 표를 만들기 전에는 알 수 없던 것이다.

### 4.2 국내외 비교는 "번호는 이어지는데 견줄 수치가 없다"에서 막힌다

| 상태 | 표준 수 | 리콜 |
|---|---|---|
| 번호로 이어지지 않음 | 158 | 626 |
| 이어지지만 비교할 수치 없음 | 43 | **127** |
| 비교 가능 | 0 | **0** |

번호 대조는 실제로 작동한다 — EN 62368-1 → KC 62368-1, EN 60335-1 → KC 60335-1 등
127건이 이어졌다. 그런데 **비교 가능이 0건**이다.

원인을 확인했다. 허용치(`test_condition`) 333건이 **전부 어린이제품 부속서 계열**에만
있고, EU 리콜이 대조되는 **전기용품(KC 6xxxx) 계열에는 0건**이다. 파서가 전기용품
계열의 표를 뽑지 못하고 있다.

이것도 안 1이 준 답이다 — 국내외 수준 비교를 하려면 **먼저 전기용품 계열의 허용치
파싱을 고쳐야 한다.** 집계 로직의 문제가 아니다.

### 4.3 시험항목은 표본이 아직 없다

| 상태 | 시험방법 수 |
|---|---|
| 후보로 나온 적 없음 | 520 |
| 검토 대기 | 4 |

`review_log`가 0건이므로 `LOW_ADOPTION`이 나올 수 없다. 제시된 적 있는 4개도
전부 검토 대기다. **표본 부족이지 저활용이 아니다.**

---

## 5. 다음에 할 일 — 실측이 정한 순서

집계 화면을 더 꾸미는 것보다 재료를 채우는 것이 먼저다.

1. **품목 확정률 올리기** (4.1) — 2,225건이 여기서 막힌다. 이것이 풀리기 전에는
   사각지대 집계가 의미 있는 숫자를 내지 못한다.
2. **전기용품 계열 허용치 파싱** (4.2) — 국내외 비교가 가능해지는 유일한 길이다.
3. **담당자 검토 기록 쌓기** — 분석 결과의 채택·반려(`/analysis/[caseId]`)와
   조항 코드 검수(`/standards/review`)를 실제로 써야 두 산출물의 판단 칸이 채워진다.
4. 그 뒤에 **안 2(스냅샷)** — 보고에 쓸 수치를 시점째 고정한다. 지금은 자료가
   바뀌면 숫자도 바뀌므로 회의 자료로 쓸 수 없다.
5. 담당 부서와 절차가 합의되면 **안 3(개선 후보 등록)**.

---

## 6. 안 2·3을 아직 만들지 않은 이유

원 계획서가 적은 그대로다.

> 처음부터 안 3으로 가면 "집계 기준이 맞는가"와 "업무 절차가 맞는가"를 한 번에
> 검증하게 된다.

지금 4장이 보여 주듯 **집계 기준부터 아직 검증 중이다.** 사각지대 후보가 1건,
비교 가능이 0건, 저활용 판정이 0건인 상태에서 스냅샷을 고정하거나 검토 절차를
붙이는 것은, 아직 뜻이 정해지지 않은 숫자에 절차를 씌우는 일이 된다.

재료가 채워져 숫자가 안정된 뒤에 안 2로 간다.

---

## 7. 완료 기준 대비 현재

원 계획서가 제시한 완료 기준에 대한 자기 점검이다.

| 완료 기준 | 상태 |
|---|---|
| 집계 정의·대상 범위·제외 사유가 문서화 | ✅ 이 문서 3장, 뷰 주석 |
| 각 숫자에서 원자료로 역추적 가능 | 🟡 상태별 분해는 되나 행에서 사건·조항으로 내려가는 링크는 아직 없다 |
| 집계 당시 기준 판·코드북 판·검색 설정 확인 | ❌ 안 2(스냅샷)의 몫 |
| 자동 미검수 태그와 검토 결과가 섞이지 않음 | ✅ `NOT_REVIEWED` 상태로 분리 |
| 강화·완화·법적 의무를 자동 확정하지 않음 | ✅ 화면 상단에 명시 |
| 표본 부족이 정상 결과처럼 표시되지 않음 | ✅ 상태로 분리하고 사각지대 후보를 맨 뒤에 둔다 |
