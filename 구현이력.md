---
version:
description: |-
  이 문서는 코드를 몰라도 "지금까지 무엇을 왜 만들었는지" 알 수 있도록 쓴다.
  작업 한 라운드가 끝날 때마다 아래에 새 항목을 추가한다. 오래된 내용을 지우지 않고 쌓아 간다
   — 나중에 "그때 왜 이렇게 정했지"를 되짚을 수 있어야 하기 때문이다.
  > (자세한 기술적 근거는 각 코드 파일 맨 위 주석에 있다. 이 문서는 그 요약이다.)
created: 2026-09-01
update: 2026-09-02T22:00:00
url:
update description: 라운드 12 — case_event(사고조사) GPC를 standard와 완전 동일한 LLM 검증·계층 하향 구조로 통합(findAndVerifyGpc 공유 함수)
---
---

## 이 문서를 쓰는 방법

- frontmatter 버전과 updated 날짜를 업데이트
- 코드를 크게 바꾸는 작업 한 라운드가 끝나면, 위쪽에 새 항목을 추가한다.
- 라운드에 종료 시간과 날짜를 작성한다.(즉 이 문서 업데이트)
- "무엇을 했는지"보다 "왜 했는지, 뭘 확인했는지"를 더 자세히 쓴다.
  (무엇을 했는지는 git 커밋 메시지를 보면 되지만, 왜 했는지는 그때 가서야 알 수 있다.)
- 전문용어가 나오면 괄호로 짧게 풀어 쓴다. 이 문서는 코드를 안 보는 사람도 읽을 수 있어야 한다.
	- 블릿 활용 및 개조식 명사형 종결(~분류하였다 -> 분류)하고 마지막은 `.`없이 문장 종료
	- 다만, 설명은 문장형태로 쉽게 풀어 쓴다.


---

## 라운드 1 — 설계 검토와 첫 골격

**한 일**
- 설계문서 3종(컨셉 v0.4, 구현설계 v0.6, 코드북 PDR v0.9.7)을 읽고 실제로 구현  가능한지 확인했다.
- `credentail.md`에 평문으로 있던 국내·해외 리콜 API 키를 `.env.local`로 옮기고 원본 파일은 삭제했다. (저장소가 비공개라 즉시 위험은 낮았지만, 평문 키를
  깃에 올려 두는 것 자체가 좋지 않은 습관이라 바로 정리했다.)
- 위해요인 코드북(2차원 분류 — 원인 HF / 결과 DT)을 자동으로 읽어 들이는 프로그램을 만들고, 실제 파일로 돌려서 문서에 적힌 숫자(HF 64개, DT 16개)와
  정확히 일치하는지 확인했다.
- KC안전기준 JSON 파일 76개를 열어 봐서, "조항이 어떤 시험방법과 연결되는지"가 파일 안에 이미 들어 있다는 것을 확인했다. (예: "의자가 넘어지지 않아야
  한다"는 조항이 "150N의 힘을 가한다"는 시험방법과 서로 짝지어져 있었다.)

**왜 중요한가**
- 이 시스템의 핵심은 "사고 원인 코드"와 "안전기준 조항"을 같은 코드로 이어 붙이는 것이다. 코드북이 정확히 안 읽히면 그 뒤 모든 게 흔들리므로, 제일
  먼저 검증했다.

---

## 라운드 2 — 데이터베이스(Supabase) 만들고 실제로 돌려 보기

**한 일**
- Supabase에 새 프로젝트(`kc-hazard-analysis`)를 만들었다.
- 설계에 필요한 표(테이블) 18개와 검색 함수들을 실제로 만들었다.
- 한국어 검색이 되는지 실측했다. PostgreSQL 기본 검색 기능은 "전도가", "전도를"처럼 조사가 붙으면 다른 단어로 착각한다 — 이게 진짜 문제인지 실제로 테스트해 봤더니 정말 그랬다. 그래서 한국어를 제대로 검색하는 별도 기능(PGroonga)을 설치했고, 이번엔 조사가 붙어도 잘 찾는 것을 확인했다.
- 가짜 사고 1건을 넣고 실제로 검색을 돌려 봤다. "의자가 옆으로 넘어져 아이가 떨어졌다"는 사고를 코드화(설계결함 + 낙상)한 뒤 검색했더니,
  정확히 그 원인·결과 코드가 붙어 있던 조항("4.3.3 안정성")을 찾아냈고, 그 조항이 가리키는 시험방법("5.9.2")까지 자동으로 이어졌다. 설계문서에
  적혀 있던 예시가 실제로 작동한 것을 확인한 셈이다.

**발견해서 고친 문제**
- Supabase는 새로 만든 함수를 기본적으로 "누구나 실행 가능"하게 열어 둔다는 사실을 발견했다. 데이터를 볼 수 있는 건 아니지만, 굳이 열어 둘 이유가 없어서 전부 잠그고 서버만 쓸 수 있게 고쳤다.

---

## 라운드 3 — 사람이 쓸 화면 만들기

**한 일**
- 화면 4개를 만들었다: 전체 현황, 안전기준 목록, 위해요인 코드 목록, 사고보고서 등록, 사건 분석 결과.
- "이 시스템은 위반 여부를 판정하지 않는다"는 원칙을 화면에도 그대로  옮겼다. 예를 들어 위험하다는 뜻으로 빨간색을 쓰지 않는다. 빨간색은
  오직 "시스템이 진짜로 고장 났을 때"(연결 실패 등)에만 쓴다. 후보 조항 목록에는 빨간 경고색을 절대 쓰지 않는다 — 시스템이 위험을
  판정하는 것처럼 보이면 안 되기 때문이다. 
- 사고보고서 PDF를 업로드하면 자동으로 글자를 뽑아내고, 그 안에 주민등록번호·전화번호·이메일 같은 개인정보가 있는지 확인한다.
  걸리면 그 파일은 분석 대상으로 넘기지 않는다(외부 AI에게 보내지 않는다는 뜻).

**중간 점검에서 반영한 것**
- 처음 문서(v0.6)를 검토한 보완본(v0.7)이 도중에 도착했다. v0.7은 "품목을 먼저 확정하지 않고 검색부터 하면 다른 제품의 시험이 섞일 수
  있다"거나 "검색 결과가 0건이라고 해서 무조건 '이 기준엔 조항이 없다'로 단정하면 안 된다(아직 준비가 덜 된 것일 수도 있다)" 같은
  지적을 했다. 두 버전(v0.6, v0.7)이 서로 다르게 말하는 부분은 어느 한쪽만 고르지 않고, 나중에 실제 데이터로 비교해 볼 수 있게 "선택
  가능한 방식"으로 만들어 두었다.

---

## 라운드 4 — 실제 데이터 채우기 · 신규 자료 감지 기능 · 정리

**한 일**

1. **환경설정 값 채움** — 승호님이 새로 발급받은 Supabase 접속 정보와 국내·해외 리콜 API 주소를 `.env.local`에 반영했다.

2. **깃(git) 이력 정리** — 이 작업들을 하는 동안, 원격 저장소(GitHub)에 다른 경로로 커밋 3개가 먼저 올라와 있어서 로컬 작업과 갈라져 있었다.
   서로 다른 파일을 건드린 커밋들이라 충돌 없이 합쳤다. 

3. **실제 데이터 적재** — 지금까지는 프로그램이 "이론적으로 맞다"는 것만 확인했는데, 이번에 처음으로 실제 데이터를 전부 넣어 봤다.
   - 위해요인 코드북 1건 (HF 64개, DT 16개)
   - 안전기준 76개 문서 전체, 조항 13,501개, 시험조건 333개,
     "이 조항은 이 시험과 연결된다"는 관계 644개
   - 넣은 뒤 개수를 다시 세어 봤더니 프로그램이 미리 계산했던 숫자와 한 자리도 틀리지 않고 똑같았다.

4. **새 자료가 들어왔을 때 자동으로 알아채는 기능** — 협회가 나중에
   기준을 개정해서 새 JSON 파일을 주면, 그게 "완전히 새로운 기준"인지  "이미 있는 기준의 개정판"인지 자동으로 구분하는 기능을 만들었다.
   - 완전히 같은 내용이면 → 아무 것도 안 함
   - 이름은 같은데 내용이 바뀌었으면 → 옛날 버전은 지우지 않고 "지금은 안 씀"으로 표시만 하고, 새 버전을 새로 넣는다. (옛 버전에 달려 있던 사람의 판단 기록은 절대 사라지면 안 되기 때문에 지우지 않는다.)
   - 완전히 새로운 이름이면 → 새로 넣는다.
   - 나중에 이 기능을 웹 주소(API)로도 호출할 수 있게 만들어 뒀다.
     지금 당장 누가 자동으로 호출하는 건 아니지만, 나중에 "자동 감시" 기능을 붙일 때 바로 쓸 수 있도록 미리 만들어 둔 것이다.

4. **사고조사보고서 5건 처리** — `사고조사보고서` 폴더에 있는 실제 PDF 5건(보조배터리 2건, LED등기구, 스팀다리미, 가습기)을 한 번에 처리하는 기능을 만들어 돌렸다.
   - **처리 중 실제 문제를 하나 발견했다.** 개인정보 탐지 기능이 "계좌번호"로 착각한 게 있었는데, 확인해 보니 진짜 계좌번호가
     아니라 보고서에 적힌 접수일자("25-11-24" 같은 형식)였다. 날짜와 계좌번호가 둘 다 숫자에 하이픈이 섞인 모양이라 헷갈린 것이다.
     실제 계좌번호는 자릿수가 훨씬 많다는 점을 이용해서 구분하도록 고쳤다. 고친 뒤에는 날짜는 더 이상 걸리지 않으면서, 진짜
     계좌번호처럼 생긴 것은 여전히 잡아내는 것을 확인했다.
   - 5건 모두 정상 처리됐다. (다만 이 상태로는 "등록"만 된 것이고, 담당자가 화면에서 원문을 확인하고 "확정" 버튼을 눌러야 실제 분석 대상이 된다 — 이것도 의도한 설계다. 사람이 확인하기 전에는 시스템이 멋대로 넘어가지 않는다.)

---

## 라운드 5 — AI 코드화 실제 가동 · 품목 확정 · 해외 리콜 연결

**한 일 (크게 넷)**

#### **① AI에게 코드를 붙이게 하되, 두 번 물어보게 했다**

앞 라운드까지는 "AI로 코드를 붙인다"고 만들어만 두고 실제로는 못 돌렸다. 
API 키가 채워져서 이번에 처음 돌렸다.

그냥 돌리지 않고 방식을 하나 정했다. **싼 모델에게 세 번 물어보고, 세 번 답이 갈리면 그때만 비싼 모델에게 다시 묻는다.**

왜 이렇게 했는가. 값을 재 봤더니 싼 모델(Luna) 세 번이 비싼 모델(Terra) 한 번보다 **3.3배 싸다.** 그런데 세 번 물어서 답이 같으면 그 답은 믿을 만하다. 답이 갈리는 건은 원래도 어려운 건이니 비싼 모델로 올린다.

여기서 한 가지를 일부러 피했다. AI에게 "네 답이 얼마나 확실하냐"고 물어 그 숫자로 판단하는 방법이 있는데, **설계문서가 그 숫자를 믿지 말라고 명시** 했다(§5.2.3). 실제로 틀린 답에도 0.9를 매긴다. 대신 **같은 질문을 세 번 해서 답이 얼마나 일치했는지**를 기준으로 삼았다. 이건 AI의 자기 평가가 아니라 관측된 사실이다.

돌려 본 결과 — 안전기준 조항 53개 중 **16개(30%)가 비싼 모델로 올라갔고, 그중 10개는 비싼 모델이 다른 답을 냈다.** 이 10개가 사람이 제일 먼저 봐야 할 목록이다.

> 정직하게 덧붙인다. 비싼 모델이 답을 **바꿨다**는 것과 **맞혔다**는 것은 다르다. 
> 어느 쪽이 옳은지는 전문가가 만든 정답지가 있어야 알 수 있고, 그건 아직 없다. 지금은 "여기가 흔들리는 구간"까지만 말할 수 있다.

#### **② 사고 사건을 실제 흐름에 연결했다 — 품목부터 정한다**

사고보고서를 넣으면 이제 이 순서로 처리된다.

```
사고보고서 → ① 품목이 무엇인가 → ② 그 품목에 적용되는 기준은 무엇인가
          → ③ 원인·결과 코드 붙이기 → ④ 관련 조항 찾기
```

**품목을 제일 먼저 정하는 게 핵심이다.** 설계문서가 한 문장으로 정리해 뒀다
— "위해요인 코드가 같아도 제품이 다르면 관련 시험이 다르다." 코드부터 찾으면 유아용 의자 사고에 전기다리미 시험이 섞여 나온다.

품목을 못 찾으면 **전체 검색을 자동으로 돌리지 않고 멈춘다.** 
억지로 아무거나 보여 주는 것보다 "못 찾았습니다"가 낫다.

품목 찾는 길은 두 갈래다. 어린이제품 33종은 이름으로 바로 찾고, 전기용품은 파일명에 품목이 없어서 **각 기준의 '적용범위' 원문을 뒤진다.** 
예를 들어 "가습기"는 KC 60335-2-98의 적용범위 문장에 적혀 있어서 그렇게 찾았다.

#### **③ 실제로 돌려 보니 AI가 옳았던 일**

사고보고서 5건을 모두 코드화했더니 **전부 원인이 "알 수 없음"으로 나왔다.**
처음엔 AI가 일을 못한 줄 알고 한 건을 직접 열어 봤다.

가습기 화재 사건이었는데, AI는 "물 부족 시 자동 전원 차단 기능 미작동으로 화재 발생"이라고 상황은 정확히 읽어 냈다. 그런데 원인 칸은 비웠다.
보고서 본문에 이렇게 적혀 있었기 때문이다 — **"제품 시험 결과 모두 안전기준에 적합", "특이사항을 식별하지 못함".**

즉 **조사에서 결함을 못 찾은 사건**이었고, AI는 없는 원인을 지어내지 않았다.

맞는 판단이었다.

그래서 이런 사건은 화면에 이렇게 알리도록 했다.

> ※ 이 사건은 원인이 확정되지 않았습니다. 아래 후보는 피해유형과 어휘만으로 넓게 건진 것이므로 코드 근거가 없습니다. 특정 시험을 단정하지 마십시오.

원인을 모르는데 결과(화재)만 가지고 "이 시험을 하십시오"라고 하면 그건 근거 없는 지목이 된다.

#### **④ 해외 리콜(트랙 B)을 붙였다**

해외 리콜 2,239건이 있는 협회 시스템(Recall Hub)에 실제로 연결했다.
그러면서 **설계 때의 전제 두 개가 실물과 다른 것을 발견했다.**

*전제 1 — "해외 리콜은 이미 코드가 붙어 있다"*
→ **아니었다.** 실제 응답에는 코드가 없고 "질식 위험", "화학적 위험", "전기적 위해 (Electrical Hazard)" 같은 자유 서술만 온다. 표기가 제각각이라 분류 기준으로 쓸 수 없다. 그래서 **해외 리콜도 우리가 코드를 붙이기로**
했다. 조항·사고·리콜 셋이 같은 코드 위에 있어야 대조가 되고, 그게 이 체계의 존재 이유다.

*전제 2 — 주소 오타*
→ `.env.local`에 적혀 있던 `recalhub.kr`(L 하나)은 존재하지 않는 주소였다.
`recallhub.kr`(L 둘)이 맞는 주소다. 고쳤다.

**그리고 설계문서가 "먼저 확인하라"고 남겨 둔 항목 하나를 실측해서 답했다.**

컨셉 문서는 "다른 나라와 우리 기준의 수준 차이를 비교하려면, 그 나라가 어떤 기준을 근거로 리콜했는지 알아야 한다. 확보 가능한 범위를 먼저 확인해야
한다"고 적어 두고 미뤄 놨었다. 2,239건 전수를 훑어서 셌다.

| 출처 | 건수 | 위반 표준 명시 | 비율 |
|---|---:|---:|---:|
| EU | 864 | 450 | **52%** |
| 중국 | 172 | 106 | **62%** |
| 영국 | 266 | 57 | 21% |
| 프랑스 | 171 | 9 | 5% |
| 미국·호주·캐나다·일본 등 | 766 | 3 | 0~1% |
| **합계** | **2,239** | **625** | **27.9%** |

**결론: 기준 수준 비교는 전체의 28%, 사실상 EU·중국·영국 건에서만 가능하다.**
나머지 72%는 사유만 적혀 있어 비교할 근거가 없다. 이걸 알고 시작하는 것과 모르고 시작하는 것은 다르다.

한 가지 더 확인했다. EU 공고의 "EN 60598-2-12"와 우리 "KC 60598-2-12"는 같은 국제표준(IEC)에서 온 같은 문서라 **번호로 바로 대조된다.** 반면 완구
표준(EN 71)이나 중국 가전 표준(GB 4706)은 우리 어린이제품 기준과 번호 체계가 아예 달라 번호로는 안 이어진다 — 그건 사람이 판단할 몫으로 남겼다.

#### **⑤ 심각한 누락 하나를 발견해서 고쳤다**

해외 리콜 분석을 돌려 보다가 결과가 이상하게 얄팍한 것을 발견했다.
조항 본문이 대부분 **"제1부의 이 항목을 적용한다"** 한 줄이었다.

이유를 찾아보니 이랬다. 전기용품 국제표준은 **"제1부(공통) + 제2-x부(품목별)"** 두 권으로 되어 있고, **제2-x부는 제1부를 고치는 개정문**이다. 실제 요구사항의
대부분은 제1부에 있다.

```
KC 60335-2-98 (가습기)   요구사항  54개  ← 우리가 보고 있던 것
KC 60335-1    (공통)     요구사항 395개  ← 통째로 빠져 있던 것
```

**가습기 사고를 분석하면서 요구사항의 88%를 못 보고 있었다.** 품목을 찾을 때 제1부를 자동으로 함께 넣도록 고쳤다. 어린이제품에서 부속서에 공통안전기준을
같이 묶는 것과 같은 구조인데, 전기용품은 그 묶음이 없어서 만들어 준 것이다.

#### **⑥ 정확도 측정표를 만들었다**

설계문서 §5.8이 요구한 비교표(코드만 / 코드+어휘 / 코드+어휘+의미 / +리랭킹을 하나씩 켜면서 재현율·오탐률을 재는 표)를 만들었다.

정답지를 어디서 가져오느냐가 갈리는 지점인데, **운영 기록을 정답지로 쓰지 않기로** 했다.  
시스템이 보여 준 후보 중에서 담당자가 고른 기록은, 시스템이 애초에 안 보여 준 조항을 정답에 넣을 수 없다. 그러면 점수가 늘 부풀려진다.
전문가가 **시스템 결과를 보기 전에** 작성한 정답지를 기본으로 삼는다.

> 지금은 정답지가 없어서 실제 수치는 아직 못 낸다. 표를 찍는 도구까지만 준비된 상태다.

#### **⑦ 순위 재조정(리랭킹)이 한 번도 작동한 적이 없었다는 것을 발견했다**

검색 결과의 순서를 AI가 다시 매기는 기능이 있는데, 그동안 계속 꺼 놓고 돌려서 아무도 몰랐다. 켜 봤더니 바로 거절당했다.

원인은 데이터베이스가 큰 정수(조항 번호표 등)를 **숫자가 아니라 글자로** 돌려주기 때문이었다. `6070`이 아니라 `"6070"`으로 온다. 프로그램 안에서는
숫자라고 적혀 있어서 자동 검사에도 안 걸린다. 조용히 틀리는 자리다.

같은 문제가 네 군데에 있었고, 그중 하나는 **정확도 측정표를 항상 0%로 만들고 있었다** — 정답 조항 번호는 글자, 시스템이 찾은 조항 번호는 숫자라
서로 대조가 안 됐던 것이다.

고친 뒤 실제로 돌려 보니 이렇게 나왔다 (가습기 화재 사건).

```
[1] 19.4     이상운전 시험 조건            재채점 0.82
[2] 24.101   온도과승방지장치는 비자동복귀형   재채점 0.76
[3] 2-98     (기준 표지)                  재채점 0.35
```

**2순위 24.101이 이 사건의 핵심이다.** 사고 보고서가 "물 부족 시 자동 전원 차단 기능 미작동"이라고 적은 그 장치를 규정한 조항이다. AI가 붙인 이유도
정확했다 — "물 부족 시 보호차단이 작동하지 않았다는 사고 경위와 밀접하다."

그리고 순위 재조정은 기준 표지 조항(2-98)을 1위에서 3위로 내렸다. 이유도 적어 놨다 — "가습기 기준 전체에는 해당하지만 구체적 시험 조건을 규정한
조항은 아니다." 이게 이 기능이 해야 할 일이다.

#### **⑧ 표시 조항이 기준마다 다르게 분류되고 있었다**

같은 성격의 절인데 제목 표기에 따라 갈렸다.

```
"표시사항"           → 표시 조항으로 분류  28건
"표시 및 사용설명서"   → 성능요건으로 분류   14건
```

국제표준(IEC 60335) 제7절은 "표시 및 사용설명서"라는 묶인 제목을 쓰는데, 분류 규칙이 그 형태를 몰랐다. 고쳤더니 성능요건 7,766 → 7,472,
표시 조항 879 → 1,173으로 정리됐다.

여기서 한 가지를 **일부러 바꾸지 않았다.** 표시 조항은 지금 검색 후보에서 빠져 있는데, 설계문서가 "사고 원인이 경고·표시와 관련될 때만 켠다"고 정해
놨기 때문이다. 다만 **그 '켜는' 장치는 아직 안 만들었다.** 화상·질식 사고에서 경고 표시는 실제로 확인하는 항목이므로, 조건부로 켜는 기능이 다음 과제로
남아 있다. 지금은 표시 조항 1,173건이 어떤 사건에서도 후보가 되지 않는다.


**왜 이 라운드가 중요한가**

- 이제 사고보고서 한 건을 넣으면 **품목 확정 → 코드화 → 조항 검색 → 시험방법**  까지 끊기지 않고 이어진다. 그전까지는 조각들만 있었다.
- 해외 리콜도 같은 엔진을 타되, **국내 유통 확인·기준 수준 비교·안전조치 검토는 별도로** 다룬다. 리콜을 사고와 똑같이 취급하면 트랙 B의 고유한 가치(제품안전기본법 13조 3항 사업자 보고의무 판단 등)가 사라진다.
- **시스템이 판단하지 않는 선을 그었다.** 국내에 같은 제품이 유통됐는지는통관·유통 자료로 확인할 일이지 이 시스템이 추측할 일이 아니다. 담당자가 확인한 결과를 받아 적기만 한다.

---

## 지금 시점에서 되어 있는 것 / 안 되어 있는 것

*(라운드 5 종료 시점 기준)*

**되어 있는 것**
- 위해요인 코드북, 안전기준 76개 문서(조항 13,501개), 사고조사보고서 5건이 모두 데이터베이스에 들어가 있다. 보고서 PDF 원본도 보관함(Storage)에 있다.
- 사고보고서 한 건을 넣으면 **품목 확정 → 원인·결과 코드화 → 관련 조항 검색
  → 시험방법 연결**까지 끊기지 않고 이어진다. 가습기 화재 사건으로 끝까지 돌려서 확인했다.
- AI 코드화가 실제로 돌아간다. 싼 모델 3회 → 답이 갈리면 비싼 모델로 승격하는 2단 방식이고, 흔들린 건을 검수 목록으로 따로 뽑아 준다.
- 해외 리콜(2,239건)에 실제로 연결된다. 검색해서 가져오고, 코드를 붙이고, 국내 기준 조항과 대조하고, 상대국이 지목한 표준과 우리 기준을 견줄 수 있다.
- 화면에서 현황을 볼 수 있고, 사고보고서를 올릴 수 있다.

**아직 안 되어 있는 것 (다음에 할 일)**
- **전체 조항 태깅**: 요구사항 조항 7,766개 중 지금까지 코드를 붙인 것은 가습기 관련 기준뿐이다. 나머지는 아직이다. 한 번에 다 돌리기 전에 담당자가 지금까지 붙은 코드를 검수해 보는 게 순서다.
- **정확도 수치**: 재는 도구(§5.8 비교표)는 만들었지만, **전문가가 만든 정답지가 없어서 실제 숫자는 아직 못 낸다.** "이 사고면 어느 조항의 시험을 의뢰하겠는가"를 담당자가 시스템 결과를 보기 전에 적어 주면 바로 잴 수 있다.
- **국내 리콜 연동**: 해외 리콜은 연결했지만 국내 리콜(주소는 받아 둠)은 아직이다. 국내 리콜은 코드가 안 붙어 있어 우리가 붙여야 한다.
- **전기용품 품목 등록**: LED등기구, 스팀다리미 등 일부 품목은 적용범위 원문 검색으로도 안 잡힌다. 담당자가 확인해서 등록해 줘야 한다.
- **화면 반영**: 이번 라운드에 새로 생긴 것들(조항 역할 구분, 품목 확정 근거, 원인 미확정 안내, 해외 리콜 분석)이 아직 화면에는 안 붙었다.
  명령줄에서만 돌아간다.

---

## 라운드 6 — 리랭킹 "되돌아감" 오인 확인 · 해외 리콜 리치 데이터 갭 분석

**한 일 (코드 변경 없음, 조사만)**

**① 리랭킹(rerank.ts)·매칭(match.ts)이 되돌아갔다는 우려 확인**

라운드 5에서 고친 bigint→Number() 정규화, effort 기반 LLM 호출이 이후 머지 과정에서
사라진 것처럼 보인다는 걱정이 있어서 실제로 확인했다.

`git diff`로 그 커밋(fb1ddc0)과 지금 최신 상태를 직접 비교해 봤더니 **두 파일 모두
차이가 하나도 없었다** — 즉 되돌아간 게 아니다. 그 뒤에 있었던 병합(머지) 커밋
두 개도 이 두 파일에는 아무 변경을 남기지 않은 "깨끗한" 병합이었다. 만약 지금도
예전처럼 동작하는 것처럼 보인다면, 코드 문제가 아니라 (1) 오래된 빌드를 보고 있거나,
(2) 실행 환경에 `OPENAI_RERANK_EFFORT` 값이 실제로 전달 안 되고 있거나, (3) 다른 작업
폴더(워크트리)를 보고 있는 경우일 가능성이 크다 — 코드 자체는 정상이다.

**② 해외 리콜 원본 테이블(Recall Hub)과 지금 쓰는 API 사이의 격차 분석**

지금 이 시스템이 해외 리콜을 가져오는 창구(`src/lib/recall/hub.ts`)는 협회
Recall Hub 관리자 시스템이 공개한 **얇은 API 하나**뿐이다. 그런데 그 관리자
시스템 뒤에는 담당자가 직접 검토·승인한 **훨씬 두꺼운 원본 표(90개 이상 컬럼)**가
따로 있다는 제보가 있어서, 그 원본 표 스키마 문서(`docs/해외리콜수집/recalls table
schema.txt`, `recalls_rows.sql` 실제 표본 데이터)를 읽고 지금 API가 주는 정보와
정확히 뭐가 다른지 대조했다.

- 지금 API가 주는 건 16개 필드뿐이다(제품명, 브랜드, 위험유형, 국가 등 기본 정보).
- 원본 표에는 있지만 API에는 없는 것 중 가장 중요한 두 가지:
  - **원인-결과 2차원 분류(HF-DT) 코드**: 이 시스템이 국내 사고에 쓰는 것과
    **똑같은 코드 체계**(`HF.*`/`DT.*`)로 이미 담당자가 분류해 둔 값이 원본 표에
    있다. 그런데 API는 이걸 안 준다. 그래서 지금 이 시스템은 해외 리콜마다
    **AI로 다시 분류하는 중복 작업**(`recalls:code`)을 하고 있는 셈이다 — 담당자가
    이미 검토해서 붙여 놓은 값을 안 쓰고 있었다.
  - **승인 상태(`approval_status`)**: 담당자 표본 100건을 세어 보니 승인(approved)
    53건, 반려(rejected) 46건, 검토대기(pending) 1건이었다. 즉 원본 표에서 승인된
    것만 골라 오면 담당자가 이미 거른, 신뢰도 높은 건만 받을 수 있는데 지금 API로는
    이 구분을 알 방법이 없다.
  - 그 밖에도 부상자 수, 원문·번역문 양쪽, 이미지·PDF 여러 장, 관리자가 고친 이력
    전체(`update_history`) 등이 원본 표에는 있지만 API에는 없다.
- 원본 표에 직접 연결하려면 그 표가 있는 Supabase 프로젝트(`xicivtmjfkpuehgsieex`)의
  DB 접속 정보가 필요한데, **지금 연결된 관리 도구 계정에는 이 프로젝트가 없었다.**
  같은 조직에 이름이 비슷한 프로젝트(`recall-management`)가 있었지만 주소(ref)가
  다르고 사용 중지 상태였다 — 확인해 보길 잘했다, 헷갈렸으면 엉뚱한 곳에 연결할
  뻔했다.
- 처음엔 `해외리콜정보_recall_table_schema.txt` 파일을 못 찾아서 "저장소에 없다"고
  적었는데, 다시 확인해 보니 **없어진 게 아니라 이번 조사 직전 커밋에서
  `해외리콜수집/recalls table schema.txt`로 이름이 바뀐 것**이었다(파일을 옮기면서
  이름도 정리한 것으로 보인다 — 잘못된 게 아니다). `recall-hub-admin화면.png`도
  실제로 있었고, 다만 아직 git에 올라가 있지 않은 상태(작업 폴더에만 있음)였다.

**왜 이 라운드가 중요한가**
- 리랭킹 문제는 실제로는 없었다는 걸 diff로 직접 확인해서, 애먼 코드를 건드리지
  않을 수 있었다.
- 해외 리콜 쪽은 "API가 부실하다"는 느낌이 실제로 어디가 얼마나 부실한지(16개 vs
  90개 이상 컬럼, 특히 사람이 검토한 HF-DT 분류와 승인 여부)를 숫자로 확인했다.
  이걸 알아야 다음에 뭘 연결할지 우선순위를 정할 수 있다.

**다음 라운드에 필요한 것 (담당자 확인 필요)**
1. `xicivtmjfkpuehgsieex` 프로젝트의 DB 접속 문자열(DATABASE_URL) — `.env.local`에
   추가해야 원본 표를 직접 읽을 수 있다.
2. 지금 하고 있는 자체 AI 분류(`recalls:code`)를 원본 표의 담당자 승인 분류로
   완전히 바꿀지, 아니면 두 방식을 한동안 같이 두고 비교해 볼지 결정 필요.
3. 원본 표를 끌어올 때 필요한 컬럼만 고를지, 통째로 다 가져와 둘지(`raw_data` 원본
   포함) 범위 결정 필요.

---

## 라운드 7 — 해외 리콜, 원본 표 직접 연결로 전환 (AI 재태깅 폐지)

**결정 사항 (담당자 확인)**
- `recall-management`(ref 다름, INACTIVE)는 이번 건과 무관한 별도 프로젝트로 확인됨.
- 자체 AI 태깅(`recalls:code`)을 원본 표의 담당자 승인 분류로 **완전 대체**.
- 원본 표는 컬럼을 고르지 않고 `raw_data` 포함 통째로 가져온다.

**한 일**
- 원본 표(`xicivtmjfkpuehgsieex` 프로젝트, `recalls` 표)에 Supabase REST 키(`sb_secret_*`)로
  직접 붙는 조회 모듈(`src/lib/recall/source.ts`)을 만들었다. 얇은 API(`src/lib/recall/hub.ts`,
  16개 필드)는 이제 아무 데서도 안 써서 지웠다.
- 적재 스크립트(`scripts/load-recalls.ts`)를 다시 짰다. 예전엔 "캐시(`recalls:fetch`) →
  AI 코드화(`recalls:code`)" 두 단계였는데, 원본 표에 이미 담당자가 승인한 위해요인
  코드가 있으므로 **한 번에 끝난다** — AI를 한 번도 안 부른다.
- `recall_cache` 표에 컬럼을 더했다(승인 상태, 심각도, 부상 건수 등 — 014 마이그레이션).

**실제로 돌려서 확인한 것 (전수, 2026-09-02)**
- 원본 표 전체 4,828건 중 승인(approved) 2,253건. 이 승인 건만 가져온다.
- 2,253건 캐시 저장 → 사건 행 신규 2,206 + 기존 47(먼저 소량으로 시험 삼아 넣어 둔
  건들과 겹침, 정상) → 품목 확정 1,511건(67%) → **코드화 2,213건(98.2%, AI 호출 0회)**.
- KC 보유 기준과 번호가 같은 위반 표준이 명시된 건 78건.
- 재실행해서 중복이 안 생기는지도 확인했다 — `case_event` 2,253건, 중복
  `external_ref` 0건. `case_tag` 7,848행이 정확히 2,213개 사건에 물려 있다(전부 일치).

**막혔다가 뚫은 문제 — 코드가 다 안 맞았다**
- 승인 건의 위해요인 코드를 우리 코드북(v0.9.7, 80개)과 전수 대조했더니 두 코드가
  안 맞았다.
  - `HF.S.STD`(주 40건·부 155건, 승인 건의 8.7%) — PDR 부록 J 변경이력을 보니
    **우리 코드북에서 v0.9 때 이미 폐기하고 8개로 세분화한 코드**였다(§5.2). v0.9.5에서는
    실제 사례를 하나씩 봐 가며 `HF.S.QC`/`HF.S.PROC`/`HF.S.NATL.DEF` 등으로 손으로
    바꿨다는 기록까지 있다 — 즉 자동으로 1:1 치환할 수 있는 코드가 아니다. 관리자
    시스템(Recall Hub)이 이 폐기 이전의 옛 코드북 기준으로 분류를 돌리고 있다는 뜻이라,
    두 시스템의 코드북이 언제부턴가 어긋나 있었다는 얘기다(거버넌스 동기화 문제 —
    Recall Hub 담당자에게 알려야 한다).
  - `HF.M.REG.VIOL`(1건) — 애초에 없던 코드로 보인다. 우리 코드북엔 `HF.M.REG.ILLEGAL`·
    `HF.M.REG.UNMANAGED`(REG 하위)와 별도로 `HF.M.VIOL`(내부규정 위반, REG와
    나란한 최상위)이 있는데, 이 둘을 합쳐서 잘못 만든 코드로 추정된다.
  - 임의로 짐작해서 매핑하지 않았다. 코드북에 없는 코드는 그 건에서 버리고,
    **주 위해요인 코드 자체가 안 맞는 40건은 아예 코드화하지 않고 미분류로
    남겼다**(부코드만 안 맞는 경우는 그 코드만 빼고 나머지로 정상 코드화했다).
    실행하면 미분류 건 목록(recall_cache id·출처·guid)이 그대로 찍힌다 — 담당자가
    건별로 봐야 한다.

**부수적으로 고친 버그**
- 이 참에 손대다가 발견한 것: `case_event`에 `external_ref` 유니크 제약이 원래
  없어서, 적재 스크립트를 재실행할 때마다 같은 리콜이 새 행으로 또 쌓이는
  상태였다(막았다고 주석엔 적혀 있었는데 실제로는 안 막혔다). `source_type +
  external_ref` 부분 유니크 인덱스를 추가해서 고쳤고, 재실행 테스트로 실제로
  중복이 안 생기는 것까지 확인했다(위 실측 참고).

**일부러 안 바꾼 것**
- 이번에도 `chunk_summary`(AI 한 줄 요약)는 안 만든다. 원본 표의 서술 필드
  (`product_description`, `hazard_description`, `recall_cause` 등)가 얇은 API 시절보다
  훨씬 풍부해서, 검색용 텍스트(`search_text`)에 그대로 넣는 것만으로 충분하다고
  보고 AI 요약 단계를 아예 건너뛰었다 — 비용도 안 들고, `buildCaseSearchText()`가
  요약 없이도 정상 동작하도록 이미 짜여 있었다(변형 A에서 "요약:" 줄만 비워짐).
- `recalls:code`(AI 코드화) npm 스크립트 자체는 지우지 않았다. 사고보고서·국내
  리콜은 여전히 이 경로로 코드화해야 하고, 해외 리콜에 대해서만 이제 할 일이
  없어질 뿐이다(이미 `case_tag`가 있으면 건너뛰는 조건이 있어 자동으로 그렇게 된다).

**다음에 할 일**
- 미분류 40건(`HF.S.STD`) — Recall Hub 담당자와 협의해서 원본 쪽 코드북을
  올리거나, 우리 쪽에서 건별로 세분 코드를 골라 줘야 한다.
- 사고보고서 쪽에도 `HF.S.STD` 처럼 옛 코드가 남아 있는지는 아직 확인 안 했다.

---

## 라운드 8 — HF.S.STD 자동 재분류 · GPC 코드 조회 · 사고사진 비전 분석

세 가지를 한 라운드에 붙였다. 앞의 두 가지(HF.S.STD, GPC)는 라운드 6·7의 뒷정리이고,
사진 분석은 완전히 새 기능이다.

### ① HF.S.STD 40건, LLM이 세부 코드로 재분류 — 이제 "미분류"가 생기면 자동으로 이렇게 처리된다

라운드 7에서 미뤄 둔 40건(관리자 쪽이 폐기된 코드 `HF.S.STD`를 붙인 건)을, 우리 쪽
코드북에서 유효한 세부 코드로 LLM이 다시 분류하도록 적재 스크립트를 고쳤다.
새 프롬프트를 만들지 않았다 — 기존 `tagCase()`(사고·리콜 코드화에 원래 쓰던 함수)는
애초에 "지금 유효한 코드 목록"만 답으로 허용하도록 짜여 있어서, 이 함수를 그대로
다시 부르기만 하면 폐기된 코드가 나올 수 자체가 없다. 그래서 앞으로 관리자 쪽에서
또 다른 옛 코드를 보내와도(코드북이 계속 바뀌므로 또 생길 수 있다) 사람이 매번
손대지 않아도 똑같이 처리된다.

관리자 값을 그대로 쓴 코드는 `review_status='approved'`, LLM이 대신 채운 축만
`'auto_unreviewed'`(검수 대상)로 남긴다 — 같은 사건 안에서도 축마다 출처를 구분해
남겨서, 나중에 담당자가 뭘 다시 봐야 하는지 바로 걸러낼 수 있게 했다.

실제로 40건 전부 돌려 봤다. 대부분(32건)은 `HF.H.ELEC.BAT`, `HF.S.NATL.DEF`처럼
구체적인 세부 코드로 갔고, 일부(8건)는 `HF.H`, `HF.S`처럼 큰 갈래 이름만 나왔다.
이건 이번에 새로 생긴 문제가 아니라 **이 시스템의 코드화 기능 전체가 원래 갖고
있던 특성**이라는 것도 확인했다 — 코드북 조회 옵션을 어떻게 주든 그 큰 갈래
이름 자체가 항상 고를 수 있는 목록에 들어 있었다(사고보고서 코드화에도 똑같이
적용된다). 다만 그런 애매한 답이 나오면 검수 대상으로 표시되게 만들어 뒀으니,
이번 경우엔 딱 설계한 대로 걸러진 셈이다.

### ② GPC(GS1 국제 품목분류) 코드 조회 — 처음 생각과 다르게 방식을 바꿨다

담당자가 "이미 있는 edge function을 부르자"고 했는데, 실제로 만들어 보니 두 가지
문제가 나와서 방식을 바꿨다.

1. 그 edge function은 후보를 1개(top-1)만 주도록 고정돼 있었다. 담당자가 원한
   "후보 3~5개"를 애초에 못 낸다.
2. 그 edge function 안에 박혀 있는 OpenAI 키가 크레딧이 다 떨어진 상태였다
   (`429 no credits remaining` 실측). 우리가 고칠 수 있는 부분이 아니다.

그런데 담당자가 이어서 보내 준 n8n 워크플로(`(GS1) [한국리콜__OECD] OECD GPC
RAG.json`)를 보니, 그 edge function이 쓰는 것과 **같은 프로젝트 안에 있는 원본
표·검색 함수**(`oecd_gpc_202405` 표, `match_documents_202405` 함수)를 직접
참조하고 있었다. 실제로 그 함수를 우리 쪽 anon 키로 직접 불러 봤더니(edge
function을 거치지 않고) **바로 실행이 됐다** — 즉 협회가 처음부터 이 함수를
외부에서 부를 수 있게 열어 둔 상태였다.

그래서 edge function은 아예 안 쓰기로 했다. 대신:
- 상품명·설명 문장을 **우리 OpenAI 키**로 임베딩한다(그쪽 크레딧 문제와 무관해진다).
- 그 벡터로 `match_documents_202405`를 직접 불러 후보를 원하는 개수(3~5개)만큼 받는다.

실제로 "가습기"로 검색해 봤다. 1위가 정확히 "가습기 - 이동형" 브릭이었다. 그런데
유사도 값 자체는 0.25 안팎으로 낮았다 — 원래 edge function의 기본 임계값(0.75)을
그대로 썼다면 **정답인 1위조차 "일치 없음"으로 걸러졌을 것**이다. 그래서 임계값으로
거르지 않고 순위(상위 몇 개)로만 판단하기로 했다. 또 하나, 사진에서 뽑은 좀 더
상세한 설명 문장으로 다시 검색했더니 1위가 "제습기"(가습기의 반대 기능)로 바뀌고
"가습기"가 2위로 밀린 적도 있었다 — 이게 정확히 담당자가 "후보 여러 개를 보고
싶다"고 한 이유였다는 걸 실측으로 확인한 셈이다. 1개만 믿었으면 틀렸을 사례다.

### ③ 사고조사보고서 첨부 사진 분석 — 새 기능, 실제로 텍스트가 놓친 단서를 찾았다

지금까지 사고보고서는 텍스트만 읽었다. PDF 안에 삽입된 사진은 있어도 그냥
파일째로 보관만 하고 아무도 들여다보지 않았다. 그런데 라운드 4·5에서 사고 5건 중
다수가 "시험 결과 안전기준 적합"이라는 문장만 남기고 원인 미확인(HF.UNKNOWN)으로
끝났던 걸 다시 보니, 사진에 텍스트에 없는 단서가 있을 수 있겠다 싶어 이번에 붙였다.

**한 일**
- PDF에서 삽입 사진만 뽑아낸다(페이지를 통째로 찍는 게 아니라 실제 이미지
  오브젝트만 — 표·글자는 안 섞인다). 가로세로 200px 미만인 것은 뺀다(로고·직인
  같은 서식 이미지가 여러 보고서에서 정확히 같은 크기로 반복되는 걸 실측으로
  확인했다). 걸러지지 않고 통과한 것 중에서도 진짜 증거 사진인지는 비전
  LLM에게 최종 판단을 맡긴다 — 놓치는 쪽보다 더 보내는 쪽이 안전하다는
  이 프로젝트의 원칙을 사진에도 그대로 적용했다.
- 뽑은 사진은 Supabase Storage(기존 원본 보관 버킷)에 저장하고, 새 표
  `source_file_image`에 사진마다 분석 결과를 남긴다.
- 비전 LLM(담당자 요청대로 최상위 모델은 아니고 이 시스템의 "품질 우선" 등급
  모델을 씀 — 연 50건 안팎이라 비용보다 품질이 우선)이 사진마다 "실제 증거
  사진인가/장식 이미지인가"부터 판단하고, 증거 사진이면 뭐가 보이는지·위해요인
  관련 관찰이 있는지 적는다. 근거 없는 원인 추정은 하지 않는다는 이 시스템의
  기존 원칙(§5.2.2)을 사진 쪽에도 그대로 옮겼다.
- 증거 사진들을 종합한 "제품 서술 한 문장"을 만들어서, 그걸 그대로 위 GPC 조회의
  입력으로 재사용한다 — 담당자가 말한 "이것도 GPC 분류에 쓸 수 있겠다"는 생각이
  실제로 맞았다.

**실제로 돌려서 확인한 것 (가습기 사고, 2026-09-02)**
- 11쪽짜리 보고서에서 사진 4장을 뽑았고, 비전 LLM이 4장 전부 증거 사진으로
  판단했다.
- 그중 4쪽 사진 2장에서 **"바닥면 내부 부품 주변에 갈색 변색 또는 얼룩이 보인다"**는
  관찰을 냈다. 이 보고서의 텍스트만으로는(원래 결론이 "시험 결과 적합") 전혀
  나오지 않았던 단서다. 텍스트 코드화 결과는 여전히 HF.UNKNOWN(원인 미확인)으로
  남아 있는데, 이 갈변 관찰이 담당자가 재검토할 때 실제로 쓸모 있을 만한 신호다.

**작업 중 실수하고 바로 고친 것**
- 이 기능을 가습기 사건으로 테스트하면서 `--force` 옵션을 썼는데, 이게 그
  사건의 **기존 확정 상태·품목·코드화 결과(라운드 5의 리랭킹 시연 데이터)를
  통째로 지워 버렸다.** 바로 알아채고 코드화를 다시 돌리고(`HF.UNKNOWN` /
  `DT.THERMAL.FIRE`로 원래와 같은 결과 확인) 확정 상태를 되돌리고 임베딩을
  다시 만들어 복구했다. 앞으로 이런 시연·데모 데이터가 있는 사건에는
  `--force`를 함부로 쓰면 안 된다는 걸 기록해 둔다.
- GPC 조회를 처음엔 edge function 기준으로 구현하고 사고 스크립트에 연결까지
  했는데, 위 ②의 이유로 도중에 직접 RPC 호출 방식으로 다시 짰다. 함수
  자체(`findGpcCandidates`)는 실제 호출로 검증했지만, 새 방식으로 사고 적재
  스크립트 전체를 다시 돌려서 확인하지는 못했다 — 위 `--force` 실수를 반복하지
  않으려고 기존 5건에는 재실행을 안 했다. **다음에 새 사고보고서가 들어오면
  그때 사진→비전→GPC 전체 경로가 실제로 이어지는지 처음으로 확인하게 된다.**

**바뀐 파일 요약**
- `src/lib/cases/extract-pdf.ts`: `extractPhotos()` 추가(사진 추출).
- `src/lib/llm/client.ts`: `structuredVisionCall()` 추가(이미지 포함 구조화 호출).
- `src/lib/llm/vision.ts`(신규): 사진 분석 프롬프트·스키마.
- `src/lib/gpc/lookup.ts`(신규): GPC 후보 조회(직접 RPC 방식).
- `src/lib/cases/item-name.ts`(신규): 품목명 추출 로직을 `tag-cases.ts`에서 뽑아내
  `load-cases.ts`와 같이 쓰게 함(중복 방지).
- `scripts/load-cases.ts`: 사진 추출·보관·분석·GPC 조회를 적재 파이프라인에 연결.
- `scripts/load-recalls.ts`: HF.S.STD 등 무효 코드 자동 LLM 재분류.
- `supabase/migrations/015_case_photos.sql`: `source_file_image` 표, Storage 버킷에
  `image/jpeg` 허용 추가.
- `package.json`: `sharp`(이미지 인코딩) 추가.

**다음에 할 일**
- 새 사고보고서가 들어올 때 사진→비전→GPC 경로 실사용 확인.
- GPC 후보를 화면에 아직 안 보여 준다 — `case_event.raw_fields.gpc_candidates`에는
  있지만 UI 연결은 다음 과제.
- HF.H/HF.S 처럼 큰 갈래 이름만 나오는 문제는 이번 라운드 범위 밖이지만, 사고보고서
  코드화에도 있는 만큼 프롬프트를 더 다듬을지는 별도로 검토해야 한다.

---

## 라운드 9 — 사진→비전→GPC 첫 실사용 확인 · GPC 화면 표시 · KC기준 GPC 착수 · 큰 갈래 이름 문제 실태 조사

라운드 8이 "다음에 할 일"로 남긴 네 가지를 그대로 이어받았다. 새 기능을 만들지 않고,
지난 라운드가 만든 것이 실제로 작동하는지 확인하고, 확인하는 과정에서 발견한 것들을
정리하는 라운드다.

### ① 사진→비전→GPC 전체 경로, 실제 사건으로 처음 끝까지 돌려 확인

DB를 열어 보니 가습기 사건의 사진 분석(4장, 증거사진 4장)은 이미 되어 있었는데
GPC 쪽(`gpc_brick_code`, `raw_fields.gpc_candidates`)은 비어 있었다. 라운드 8이
"GPC 조회 함수 자체는 검증했지만 사고 적재 스크립트 전체를 다시 돌려서 확인하지는
못했다"고 남긴 바로 그 지점이다.

GPC 조회 함수를 단독으로 먼저 불러 봤더니 `self-signed certificate in certificate
chain` 오류로 실패했다 — 그런데 이건 진짜 버그가 아니라, 이 프로젝트의 네트워크
호출 스크립트가 전부 쓰는 `NODE_OPTIONS=--use-system-ca` 없이 직접 실행해서 생긴
내 테스트 환경 문제였다. 그 옵션을 붙이자 정상적으로 후보 5개가 나왔다(2위가
"가습기 - 이동형" 브릭 — 타당한 결과).

그래서 실제 파이프라인(`npm run cases:load -- --only 가습기 --force`)을 그대로
다시 돌렸다. 사진 4장 추출 · 비전 분석 · GPC 후보 5개 조회 · 1위 승격까지
**한 번에 이어졌다.** 다만 1위로 뽑힌 건 "가습기"가 아니라 "제습기 - 이동형
(전기식)"이었다(2위가 정답 "가습기 - 이동형", 3위가 "가습기 - 고정형") — 이건
라운드 8이 이미 실측한 것과 같은 현상(절대 유사도가 낮아 순위가 흔들릴 수 있음)이고,
그래서 화면에도 1위만 보여 주지 않고 순위 전체를 보여 주기로 했다(② 참고).

**실수하고 바로 복구한 것** — `--force`를 다시 썼다가, 라운드 8이 이미 한 번
겪었던 바로 그 사고를 또 냈다. 가습기 사건의 확정 상태·품목(가습기)·코드
(HF.UNKNOWN/DT.THERMAL.FIRE)·검색 결과(match_run, 라운드 5의 리랭킹 시연 데이터)가
전부 지워졌다. 확인 직후 `npm run cases:code -- --case <id> --all` →
`npm run embed -- --cases` → `npm run search -- --case <id>` 순서로 다시 돌려서,
라운드 8이 기록해 둔 것과 정확히 같은 결과(HF.UNKNOWN/DT.THERMAL.FIRE, 확정 상태,
match_run 20건)로 복구했다. **이 사건에는 절대 `--force`를 쓰면 안 된다** —
라운드 5·8 이후로 이 사건이 시연용 골든 데이터가 됐다는 걸 이번에 한 번 더
확인한 셈이니, 다음에 또 잊지 않도록 이번에도 다시 적어 둔다.

### ② GPC 후보를 사건 분석 화면(화면 D)에 표시

`case_event.raw_fields.gpc_candidates`에는 있었지만 화면에 없던 것을
`src/app/analysis/[caseId]/page.tsx`의 "품목·적용기준" 섹션 바로 아래에 추가했다.
1위만 확정처럼 보여 주지 않고 순위 전체 + 유사도 값을 그대로 보여 주고, 1위가
확실한 답이 아닐 수 있다는 문구를 함께 넣었다 — ①에서 실측한 "제습기가 1위로
나온" 사례가 바로 이 판단의 근거다. GPC 후보가 없는 사건(사진이 없거나 아직
분석 전인 경우)에는 이 섹션 자체를 숨긴다.

### ③ KC안전기준(standard)에 GPC 코드 부여 — 착수만, 전량 배치는 안 돌림

`public.standard`에 `gpc_brick_code`·`gpc_candidates` 컬럼만 추가했다
(`016_standard_gpc.sql`). 대상을 `standard`로 잡은 이유는 GPC가 "제품이 무엇인가"를
분류하는 것이고 `standard.item_name`이 문서 1건당 그 제품명을 이미 갖고 있기
때문이다 — `clause`는 개별 요건 행이라 제품 단위가 아니다.

`--limit`을 필수로 받는 시험용 스크립트(`scripts/tag-standards-gpc.ts`,
`npm run standards:gpc`)를 만들어 5건만 돌려 봤다. **기술적으로는 문제없이
동작했지만(호출·저장 다 됨), 정확도는 사고사진 GPC보다 눈에 띄게 나빴다:**

| 표준 | item_name | GPC 1위 결과 |
|---|---|---|
| 안전인증 부속서 1 | 어린이용 물놀이기구 | 어린이용 요실금 제품 — 오답 |
| 안전인증 부속서 3 | 가정용압력냄비및압력솥 | 자기기압계(비전기식) — 오답 |
| 안전확인 부속서 16 | 유아용 캐리어 | 기타 유아 안전/보안/감시 — 애매 |
| 안전확인 부속서 70 | 휴대용 예초기의 날 및 보호덮개 | 개인용 안전장비 세트 — 애매 |
| 공급자적합성 부속서 1 | 어린이용 가죽제품 | 기타 유아 급식용품 — 오답 |

**왜 이런 차이가 나는지 짐작 가는 이유** — 사고사진 GPC는 비전 LLM이 사진을 보고
뽑아낸 상세한 서술 문장("투명 물 수위창과 하단 조작부가 있는 흰색 원통형 전기
가습기…")을 조회 입력으로 쓰는데, 기준 쪽은 지금 `item_name` + `display_name`
(예: "가정용압력냄비및압력솥" + "안전인증 부속서 3(가정용압력냄비및압력솥)")처럼
짧고 건조한 행정 분류명만 넣고 있다. 임베딩 유사도 검색은 입력 문장이 구체적일수록
잘 맞는데, 지금 입력은 그 반대다. **이건 다음 라운드에서 담당자가 판단할
문제다** — 조회 입력에 조항 본문 일부(적용범위 문장 등)를 더 넣을지, 아니면
기준 76건 규모에서는 GPC가 애초에 잘 안 맞는 방식인지.

전체 76건(`is_current`) 배치는 이번 라운드에서 돌리지 않았다 — 비용도 들고,
위 표처럼 정확도가 검증 안 된 상태로 76건 다 채우면 나중에 되돌리기 번거롭다.
`item_name`이 null인 표준(KC 60335-2-xx류 베이스표준, 조회할 품목명 자체가 없음)도
이 방식 밖이라 별도 검토가 필요하다.

### ④ HF.H/HF.S처럼 큰 갈래 이름만 나오는 문제 — 실태만 조사(프롬프트는 안 고침)

라운드 8이 사고코드화(case_tag)에서 "이 시스템 전체가 원래 갖고 있던 특성"이라고
확인한 것과 같은 문제를, 이번엔 조항 코드화(clause_tag)에서도 실측했다.

| 대상 | 전체 코드화 건수 | HF.H 맨몸(하위 없이) | HF.S 맨몸 | 합계 비율 |
|---|---|---|---|---|
| 조항(clause_tag) | 373건 | 17건 | 30건 | 47건 (12.6%) |
| 사건(case_tag) | 2,263건 | 19건 | 13건 | 32건 (1.4%) |

조항 쪽이 사건 쪽보다 9배 가까이 높다. 왜 그런지 보려고 `HF.S` 맨몸으로 붙은
조항 8건의 본문을 직접 읽었는데, **거의 다 KC 60335-1(전기용품 공통기준)의
순수 시험절차·측정값 조항**이었다(예: "누설 전류는 시험 전압을 인가한 후 5초
이내에 측정한다", "온도 상승 값은 표3에서 정한 값을 초과하여서는 안된다").
이런 조항은 애초에 특정 위해요인을 서술하지 않는다 — 회사가 국내표준을
안 지켰다거나(HF.S.NATL) 내부규정이 없다는(HF.S.CORP) 얘기가 전혀 아니다.
그런데도 모델은 (아마 뭐라도 골라야 해서) `HF.S`라는 큰 갈래 이름을 골랐다.

leaf 코드가 코드북에 없어서 생기는 문제가 아니라는 것도 확인했다 —
`HF.H.ELEC.INS`(94건), `HF.H.PHY.SMALL`(246건) 같은 세부 코드가 실제로
가장 많이 쓰이고 있다. 즉 모델이 leaf를 고를 수 없어서가 아니라, **순수
시험절차 조항처럼 위해요인 자체가 애매한 경우에 `HF.UNKNOWN` 대신 `HF.S`
맨몸을 고르는 경향**으로 보인다. 이번 라운드에서는 프롬프트를 고치지
않았다 — 근거만 정리해 뒀으니, 프롬프트를 바꿀지(예: "순수 시험절차만 있고
위해요인 서술이 없으면 HF.UNKNOWN을 쓴다"는 규칙 추가) 담당자가 다음 라운드에서
판단하면 된다.

**바뀐 파일 요약**
- `src/app/analysis/[caseId]/page.tsx`: GPC 후보 순위 표시 섹션 추가.
- `supabase/migrations/016_standard_gpc.sql`(신규): `standard.gpc_brick_code`,
  `standard.gpc_candidates` 컬럼.
- `scripts/tag-standards-gpc.ts`(신규): 기준 GPC 조회 시험용 스크립트(`--limit` 필수).
- `package.json`: `standards:gpc` 스크립트 추가.
- 조항 코드화(`clause_tag`)·사건 코드화(`case_tag`)·기준 GPC 시험(`standard`) 5건은
  실제 DB 값을 바꿨다(모두 위 표에 실측치로 남김). `scripts/tag-clauses.ts`,
  `scripts/tag-cases.ts` 등 코드 자체는 고치지 않았다.
- `docs/구현이력.md` → 저장소 루트 `구현이력.md`로 위치 이동(같은 라운드 안에서
  다른 요청으로 처리 — 문서를 찾기 더 쉬운 위치로 옮기고 `CLAUDE.md`·
  `src/lib/env.ts`의 경로 참조도 함께 갱신했다).

**다음에 할 일**
- 기준(standard) GPC — 조회 입력을 더 구체적으로 만들지(적용범위 문장 등 추가),
  아니면 이 규모에서 GPC 방식 자체를 재검토할지 판단 필요.
- 기준(standard) 전량 배치(76건) — 위 판단이 끝난 뒤에.
- `HF.H`/`HF.S` 맨몸 문제 — "순수 시험절차 조항은 HF.UNKNOWN" 같은 프롬프트 규칙을
  추가할지 판단 필요. 사건 쪽(1.4%)보다 조항 쪽(12.6%)이 훨씬 크므로, 손댄다면
  조항 프롬프트(`SYSTEM_CLAUSE`)부터가 우선순위가 높아 보인다.
- `item_name`이 null인 베이스표준(KC 60335-2-xx류)은 GPC 조회 대상 자체가 아니다 —
  이 표준들에 GPC를 붙이려면 다른 입력(예: 연결된 부속서의 item_name)을 빌려와야
  한다.

---

## 라운드 10 — 기준(standard) GPC 조회 입력 보강 · HF.UNKNOWN 프롬프트 규칙 추가 · Netlify 배포 완료

라운드 9가 "판단 필요"로 남긴 두 가지에 대해 담당자가 방향을 정해 주어서 바로
반영했다. 새 기능은 아니고, 이미 있는 것을 고치는 라운드다.

### ① 기준 GPC 조회 입력 — item_name 단독에서 item_name + scope_text 로

담당자 판단: "명칭과 적용범위 부분 등을 복합적으로 전달해서 받으면 될 듯합니다."
DB를 열어 실제로 쓸 만한 컬럼이 있는지부터 확인했다. `public.standard.scope_text`
(012_product_scope.sql이 "적용범위(SCOPE) 조항 본문을 모은 것"이라 정의해 둔
컬럼)가 있었고, GPC 대상인 item_name 있는 표준 33건 전량에 이미 채워져 있는 것을
실측으로 확인했다(예: KC 60335-2-13 "요리할 때 식용유를 사용하는 가정용 전기
튀김기, 전기 프라이팬"). `scripts/tag-standards-gpc.ts`가 조회 입력으로 쓰던
`display_name`("안전인증 부속서 3(가정용압력냄비및압력솥)" 같은 행정 분류명)을
이 `scope_text`로 바꿨다(없으면 `display_name`으로 폴백).

라운드 9에서 오답이 났던 바로 그 5건을 GPC 컬럼만 초기화하고 다시 돌려 비교했다.

| 표준 | item_name | 이전(display_name) | 이번(scope_text) |
|---|---|---|---|
| 안전인증 부속서 1 | 어린이용 물놀이기구 | 어린이 요실금 제품 — 오답 | 어린이 요실금 - 속옷(일회용) — 오답 |
| 안전인증 부속서 3 | 가정용압력냄비및압력솥 | 자기기압계(비전기식) — 오답 | **압력솥 — 정답** |
| 안전확인 부속서 16 | 유아용 캐리어 | 기타 유아 안전/보안/감시 — 애매 | **유아용 캐리어 — 정답** |
| 안전확인 부속서 70 | 휴대용 예초기의 날 및 보호덮개 | 개인용 안전장비 세트 — 애매 | 온상/냉상/모종상자/육묘용보호덮개 — 오답 |
| 공급자적합성 부속서 1 | 어린이용 가죽제품 | 기타 유아 급식용품 — 오답 | 유아 목욕안전 제품 — 오답 |

5건 중 0건 정답 → 2건 정답으로 개선됐지만 완전히 해결되지는 않았다. 실제
scope_text 원문을 읽어 보니 이유가 보인다 — 이 문장들은 "이 기준은 ~에 대하여
규정한다" 형식의 법령 문체라서, 물놀이기구 건은 "착용형/비착용형 수영보조용품"
이라고만 쓰여 있고 "물놀이기구"라는 단어 자체가 없다. 가죽제품 건도 "만 13세
이하의 유아 및 아동이 사용하는 제품…"이라는 일반 문구만 있고 "가죽"이라는
단어가 아예 없다. item_name으로 그 단어를 보충해도 scope_text 쪽 문장이 길어서
희석되는 것으로 보인다. 사고사진 GPC(비전 LLM이 뽑은 구체적 서술문)만큼 좋은
입력을 기준 쪽에서 구하기는 근본적으로 더 어렵다는 뜻이라, 이 방식의 한계로
일단 기록해 둔다. 전량 배치(76건)는 이번에도 안 돌렸다 — 정확도가 완전히
검증되지 않았다.

### ② HF.H/HF.S 맨몸 문제 — "순수 시험절차면 HF.UNKNOWN" 규칙 추가

담당자 판단: "순수 시험절차 조항에 HF.S를 고르는 건 잘못됐다." 라운드 9가 추정한
원인(모델이 뭐라도 골라야 해서 HF.S 맨몸을 고르는 경향)이 맞다고 확인된 것으로
보고, `src/lib/llm/tagging.ts`의 `SYSTEM_CLAUSE`(조항 태깅 프롬프트)에 규칙을
추가했다: 시험절차·측정방법·합격판정값만 서술하고 위해요인 자체가 없으면
hf_primary를 HF.UNKNOWN으로 두고, 목록에 마땅한 코드가 안 보인다고 HF.S를 대신
고르지 말라는 것과, HF.S는 조항이 국내표준 미준수(HF.S.NATL)나 내부규정
부재(HF.S.CORP)를 실제로 서술할 때만 쓰라는 것을 명시했다.

이번 라운드에서는 프롬프트만 고쳤다 — 기존에 이미 태깅된 373건(그중 47건이
HF.H/HF.S 맨몸)을 새 규칙으로 다시 태깅하지는 않았다. `npm run tag -- --retag`는
클로즈(clause) 단위 필터가 없어 47건만 골라 다시 돌릴 수 없고, 검수 승인
안 된 조항 전체(373건)를 다시 부르게 된다 — 비용은 크지 않지만(라운드
9 실측 기준 조항 1건당 약 $0.0022, 373건이면 1달러 안팎) 재태깅은 임베딩도
같이 무효화시키므로(`npm run embed` 재실행 필요) 담당자 승인 없이 진행하지
않았다. 다음 라운드에서 재태깅 여부를 정하면 된다.

### ③ Netlify 배포 완료 (담당자 확인)

라운드 9와 같은 요청에서 병렬로 진행하던 Netlify 배포(`netlify.toml` 생성,
CLI 설치까지는 이 세션이 하고 로그인·프로젝트 연결·환경변수 등록은 담당자가
직접 진행하기로 남겨 둔 것)를 담당자가 완료했다. 프로젝트명
`kc-regulation-product-hazard-analysis`, houuya 팀 아래 GitHub 연동, 환경변수
전부(시크릿 24개 + `NEXT_PUBLIC_*` 2개) 등록 확인(스크린샷). 배포 진행 중인
상태를 확인했다 — 실제 기능(DB 조회 등)이 프로덕션에서 되는지는 다음 라운드에서
확인이 필요하다.

**바뀐 파일 요약**
- `scripts/tag-standards-gpc.ts`: 조회 입력을 `display_name`에서 `scope_text`로 변경.
- `src/lib/llm/tagging.ts`: `SYSTEM_CLAUSE`에 "순수 시험절차면 HF.UNKNOWN" 규칙 추가.
- `standard` 테이블: 라운드 9 시험 5건의 `gpc_brick_code`/`gpc_candidates`를 새 결과로 재기록(실제 DB 값 변경).
- `netlify.toml`, `.gitignore`(`.netlify/` 제외): 라운드 9에서 이어진 Netlify 설정.

**다음에 할 일**
- ~~조항 코드화 47건(HF.H/HF.S 맨몁) 재태깅 여부~~ — 담당자 승인 받아 진행함. 결과는
  라운드 11 "④ HF.S 맨몸 문제" 절 참고(도중에 범위를 초과한 사고가 있었고, 최종 47→10건).
- ~~기준(standard) GPC — scope_text로도 못 잡는 5건 중 3건 문제~~ — 라운드 11에서
  LLM 검증 레이어로 이어서 다룸(같은 5건 재검증 결과 오답 0건).
- Netlify 프로덕션에서 DB/OpenAI 호출이 실제로 되는지 확인. (미확인 — 다음 라운드)

---

## 라운드 11 — 기준 GPC에 LLM 검증 레이어 추가 · HF.S 맨몸 재태깅(별도 진행)

라운드 10이 "scope_text로도 못 잡는 5건 중 3건" 문제를 남겼는데, 담당자가 방향을
"LLM을 GPC 매칭에 추가로 붙이되, 입력 컬럼을 정확성 우선으로 다시 검토해서
최대한 제공"으로 정해 줘서 그대로 반영했다.

### 왜 순위 1위만 보는 방식으로는 한계가 있었는지 (분석)

3건을 후보 top-20까지 넓혀서 직접 봤더니 서로 다른 두 문제가 섞여 있었다.

1. **순위만 밀린 경우** — 정답이 후보 안에 있는데 임베딩 1위가 아니다.
   "어린이용 물놀이기구"는 후보 11위에 "목욕/수영용 물놀이완구"가 있었는데
   1~3위는 전부 무관한 "요실금 제품"이었다. 절대 유사도 자체가 낮아서(0.25~0.4대)
   순위를 그대로 믿을 수 없다는 뜻이다.
2. **후보 안에 정답이 아예 없는 경우** — "휴대용 예초기의 날 및 보호덮개"는
   top-20 안에 예초기·정원공구 관련 항목이 하나도 없었다(정원용 기압계·습도계·
   개인보호구류만 나왔다). "어린이용 가죽제품"의 "용어의 정의" 조항을 직접
   읽어봤더니 "36개월 이상 만 13세 이하의 어린이가 사용하는 가죽제품"이라고만
   되어 있고 "가죽"이 구체적으로 무엇인지(가방·신발·벨트 등) 서술이 없었다 —
   KC기준 자체가 여러 GPC 브릭에 걸친 우산 카테고리라 애초에 "정답 브릭 하나"가
   존재하지 않을 수 있다는 뜻이다.

1번은 후보를 다시 읽고 고르는 LLM 판정으로 고칠 수 있지만, 2번은 후보에 없는
것을 골라낼 수 없다 — 이 경우엔 "맞는 게 없다"고 정직하게 답하는 것이 맞고,
지금까지처럼 임베딩 1위를 억지로 확정해 저장하는 게 오히려 더 나쁜 방식이었다.

### ① 조회 입력 재검토 — scope_text 외에 어떤 컬럼이 있는지 다시 봤다

`clause` 테이블에 `clause_role='DEFINITION'`(용어의 정의, 전체 817건)과
`level_name='서문'`(전문) 조항이 있다는 걸 확인했다. 특히 "어린이용 물놀이기구"의
서문에는 "에어매트리스, 에어베게, 펀치볼, 비치볼과 같이 물에서 사용할 우려가
있는 제품도 검사대상에 포함"처럼 scope_text보다 훨씬 구체적인 제품 예시가
있었다(서문은 clause_role이 SCOPE가 아니라 OTHER라서 지금까지 scope_text
집계에는 안 잡혔던 것도 확인). 정의 조항은 표준마다 148~818자 정도로 짧아
비용 부담 없이 전량 포함할 수 있는 것도 확인했다.

그래서 `scripts/tag-standards-gpc.ts`의 조회·검증 입력을
**품목명 + 기준명 + 인증구분(cert_scheme) + 적용범위(scope_text) + 서문 + 용어의
정의(DEFINITION 조항 전체)**를 조합한 문장으로 바꿨다(`buildProductContext()`).
가죽제품처럼 정의 조항 자체에 서술이 없는 경우는 이렇게 입력을 넓혀도
못 잡는다는 것을 예상하고 진행했다 — 아래 검증 결과가 그걸 확인해 준다.

### ② LLM 검증 레이어 추가 (`src/lib/gpc/verify.ts`, 신규)

`findGpcCandidates()`가 주는 후보를 5개가 아니라 **15개**까지 넓히고
(`CANDIDATE_COUNT`), `verifyGpcMatch()`가 그 후보 목록을 구조화 출력(JSON
Schema, `tagging.ts`와 같은 방식)으로 검토해 실제로 맞는 것을 고르거나
`'NONE'`을 고르게 했다. brick_code를 후보 목록의 enum으로 고정해 목록에
없는 코드를 지어내지 못하게 막은 것도 `tagging.ts`와 같은 원리다. 기존
`gpc_brick_code`(임베딩 1위, 미검증) 컬럼은 의미를 안 바꾸고 그대로 뒀다 —
`case_event.gpc_brick_code`와 같은 뜻을 유지해야 두 테이블의 같은 이름
컬럼이 안 헷갈린다. 검증된 답은 새 컬럼 `gpc_verified_brick_code` +
근거가 담긴 `gpc_verification`(jsonb)에 남긴다(`016_standard_gpc.sql`을
건드리지 않고 `017_standard_gpc_verification.sql`로 추가).

### ③ 같은 5건으로 재검증한 결과

| 표준 | 라운드10(top-1, scope_text만) | 라운드11(후보15 + LLM검증) |
|---|---|---|
| 어린이용 물놀이기구 | 오답(요실금 속옷) | **정답 확정** — 수영 훈련 보조기구 (확신 0.93) |
| 가정용압력냄비및압력솥 | 정답(압력솥) | **정답 유지** — 압력솥 (확신 0.99, 임베딩1위는 이번에 자기기압계로 바뀌었지만 LLM이 정정) |
| 유아용 캐리어 | 정답 | **정답 유지** (확신 0.99) |
| 휴대용 예초기의 날 및 보호덮개 | 오답(애매) | **NONE**(확신 0.99) — "후보에는 예초기·절단날·보호덮개에 해당하는 브릭이 없다"고 LLM이 정확히 판단 |
| 어린이용 가죽제품 | 오답 | **NONE**(확신 0.98) — "후보들은 유아 식기·기저귀·위생용품에 한정되어 있고 가죽제품에 해당하는 브릭이 없다"고 LLM이 정확히 판단 |

5건 중 **오답 0건**: 정답 확정 3건(확신 0.93~0.99), 정직한 미확정(NONE) 2건.
라운드 10까지는 5건 모두 뭔가 채워졌고 그중 3건이 오답이었는데, 이번엔 애초에
GPC로 못 잡는 2건을 LLM이 스스로 걸러냈다는 게 핵심 차이다. NONE으로 나온
2건은 여전히 "GPC로는 답이 없다"는 뜻이지 실패가 아니다 — 담당자가 수동으로
판단해야 하는 항목으로 남는다.

전량 배치(76건)는 이번에도 안 돌렸다 — 5건 검증만으로 방식을 확정하기엔
표본이 작고, LLM 검증 호출이 하나 늘어난 만큼 비용도 더 든다(추정: 후보 15개 목록
+ 조합 텍스트가 들어가는 호출 1회당 수 센트 수준, 76건이면 수 달러대로
여전히 작지만 표본을 좀 더 늘려 확인한 뒤 결정하는 게 안전하다고 판단).

### ④ HF.S 맨몸 문제 — 프롬프트 규칙을 기존 데이터에도 적용 (별도 subwork로 진행)

라운드 10에서 추가한 "순수 시험절차면 HF.UNKNOWN" 규칙(`SYSTEM_CLAUSE`)을
기존에 태깅된 373건에도 적용해 달라는 담당자 승인을 받아, 별도 백그라운드
작업(subwork)으로 진행했다.

**사고: `--retag`가 373건이 아니라 6,392건을 대상으로 잡았다**

담당자 승인은 "이미 태깅된 373건을 새 규칙으로 다시"였는데, `npm run tag --
--retag`를 그대로 돌렸더니 실제로는 승인(`review_status='approved'`) 안 된
`clause_role='REQUIREMENT'` 조항 **전체(6,392건)**를 대상으로 잡았다 —
스크립트가 "이미 태깅됐는지"가 아니라 "승인됐는지"만 걸렀기 때문이다(그때는
승인된 행이 0건이었으므로 사실상 전량이 걸린 것). 이 사실을 뒤늦게
`tagging_run` 표에서 `clause_count = 6392`로 확인하고 즉시 중단시켰다.

그런데 첫 중단 시도(`TaskStop`)는 실제로는 실패했다 — Windows에서
`npm run tag` 가 npm → cross-env → tsx → node 4단 자식 프로세스로 실행되는데,
`TaskStop`은 최상위 셸만 종료하고 그 아래 자식 프로세스 트리는 계속 살아
있었다. 그 사실을 모른 채 "중단됐다"고 보고했다가, 이후 DB를 다시 조회해서
같은 프로세스가 15분 넘게 계속 조항을 태깅하고 있었다는 것을 발견했다
(`clause_tag.created_at`이 조회 시점 바로 몇 초 전까지 계속 찍히고 있었다).
`tasklist`/`Get-CimInstance Win32_Process`로 살아있는 node 프로세스 4개의
실제 커맨드라인을 확인해 `taskkill //F`로 프로세스 트리 전체를 강제
종료하고 나서야 완전히 멈췄다.

이 사고로 원래 계획에 없던 **125개 조항**이 새 프롬프트로 먼저 태깅됐다
(주로 조항 ID 4~276번대 — 원래 목표였던 47건은 4252~11373번대라 겹치지
않았다). **이 125건은 틀린 데이터가 아니다** — 새 규칙이 적용된 정상적인
태깅 결과이고, 이전에는 아예 태깅이 안 돼 있던 조항들이라 오히려 코드화
범위가 늘어난 것뿐이다. 다만 담당자가 승인한 범위(47건 교정) 밖의 일이고
예상 못 한 LLM 호출 비용이 발생했으므로, 지우지 않고 그대로 남긴 채 이렇게
기록해 둔다. clause_id 목록은 커밋에 포함되지 않는 DB 값 변경이라 여기
텍스트로만 남긴다: 4, 8, 9, 10, 11, 15, 19–83(연속 아님, 다수), 118–164,
206–211, 243–276 범위에 흩어진 125건(정확한 전체 목록은 `clause_tag` 표에서
`created_at >= '2026-09-02T06:30:02'`이고 `tagging_version`에 `round10fix`가
안 붙은 행으로 재현 가능).

**정작 대상이었던 47건 교정**

`npm run tag -- --retag`로는 47건만 정확히 겨냥할 수 없어서(개별 clause_id
필터가 없음), 라운드 9가 특정한 47개 clause_id를 그대로 받아 별도의 임시
스크립트로 처리했다. 주의한 점 하나 — `tagging_version` 문자열이 모델·코드북
버전만으로 정해져서 재실행해도 이전과 완전히 같은 문자열이 나온다. 그 상태로
그냥 insert만 하면 `on conflict (clause_id, axis, code, tagging_version)`가
코드가 다르면 충돌로 안 잡아서, 옛 `HF.S` 행과 새 `HF.UNKNOWN` 행이 같은
조항에 `is_primary=true`로 동시에 남는 사고가 날 수 있었다. 그래서 이 47건에
한해 기존 `clause_tag` 행을 먼저 지우고 새로 넣는 방식으로 짰다(버전 문자열
끝에 `-round10fix`를 붙여 구분도 남겼다). 47건 전부 성공(실패 0)했고, 이어서
`npm run embed`로 재태깅·사고로 함께 무효화된 임베딩(총 172건)을 전부
다시 채웠다.

**최종 확인 수치** (모든 백그라운드 프로세스 완전 종료 확인 후 재측정)

| 항목 | 사고 전(라운드 9) | 지금 |
|---|---|---|
| HF.H/HF.S 맨몸(최신 태깅 기준) | 47건 | **10건** |
| `review_status='approved'` 행 | 0건 | 0건 — 그대로 보존 확인 |
| 태깅된 조항 총수(distinct clause_id) | 373건 | 498건(373 + 사고 125) |
| 임베딩 없는 태깅 조항 | — | 0건(전부 재생성 확인) |

47건 중 37건이 이번 교정으로 코드가 바뀌었다(대부분 `HF.UNKNOWN`으로,
일부는 `evidence_span`이 실제로 있는 조항이라 세부 leaf 코드로 바뀜 — 예:
"4.1.1"은 `HF.H.PHY.FRAC,HF.M.DES`로). 남은 10건은 재태깅해도 여전히
`HF.H`/`HF.S` 맨몸으로 나온 것들인데, 이건 오류가 아니라 모델이 "정말
애매해서 대분류까지만 판단 가능"으로 다시 판단한 경우로 보인다 — 47 → 10은
"완전히 0건"이 목표가 아니라 "순수 시험절차 조항이 더는 HF.S로 잘못 붙지
않는 것"이 목표였다는 점에서 실측상 목표를 달성한 결과로 본다. 다만 10건이
왜 여전히 맨몸인지는 다음 라운드에서 개별 확인이 필요하면 할 수 있다.

**이번 사고에서 얻은 교훈** (다음에 비슷한 배치를 돌릴 때 참고)
- `npm run tag -- --retag`는 "승인 안 된 것 전체"를 대상으로 하지 "이미
  태깅된 것만"이 아니다. 특정 조항 몇 건만 교정하고 싶으면 이 플래그를
  그대로 쓰면 안 되고, clause_id를 직접 지정하는 별도 스크립트가 필요하다
  (이번에 쓴 방식).
- Windows에서 `npm run <script>`로 띄운 프로세스는 다단 자식 프로세스
  트리라, 상위 셸만 종료해서는 실제로 안 멈출 수 있다. 정말 멈췄는지는
  DB의 `created_at`처럼 실시간으로 갱신되는 값으로 재확인해야 하고,
  안 멈췄으면 `Get-CimInstance Win32_Process`로 실제 자식 PID를 찾아
  `taskkill //F`로 트리 전체를 종료해야 한다.

### ⑤ "가죽제품은 GPC에 대응 브릭이 없다"는 판단이 틀렸다는 걸 담당자가 잡아냄

③에서 "가죽제품·예초기는 GPC 카탈로그에 대응 브릭이 없는 우산 카테고리"라고
정리했는데, 담당자가 "가죽제품이 없다는 게 이상하다"며 원본 자료(로컬 GPC
참고 JSON, n8n 워크플로 2개, `oecd_gpc_202405`/`recalls_oecd_staging` 실제
테이블)를 짚어 줬고, 다시 파보니 그 판단이 틀렸다는 게 드러났다.

**로컬 JSON은 샘플이었다** — `docs/OECD리콜등록/supabase/gpc table/
oecd_gpc_202405_rows.json`을 파싱해 보니 100건뿐이었다. 원격 테이블을 직접
조회하니(anon 키로 unrestricted 접근 가능) **5,281건**이었고, "가방"(10건),
"벨트"(10건), "지갑"(1건) 브릭이 실제로 있었다("가죽" 자체는 0건이지만
가방·지갑·벨트가 실제 지칭 대상이다). 표준 9의 6.2조(표시사항, MARKING
역할이라 지금까지 조회 입력에 안 들어갔던 조항)에도 "지갑류, 가방류,
벨트류, 신발류"라고 정확히 나열돼 있었다.

**그런데 이걸 조회 입력에 넣어도(top-50까지 넓혀도) 여전히 못 찾았다** —
"가방" 단어 하나만 조회해도 "일회용 식품포장 랩"이 1위로 나올 만큼, 이
임베딩 인덱스(`oecd_gpc_202405`, text-embedding-3-small) 자체가 짧은 한국어
명사 질의에 근본적으로 약하다는 걸 확인했다(반면 "벨트"는 단독 조회로도
브릭이 잡혔다 — 일관되게 약한 게 아니라 항목별로 들쭉날쭉하다). 별도
RPC(`match_gpc_vector`, 더 풍부한 정의·속성을 담은 `gpc_bricks` 테이블을
대상으로 하는 것으로 보임)도 비교해 봤는데 "벨트"는 더 잘 찾았지만
"압력솥"·"유아용 캐리어"는 오히려 못 찾아서, 어느 한쪽이 우월한 게 아니라
서로 다른 곳에서 실패하는 구조였다.

**담당자 판단: 새 RPC를 섞지 말고, 이 협회가 이미 실제로 운영 중인 파이프라인을
그대로 재활용하라.** 해외리콜 OECD 일일 처리 워크플로
(`docs/OECD리콜등록/(GS1) [한국리콜__OECD] OECD GPC RAG.json`,
`recalls_oecd_staging.assignedGpc{Brick,Class,Family,Segment}Code`에 저장)를
분석해 보니, 우리와 같은 `oecd_gpc_202405`/`match_documents_202405`를 쓰지만
**Brick이 후보와 명확히 안 맞으면 NONE으로 포기하지 않고 Class→Family→
Segment로 단계적으로 내려가며 답하는 설계**였다. 지금까지 우리 방식은
"Brick 확정 아니면 NONE" 이분법이라 이 완충 구간이 없었다.

**반영**: `src/lib/gpc/verify.ts`를 계층적 하향 구조로 다시 짰다 — Brick/Class/
Family/Segment 코드를 전부 하나의 enum으로 모아 LLM이 고르게 하고, 실제로
어느 계층인지·그 상위 계층 전체(Segment~Class)가 무엇인지는 모델의 자기보고를
믿지 않고 후보 목록과 대조해 서버가 직접 판정한다(`resolveHierarchy()`,
`tagging.ts`의 `validateCodes()`와 같은 원리). 담당자가 "Brick이 소속된
Class/Family/Segment도 알려줘야 한다"고 지적해서 — Brick이 확정돼도 그
상위 계층 전체를 같은 후보 행에서 그대로 채워 저장하도록 만들었다
(`recalls_oecd_staging`이 네 컬럼을 항상 같이 채우는 것과 동일).
017의 `gpc_verified_brick_code`/`gpc_verification`(이분법 전용) 대신
`018_standard_gpc_hierarchical.sql`로 `gpc_verified_level` +
`gpc_verified_{segment,family,class,brick}_{code,title}` 8개 컬럼을 새로
만들었다(017은 이미 적용된 마이그레이션이라 고치지 않고 새 파일로 이었다).

**같은 5건으로 다시 검증한 결과**: 3건은 그대로 BRICK 확정 유지(물놀이기구·
압력솥·유아용 캐리어), **예초기 날/보호덮개는 NONE 대신 SEGMENT "잔디/정원
용품"(확신 0.62)로 하향 확정** — 정확한 Brick은 아니지만 완전히 빈 답보다
훨씬 유용하다. 가죽제품은 이번에도 NONE이었는데, 후보 15건 전체가 여전히
"유아 급식/기저귀/위생/식품" 계열뿐이라 Segment 단계까지 내려가도 의류·
액세서리 쪽 후보 자체가 없었다 — 이건 이제 "설계의 한계"가 아니라 "이
임베딩 인덱스가 이 사례에서 진짜로 후보를 못 찾는다"는 정직한 결과로 볼 수
있다.

**바뀐 파일 요약**
- `src/lib/gpc/verify.ts`(신규 → 라운드 11 안에서 계층적 하향으로 재작성):
  LLM GPC 후보 검증(`verifyGpcMatch`), Brick→Class→Family→Segment 단계적 하향.
- `scripts/tag-standards-gpc.ts`: 조회 입력에 서문·정의 조항 추가, 후보 5→15개,
  검증 단계 추가, 대상 필터를 `gpc_brick_code is null`에서 `gpc_verification is null`로 변경,
  계층 전체 컬럼 저장으로 갱신.
- `supabase/migrations/017_standard_gpc_verification.sql`(신규, 라운드 11 안에서
  018로 대체): `standard.gpc_verified_brick_code`, `standard.gpc_verification` 컬럼 추가.
- `supabase/migrations/018_standard_gpc_hierarchical.sql`(신규): `gpc_verified_brick_code`
  단일 컬럼을 걷어내고 `gpc_verified_level` + `gpc_verified_{segment,family,class,brick}_{code,title}`
  로 계층 전체를 담도록 확장.
- `standard` 테이블: 시험 5건의 GPC 관련 컬럼 전체를 새 결과로 재기록(실제 DB 값 변경).
- `clause_tag`/`clause` 테이블: 조항 코드화 데이터 변경(실제 DB 값 변경, 파일 아님) —
  사고로 125건 신규 태깅, 목표였던 47건 교정(37건 코드 변경), 관련 임베딩 172건 재생성.
  이 라운드에서 새로 남은 코드 파일은 없다(교정용 임시 스크립트는 작업 후 삭제).

**다음에 할 일**
- 기준(standard) GPC 전량 배치(76건) — 계층적 하향까지 반영해 방식은 이번 라운드로
  확정된 것으로 보이지만, 표본을 좀 더 늘려(예: 15~20건) 확인한 뒤 담당자 승인으로
  전량 진행할지 결정.
- 화면(analysis 페이지)의 GPC 후보 표시를 `gpc_verified_level`/`gpc_verified_*` 계층
  정보도 함께 보여주도록 업데이트할지 검토 — 지금은 라운드 9에서 만든 화면이
  `gpc_candidates` 순위만 보여주고 검증 결과는 아직 화면에 없다.
- ~~조항 코드화 47건(HF.H/HF.S 맨몸) 재태깅~~ — 완료(47 → 10건). 남은 10건이 왜 여전히
  맨몸인지 개별 확인이 필요하면 다음 라운드에서.
- 사고로 함께 태깅된 125건이 실제로 품질 문제가 없는지 — 급하지 않지만, 검수 단계에서
  이 125건도 다른 조항과 동일하게 눈에 띄면 좋겠다(별도 표시는 안 해 뒀다).
- "가방"/"지갑"류를 짧은 한국어 명사만으로 못 찾는 이 임베딩 인덱스의 한계 —
  이 라운드에서는 KC기준 GPC 매칭만 계층적 하향으로 완충했는데, 해외리콜 OECD
  파이프라인도 같은 인덱스를 쓰므로 같은 한계를 안고 있을 수 있다. 급한 건
  아니지만 인지해 둘 필요가 있다.

---

## 라운드 12 — case_event(사고조사) GPC를 standard와 완전히 같은 구조로 통합

담당자가 "라운드 11의 GPC 검증 로직이 사고조사 GPC 코드 부여에도 쓰이는
것이지요?"라고 물어서 확인했더니 **아니었다** — `scripts/load-cases.ts`의
`processPhotos()`는 여전히 라운드 9 이전 방식(후보 5개 중 임베딩 1위를
검증 없이 그대로 `case_event.gpc_brick_code`에 확정)이었다. "최대한
공유하도록 리팩터링해 달라"는 요청을 받아, 실행 전에 계획 모드로 탐색·
설계·질문 확인을 거친 뒤 반영했다.

### 결정 사항 (질문으로 확인)

1. **저장 구조를 완전히 동일화** — `case_event`에도 `standard`와 똑같은
   12개 전용 컬럼(계층 전체)을 만들기로 했다. "가볍게(jsonb 하나만)" 안도
   있었지만, `standard` 자신이 라운드 11에서 017(단일 컬럼)→018(계층 전체)로
   이미 한 번 버린 방식이라 같은 실수를 반복할 이유가 없다는 게 이유였다.
2. **후보 개수도 5→15로 통일** — 라운드 10~11에서 실측한 "후보를 좁게
   뽑으면 정답이 밀려난다"는 근거가 사고사진 GPC에도 똑같이 적용된다고
   보고, 임베딩 RPC 호출만 늘리고(LLM 검증 호출은 그대로 1회) 통일했다.
3. **분석 화면(case_event 패널)도 이번에 함께 갱신** — `standard`의 GPC를
   화면에 새로 노출하는 건 범위 밖으로 남겼다(라운드 9~11 동안 만든
   `standard` 쪽 GPC는 지금도 화면 어디에도 안 보인다 — 별개 결정 필요).

### 무엇을 공유하게 했는가

`findGpcCandidates()`(조회)와 `verifyGpcMatch()`(검증, 계층적 하향)는
이미 `standard`·`case_event` 어느 쪽에도 쓸 수 있게 범용으로 짜여
있었는데, "후보 조회 → 없으면 NONE 처리 → 있으면 검증"이라는 오케스트
레이션 자체가 각자 따로였다(그리고 `case_event` 쪽엔 검증 단계 자체가
없었다). 이 오케스트레이션을 `src/lib/gpc/assign.ts`의 `findAndVerifyGpc()`
하나로 뽑아 두 스크립트(`scripts/tag-standards-gpc.ts`,
`scripts/load-cases.ts`)가 똑같이 부르게 했다(CLAUDE.md §9 — 여러
진입점이 같은 로직을 쓰면 `src/lib/`로 뽑는다). 후보 개수 상수
(`DEFAULT_GPC_CANDIDATE_COUNT = 15`)도 `src/lib/gpc/lookup.ts`에 하나로
모았다.

두 파이프라인이 여전히 다른 건 "조회 입력을 무엇으로 조합하는가"뿐이다 —
`standard`는 적용범위·서문·정의 조항을 모으고, `case_event`는 사고사진
비전 분석 서술(`vision.productDescription`)을 쓴다. 이건 억지로 공유
함수로 묶지 않았다(CLAUDE.md §2 — 입력 스키마 자체가 다른데 공유 함수를
만들면 오히려 부자연스럽다).

### 새 마이그레이션과 백필

`019_case_event_gpc_hierarchical.sql`로 `018_standard_gpc_hierarchical.sql`
과 같은 컬럼명 세트(`gpc_verified_level` + `gpc_verified_{segment,family,
class,brick}_{code,title}` + `gpc_candidates`/`gpc_verification` 전용
컬럼)를 `case_event`에 추가했다. 기존에 `raw_fields.gpc_candidates`에
들어 있던 값(실측: 5건 중 1건 — 아래 참고)을 새 컬럼으로 1회 백필했고,
`raw_fields`의 그 키는 지우지 않고 흔적으로 남겼다. 검증 컬럼은 과거
데이터에 채울 근거가 없어 NULL로 뒀다(재처리해야 채워진다).

### 실측 검증 — 골든 데이터를 건드리지 않고 확인한 방법

`사고조사보고서/`에 실제로 적재된 사건 5건 중 사진·GPC 데이터가 있는 건
`(26)27★ 가습기 사고조사 보고서`(case_event id 4639) 1건뿐이었는데, 이건
**절대 `--force`로 재처리하면 안 되는 시연용 골든 데이터**다(라운드 9·11
에서 두 번이나 실수로 지웠다가 복구한 바로 그 사건). 그래서 이번엔
`npm run cases:load --force`로 실물 재처리하는 대신:

1. `findAndVerifyGpc()`를 이 사건의 실제 입력(품목명 "가습기", 라운드 9
   기록에 남은 실제 비전 서술 "투명 물 수위창과 하단 조작부가 있는 흰색
   원통형 전기 가습기")으로 **DB에 쓰지 않고** 직접 호출해 새 코드 경로만
   확인했다. 결과: **BRICK "가습기 - 이동형"(10005331)을 확신 0.99로
   정확히 찾았다** — 기존에 이 사건에 저장돼 있던 값(임베딩 1위를 검증
   없이 확정한 "제습기 - 이동형(전기식)", 오답)과 다르게, 새 파이프라인은
   같은 카테고리 문제(가습기/제습기 헷갈림)를 실제로 고친다는 걸 확인했다.
2. `npm run db:push`로 019 적용 후 `select count(*) filter (where
   raw_fields ? 'gpc_candidates')` vs `count(*) filter (where gpc_candidates
   is not null)`를 비교해 백필이 정확히 1건→1건으로 됐는지 확인했다.
3. `npm run dev`로 로컬 서버를 띄우고(포트 3000이 이전 실수로 남은 좀비
   프로세스에 물려 있어 3001로 뜬 것도 확인 후 정리) 실제
   `/analysis/4639` 페이지를 그대로 열어 새 쿼리·화면 코드가 에러 없이
   렌더링되는지 확인했다 — 이 사건은 아직 재처리 전이라
   `gpc_verified_level`이 NULL인 "검증 전" 상태이고, 화면에 "LLM 검증
   전 데이터입니다"라는 새 문구와 기존 후보 5건 목록이 정상적으로
   나오는 것을 확인했다.

BRICK 이외의 분기(CLASS/FAMILY/SEGMENT/NONE 확정, 검증됨 배지)는 실제
사건 데이터로는 라이브 확인하지 못했다 — 같은 검증 함수(`verifyGpcMatch`)
자체는 이번 라운드 전체를 통해 `standard` 쪽에서 이미 여러 번(BRICK·
SEGMENT·NONE 각각) 실측 검증됐으므로 로직 자체의 신뢰도는 확보됐다고
보지만, 화면 렌더링까지 실물로 본 건 "검증 전" 분기뿐이라는 점은
투명하게 남긴다. 다음에 새 사고조사보고서가 들어와 사진이 있으면
자연스럽게 검증되는 분기를 실물로 볼 수 있다.

**바뀐 파일 요약**
- `src/lib/gpc/assign.ts`(신규): `findAndVerifyGpc()` — 조회+검증 공유 오케스트레이션.
- `src/lib/gpc/lookup.ts`: `DEFAULT_GPC_CANDIDATE_COUNT = 15` 상수 추가(공유).
- `scripts/tag-standards-gpc.ts`: `findAndVerifyGpc()` 사용으로 단순화(후보
  없음 분기 제거), 헤더 주석에서 case_event와의 대비 문구 정정.
- `scripts/load-cases.ts`: `processPhotos()`가 `findAndVerifyGpc()`를 쓰도록
  교체, `case_event` 12개 컬럼 전체 기록, 콘솔 로그·주석 갱신.
- `supabase/migrations/019_case_event_gpc_hierarchical.sql`(신규):
  `case_event`에 계층 컬럼 9개 + `gpc_candidates`/`gpc_verification` 전용
  컬럼 추가, `raw_fields.gpc_candidates` 1건 백필.
- `src/app/analysis/[caseId]/page.tsx`: 로컬 `GpcCandidate` 재선언 제거하고
  `src/lib/gpc/lookup.ts`에서 import, 쿼리에 계층 컬럼 전체 추가, GPC 패널을
  검증됨(BRICK/CLASS/FAMILY/SEGMENT)·NONE·검증 전 세 상태 모두 그리도록 재작성.
- 재처리 실물 테스트는 하지 않았다(골든 데이터 보호) — `case_event` 테이블
  값 변경 없음, DB는 마이그레이션(019)과 백필 1건만 바뀌었다.

**다음에 할 일**
- 다음에 실제 새 사고조사보고서(사진 포함)가 들어오면 그걸로 BRICK 이외
  분기(CLASS/FAMILY/SEGMENT/NONE)의 화면 렌더링을 실물로 확인.
- `cases:load`에 `--limit` 안전장치가 없는 기존 격차 — 이번 리팩터링과는
  무관하지만 여전히 남아 있다.
- `load-recalls.ts`(RECALL_DOMESTIC/RECALL_OVERSEAS)에는 아직 GPC 부여
  로직이 연결돼 있지 않다 — 필요하면 `findAndVerifyGpc()`를 그대로 가져다
  쓸 수 있다.

---

