---

## 라운드 57 — 캐시는 우리에게 손해였고, 미확정 674건은 절반이 공짜로 풀렸다

**한 일**
- 프롬프트 캐시를 파고들어, 우리가 **할인은 못 받고 웃돈만 내고 있을 수 있다**는 것을 실측으로 밝혔다(056)
- 「이 제품이 어린이제품인가」를 담당자가 확정하는 칸과 화면을 만들었다(055) — 확정하면 적용 기준이 바뀐다
- AI가 지어낸 검색용 문장(HyDE)을 분석 화면에서 볼 수 있게 했다
- 어린이제품 고시 2건과 제품안전기본법 제13조를 `docs/제품안전법제도/`에 정리했다
- 품목 미확정 674건을 뜯어보고, LLM 없이 308건을 확정했다

### 캐시를 고치려다 캐시가 손해라는 것을 알았다

라운드 56에서 「캐시 적중률 0%는 프롬프트가 캐시 걸릴 모양이 아니어서」라고 적었다. 그래서 고정 덩어리를 앞으로 보내고 변하는 값을 뒤로 미는 손질을 했다 — 적용범위 후보 블록을 제품명·사고 서술보다 앞에, 별칭 생성의 예시(무작위였던 것을 이름 순으로 고정)를 품목 목록보다 앞에.

고쳐 놓고 다시 쟀는데 **여전히 0이었다.** 그래서 응답의 `usage` 원문을 열어 봤다.

```json
"prompt_tokens_details": { "cached_tokens": 0, "cache_write_tokens": 3033 }
```

칸이 하나 더 있었다. 같은 프롬프트로 세 번 연속, 60초 뒤, 3분 뒤에 불러도 매번 **기록 3,033 · 적중 0**이었고 `prompt_cache_key`를 붙여도 같았다. 프롬프트 모양의 문제가 아니라 **적중 자체가 이 계정·모델에서 잡히지 않는다.**

그리고 단가표를 다시 보니 이것이 왜 중요한지가 드러났다.

    gpt-5.6-terra   입력 $2.00 · 캐시 적중 $0.20 · 캐시 기록 $2.50   (100만 토큰당)
    gpt-5.6-luna    입력 $0.20 · 캐시 적중 $0.02 · 캐시 기록 $0.25

**캐시 기록은 일반 입력보다 비싸다.** 적중이 한 번이라도 뒤따르면 남는 장사인데(기록 1.25 + 적중 0.1n < 1.0(n+1)은 n ≥ 1에서 성립), 기록만 하고 못 읽으면 그냥 25% 더 낸 것이다. 라운드 56이 「모르는 것을 0으로 세면 비용이 적어 보인다」고 적어 놓고, 정작 이 칸을 몰라서 같은 실수를 하고 있었다.

**다만 기록 토큰이 실제로 그 웃돈으로 청구되는지는 우리가 확인하지 못했다.** 단가표에 칸은 있는데 조건이 적혀 있지 않고, 청구서를 우리가 보지 않는다. 그래서 문맥 길이와 같은 방식으로 **범위에 담았다** — 낮은 쪽은 기록분을 일반 입력으로, 높은 쪽은 기록 단가로 계산한다. 화면(`/llm`)은 적중 0인데 기록이 쌓이면 「쓰기만 하고 읽지 못하는 중」이라고 경고한다. **계정에서 프롬프트 캐시가 켜져 있는지 확인이 필요하다.**

프롬프트 순서를 고친 것은 되돌리지 않았다. 손해가 없고, 캐시가 켜지는 날 바로 이득이 되며, 무엇보다 **재려면 먼저 세고 있어야 하기 때문**이다.

### 어린이제품 판정을 사람에게 받고, 그 판단이 기준을 바꾼다

라운드 55는 어린이제품 여부를 법정 품목군으로만 판정했다. 가이드라인 고시가 드는 결정요소(색상·포장·광고·소비자 인식·가격대)는 제품 실물을 봐야 하는 판단이라 기계가 흉내 내면 안 되기 때문이다. 그런데 **사람이 판정한 결과를 받아 적을 자리가 없었다.** 담당자가 「토끼 나무 기차」를 보고 어린이제품이라고 정해도 그 판단이 어디에도 남지 않았다.

`case_event.child_product_check`(UNCHECKED/CHILD/NOT_CHILD/UNKNOWN)와 근거 칸을 만들고, 분석 화면에 버튼을 붙였다. 확정하면 **적용 기준이 실제로 바뀐다** — 실측으로 확인했다.

    [40] "장난감(타악기)" + NOT_CHILD
      전: 공통안전기준 + 안전확인 부속서 6(완구)   →   후: 부속서 6 만
    [34] "스팀식 가습기" + CHILD
      전: KC 60335-1 + 60335-2-98                →   후: + 공통안전기준

기계 판정과 사람 판정이 어긋나면 사람이 이긴다. 기계는 품목군으로 미루어 짐작한 것이고 사람은 실물을 봤기 때문이다.

### 미확정 674건 — 절반 가까이가 「이미 사전에 있는데 안 붙은 것」이었다

무엇이 막고 있는지부터 셌다.

    미확정 674건 (해외 리콜 661 · 사고 13) · 품목명조차 없는 것은 3건뿐
    서로 다른 품목명 493종 — 이만큼만 풀면 미확정이 사라진다
    용어 사전에 이미 대응이 있는데 미확정인 것 34건

마지막 줄이 실마리였다. 열어 보니 두 종류다. 「자전거 헬멧」처럼 **의미검색(SEMANTIC)으로 사전에 쌓인 대응**은 쓸 수 있는데, 그 사건들이 사전에 항목이 생기기 **전에** 처리돼 그대로 남아 있었다. 「LED등기구」처럼 LLM 제안(미검수)인 것은 담당자 확정 전에는 쓰지 않기로 한 규칙(라운드 42) 그대로다.

그래서 LLM을 끄고 한 바퀴 돌렸다 — 사전 조회와 원문검색만 쓰므로 **비용이 0이다.**

    확정 308 / 미확정 486 / 전체 794
      적용범위 검색 163 · 용어 사전 144 · 품목명 일치 1

**308건이 공짜로 풀렸다.** 남은 486건에 의미검색(LLM)을 돌리는 중이고, 그 결과와 비용은 다음 라운드에 적는다. LLM 제안 미검수로 막힌 것은 코드가 아니라 검수가 풀 문제다.

### 곁다리 셋

**HyDE 문장을 보여 준다.** 051부터 저장만 하고 화면에 내보내지 않았는데, 이 문단이 **어떤 조항이 후보로 떠오르는지를 바꾸므로**(재현율 23.8% → 25.2%) 되짚을 때 빈칸이면 경로를 절반만 보는 셈이다. 접어 둔 채로 넣고, 열기 전에 「AI가 지어낸 문장이며 기준 원문이 아니다」를 먼저 밝힌다.

**어린이제품 고시 둘을 정리했다.** 「개별안전기준이 있는 공급자적합성확인대상 어린이제품」 고시의 부속서는 14종인데 우리가 가진 것은 5종이고, 「부적합시 처리기준」 고시는 시험항목마다 경·중·최중 결함 등급을 매긴다 — 유해원소·프탈레이트가 거의 다 최중결함이라, 공통안전기준을 빠뜨리면 가장 무거운 등급을 통째로 못 보게 된다는 뜻이다.

**국내 유통 확인은 별도 프로젝트로 못박았다.** 제품안전기본법 제13조 제3항이 사업자에게 즉시 보고 의무를 지우는 그 판단인데, 답하려면 대형 온라인쇼핑몰의 유통 상품을 모니터링해야 한다 — 다루는 자료도 규모도 이 저장소의 일과 다르다. 이쪽은 **받아 적는 칸과 화면까지만** 갖추고 판정 로직을 끌고 오지 않는다.

---

## 라운드 56 — AI를 관리 대상으로 다룬다 (전용 화면 · 캐시 단가 · 스키마 전수 점검)

**한 일**
- 🤖 AI 사용과 비용 화면을 따로 만들고(`/llm`), 좌측 메뉴를 네 묶음으로 다시 나눴다
- 부르는 자리 목록을 코드 한 곳(`src/lib/llm/catalog.ts`)에 두어 화면과 문서가 같은 것을 보게 했다
- 캐시 적중 토큰과 「무엇을 처리하다 부른 것인지」를 기록하기 시작했다(054)
- 사진 분석을 코드 부여와 갈라 따로 세기 시작했다
- 구조화 출력 열 자리를 전수 점검해 셋을 고쳤다
- `.env.local`에 단가가 아예 없어 모든 금액이 「단가 미등록」이던 것을 채웠다

### 「다 된 건가요」라는 질문이 세 곳을 열었다

담당자가 LLM 모델 관리가 끝난 것인지 물었다. 코드를 훑어보니 골격은 서 있었다 — 모델명은 환경변수로 갈리고, 만든 결과에 모델명이 함께 저장되고, 부르는 자리 전부가 `llm_call`에 기록된다. 그런데 셋이 어긋나 있었다.

**하나. 사진 분석 비용을 셀 수 없었다.** 비전 호출이 `purpose='tagging'`으로 기록돼 코드 부여와 한 줄로 합쳐졌다. 둘 다 같은 급의 모델(terra)을 쓰므로 모델명으로도 갈라낼 수 없다. 사진은 호출 한 번이 비싼데, 얼마인지 물어도 답할 수 없는 상태였다.

**둘. 「이 사건 한 건에 얼마 들었나」에 답할 수 없었다.** 052가 `llm_call.case_id`·`standard_id` 칸을 만들어 두었는데 삽입문이 그 칸을 넣지 않아 계속 비어 있었다. 칸이 있다는 사실만으로는 아무것도 되지 않는다.

**셋. 화면의 금액이 전부 「단가 미등록」이었다.** `.env.local`에 `LLM_PRICES`가 아예 없었다. 다행히 이 설계가 값어치를 했다 — 모르는 단가를 0으로 세지 않기로 해 두었기 때문에 화면이 「$0」이 아니라 「모른다」고 말하고 있었고, 그래서 빠졌다는 사실이 드러났다. 채우고 나니 최근 30일이 **호출 926건 · $4.46~$8.45**로 처음 숫자가 됐다.

### 캐시된 입력이 일반 입력의 1/10이라는 것을 이제야 셈에 넣는다

2026-09-08 단가표를 다시 확인하다 알았다. 같은 프롬프트 앞부분을 다시 보내면 그 부분은 「cached input」으로 1/10 단가에 청구된다(terra 기준 $2.00 → $0.20). 이 체계는 입력 토큰이 출력의 스무 배가 넘으므로 이 차이가 금액을 좌우한다.

그런데 **재 보니 적중률이 0%였다**(호출 926건 · 입력 1,825,769토큰). 캐시는 프롬프트의 앞부분이 글자 그대로 같을 때만 걸리는데, 우리 프롬프트는 품목명·사고 서술처럼 매번 다른 값이 앞쪽에 온다. 그러니 이 0%는 「캐시가 고장났다」가 아니라 **「아직 캐시가 걸릴 모양으로 짜지 않았다」**는 뜻이고, 고치려면 고정 덩어리(코드북 코드 목록, 후보 기준의 적용범위 원문)를 앞으로 보내고 매번 달라지는 값을 뒤로 밀어야 한다. 그 개편은 다음 일이고, **효과를 숫자로 보려면 먼저 세고 있어야 하므로** 기록부터 붙였다.

### 구조화 출력 전수 점검 — 셋이 「부탁만 하고」 있었다

열 자리를 하나씩 열어 보았다. 일곱은 고를 대상을 enum으로 못박아 두어 없는 값을 만들어 낼 수 없었다. 나머지 셋이 문제였다.

**품목 기준 제안**은 기준 이름을 자유 문자열로 받고, 목록에 없는 이름이면 코드가 조용히 버렸다. 프롬프트에는 "목록에 없는 기준명을 지어내지 않는다"라고 적혀 있다 — 부탁은 하고 있었던 셈이다. 문제는 이름을 한 글자만 다르게 적어도 그 제안이 통째로 사라지는데 **버린 건수를 아무도 세지 않았다**는 것이다. 조용히 사라지는 고장은 화면에 아무 흔적도 남기지 않는다.

**검색어 생성**도 대상 품목명이 자유 문자열이었고, **사진 분석**은 쪽 번호를 정수면 무엇이든 받았다. 쪽 번호가 어긋나면 사진 설명이 엉뚱한 쪽에 붙는데, 사람이 사진과 대조하지 않는 한 드러나지 않는다.

셋 다 enum으로 못박고, 그래도 어긋난 답이 오면 몇 건을 버렸는지 로그로 남긴다. **「프롬프트로 부탁한다」와 「스키마가 못 하게 만든다」는 다르다** — 앞은 대체로 지켜지고 뒤는 항상 지켜진다.

확신도(confidence)에는 스키마로 범위를 걸 수 없다는 것도 확인했다. OpenAI 구조화 출력이 `minimum`·`maximum`을 지원하지 않는다. 받는 쪽에서 0~1로 자르는 지금 방식이 맞다.

### 화면을 운영에서 떼어 낸 이유

담당자가 「운영과 묶이긴 하지만 LLM 현황 메뉴를 따로 만드는 게 낫지 않나」라고 물었고, 만들고 보니 그 판단이 맞았다. 운영은 「지금 제대로 돌고 있는가」를 보는 자리다. AI는 성격이 다르다 — 무엇을 물어보고 있고, 답을 어떻게 받고, 근거가 어디 남고, 돈이 얼마나 드는지는 상태 점검이 아니라 **관리 대상**이다. 운영 화면 한 절에 눌러 담으면 표 하나로 줄어들어 정작 「어디에 어떤 모델을 쓰나」에 답할 수 없다.

새 화면은 일곱 절이다 — 한눈에(횟수·비용·캐시 적중률), AI를 부르는 자리, 용도별 비용, 모델별 비용, 날짜별 추이, 지금 쓰고 있는 단가, 최근 호출 스무 건. 운영 화면에는 요약 한 줄과 링크만 남겼다.

**부르는 자리 표는 코드에서 온다.** 손으로 적은 표는 코드가 바뀔 때 조용히 틀린다 — 이번에 발견한 사진 분석 문제가 정확히 그랬다(문서에는 열 번째 자리로 적혀 있는데 기록은 태깅과 합쳐져 있었다). 그래서 `catalog.ts`에 자리 목록을 두고 화면이 그것을 읽는다. 모델 이름은 실행 시점에 환경변수에서 읽으므로 배포에 실제로 설정된 값이 보인다.

### 라운드 55에서 미뤄 둔 재확정을 돌렸다

라운드 55는 「`npm run cases:rescope`를 돌리면 화면 근거에도 반영되지만 LLM 비용이 있어 이번에는 돌리지 않았다」로 끝냈다. 담당자가 돌리라고 해서 두 번에 나눠 돌렸다.

먼저 용어 사전으로 붙은 469건을 LLM 없이 다시 태웠다(값싼 경로만 쓰므로 공짜다). 확정 469 · 미확정 0. 이어서 적용범위 원문검색으로 붙은 1,382건을 LLM을 켠 채 태웠다 — 이쪽은 후보가 둘 이상이면 모델에게 되물어 거르는 단계가 있어서, 끄고 돌리면 그 거르기가 빠져 결과가 되레 넓어진다.

    확정 1,182 / 미확정 200 / 전체 1,382
      적용범위 검색 1,043 · 적용범위 의미 검색 100 · 용어 사전 39

**미확정 200건은 낡은 답이 지워진 것이다.** `--redo` 는 다시 봐서 못 찾으면 예전 답을 지운다(라운드 54에서 정한 규칙). "안전 조끼 → 전기다리미" 같은 낡은 답을 남겨 두면 담당자가 그것을 근거로 검토하게 되므로, 이 체계에서 **틀린 답은 빈칸보다 나쁘다**고 보기 때문이다.

돌린 결과 **공통안전기준이 근거에 적힌 사건이 72건 → 911건**(해외 리콜 899 · 사고보고서 12)이 됐다.

이 배치가 쓴 돈도 새 화면이 그대로 보여 준다 — **최근 24시간 1,530회 · 입력 2,939,764토큰 · $7.20~$13.62**(적용범위 후보 거르기 930회 $4.63~$8.76 · 의미검색 300회 $2.55~$4.85 · 임베딩 300회 $0.01). 「배치 한 번에 얼마 드는가」를 처음으로 숫자로 답할 수 있게 됐고, 줄일 곳도 같이 보인다 — 입력이 출력의 23배이므로 **넣는 원문의 길이**가 값을 정한다.

### 메뉴를 네 묶음으로

전에는 번호 없는 항목 다섯이 한 줄로 이어져 있었다. 성격이 제각각인데 생김새가 같으니 산출물인 「KC안전기준 개선 요인」과 시스템 화면인 「운영」이 같은 무게로 보였다. AI 화면 자리를 정해야 해서 이 참에 나눴다 — **업무 흐름(1~3) · 산출물 · 사전과 참고 · 시스템**. 🤖 표시의 뜻(그 화면에서 AI가 관여한다)도 메뉴 아래에 적고 AI 화면으로 링크했다. 표시만 있고 뜻이 어디에도 없으면 장식으로 읽힌다.

---

## 라운드 55 — 어린이제품 공통안전기준이 절반은 빠지고 절반은 엉뚱한 데 붙어 있었다

**한 일**
- 「어린이제품 공통안전기준」·「어린이제품 가이드라인」 고시 원문을 법령 조회로 받아 `docs/제품안전법제도/`에 정리했다
- 어린이제품으로 확정된 사건에는 어느 경로로 확정됐든 공통안전기준을 함께 붙이게 고쳤다(`withChildCommon`)
- 어린이제품이 아닌 품목 16종에 잘못 걸려 있던 공통안전기준 연결을 뗐다(053)
- LLM 모델 관리와 해외 리콜↔KC기준 대조 기능의 현재 상태를 코드와 DB 로 확인했다

### 담당자가 짚은 한 줄이 정확히 맞았다

「어린이라고 된 제품들은 어린이제품 공통안전기준을 적용해야 한다」는 말이었다. 추측하지 않고 고시 원문을 받아 확인했더니 「어린이제품 공통안전기준」 1. 적용범위의 비고가 그대로 적고 있었다 — "개별 안전기준이 있는 어린이제품은 개별 안전기준과 어린이제품 공통안전기준을 모두 적용한다"(「어린이제품 안전 특별법 시행규칙」 제2조 제1항~제3항). 즉 부속서만 보는 것은 규정 위반이 아니라 **요구사항의 한쪽을 통째로 못 보는 일**이다.

그리고 우리 체계는 이 규칙을 두 방향으로 어기고 있었다.

**하나 — 어린이제품인데 안 붙었다.** 공통안전기준은 `standard_applicability` 의 `COMMON` 관계로만 붙었고, 그 관계를 쓰는 경로는 「등록된 품목 이름·별칭이 정확히 일치할 때」 하나뿐이었다. 실제로 그 경로를 탄 사건은 2,405건 중 47건(1.9%)이다. 나머지는 용어 사전(468건)과 적용범위 원문검색(1,397건)으로 확정되는데, 그 경로에는 공통기준을 더하는 자리가 없었다. 어린이제품 부속서를 근거로 확정된 사건을 세어 보니 **593건(사고 5 · 해외 리콜 588)이 공통안전기준 없이 분석되고 있었다.** 이미 붙어 있던 것은 72건이다.

이것이 왜 중대한가 하면, 유해원소 용출·함유량, 프탈레이트계 가소제, 자석 삼킴(자속 지수 50 kG²mm²), 작은 부품 실린더 같은 요구사항이 **개별 부속서가 아니라 공통기준에만** 있기 때문이다. 빠지면 그 사고 유형은 "대응하는 국내 기준이 없다"로 보인다 — 기준 사각지대가 아닌데 사각지대처럼 보이는, 가장 나쁜 종류의 오답이다.

**둘 — 어린이제품이 아닌데 붙었다.** `scripts/seed-scopes.ts` 가 이름이 붙은 기준 33종을 품목으로 만들면서 `category` 를 전부 `'어린이제품'` 으로 박아 넣고 있었다. 법정 품목 대응표로 다시 판정하니 33종 중 16종이 생활용품이었다 — 가스라이터·우산 및 양산·디지털도어록·건전지·헬스기구·휴대용 예초기의 날 및 보호덮개 …. 이들 전부에 어린이제품 유해원소·프탈레이트 시험이 적용 근거로 걸려 있었다. 공통안전기준의 적용범위(만 13세 이하 어린이가 사용하거나 어린이를 위하여 사용되는 물품)를 정면으로 벗어난다.

    고치기 전   품목 33종 전부 '어린이제품' · COMMON 연결 32건
    고친 뒤     어린이제품 17종 / 생활용품 16종 · COMMON 연결 16건 (잘못 걸린 것 0건)

### 어린이제품인지 어떻게 판정하는가 — 코드가 하지 않는 일을 먼저 정했다

「어린이제품 가이드라인」 고시는 판정 요소로 색상(빨강·파랑 등 밝은 원색), 캐릭터 장식, 포장·광고 대상, 소비자 인식, 가격대까지 든다. 제품 실물과 판매 맥락을 봐야 하는 것들이라 **코드가 흉내 내면 그럴듯한 오답이 나온다.** 그래서 코드는 이미 확정된 법정 품목 정보(`product_taxonomy.item_group`, 담당자가 확정한 `taxonomy_standard`)로만 판정하고, 그 둘로 닿지 않을 때만 표시명에 「어린이·유아·아동」이 들어가는지 본다 — 「어린이용 인라인스케이트」가 실제로 대응표에 없어서 넣은 보강이다.

한 품목이 어린이제품과 생활용품 양쪽에 걸리면(「방한용·패션용·스포츠용 마스크」) 어린이제품으로 본다. 붙여서 생기는 손해는 담당자가 시험항목 한 줄을 더 보는 것이고, 빼서 생기는 손해는 유해원소 항목을 통째로 놓치는 것이다.

### 고친 뒤 실측

    "유아용 의자"        부속서 8 + 공통안전기준          (전과 같음, 품목 경로)
    "전기요"             KC 60335-2-17 + 제1부 + 유아용 섬유제품 + 공통안전기준   ← 새로 붙음
    "가스라이터"         부속서 5 만                       ← 공통안전기준 떨어짐
    "우산 및 양산"       부속서 20 만                      ← 공통안전기준 떨어짐
    "가습기"             KC 60335-2-98 + 제1부             (어린이제품 아님 — 변화 없음)

「전기요」가 이 고침을 한눈에 보여 준다. 담당자 사전이 전기용품(KC 60335-2-17)과 어린이제품(유아용 섬유제품)을 함께 지정해 둔 품목인데, 전에는 전기용품 제1부만 따라붙고 어린이제품 쪽 공통기준은 빠져 있었다.

분석 실행이 쓰는 `standardsForCase` 도 같은 함수를 타므로, 사건을 다시 확정하지 않아도 다음 분석부터 반영된다. 다만 `case_event.scope_evidence` 에 적힌 근거 문장은 옛것이라, 화면에 「공통안전기준 포함」이 보이게 하려면 `npm run cases:rescope` 를 한 번 돌려야 한다(LLM 비용이 있어 이번에는 돌리지 않았다).

### 곁다리로 확인한 것 둘

**LLM 모델 관리는 설계대로 서 있다.** 모델명은 코드에 박히지 않고 환경변수 여섯 개(`OPENAI_BULK_MODEL`·`ESCALATE`·`RERANK`·`VISION`·`EMBEDDING`)로 갈리며, 만든 결과에 모델명이 함께 저장된다(`clause_tag.tagging_model`·`clause.embedding_model`·`match_run.rerank_model`). 부르는 자리 열 곳 전부가 `llm_call` 에 용도·모델·토큰·성공 여부를 남기고 있었다. **한 곳이 어긋난다** — 사고 사진 분석(비전)이 `purpose='tagging'` 으로 기록돼 코드 부여와 한 줄로 합쳐진다. 둘 다 상위 모델을 쓰므로 모델명으로도 갈라낼 수 없어, 사진 분석에 얼마 쓰는지는 지금 셀 수 없다.

**해외 리콜↔KC기준 대조는 이어지는 비율 자체가 낮다.** 리콜 2,334건 중 공고에 위반 표준이 적힌 것이 658건(28.2%)이고, 그 번호가 우리 보유 기준과 완전히 같아 대조된 것은 **81건(3.5%)** 이다. 전기용품 KC 기준이 IEC 번호를 그대로 쓰기 때문에 EU 의 "EN 60598-2-12" 와 우리 "KC 60598-2-12" 는 이어지지만, EN 71(완구)·GB 4706(중국 가전)은 우리 어린이제품 기준과 번호 체계가 아예 달라 번호로는 닿지 않는다. 어린이제품 쪽 리콜이 이 대조에서 통째로 빠진다는 뜻이고, 이번에 붙인 공통안전기준이 그 빈자리를 일부 메운다 — 유해원소·자석·작은 부품은 번호가 아니라 위해요인으로 이어지기 때문이다. 한편 국내 유통 동일성(`domestic_check`)은 2,334건 전부 `UNCHECKED` 다. 제품안전기본법 제13조 제3항의 사업자 보고 의무가 걸린 판단이라 시스템이 추정하지 않고 담당자 확인값을 받도록 해 둔 칸인데, 아직 아무도 채우지 않았다.

---

## 라운드 54 — 담당자 질문이 결함 다섯을 찾아냈다 (붙는 기준 83종 → 10종)

**한 일**
- 프로세스·용어 설명 문서를 새로 썼다(`docs/전체_프로세스와_용어.md`)
- 적용범위 의미검색이 저장된 임베딩을 쓰게 고쳤다 — 매번 다시 만들고 있었다
- 원문검색을 「구 먼저, 조각은 나중」으로 바꾸고, 여럿 걸리면 모델에게 되묻게 했다
- 제외어를 API 로 함께 내보낸다(`/api/keywords/stopwords`)
- 리콜 수집 조건(「승인」 + 「관리 대상」)을 실측으로 확인하고 근거를 코드에 남겼다
- AI 호출 여덟 곳을 감사하고, 근거가 저장되지 않던 한 곳을 고쳤다(051)

### 질문이 곧 진단이었다

담당자가 문서를 읽고 물은 것이 넷이다. 하나하나가 실제 결함을 가리켰다.

**"모두 빗나가면 LLM 을 부를 게 아니라 중간에 점검해야 하지 않나?"** 맞았다. 다섯 갈래 중 ④ 원문검색만 아무 검증 없이 최대 10종을 담고 있었다. 정작 더 조심스러운 ⑤ 의미검색은 모델에게 되물어 확인하는데, 훨씬 넓게 걸리는 쪽이 그냥 통과한 것이다. 순서가 뒤집혀 있었다.

**"안전은 몰라도 전기는 빼면 안 된다. 전기요·전기자전거에서 쓰인다."** 이것도 맞았고, 재 보니 고칠 곳이 다른 데 있었다.

    낱말 하나가 걸리는 기준 (적용범위가 있는 73종 중)
      안전 56종(77%) · 사용 56종(77%) · 기기 41종(56%) · 전기 40종(55%) · 어린이 17종(23%) · 자전거 3종(4%)

    같은 품목명을 두 방식으로 던지면
      "안전 조끼"     조각 OR: 56종  /  전체 구만: 0종
      "전기 자전거"   조각 OR: 42종  /  전체 구만: 1종   ← 정확히 맞는 1종
      "전기요"        조각 OR:  0종  /  전체 구만: 0종

「전기」는 단독으로는 40종에 걸리지만 「전기 자전거」라는 **구 전체로 던지면 정확히 1종**을 집는다. 즉 낱말이 문제가 아니라 **구를 쪼개 OR 로 붙이는 것**이 문제였다. 낱말 목록을 만들어 관리하지 않기로 한 이유이기도 하다 — 목록은 언젠가 반드시 빼면 안 되는 말을 뺀다.

**"적용범위는 이미 임베딩돼 있는데 값싸게 못 쓰나?"** 이건 뼈아팠다. 라운드 50 에서 `standard.scope_embedding` 에 저장해 두고 76종 전부 채워 놓았는데, ⑤ 의미검색은 그것을 **안 쓰고 부를 때마다 기준 73종의 적용범위 27,985자를 통째로 다시 임베딩**하고 있었다. 대응표를 만드는 `link-standards.ts` 만 저장된 것을 쓰고 있었다. 라운드 50 에서 컬럼을 만들면서 이쪽을 함께 고치지 않은 것이다. 리콜 471건에 돌렸다면 1,300만 자를 헛되이 임베딩할 뻔했다.

**"hybrid search 와 HyDE 는 어디에 쓰이나?"** 문서에 그 구분이 없었다. 검색이 두 군데에서 일어나는데 찾는 대상이 다르다 — 품목 확정은 「기준 76종 중 몇 종」을, 조항 검색은 「조항 13,501개 중 후보」를 찾는다. 하이브리드와 HyDE 는 뒤쪽에만 쓴다. 품목 확정에 HyDE 를 쓰지 않는 것은 의도된 것이다. 품목을 틀리면 뒤가 전부 어긋나므로 지어낸 문장을 근거로 삼지 않는다.

### 고친 뒤 실측

원문검색으로 5종 이상 붙어 있던 리콜 품목 12건을 다시 태웠다.

    원문검색이 붙인 기준 합계   83종 → 10종

      "2구 범용 벽면 콘센트"   8종 → 1종        "AC 플러그"       5종 → 1종
      "USB 전원 어댑터"       5종 → 1종        "USB 충전기"      7종 → 1종
      "DIY 수제 식품 장난감"   8종 → 2종        "Balbali 전기 주전자"  10종 → ⑤ 의미검색
      "LED 조명 기구"         8종 → 못 찾음     "CR2032 배터리"    5종 → 못 찾음

「성인 휴대용 침대 난간」은 10종에서 못 찾음이 됐는데 이것이 정답이다 — 성인용이라 유아용 의자·침대 매트리스 기준에 해당하지 않고, 모델이 그렇게 판정했다. 「어린이 자전거 헬멧」은 *"헬멧을 별도로 규정한 기준은 없다. 어린이용 자전거 기준은 자전거 자체에만 적용된다"* 고 근거를 댔다.

**대신 「못 찾음」이 는다.** 품목이 안 붙으면 분석이 시작되지 않으므로 확정률은 떨어진다. 그래도 이 방향이 맞다 — 이 체계에서 틀리는 것은 안 고르는 것보다 나쁘다(v0.7 §3.2). 「LED 조명 기구」가 못 찾음이 된 것도, 전에 엉뚱한 8종을 붙이던 것보다 낫다. 그 품목은 대응표(다리 2)로 풀어야 하는데 그쪽이 확정 0건이라 막혀 있다.

### 리콜 수집 조건 — 이미 맞았고, 다만 읽을 수가 없었다

담당자가 「관리대상 Yes && 승인상태 승인」일 때만 불러오는지 물었다. 확인해 보니 조건은 이미 그렇게 걸려 있었다. 문제는 코드에서 그 칸 이름이 `korea_relevance` 라 「관리 대상」인지 알아볼 수가 없다는 것이었다. 그래서 원본에서 직접 세어 근거를 주석에 남겼다.

    승인 · 관리대상 O   2,336건   ← 우리가 받아 오는 것
    승인 · 관리대상 X       0건
    반려 · 관리대상 O       4건   ← 두 칸이 따로 움직인다는 증거
    반려 · 관리대상 X   2,634건

「승인이면서 관리대상 아님」이 0건이라 지금은 두 조건이 겹쳐 보이지만, **반려인데 관리대상이 켜진 것이 4건 있다.** 관리대상만으로 거르면 그 4건이 섞여 들어온다. 두 조건을 AND 로 함께 걸어야 하는 근거다. 우리 쪽 적재는 2,334건으로 원본과 2건 차이라 수집은 최신이다.

### AI 답변을 감사했다 — 여덟 곳 중 일곱은 멀쩡했고 한 곳이 비어 있었다

담당자가 물었다. **"구조화 출력이 맞는지, 판단 근거를 제시하도록 되어 있는지, 그 근거를 DB 에 저장하는지."** 여덟 군데를 하나씩 봤다.

구조화 출력은 전부 맞았다. `response_format: json_schema` 에 `strict: true` 라 모델이 형식을 어길 수 없고, 기준을 고르는 자리는 후보 id 를 `enum` 으로 못박아 **없는 기준을 지어내는 것이 구조적으로 불가능**하다. 코드 부여는 `evidence_span`(원문에서 그대로 인용한 구간)을 필수로 두어 요약만 쓰고 근거를 빠뜨리는 일을 막고 있었다.

근거 저장도 일곱 곳은 되어 있었다 — 의미검색·후보 거르기·품목 제안은 `scope_term.evidence` 와 `case_event.scope_evidence` 로, GPC 검증은 `standard.gpc_verification` 으로, 재채점은 `match_result.rerank_reason` 으로, 코드 부여는 `case_tag`·`clause_tag` 로, 법정 품목 잇기는 `taxonomy_standard.evidence` 로 간다.

**빈 곳은 HyDE 하나였다.** 그런데 `hyde.ts` 는 돌려주는 값에 이렇게 적어 두었다 — *"화면에 보이지 않는다 — **되짚기 위해** 돌려줄 뿐이다."* 정작 부르는 쪽(`run.ts`)은 임베딩만 꺼내 쓰고 그 문단을 버리고 있었다. 적어 둔 의도가 지켜지지 않은 것이다.

이게 왜 문제인가. HyDE 는 사고 서술로 「답에 해당할 법한 조항」을 지어내 의미 갈래의 질의로 쓴다. 즉 **어떤 조항이 후보로 떠오르는지를 바꾼다**(재현율 23.8% → 25.2%). 문단이 남지 않으면 담당자가 "왜 이 조항이 나왔지?"를 되짚을 때 그 자리가 빈칸이 된다. 031 이 `query_text`·`query_keywords` 를 남긴 것과 같은 까닭이다.

`match_run.hyde_text` 를 만들어 남기게 했다(051). 다만 **지어낸 문단은 기준 원문이 아니다.** 화면에 그대로 보이면 실제 조항으로 오해할 수 있으므로, 보여 줄 때는 「AI 가 지어낸 검색용 문장」임을 밝혀야 한다.

### 사전을 넘겨받는 쪽이 우리보다 넓게 매칭하고 있었다

지난 라운드에 "다른 시스템은 `/api/keywords/export` 를 그대로 부르면 된다"고 안내했는데 불충분했다. 우리 코드는 검색어를 쓸 때 네 조건을 함께 건다 — 정확 일치만 · 여러 품목에 걸친 말 제외 · 확정된 것만 · **제외어 제외**. 그런데 제외어는 API 로 나가지 않았다. 받는 쪽은 마지막 규칙을 모른 채 쓰게 된다. 제외어 내보내기를 만들고 README·화면에 짝으로 알렸다.

### 실측 확인
- `npx tsc --noEmit` · `npx next build` 통과, 새 라우트 `/api/keywords/stopwords` 등록 확인
- 원문검색 표본 12건에서 붙는 기준 83종 → 10종
- 저장된 임베딩이 기존 코드와 **같은 글**(`display_name` + 적용범위)로 만들어진 것을 확인하고 바꿨다 — 후보 집합이 달라지지 않는다
- 리콜 재확정 회귀 없음(값싼 경로만, 새로 들어온 리콜 3건 확정)
- 051 적용 후 사건 4639(가습기)를 실제로 분석해 `hyde_text` 가 354자로 저장되는 것을 확인했다 — *"가열식 가습기는 정상 사용 상태 및 물 부족, 급수 중단, 수위감지부 고장 … 발열부가 과열되어 화재, 발연 또는 외함의 위험한 온도상승이 발생하여서는 안 된다"*
- **제외어 표에 파싱 찌꺼기가 섞여 있다** — 124개 중 23개가 검색 문법·메모(`+국가, +정부`, `*목표 : …`)이고 숫자 코드도 다수다. 엑셀에서 읽어 들일 때 걸러지지 않았다. 매칭에는 해가 없지만(그런 글자와 정확 일치할 품목명이 없다) 밖으로 내보내는 목록이므로 다음에 정리할 거리로 남긴다

## 라운드 53 — 리콜에 품목은 이미 붙어 있었다, 진짜 막힌 곳은 검수였다

**한 일**
- 리콜을 품목 재확정 배치에서 빼놓고 있던 필터를 고쳤다(`rescope-cases.ts`)
- 큰 묶음에 LLM 을 함부로 부르지 않도록 `--no-llm` 스위치를 달고, 값싼 경로만으로 18건을 붙였다
- 검수 화면에 키보드 단축키를 붙였다(`j·k` 이동 · `a` 확정 · `r` 반려)

### 내가 처음에 잰 숫자가 틀렸다

담당자가 준 우선순위는 셋이었다 — 검수(대응 502건·검색어 4,943개), 사전 공유, 리콜 1,553종에 법정 품목 붙이기. 3번을 보려고 DB 를 재고 **"리콜 2,299건 중 품목이 붙은 것은 40건뿐"** 이라고 보고했다. 틀린 숫자였다.

`product_scope_id` 는 「등록된 품목표(`product_scope`)에 이름이 있는 것」만 세는 칸이다. 실제 품목 확정은 `scope_evidence` 에 기록된다. 그것으로 다시 세니 그림이 완전히 달랐다.

    적용범위 원문검색  1,406      용어 사전 358      없음 477
    등록된 품목          40      의미검색(LLM) 12   검색어 사전 6

**품목은 이미 79%(1,822/2,299)에 붙어 있었다.** 배치를 돌리기 전에 다시 재 보지 않았으면 "리콜에 품목이 없다"는 틀린 전제 위에서 작업할 뻔했다.

### 그런데 담당자가 말한 「법정 품목」에 닿는 것은 6건뿐이었다

품목이 붙었다고 다 같은 것이 아니다. 담당자가 3번에서 말한 것은 **법정 품목**(고시가 정한 623종)이고, 거기로 가는 길은 검색어 사전 경로 하나다. 그것이 6건이었다. 나머지 1,406건을 채운 적용범위 원문검색은 품목명 낱말이 적용범위 어딘가에 나오는 기준을 최대 10종까지 담는 넓은 그물이라, 품목별 집계의 축으로 쓸 수 있는 것이 아니다.

그래서 그 다리만 따로 쟀다. 검수를 마치면 무엇이 열리는지 숫자로 보고 싶었다.

    리콜 2,299건 · 품목명 1,553종

      지금 (확정된 검색어만)        리콜  99건 · 품목명  35종
      검수를 마쳤다면 (미검수 포함)   리콜 362건 · 품목명 162종     3.7배

      법정 품목 → 기준 대응표        확정 0 / 285

**둘째 다리가 확정 0건이라 리콜에서 기준까지 가는 길은 지금 완전히 막혀 있다.** 3번은 1번이 끝나야 열린다. 라운드 49에서 겪은 것과 같은 모양이다 — 결선은 옳았는데 병목이 다른 곳에 있었다.

### 리콜은 배치에서 아예 빠져 있었다

`rescope-cases.ts` 가 `source_type = 'ACCIDENT'` 로 박혀 있었다. 품목명도 코드도 다 있는데 품목만 없는 사건을 다시 붙이라고 만든 배치인데, 정작 그 상태에 있던 리콜이 조건에서 원천 제외되고 있었다. `resolveProductScope()` 자체는 이미 리콜에도 쓰는 함수다(최초 코드화 때 `tag-cases.ts` 가 `--source-type RECALL_OVERSEAS` 로 부른다). 막고 있던 것은 필터 한 줄이었다.

### LLM 을 그냥 켜지 않은 이유

고치고 나서 바로 2,299건에 돌리려다 멈췄다. 값싼 경로가 모두 빗나가면 마지막에 임베딩과 LLM 을 부르고, 붙으면 `scope_term` 에 **미검수로 새로 쌓는다.** 한 건이면 알맞지만 수백 건이면 이야기가 다르다.

1. 검수 대기가 이미 5,445건(검색어 4,943 + 대응 502)인데 거기에 더 얹는다
2. 리콜 품목명은 "녹색 외계인 의상", "물놀이 공(홍학, 유니콘, 게 무늬)" 같은 번역어다. 억지로 고르면 틀린다 — 이 체계에서 틀리는 것은 안 고르는 것보다 나쁘다(v0.7 §3.2)
3. 무엇보다 **검수만 하면 LLM 없이 362건이 열린다.** 더 싸고 정확한 길이 있는데 비싼 길부터 갈 이유가 없다

그래서 `--no-llm` 스위치를 달고 값싼 경로만 돌렸다. 대상 489건 중 18건이 붙었고 전부 용어 사전 경로였다. LLM 경로는 담당자 판단을 받아 따로 정하기로 남겨 둔다.

### 검수를 대신하지 않고 빠르게만 했다

담당자가 "검수를 빠르게 하는 보조 기능"을 원했다. 자동 승인은 만들지 않았다 — 법정 품목 → 기준 대응이 틀리면 그 품목으로 이어지는 **모든** 사고·리콜에 엉뚱한 기준의 시험이 근거로 제시된다. 한 번에 여러 건을 넘기는 장치는 이 무게에 맞지 않는다.

대신 손동작만 줄였다. 행을 옮기고(`j`·`k`) 확정·반려하는(`a`·`r`) 단축키다. 한 건에 2초씩만 줄여도 5,445건이면 3시간이다. 판단은 한 건에 한 번씩 그대로 남는다.

만들면서 걸린 것은 **확정하면 그 행이 목록에서 빠진다**는 점이었다. 포커스가 사라진 행에 있었으므로 그대로 두면 한 건 처리할 때마다 마우스로 되돌아가야 해서 단축키를 만든 뜻이 없어진다. 그래서 목록이 다시 그려지는 것을 지켜보다가 같은 자리 — 이제 다음 행이 와 있는 자리 — 로 포커스를 돌려준다.

검색어 쪽은 이미 품목 단위 일괄 승인이 있어 새로 만들지 않았다.

### 실측 확인
- `npx tsc --noEmit` · `npx next build` 통과 (`npm run lint` 은 이 환경에 eslint 가 설치돼 있지 않아 실행하지 못했다)
- 리콜 재확정 `--no-llm` 실행 — 대상 489건 중 18건 확정(전부 용어 사전 경로), LLM 호출 0회
- `/keywords?view=link` 응답에서 검수 행 40개와 승인·반려 표시가 각 40개 붙는 것 확인
- **키 입력 자체는 확인하지 못했다** — 이 저장소에 브라우저 자동화 도구가 없고 그것만 위해 들이지 않았다. 담당자가 화면에서 눌러 봐야 한다

## 라운드 52 — 법정 품목 502종을 모두 이었다, 그리고 문서 줄바꿈을 바로잡았다

**한 일**
- 생활용품·어린이제품 335종을 마저 이어 법정 품목 502종을 모두 처리했다
- 마크다운 문서의 강제 줄바꿈을 풀고 CLAUDE.md §5 에 규칙으로 남겼다

### 전체 대응 현황

    생활용품    전체 168 · 이어짐  61 · 기준없음 107 · 평균확신 94%
    어린이제품   전체 147 · 이어짐 115 · 기준없음  32 · 평균확신 90%
    전기용품    전체 187 · 이어짐 109 · 기준없음  78 · 평균확신 92%

    이어진 대응 285건이 기준 53종을 가리킨다

넓혀도 되겠다고 판단한 근거는 라운드 51의 채점이다 — 정답이 있는 30종에서 **틀림이 0%** 였다. 그 정답지가 마침 부속서(생활·어린이) 계열이라 이번 확장의 근거로 딱 맞았다.

「기준 없음」이 217건인 것은 나쁜 신호가 아니다. 우리가 가진 기준이 76종뿐이라 법정 품목 502종을 다 덮을 수 없는 것이 당연하고, 그 217건이 곧 **「적재해야 할 기준」 목록**이다.

### 담당자 지적 — 문서 줄바꿈이 이상하다

마크다운 문서에서 문장이 엉뚱한 자리에서 끊긴다는 지적을 받았다. 재 보니 **내가 저장소 방식을 어긴 것**이었다.

    README.md              최대 313자   기존 문서는 문단을 한 줄로 쓴다
    설계문서 v0.6           최대 428자
    구현이력.md             최대 509자

    내가 쓴 04-2 개발서      최대 119자   90자쯤에서 강제로 끊었다
    내가 쓴 docs/스키마.md    최대 100자

코드 주석 습관을 그대로 가져온 것인데, 한국어는 조사·어미가 붙어 있어 끊기는 자리가 어색해진다. 게다가 한 낱말만 고쳐도 문단 전체를 다시 접어야 해 diff 가 지저분해진다.

문단을 한 줄로 되돌리면서 **낱말 순서가 그대로인지 대조해 확인**했다 — 내용은 하나도 바뀌지 않아야 하기 때문이다. 코드블록·표·목록 표시는 건드리지 않았다. 다시 이런 일이 없도록 CLAUDE.md §5 에 규칙으로 적었다.

### 실측 확인
- 법정 품목 502종 전부 대응표에 담김(이어짐 285 · 기준없음 217)
- 문서 다섯 개 줄바꿈 정리, 낱말 대조로 내용 동일 확인

## 라운드 51 — 거절도 산출물이다, 그리고 정답지로 채점했다 (틀림 0%)

**한 일**
- "이을 기준을 못 찾았다"를 사유와 함께 담고 화면에 보여 준다(050)
- 정답지 대조 채점을 만들어 정확도를 처음 숫자로 쟀다(`npm run link:eval`)

### 담당자가 두 곳을 짚었다

라운드 50을 보고받은 담당자가 물었다. **"거절했다는 메시지는 주는 거죠? 그리고 LLM으로 검증한 거죠?"** 확인해 보니 둘 다 부족했다.

**거절은 사실상 버려지고 있었다.**

    콘솔에   한 묶음당 두 건만 찍었다 (84건 중 대부분 안 보임)
    DB에     아무것도 저장하지 않았다
    화면에   나오지 않았다

이 저장소가 이미 세운 원칙과 어긋난다. `match.ts` 에 *"결과 0건을 버리지 않는다 — 대응 조항이 없음이 그 자체로 산출물이다"* 라고 적어 놓고, 품목 쪽에서는 안 지켰다.

**검증은 아예 안 했다.** LLM 이 고른 것은 생성이지 검증이 아니다. 04-2 문서에 "되물어 확인한다"고 적어 두고 구현하지 않았고, 정확도를 숫자로 잰 적도 없었다 — 표본 20건을 눈으로 본 것과 품목 확정률(58→63건)이 전부였다.

### 거절을 담으면 두 가지가 생긴다

`standard_id` 를 비울 수 있게 하고 사유를 `evidence` 에 남긴다(050).

1. 담당자가 그 자리에서 바로잡는다 — 「아니다, 기준이 있다」
2. 정말 기준이 없는 품목은 **「적재해야 할 기준」 목록**이 된다. 실제로 모델이 *"자동판매기는 KC 60335-2-75 가 필요한데 목록에 없다"* 고 짚었다. §4.2 의 "필요한 기준 8종이 없다"와 같은 갈래의 발견이다.

전기용품 187종이 이제 전부 담겼다 — 이어짐 109 · 기준 없음 78.

### 정답지는 이미 있었다

부속서 기준 33종 중 32종은 파일에 품목명이 적혀 있어 법정 품목과 **글자로 이미 이어져 있다.** 그것을 가리고 물어 몇 개를 맞히는지 재면 된다. 별칭 채점 때 쓴 방식 그대로다.

    정답이 있는 30종 중
      맞힘     26종 (86.7%)
      틀림      0종 (0.0%)
      못 고름   4종 (13.3%)

**틀림이 0건인 것이 핵심이다.** 이 체계에서는 틀리는 것이 안 고르는 것보다 훨씬 나쁘다(v0.7 §3.2) — 그럴듯한 오답은 담당자가 검수해도 걸러내기 어렵다. 그래서 채점표를 「정확도」 하나로 뭉뚱그리지 않고 **틀림과 못 고름을 나눠** 적었다. 뭉뚱그리면 그 차이가 사라진다.

못 고른 4건도 이유가 합당했다.

    기타 제품류        "품목명이 포괄적이고 참고 분류도 서로 다른 제품군을 포함한다"
    공기주입물놀이기구   "기준의 적용범위가 공기주입보트·수영보조용품으로 한정 열거돼 있다"

### 실측 확인
- `npx tsc --noEmit` · `npx next build` 통과
- 대응표 187건(이어짐 109 · 기준 없음 78), 전부 미검수
- `/keywords?view=nomatch` 확인 — 사유가 그대로 보이고, 「기준이 없는 게 맞다 / 아니다, 기준이 있다」 두 버튼이 붙는다

## 라운드 50 — 끊긴 다리를 적용범위·제목으로 이었다 (사고 58 → 63건)

**한 일**
- 적용범위 임베딩을 한 번만 계산해 저장(048), 없으면 **제목**으로 대신
- 법정 품목 ↔ KC기준 대응표(049) — 적용범위를 뜻으로 견주어 잇고 담당자가 확정
- 품목 확정 경로에 결선, `/keywords` 에 「법정 품목 → 기준」 검수 탭

### 담당자가 짚어 준 것이 맞았다

라운드 49에서 "전기용품 기준 43종에 `item_name` 이 비어 있어 못 잇는다"고 적었는데, 담당자가 **"적용범위라는 것이 있고 제목도 있다, 그것으로 이어야 한다"** 고 했다. 세어 보니 그 말이 맞았다.

    품목명   33종    ← 내가 본 것
    적용범위  73종
    제목     40종

### 그런데 글자로는 안 된다 — 이것도 재 봤다

    법정 품목명으로 적용범위 전문검색   187종 중 7종(4%)
    부분 문자열까지 허용해도          187종 중 11종

    법정 품목  "모발건조기"                        고시의 말
    적용범위   "피부 또는 모발을 손질하기 위한 전기기기"    기준의 말

**뜻으로 이어야 한다.** 그리고 그 장치는 이미 있었다(`scope-semantic.ts`) — 적용범위를 임베딩해 후보를 좁히고 LLM 이 고른다. 지금까지는 사건마다 런타임으로 돌았는데, **법정 품목 목록에 미리 한 번 돌려 대응표로 담아 두면** 한 번 확정한 것이 그 품목으로 이어지는 모든 사고·리콜에 재사용된다.

### 돌려 보다 내 실수를 둘 찾았다

**하나. 20종이 전부 버려졌다.** 모델이 `KC 62368-1`(오디오·비디오 기기)을 골랐는데 코드가 "후보에 없다"며 버렸다. 확인해 보니 **모델이 맞고 내 코드가 틀렸다.**

원인은 `postgres.js` 가 bigint 를 **문자열**로 준다는 것이었다. `"37"` 과 `37` 을 비교하니 늘 어긋났다. `eval.ts` 가 재현율 0% 로 겪었던 것과 똑같은 함정이다.

**둘. 임베딩을 의심했는데 멀쩡했다.** 후보 좁히기가 나쁜 줄 알고 진단해 보니 `KC 62368-1` 이 **1위로 정확히 맞히고 있었다.** 진단을 안 했으면 멀쩡한 것을 고칠 뻔했다.

고친 뒤 20종이 모두 이어졌고 근거도 적용범위 원문을 정확히 짚었다.

    비디오카메라 → KC 62368-1  확신 99%
      "비디오카메라는 오디오·비디오 전기·전자기기에 해당하며, KC 62368-1은
       정격전압 600V 이하의 오디오·비디오 기기 안전에 적용한다"

### 등기구는 제목으로 풀렸다

전기용품 187종을 돌렸더니 103건이 이어지고 84건은 모델이 거절했다. 그런데 **등기구가 여전히 안 풀렸다.** 원인을 보니 KC 60598-2-1·2-2·2-4 는 적용범위가 **0자**라 임베딩이 없었고, 그래서 후보에조차 들어가지 못했다.

그런데 제목은 분명했다.

    제2-1부: 고정형 등기구-개별요구사항
    제2-2부: 매입형 등기구-개별요구사항
    제2-4부: 이동형 등기구-개별요구사항

담당자가 "**제목도요**"라고 한 자리가 정확히 여기였다. 적용범위가 없으면 제목으로 임베딩하도록 고치자 「일반조명기구 → KC 60598-2-4」가 확신 70%로 이어졌다. 확신을 낮게 준 것도 적절하다 — 설치 형태(고정형·매입형·이동형)를 사고 서술만으로는 가릴 수 없기 때문이다. 담당자가 검수할 자리다.

### 효과 — 사고 58건 → 63건

두 사전을 모두 확정한 상태를 흉내 내어 쟀다.

    지금        58/71건    못 찾음: LED등기구 5 · 할로겐등기구 2 · 비대상 2 · 품목명 없음 3 · 자동판매기 1
    둘 다 확정   63/71건    LED등기구 5건이 풀렸다

사슬이 넷이라 **하나라도 미검수면 통과하지 못한다.**

    검색어 "LED 등기구" → 조명기기 > 일반조명기구      (AI 제안 · 미검수)
    법정품목표          조명기기 > 일반조명기구 > LED등기구
    대응표             일반조명기구 → KC 60598-2-4     (AI 제안 · 미검수)

이것은 결함이 아니라 설계다. 품목이 틀리면 그 품목으로 이어지는 **모든** 사고·리콜에 엉뚱한 기준의 시험이 근거로 제시된다 — 검색어 하나가 틀린 것보다 파급이 크다.

### 검수 순서를 자료가 정한다

대응 108건을 전부 똑같이 보여 주지 않는다. **실제 사고·리콜에 걸리는 품목**이 먼저이고, 그중 **확신이 낮은 것**이 먼저다. 확신 99%짜리를 앞에 놓으면 담당자가 지루해져 정작 위험한 것에 눈이 무뎌진다.

### 실측 확인
- `npx tsc --noEmit` · `npx next build` 통과
- 적용범위 임베딩 76종(적용범위 73 + 제목 3), 모델 하나로 통일
- 대응표 108건 · 평균 확신 94% · 전부 미검수
- 기존 성능 그대로 — 재현율 23.8%, 오탐 87.9% (떨어지지 않았다)
- `/keywords?view=link` 확인 — 걸리는 건수 순(29·27·18건…)으로 정렬

### 남은 것
- 할로겐등기구 2건은 여전히 안 풀린다. 「가목을 제외한 그밖의 조명기구」에 붙는데 그 법정 품목이 대응표에서 거절됐다(적용범위에 근거가 없다고 봄)
- 생활용품·어린이제품 436종은 아직 잇지 않았다. 전기용품 효과를 보고 넓힌다

## 라운드 49 — 다섯 언어를 잇다 (법정품목표·검색어 사전·AI 별칭·위험 진단)

**한 일**
- 협회 자료 두 종을 적재 — 법정 품목↔GPC 2,148행, 담당자 검색어 1,039개
- AI 로 검색어를 보태고(4,943개) **채점 장치**를 함께 만들었다
- 잘못 붙은 검색어를 찾아내는 위험 진단과 `/keywords` 화면
- 검색어 사전을 품목 확정 경로에 결선
- `04-2_다섯_언어의_지도와_구현_상세_계획.md` 작성

### 어휘를 세어 보니 문제가 있는 곳이 달랐다

담당자가 짚어 준 것이 맞았다 — 사고조사보고서는 법정어와 일상어가 **섞인 글**이다.

    사고보고서 품목명    38종  → 법정 품목과 84% 일치
    해외 리콜 품목명  1,553종  → 법정 품목과  6% 일치

담당자는 법정 용어를 쓴다. **진짜 구멍은 리콜에 있었다.** 사고보고서용 사전을 키우려던 방향이 틀렸던 것이다.

### 서랍에 이미 사전이 있었다

"AI 로 별칭을 지어내자"고 계획을 세웠는데, 담당자가 넣어 준 「일일동향보고 검색용 데이터」에 **손으로 만든 검색어 1,039개**가 들어 있었다. 지어낸 말은 아무도 안 쓰는 말일 수 있지만 이건 실제로 쓰는 말이다.

그래서 한 번은 "AI 생성을 취소한다"고 적었는데, 담당자가 **"AI 별칭도 중요하다, 보완해서 넣고 검증하자"** 고 했고 그 말이 맞았다. 사람 사전이 미완성이었다 — 법정 품목 623종 중 217종에만 별칭이 있었다.

### 채점을 먼저 만든 것이 값어치를 했다

사람 사전을 **가리고** AI 에게 만들게 해서 채점했더니 재현율이 9.6% 였다. 처참한 숫자였는데, 예시를 보니 **AI 가 못한 것이 아니라 내 채점이 틀린 것**이었다.

    「망간 건전지」
      사람: 리튬전지 · 수은전지 · 버튼셀 · 알카라인배터리 …22개
      AI  : 망간전지 · 아연망간전지 · 망간 배터리 …7개

사람 사전은 사전적 별칭이 아니라 **뉴스를 훑는 검색 그물**이었다. 원본 이름이 「일일동향보고 **검색용** 데이터」인데 그걸 놓쳤다. 목적을 프롬프트에 적고 사람이 만든 실제 예를 보여 주자(few-shot) 이렇게 됐다.

    고치기 전   재현율  9.6%   새 제안 97개 중 리콜에 걸림 10개
    고친 뒤     재현율 26.9%   새 제안 236개 중 리콜에 걸림 21개

**지시문을 다듬는 것보다 실제 예를 보여 주는 것이 훨씬 효과가 컸다.**

전량 생성 결과는 검색어 5,152개(4,943개 저장), 리콜 커버리지 **81종 → 245종(3배)** 이다.

### 부분 일치가 실제로 위험했다

담당자가 "고압세척기 → 진공청소기 같은 것을 보완하라"고 지적했다. 세어 보니 검색어 5,982개 중 **600개가 두 품목 이상에 걸쳐** 있었다.

    "헤어드라이어" → 모발건조기(사람) vs 모발관리기(AI)
    "자전거 헬멧"  → 3개 품목
    "어댑터"      → 이 말을 품는 검색어가 16개

그래서 위험을 세 가지로 재는 진단을 만들었다(충돌·과매칭·실사용). 그리고 **검수 순서를 자료가 정하게** 했다 — 5,982개를 다 볼 수 없으니 *실제로 걸리면서 충돌하는 것*을 맨 위에 놓는다. 실제로 걸리는 것은 382개뿐이다.

검색에 쓸 때는 조건 셋을 모두 만족해야 한다. **정확 일치만 · 충돌하는 말은 제외 · 확정된 것만.** 협회가 정한 제외어 124개도 뺀다.

### 결선했는데 아직 효과가 없다 — 병목이 다른 곳이었다

검색어 사전을 품목 확정 경로에 넣었다. 그런데 재 보니 기여가 리콜 99건·기준 2종뿐이다. 단계별로 세어 원인을 찾았다.

    전체 5,982 → 확정된 것 1,039 → 충돌 없는 것 677 → 기준까지 도달 227

**진짜 병목은 전기용품 기준 43종에 품목명(item_name)이 비어 있다는 것이다.** 법정 품목까지는 가는데 기준으로 못 넘어간다. 담당자 사전이 주로 전기용품(156항목)을 다루는데 하필 그쪽이 막혀 있다.

결선 자체는 옳고, 43종을 채우면 그때 효과가 난다. 문서에 다음 우선순위로 적었다.

### 시스템 전체를 훑었다

**돌아가지만 사람이 지나가야 할 문 앞에 대부분 멈춰 있다.**

    사고보고서 확정   71건 중 5건        확정 전에는 분석이 시작되지 않는다
    분석된 사건      9건
    담당자 판단      0건                review_log 가 비어 있다
    AI 검색어 검수   4,943개 대기
    리콜 GPC        0건                이제 법정품목표로 붙일 수 있다
    리콜 수집 배치   실패 62건(504·502)

자료는 갖춰져 있다 — 기준 76종·조항 13,501·태깅 6,400·임베딩 6,400, 리콜 2,299건 전량 임베딩. 산출물 뷰도 사각지대 215행·국내외대조 202행·시험항목 524행이 나온다.

### 실측 확인
- `npx tsc --noEmit` · `npm run lint`(경고 2건, 기존) · `npx next build` 통과
- 로컬 서버로 `/keywords` 확인 — 위험 항목 82건 표시, CSV 5,982줄 내려받기
- 품목 확정 재측정: 사고 71건 중 58건(82%) 유지 — **기존 결과를 망가뜨리지 않았다**

## 라운드 48 — 절 묶음으로 재현율을 처음 올렸다 (16.2% → 23.8%)

**한 일**
- 왜 절반이 답을 못 내는지 진단하고, 원인에 맞는 처방을 찾아 실제 검색에 반영했다
- `src/lib/search/group-section.ts` 추가, `runAnalysis` 가 이제 절로 묶어 좁힌다

### 진단이 예상을 뒤집었다

"조항 절반에 원인 코드가 없어서"라고 짐작하고 있었다. 정답 조항 137건이 왜 안 나왔는지 하나씩 갈라 보니 달랐다.

    적중          38건 (28%)
    순위에서 밀림   67건 (49%)   ← 코드도 있고 검색도 되는데 상위 20 밖
    검색 불가      18건 (13%)   ← 껍데기라 본문·임베딩이 없다
    범위 밖        12건 ( 9%)
    미태깅          0건         ← 하나도 없었다

**미태깅이 0건이었다.** 절반은 재현 문제가 아니라 **순위 문제**였다. 후보를 200으로 넓히면 걸린다. 짐작으로 태깅 보강부터 했으면 헛일을 할 뻔했다.

### 처방 — 형제 조항의 근거를 절 단위로 모은다

15.1 이 50위, 15.1.1 이 80위, 15.2 가 120위면 개별로는 다 밀리지만 **절 15 는 세 번 걸린 셈**이다. 그 사실을 세는 것이 절 묶음이다.

점수 내는 방식 셋을 나란히 쟀다(CLAUDE.md §6).

    ①+②+③ 하이브리드      재현율 16.2%  오탐 89.5%  상위5 14.5%   ← 기준선
    + 절 묶음(합)         재현율 12.0%  오탐 90.2%  상위5 11.9%
    + 절 묶음(최대)        재현율 23.8%  오탐 87.9%  상위5 14.6%
    + 절 묶음(상위3)       재현율 21.8%  오탐 87.9%  상위5 11.4%

「합」이 나쁜 이유는 조항 수가 많은 절이 그냥 이기기 때문이다. 「최대」는 가장 잘 맞는 조항 하나로 절을 대표시켜 크기 편향이 없다.

**재현율이 오르면서 오탐도 함께 줄었다.** 이번 세션에서 이런 결과는 처음이다. 그래서 원인 다리와 달리 기본 목록에 바로 반영했다 — 잘 찾던 것을 망가뜨리지 않고 더 찾는다.

### 넓게 잡을수록 좋은 것은 아니었다

담당자 요청으로 후보를 600까지 넓혀 봤다.

    + 절 묶음(200건)   재현율 23.8%
    + 절 묶음(600건)   재현율 20.8%   ← 오히려 떨어졌다

잡음이 늘어 절 순위가 흐려진다. "넓게 잡아 좁히기"는 맞지만 무한정 넓히면 안 된다는 것을 숫자가 말해 줬다. 200 에서 멈춘다.

### 절 묶음과 리랭킹은 서로를 돕는다

    + 리랭킹(단독)        재현율 16.2%  상위5 29.1%
    + 절 묶음 + 리랭킹     재현율 23.8%  상위5 32.8%   ★ 채택

리랭킹 단독으로는 재현율이 오르지 않는다 — 후보를 새로 만들지 않기 때문이다. 절 묶음이 먼저 정답을 20건 안에 올려놓아야 리랭킹이 그것을 상위 5건으로 끌어올린다. **담당자가 실제로 읽는 상위 5건 포함률이 14.5% → 32.8% 로 두 배 넘게 올랐다.**

### 계열 걱정은 기우였다

담당자가 "IEC 는 몰라도 생활·어린이 기준은 다르다"고 지적해 계열별로 나눠 쟀다.

    전기용품      39건   16.6% → 23.5%   (좋아짐 10 · 나빠짐 12)
    생활·어린이     8건   16.7% → 28.4%   (좋아짐  2 · 나빠짐  1)

**두 계열 모두에서 듣는다.** 계열별 계위 차이(전기용품은 절 단위로, 생활·어린이는 조항 단위로 적는다)는 **보여주는 방식**의 문제였지 **찾는 방식**의 문제가 아니었다. 다만 생활·어린이는 8건뿐이라 얇다.

### 비용을 재고 장치를 달았다

리랭킹 모델(`gpt-5.6-terra`)의 단가로 계산하니 채점 1회(47건 × 리랭킹 2행)가 약 4,200원이었다. 담당자가 정한 한도(1만원)에 근접해 있었다.

운영 비용은 문제가 아니다 — 사건 1건 분석에 호출 1회, 약 45원이다. 비싼 것은 채점이다. 그래서 `--no-llm` 을 달았다. 검색 쪽만 손보는 동안에는 LLM 행을 빼고 돌린다.

### 실측 확인
- `npx tsc --noEmit` 통과, `npm run lint` 경고 2건(기존)
- `npx tsx scripts/search.ts --case 4639` 로 운영 경로 확인 — 절 묶음으로 20건을 뽑고 리랭킹이 순서를 다듬는 것을 눈으로 확인
- 사건 5553 은 절 묶음이 안 듣고 원인 다리만 듣는다(2건). 둘이 서로 다른 실패를 겨냥한다는 관찰과 일치한다

## 라운드 47 — 숫자 표기를 바로잡고, 계열마다 계위가 다르다는 것을 실측했다

**한 일**
- 원인 후보의 비율 표기에 분모와 출처를 넣고, 확률로 오해되지 않게 문구를 고쳤다
- 「특이도」로 잘못 쓴 칸을 「향상도」로 바로잡았다
- 전기용품과 생활·어린이 기준에서 담당자가 적는 계위가 다르다는 것을 실측했다

### 담당자 지적 — "%와 건의 의미가 모호하다"

`과열 59%(240건)` 만 보이면 **"이 사건이 과열일 확률 59%"** 로 읽힌다. 아니다. 해외 리콜이라는 다른 자료에서 관찰된 비율이고, 이 가습기와는 무관하게 계산된 값이다.

문제가 둘이었다.

    분모가 안 보인다   409건이 어디의 409건인지 화면에 없었다
    합이 100%가 아니다 59+39+25+13 = 136%. 한 사건에 원인 코드가 여럿 붙기 때문인데
                      화면만 보면 나눠 가진 몫처럼 읽힌다

그래서 줄마다 분모와 출처를 적고, 확률이 아니라는 것과 합이 100%를 넘는다는 것을 표 옆에 명시했다.

    지금까지  같은 피해 409건 중 240건 · 59%
    고친 것   해외 리콜 409건 중 240건에 함께 적힘 (59%)

### 용어를 잘못 썼다

향상도(lift)를 「특이도」라고 적어 두었다. 통계에서 특이도(specificity)는 전혀 다른 지표라, **아는 사람이 볼수록 더 헷갈리는** 표기였다. 화면·CSV 머리글을 모두 고쳤다.

숫자를 만드는 일보다 그 숫자를 뭐라고 부르는지가 더 중요할 때가 있다. 담당자는 화면의 이름만 보고 뜻을 짐작하기 때문이다.

### 계열마다 담당자가 적는 계위가 다르다 (실측)

담당자 지적: *"IEC 기준은 정확할지라도 생활, 어린이 기준들은 다릅니다."*

정답셋 140건이 지목한 조항의 계위를 세어 봤다.

    전기용품(IEC 계열)      절 단위 69건(64%) · 조항 단위 39건
    생활·어린이(부속서)      절 단위  2건( 6%) · 조항 단위 30건(94%)

**정반대다.** 전기용품에서는 "절 15"처럼 절 단위로 적고, 생활·어린이에서는 거의 전부 조항 단위로 적는다. 절 껍데기 비율도 전기용품 55%, 생활·어린이 46%로 다르다.

§7.2 "절 단위로도 제시하기"를 전체에 똑같이 적용하면 생활·어린이 쪽이 오히려 나빠진다. **계열별로 다르게 동작해야 한다** — 이건 실측 없이 IEC 구조만 보고 설계했으면 그대로 틀렸을 자리다.

기준 계열은 `standard.cert_scheme` 으로 갈린다(전기용품 43종 · 부속서 33종).

### 실측 확인
- `npx tsc --noEmit` · `npx next build` 통과
- 로컬 서버로 문구 확인 — 분석 화면 원인 후보 14줄에 분모·출처 표시, 코드북 표 머리글 「함께 적힌 건수 / 비율 / 향상도」, CSV 머리글도 같이 바뀜

## 라운드 46 — 원인 다리를 담당자 손에 쥐여 주다 (화면과 내려받기)

**한 일**
- `/codebook` 에 「결과 → 원인 다리」 추가 — 피해유형별 원인 후보를 근거·비율·특이도와 함께 조회
- 사건 분석 화면에 원인 후보 고르기와 **두 번째 목록** 추가
- 분석 결과·원인 다리 CSV 내려받기 (`/api/analysis/[caseId]/export`, `/api/codebook/cause-bridge/export`)

### 왜 화면을 만들었나

라운드 45에서 표는 만들었지만 담당자가 볼 길이 없었다. 명령줄로만 확인할 수 있는 자료는 결국 안 쓰인다. 그리고 이 표가 쓸 만한지는 숫자가 아니라 담당자가 판단할 일인데, 눈으로 본 적이 없으면 그 판단을 받을 수도 없다.

### 기본 목록을 덮지 않는 것이 설계의 전부다

라운드 45 실측이 말한 것은 "만들지 마라"가 아니라 **"자동으로 켜지 말고 담당자 손에 쥐여 줘라"** 였다. 그래서 화면을 이렇게 나눴다.

    기본 목록      피해유형을 다루는 조항        지금까지 하던 것 그대로 · 손대지 않는다
    원인 후보      이 피해에서 실제 원인이 뭐였나  담당자가 고른다
    두 번째 목록    고른 원인을 확인할 시험        기본 목록 위에 따로 붙는다

자동으로 섞으면 잘 찾던 8건이 나빠지지만, 따로 두면 나빠지는 건이 0이 된다. 아무것도 못 찾던 자리에만 새로 답이 생긴다.

주소에 선택을 싣는다(`?cause=HF.H.ELEC.INS`). 화면을 다시 열거나 남에게 보내도 같은 목록이 나오고, 내려받기 링크에도 그대로 실려 화면과 파일이 어긋나지 않는다.

### 두 번째 목록은 저장하지 않는다

화면을 그릴 때 검색을 한 번 더 도는 것이고 `match_run` 에 남기지 않는다. 남길 것은 담당자가 채택·반려한 판단이지 목록을 펼쳐 본 사실이 아니다. 저장하면 그 실행이 "마지막 분석"이 되어 기본 목록을 밀어내는데, 그것이 바로 라운드 45에서 하지 않기로 한 일이다.

### 붙이는 자리를 한 번 틀렸다

처음에는 원인 후보 패널을 "결과가 있을 때"에만 넣었다. 실제로 띄워 보니 **아무것도 못 찾은 사건에서 패널이 안 나왔다.** 그 자리가 바로 이 기능이 필요한 자리인데 정확히 거기서 빠져 있었다. 조건 밖으로 꺼냈다. 화면은 띄워 봐야 안다.

### 내려받기를 넣은 이유

담당자가 화면 다음에 하는 일은 시험 의뢰서를 쓰는 것이다. 조항 번호를 손으로 옮겨 적게 두면 옮기다 틀리고, 무엇보다 화면 밖으로 나가는 순간 이 체계가 만든 근거가 사라진다. 두 목록을 한 파일에 담되 `구분` 칸으로 출처를 남긴다 — 성격이 다른 둘을 섞어 놓고 출처를 지우면 받는 사람이 판단할 수 없다.

원인 다리 CSV 는 확인 경로로 거르지 않고 다 내보낸다. 담당자가 엑셀에서 「법령」 줄만 모아 인증·표시 점검 목록으로 쓸 수 있어야 하기 때문이다.

### 실측 확인
- `npx next build` 통과 · 새 라우트 2개 등록 확인
- 로컬 서버로 실제 확인
  - `/codebook` 「결과 → 원인 다리」 표시, CSV 175줄 내려받기 정상
  - `/analysis/4639`(가습기 화재·원인 미상) 원인 후보 5개 표시 — 과열 59%(240건) · 절연 불량 39%(158건) · 전기적 위해 25% · 배터리 13% · 비화재 열적
  - `?cause=HF.H.ELEC.INS` 로 두 번째 목록 20건 생성
  - 분석 CSV 41줄(기본 20 + 원인 20 + 머리글) 확인
- `npx tsc --noEmit` · `npm run lint` 통과(기존 경고 2건 외 없음)

### 곁가지
- CSV 규칙을 `src/lib/csv.ts` 로 모았다. 내보내는 자리가 셋이 되어 각자 복사해 두면 한쪽만 고치게 된다(CLAUDE.md §9). 엑셀이 한글을 깨뜨리지 않게 BOM 을 붙이는 규칙도 여기 한 곳에 있다

## 라운드 45 — 결과에서 원인으로 가는 다리를 놓고, 자동으로 켜면 안 된다는 것을 확인했다

**한 일**
- `04-1_결과에서_원인으로_가는_다리.md` 작성 — §7.1 하나만 떼어 상세 설계로 옮김
- 코드북에 표 두 개 추가(`codebook/sql/003`), `npm run cause:build` 로 채움
- `docs/스키마.md` 작성하고 README 에서 연결
- CLAUDE.md §10 — 제도·법령 해석은 `docs/제품안전법제도/` 를 먼저 본다

### 왜 만들었나

사고조사보고서 70건 중 54건(77%)이 원인 미상이다. 조사 자체가 원인을 특정하지 못한 것이라 문서에서 뽑을 수가 없다. 그래서 시스템은 피해유형으로만 조항을 찾고, "화재 조항"은 찾지만 "화재의 원인을 확인할 시험"은 못 찾는다.

**근거 자료가 이미 DB에 있었다.** 해외 리콜 2,299건 중 2,072건에 원인이 붙어 있다 (미상 10%). 리콜은 원인이 밝혀진 자료다. 여기서 "어떤 피해에 어떤 원인이 따라왔는가"를 세면 사전이 된다. §7.1은 담당자 목록 172행을 근거로 삼자고 했는데, 실제로는 30배 두꺼운 자료가 이미 적재돼 있었다.

### 담당자가 알려 준 개념 하나가 설계를 바꿨다 — 불량과 불법

처음에는 M-SHELL 대분류(하드웨어는 쓰고 관리는 뺀다)로 거르려 했다. 겉모습이었다. 담당자가 짚어 준 진짜 축은 불량과 불법이다.

```
불량   안전기준의 기능·성능에 불합격     시험을 해 봐야 안다      → 시험항목
불법   인증·표시·부품변경 신고 위반      법령을 보면 판정된다     → 인증·표시 확인
```

주신 `docs/제품안전법제도/★★★★업무이해 20230405.xlsx` 에 그대로 적혀 있었다 — *"불법은 기준이 있어 판단이 가능하나, 리콜은 안전성조사가 선행되어야 함"*.

이게 왜 중요한가. 화재 사고에서 「법적 인증 고의 위반」이 통계적으로 상위에 올라온다 (84건, 리프트 1.84). 그런데 **불법 제조 여부를 판정하는 KC 시험 조항은 없다.** 시험항목 목록에 섞이면 담당자가 확인할 수 없는 것을 확인하라고 하는 셈이다.

그래서 코드북에 「확인 경로」 칸을 만들었다(TEST 31 · LEGAL 4 · GAP 6 · OTHER 23). 불법을 버리지는 않는다 — "이 제품군은 시험보다 인증·표시를 먼저 봐야 한다"는 다른 신호이므로 다른 출구로 내보낸다.

**표시사항은 안전기준 안에 있어도 불법이다.** 라운드 44에서 보류를 풀었던 열냉각시트 사건이 정확히 그 경우였다. 「어린이제품 공통안전기준 5. 표시사항」 미표시로 경결함을 받았는데, 안전기준 문서 안의 조항이지만 성격은 표시 의무 위반이다. 다행히 조항 쪽은 `clause_role` 이 `REQUIREMENT` 와 `MARKING` 을 이미 나눠 두고 있었다.

### 구현하면서 실측이 설계를 세 번 뒤집었다

**① 리프트로 줄을 세우면 안 된다.** 리프트는 드문 코드일수록 커진다. 화재 상위 5가 비화재 열적사고(31건)·과열(240건)·보호회로 부재(14건)·배터리(77건)·하드웨어(9건)로 채워지고 **절연 불량(158건·38.6%)이 7위로 밀렸다.** 정작 담당자가 지목한 내습성·누설전류를 찾아 줄 코드였다. 리프트는 "아무 데나 붙는 원인"을 쳐내는 문지기로 두고(설계결함 0.96 탈락), 순위는 신뢰도로 매기도록 고쳤다.

**② 원인으로 찾을 때는 피해유형을 빼야 한다.** 코드 갈래는 일치한 코드 *개수*로 순위를 매긴다(`order by hit_full desc`). 사건 5553은 화재·화상 두 코드를 가져서 둘 다 붙은 조항이 hit=2로 앞자리를 채웠고, 추정 원인 하나만 붙은 절 15.1은 hit=1이라 잘렸다. **후보를 60까지 늘려도 0건이었는데, 피해유형을 빼자 즉시 4건이 잡혔다.**

계산 문제만이 아니다. 결과로 찾는 것("화재를 다루는 조항")과 원인으로 찾는 것 ("누전 가능성을 확인할 시험")은 서로 다른 질문이다.

**③ 자동으로 켜면 안 된다.** 이것이 이번 라운드의 핵심 발견이다.

    구성                   재현율    오탐률   상위5포함률
    ①+②+③ 하이브리드        16.2%    89.5%      14.5%   ← 기준선
    + 원인 다리(대체)         13.1%    90.5%      15.0%
    + 원인 다리(병합)         14.6%    89.3%      16.5%

어느 형태든 평균 재현율이 **떨어졌다.** 판정 기준대로면 채택 불가다.

### 그런데 건별로 보면 이야기가 다르다

    47건 중 23건(절반)은 하이브리드가 한 건도 못 찾는다
      그 23건 중 대체 방식이 5건을 살렸다   5553 · 5556 · 5557 · 5561 · 5577
    이미 찾고 있던 24건 중 병합이 나쁘게 만든 것 8건

평균이 떨어진 이유가 여기 있다. 다리가 **이미 잘 찾고 있던 사건의 순위를 흔들면서** 값을 깎았다(사건 5560은 19건 → 14건). 반대로 5553은 다른 어느 구성도 0건인데 대체 방식만 2건을 찾았다 — 담당자가 지목한 그 조항이다.

**그래서 결론이 바뀌었다.** 다리는 기본 순위를 고치는 장치가 아니라, 아무것도 못 주던 자리에 줄 것을 만드는 장치다. 기본 검색에는 반영하지 않고, 담당자가 원인 후보를 고르면 **두 번째 목록**으로 보여 준다.

설계 §7에 "자동으로 반영하지 않고 담당자가 고르게 한다"고 적었을 때 이유는 "품목을 아는 사람만 후보를 걸러낼 수 있다"였다. 숫자를 보니 이유가 하나 더 있었다 — 자동으로 섞으면 잘 되던 것까지 망가진다.

### 더 큰 사실이 함께 드러났다

**정답셋 47건 중 23건(49%)에서 시스템이 한 건도 맞히지 못한다.** 평균 재현율 16.2%보다 이쪽이 더 중요하다. 절반은 성능이 낮은 것이 아니라 아예 답을 못 내고 있다. 다리는 그중 5건을 건드렸을 뿐이고, 나머지 18건은 다른 원인을 봐야 한다.

### 실측 확인
- 리콜 2,299건 → 다리 175줄(시험 가능 111 · 불법 23 · 기준공백 19 · 기타 22)
- 확인 경로 초기값 64개 코드에 규칙으로 자동 부여
- 정답셋 47건 채점 2회 완료, `npx tsc --noEmit` 통과
- 곁가지로 찾은 결함: `eval.ts` 의 `--case` 필터가 늘 빈 목록을 돌려주고 있었다. 정답셋의 `caseId` 가 DB bigint 에서 나와 문자열인데 숫자와 비교하고 있었다. 전체 실행은 정상이라 드러나지 않던 것이다

### 다음 판단 자료
- `docs/제품안전법제도/안전기준목록조사 취합.xlsx` 에 품목→기준 대응 558행 (전기 446 · 생활 75 · 어린이 37). §4.2의 "기준 8종이 없다"와 §5의 품목 확정(83%)을 직접 푸는 자료다. 현재 적재는 76종이다

## 라운드 44 — "계좌번호 2건" 보류가 오탐이었다 (개인정보 탐지 정밀화)

**한 일**
- 담당자가 라운드 42에서 보류된 사고보고서 1건("계좌번호 2건 발견")의 원본 PDF를 직접 열어 "개인정보가 없다"고 지적 → 원인을 추적해 실제로 오탐임을 확인
- `src/lib/cases/extract-pdf.ts` 의 계좌번호 탐지 패턴을 제거

### 무엇이 잘못 잡혔나

DB에 저장된 추출 텍스트(`source_file.id=82`, "(25)6 열냉각시트 사고조사 보고서_비식별화_f.pdf")에 정규식을 직접 돌려 확인했다. "계좌번호"로 잡힌 두 곳은 전부 같은 문자열이었다.

    제조자명   ㈜메이클린
    주소 및 전화번호   경기도 포천시 내촌면 포천로 327-7
                      전화번호 : 070-4228-05013

이건 제품 표시사항(라벨) 준수 여부를 확인하는 표에 실린 **제조사의 대표 전화번호**다. 개인 계좌번호가 아닐 뿐 아니라, 개인정보 자체가 아니다 — 법인이 법률상 의무로 공개하는 연락처다. 신고인 등 실제 개인 관련 항목은 이미 파일 안에서 "ㅇㅇㅇ"로 비식별 처리돼 있었다.

### 왜 "계좌번호"로 잡혔나

전화번호 탐지 정규식(`/\b01[016789][-\s]?\d{3,4}[-\s]?\d{4}\b/g`)은 010/011처럼 **휴대폰 번호만** 잡는다. "070"으로 시작하는 인터넷전화(대표번호) 형식은 이 패턴을 통과하지 못하고, 그다음 순서였던 계좌번호 패턴(하이픈으로 나뉜 숫자열, 총 9자리 이상)에 우연히 걸려 잘못된 이름표가 붙었다.

### 정밀화 대신 삭제를 택한 이유

CLAUDE.md §7 은 오탐이 생기면 "더 정밀한 조건을 추가"하라고 적어 뒀지만, 이번엔 전화번호 패턴에 070 형식을 추가하는 대신 **계좌번호 탐지 자체를 없앴다.** 담당자 확인: 사고조사보고서에는 계좌번호가 등장할 이유가 없다. 없는 것을 잡으려는 패턴이 대표번호·문서번호 같은 숫자열을 오탐하는 비용만 계속 낼 이유가 없었다.

`minDigits` 메커니즘(패턴별 최소 자릿수 필터)도 함께 지웠다 — 오직 이 계좌번호 패턴 하나를 위해 만든 것이었고, 남은 네 패턴(주민등록번호·전화번호· 이메일·카드번호) 중 쓰는 곳이 없어 고아 코드가 됐기 때문이다(CLAUDE.md §3).

**실측 확인**
- 보류됐던 원본 텍스트에 수정된 `detectPii()`를 다시 돌려 개인정보 후보 0건 확인
- 같은 파일을 `--force`로 재적재 → 정상 적재 성공(개인정보·스캔본 보류 0건)
- DB 확인: `ACCIDENT_PDF` 전체 71건 중 `error`(보류) 0건 — 라운드 42 시점의 67건에서 이후 라운드들에서 더 쌓여 71건이 됐고, 그중 보류였던 1건도 이제 풀렸다
- `npx tsc --noEmit` 통과

## 라운드 43 — 용어 사전의 네 꼭짓점 실측과 남은 일 총정리

**한 일**
- `04_2차 보완을 위한 분석.md` 에 5장(용어 사전)과 9장(남은 일 전부)을 더했다

### 사전의 네 꼭짓점 중 하나가 끊겨 있다

이어야 할 것이 넷이다 — 일상 용어, KC 기준, GPC 브릭, 해외 리콜 품목명. 실측해 보니 리콜 쪽이 통째로 비어 있었다.

    일상 용어 → KC 기준      용어 39종 · 대응 54건    (사고보고서에는 충분)
    일상 용어 → GPC 브릭     37건                   (사고보고서에는 충분)
    GPC 브릭 자료           5,318건                (완비)
    사고 → GPC             70건 중 8건
    리콜 → GPC             2,259건 중 **0건**       ← 끊김
    리콜 품목명이 사전에 있는가  1,532종 중 **13종(0.8%)** ← 끊김

사고보고서 63건에서 뽑은 용어 39종으로 리콜 품목명 1,532종을 감당할 수 없다. 규모가 40배 차이 난다.

**이것이 3번째 산출물을 막고 있다.** `/insights` 의 사각지대 집계에서 2,225건이 「품목 미확정」에 걸려 있는데 그 대부분이 리콜이다. 사고와 리콜을 같은 품목군으로 묶어야 "이 위해요인에 대응 조항이 없다"를 말할 수 있고, 묶는 키가 GPC 인데 리콜에 GPC 가 하나도 없다.

**채우는 순서를 비용 순으로 적었다.** 먼저 협회 원본 표에 GPC 컬럼이 있는지 확인한다(비용 0). `recall_cache.raw` 에 원본이 통째로 있으므로 조회 한 번이면 안다. 없으면 빈도 상위 품목명부터 사전에 넣고, 그다음 의미 검색으로 붙인다. 전부 할 이유는 없다.

### 남은 일을 한 곳에 모았다

세션 내내 흩어져 있던 것을 9장에 모두 넣었다. 여섯 갈래로 나눴다.

    9.1 협회에 요청할 것      우리가 못 고치는 것 (기준 8종·원본 오염·리콜 GPC)
    9.2 자료를 채워야 풀리는 것  입구는 다 만들었고 쌓는 것은 사람 몫
    9.3 코드로 할 일          우선순위 6개
    9.4 결정을 기다리는 것      3건
    9.5 하지 않기로 판정한 것    6건 — 왜 안 하는지의 근거와 함께
    9.6 알고 있는 결함         3건

**9.5 를 따로 둔 이유가 있다.** "안 한 것"과 "안 하기로 정한 것"은 다르다. 근거를 적어 두지 않으면 나중에 누군가 같은 것을 다시 검토하게 된다. 태깅 예약 장부·태그 버전 단일화·서브쿼리 정리·server-only·GPC 기준 선택· 시험방법 연결 가설이 모두 실측으로 접은 것들이다.

**실측 확인**
- 문서 자체검증(humanize-korean): S1 잔존 0건 · S2 2건 · K 패턴 0건 → 등급 A 남은 2건은 실제 진행 상태를 말하는 자리라 규칙의 예외에 해당한다
- 문서 11,282자

## 라운드 42 — 사람이 정한 시험항목과 처음으로 견주다 (재현율 실측)

**한 일**
- 사고보고서 67건 적재, 조항 5,631건 태깅, 임베딩 6,400건
- 담당자 엑셀로 정답지를 만들어 §5.8 재현율을 처음 측정
- 건별 대조 증빙을 `docs/분석/2026-09-05_시험항목_비교/` 에 남김
- `04_2차 보완을 위한 분석.md` 작성 (humanize-korean 자체검증 등급 A)

### 숫자

    조항 단위 재현율   16.2% (하이브리드)   오탐률 89.5%   상위5 포함률 30.4%
    절 단위 적중      38/137 (27.7%)
    사람·시스템 상위 10절 겹침  6개 (11, 19, 4, 5, 6, 7)

리랭킹이 상위5 포함률을 14.5% → 30.4% 로 두 배 올렸다. 재현율은 그대로이고 순서만 좋아진 것인데, 담당자가 실제로 보는 것은 상위 몇 건이라 체감은 이쪽에 가깝다.

### 왜 낮은가 — 원인은 검색이 아니었다

사건 5553(전기머리인두)을 그대로 놓고 봤다.

    코드: HF.UNKNOWN / DT.THERMAL.FIRE
    사람   → 절 15(내습성), 절 16(누설전류)
    시스템 → 절 11(온도 상승), 절 19(이상운전)

못 찾은 조항의 코드를 확인하니 **태깅은 정확했다.**

    절 11 온도 상승  → DT.THERMAL.FIRE      (사건과 일치 → 찾음)
    절 15 내습성    → DT.ELECTRIC.SHOCK    (불일치 → 못 찾음)

시스템은 **결과**(화재)에서 출발해 화재 코드가 붙은 조항을 찾았다. 논리적으로 옳다. 담당자는 **원인 가설**("머리인두에 습기가 들어가 누전됐을 수 있다")을 세우고 그 가설을 확인할 시험을 골랐다.

**시험은 원인을 확인하려고 하는 것이다.** 결과가 같은 조항을 모으는 것으로는 조사에 필요한 시험이 나오지 않는다. 그런데 사고보고서 70건 중 54건(77%)이 HF.UNKNOWN 이라 원인 코드로 검색할 수도 없다. **결과에서 원인 후보로 건너가는 다리가 없다** — 이것이 이 체계의 가장 큰 공백이고, 이번 분석의 핵심 발견이다.

### 세운 가설이 틀린 것도 적어 둔다

"담당자가 지목한 것은 시험방법 조항인데 평가는 성능요건만 본다"는 가설을 세우고 확인했더니 **틀렸다.** 시험방법 연결까지 정답에 포함해도 887건 중 1건만 더 잡혔다. 정답이 대부분 성능요건 절이라 연결 경로가 도울 여지가 없었다.

### 측정 방식 자체를 한 번 고쳤다

처음 잰 재현율은 0% 였다. 원인을 보니 `eval.ts` 가 조항 번호를 **정확히 일치**하는 것만 정답으로 쳤다. 담당자는 절 단위("절 15")로 적는데 우리 색인은 조항 단위 (15.1, 15.2 …)다. IEC 계열에서 절 자체는 제목만 있어 본문이 3자짜리 껍데기다 — 코드도 임베딩도 붙지 않아 애초에 검색될 수 없다.

그 상태로 잰 숫자는 시스템 성능이 아니라 계위 차이를 잰 것이었다. 하위 조항까지 정답으로 치도록 고치고 다시 쟀다. **재기 전에 재는 방식이 맞는지 먼저 확인해야 한다는 것을 다시 배웠다.**

### 임베딩 배치가 한 건 때문에 통째로 멈춰 있었다

조항 2,994건을 임베딩하다 400 으로 죽었다. 원인은 **한 건**(14,584자)이 모델의 8,192 토큰 한도를 넘긴 것이었다. 평균은 280자다. 만분의 일짜리 예외가 전부를 막았다. `embedBatch` 에 길이 안전장치를 넣었다.

### 실측 확인
- 적재 66/67 성공(1건 개인정보 보류) · 태깅 5,631건 실패 0 · 임베딩 6,400건
- 품목 확정 70건 중 58건(83%)
- 정답셋 사건 49건 · 기대 조항 140건
- 건별 대조 47건을 파일로 남김(txt·csv). `npm run eval:compare` 로 재생성 가능
- 문서 자체검증: S1 잔존 0건 · S2 1건 · K 패턴 0건 → 등급 A

## 라운드 41 — GS1 GPC 직접 조달과 용어 사전 관리 화면

**담당자 질문 둘** "GS1으로 직접 사이트 등으로부터 GPC 얻는 건 가능?" "용어 사전을 GPC 기준으로 만드는 거네요? export/import 그리고 사람이 추가·삭제하는 UI는 어떨까요?"

### GS1 직접 조달 — 가능하다 (실측)

    https://ref.gs1.org/standards/gpc/2026-05/  →  200 application/zip 11.1MB

인증·등록·결제가 없다. zip 안에 EN.json(32.6MB)·EN.xml·EN.xlsx·Delta·릴리스 노트가 들어 있고, `{LanguageCode, DateUtc, Schema[]}` 구조에 Level/Code/Title/Definition/ DefinitionExcludes/Active/Childs 가 4계위로 중첩돼 있다. **Delta 파일이 있어 다음 판부터는 바뀐 것만 반영하면 된다.** 판올림은 연 2회(5월·11월).

협회 색인(`oecd_gpc_202405`)과 견주면

    판     GS1 2026-05          vs  협회 2024-05 (네 판 뒤처짐)
    브릭   5,318                vs  5,281
    언어   영어(정본)            vs  한국어 번역본
    정의문 있음                  vs  없음(제목만)

**정의문이 없다는 것이 컸다.** 브릭 제목은 짧아서 "개인용 온열/마사지용품 (동력)" 처럼 여러 물건을 뭉뚱그리는데, 정의문에는 무엇이 들어가고 빠지는지가 적혀 있다.

그래서 043 으로 우리 표를 만들고, GS1 최신판 + 협회 한국어를 브릭 코드로 조인했다. **한국어가 5,249/5,318(99%)에 붙었다.** 나머지 69건은 2024-05 이후 신설 브릭이다. 정의문을 넣어 임베딩까지 만들었다(5,318건 전부).

### 그런데 GPC 로는 기준을 고를 수 없다 — 이번에도 실측이 정했다

담당자 엑셀의 GPC 는 39조합 전부 BRICK 계위였다. 즉 GPC 자체가 broad 한 게 아니라 **BRICK 조차 기준 선택에는 부족했다.** 한 브릭이 여러 품목을 묶는 4건 중 3건에서 기준이 달랐다.

    10000759 개인용 온열/마사지용품 (동력)
      눈마사지기   → KC 60335-2-17 + 부속서 74
      손목마사지기  → KC 60335-1 + KC 60335-2-17
      전기찜질기   → KC 60335-1 + KC 60335-17 + KC 60335-2-17

반대로 같은 품목이 여러 브릭에 걸리기도 한다(LED등기구 → 10008403, 10008404). 협회 색인에서 검색해 보니 **"전기요"라는 말이 GPC 에 아예 없다.** 즉 GPC 를 쓰려면 "전기요 → 개인용 온열/마사지용품" 이라는 번역이 먼저 필요한데, 그것이 바로 원래 풀려던 문제다. 순환이다. GS1 최신판을 받아도 이 순환은 그대로다.

**그래서 역할을 나눴다.** 기준 선택은 용어 사전(042), 품목군 묶기는 GPC(043·044). 용어를 앵커로 두고 두 축을 매단다 — 해외 리콜은 영어로, 우리 사고는 한국어로 적히니 집계에는 언어 독립 키가 필요하다.

### 관리 화면 (`/terms`)

담당자 제안대로 만들었다. 이유가 셋이다.
1. AI 가 찾은 대응이 계속 미검수로 쌓인다. 판정할 자리가 없으면 사전이 검토되지 않은 채 자란다
2. **담당자는 실제로 엑셀로 일한다** — 이 프로젝트의 정답지도 엑셀로 받았다. 내보내기·가져오기가 없으면 이 사전은 결국 안 쓰이게 된다
3. 지금 45건이라 손볼 만하다. 500건이 되면 늦는다

**가져오기는 두 단계다.** 사전이 바뀌면 검색 결과가 바뀌고, 조용히 바뀌는 것이 이 체계에서 가장 나쁜 고장이라 무엇이 바뀌는지 먼저 보여 준 뒤에만 적용한다. CSV 에 없는 행은 지우지 않는다 — 일부만 잘라 온 파일로 사전이 통째로 날아가면 안 된다.

### 화면이 왜 필요한지 실물로 드러났다

내보내기를 검증하다 AI 가 넣은 항목에서 이것을 봤다.

    (비대상)  →  KC 60335-2-32 (마사지기)  확신 0.98

품목명이 아니다. 파일명 "(비대상)충전케이블" 에서 괄호 부분만 잘려 들어갔고, AI 가 확신 0.98 로 엉뚱한 기준을 붙였다. **확신도가 높아도 틀린다.** 사람이 지울 자리가 필요하다는 근거가 그대로 나온 셈이다.

**실측 확인**
- GS1 2026-05 내려받아 브릭 5,318건 적재 · 한국어 99% · 정의문 100% · 임베딩 100%
- 엑셀의 GPC 대응 37건을 담당자 확정으로 채움
- CSV 왕복 시험: 같은 파일을 되돌려 넣으면 **변화 0건**, 새 행은 "새로 추가"로 잡힘, 없는 기준을 가리키는 행은 차단, 적용 1건 후 되돌리기 확인
- 엑셀이 여는 것을 보장하려고 BOM 을 붙였다 — 없으면 한글이 깨진 채로 열린다
- typecheck · build · eslint 오류 0

## 라운드 40 — 법령 용어와 일상 용어의 간극을 자료로 메운다 (품목 확정 10% → 83%)

**담당자 지적** "적용범위는 법령 용어를, 사고보고서는 일상 용어를 쓰는 것에 대해 좀 더 보강할 수 없을까요? 이런 것들이 많이 있어요"

**무엇이 문제였나 — 실측으로 드러난 것** 사고보고서 70건을 적재하고 품목 확정을 돌렸더니 **7건(10%)** 만 붙었다. 원인을 파 보니 어휘였다.

    전기요      ← KC 60335-2-17 은 "전기 담요, 패드들, 의류" 라고 쓴다
    전기레인지   ← KC 60335-2-6 은 "거치형 조리레인지, 호브, 오븐" 이라고 쓴다
    전동킥보드   ← 부속서 72 는 "전동이륜평행차, 전동외륜보드" 라고 쓴다
    모발건조기   ← KC 60335-2-23 은 "피부 또는 모발을 손질하기 위한 기기" 라고 쓴다

설계문서 §5.2.1 이 조항 검색에서 지목한 간극("기준은 전도, 사고는 넘어짐")이 품목 이름에서 똑같이 난다. 글자를 맞추는 방법으로는 넘을 수 없다.

**중간에 원본 오염도 하나 찾았다** `11. 안전확인 부속서 72(전동보드)_result.json` 에 **"전동 Mickey보드"** 가 9번 나온다. 원래 "전동킥보드"다. 협회 원본 파일이 오염돼 있고, 이것 때문에 전동킥보드 사건이 글자 검색으로는 절대 기준을 못 찾았다. 협회에 알려야 한다.

**세 단계로 풀었다**

1. **의미 검색 + 모델 확인** (`scope-semantic.ts`) 품목 이름을 임베딩해 적용범위와 견주니 1위 정확도가 5건 중 2건이었다(전기요는 10위). 품목 이름 하나는 신호가 너무 짧다. 그런데 **상위 10위 안에는 5건 모두** 들어왔다. 그래서 이 저장소가 이미 쓰는 방식을 그대로 썼다 — 뜻으로 후보를 좁히고 고르는 일은 모델에게 맡긴다(`findAndVerifyGpc` 와 같은 구조). 사람이 정한 답과 10건 중 8건 일치.

2. **용어 사전** (042, `scope_term`) 의미 검색만으로는 두 가지가 남는다 — 매번 AI 를 부르고, 모델이 흔들리면 결과도 흔들린다. **한 번 정해진 대응은 지식이고, 지식은 저장해야 한다.** 엑셀이 품목 34종의 담당자 확정 대응을 주므로 그것을 사전에 채웠다(47건). 의미 검색이 새로 찾은 것은 미검수로 쌓이고, 담당자가 확인하면 확정된다.

3. **조회 순서** — 사전 → 등록 품목 → 글자 검색 → 의미 검색 사람이 정한 것을 두고 기계가 다시 고를 이유가 없다.

**결과**

    품목 확정   7건(10%)  →  58건(83%)
    사전        담당자 확정 45건 + AI 미검수 6건

**"못 찾는" 것의 진짜 이유가 드러났다** 남은 12건은 검색 실패가 아니었다. 엑셀이 지목한 기준이 **우리 DB 에 없다.**

    LED등기구    → KC 60598-1, KC 61347-2-1   (미적재)
    할로겐등기구   → KC 61058-1                 (미적재)
    전기스토브    → KC 60335-2-30              (미적재)
    후드믹서     → KC 60335-2-14              (미적재)
    전기자전거    → ISO 4210-6:2023            (KC 기준 아님)
    자동판매기    → 엑셀에도 기준이 '-'

원인이 다르면 해야 할 일도 다르다. 이건 검색을 고칠 일이 아니라 기준 JSON 을 받아 적재할 일이다. `npm run scopes:seed-terms` 가 이 목록을 그대로 찍어 준다.

**왜 이 구조가 이 프로젝트에 맞는가** 이 사전은 §10 이 말한 "판단이 쌓여 자산이 된다"를 품목 확정에서 그대로 구현한 것이다. 쓸수록 AI 호출이 줄고, 결과가 안정되고, 담당자가 고칠 수 있는 형태로 남는다.

**실측 확인**
- 이전에 전부 실패하던 품목들이 담당자 확정 근거와 함께 해결되고, 엑셀 정답과 일치 (전기요·전기찜질기·전기자전거·모발건조기·후드믹서)
- `모발건조기 → KC 60335-1 + KC 60335-2-23` — 엑셀이 적은 두 기준과 정확히 같다. 제1부는 기존 `withGeneralPart` 가 자동으로 더한다
- typecheck · eslint 오류 0

## 라운드 39 — 3번째 산출물(KC안전기준 개선 요인) 안 1 구현

**한 일**
- 집계 뷰 세 개를 만들었다 (041)
- `/insights` 화면을 만들고 좌측 레일에 붙였다
- `03_3번째 산출물 구현 설계서.md` 를 썼다

**왜 안 1(읽기 전용)부터인가** 원 계획서의 권고 그대로다 — 표를 먼저 만들면 집계 정의가 틀렸을 때 그 표까지 버려야 한다. 지금은 "이 집계 정의가 실제 데이터에서 성립하는가"를 보는 단계다.

**이 기능의 전부는 0건의 뜻을 나누는 것이다** "대응 조항이 없다"·"아직 분석을 안 했다"·"품목이 확정되지 않았다"는 전혀 다른 말인데 집계하면 전부 0으로 보인다. 그래서 판정에 순서를 뒀다 — 품목 미확정 → 코드 미부여 → 분석 미실행 → 검토 대기 → **그제야** 사각지대 후보. 품목이 없으면 적용할 기준을 정할 수 없으므로 "대응 조항이 없다"고 말할 자격이 없다.

**첫 집계가 곧바로 세 가지를 알려 줬다 — 이것이 안 1 을 먼저 만든 값어치다**

1. **지금 병목은 사각지대가 아니라 품목 확정이다.** 전체 2,264건 중 품목이 확정된 것이 **39건(1.7%)**뿐이고, 2,225건이 그 단계에서 막힌다. 사각지대 후보로 올라온 것은 **단 1건**이다. 즉 이 산출물이 지금 말하는 것은 "기준에 구멍이 있다"가 아니라 "품목 확정률을 올리기 전에는 기준 개선을 논할 수 없다"이다.

2. **국내외 비교는 "번호는 이어지는데 견줄 수치가 없다"에서 막힌다.** 번호 대조 자체는 작동한다 — EN 62368-1 → KC 62368-1 등 127건이 이어졌다. 그런데 **비교 가능이 0건**이다. 원인을 확인했더니, 허용치 333건이 전부 **어린이제품 부속서 계열**에만 있고 EU 리콜이 대조되는 **전기용품(KC 6xxxx) 계열에는 0건**이었다. 파서가 전기용품 계열의 표를 못 뽑고 있다. 집계 로직의 문제가 아니라 재료의 문제다.

3. **시험항목은 표본 부족이지 저활용이 아니다.** `review_log` 가 0건이라 `LOW_ADOPTION` 판정이 나올 수 없다. 제시된 적 있는 4개도 전부 검토 대기다. 이것을 "저활용"으로 세었다면 없는 신호를 만들어 냈을 것이다.

**세 가지 다 표를 만들기 전에는 알 수 없던 것이다.** 안 2(스냅샷)부터 갔다면 아직 뜻이 정해지지 않은 숫자를 고정하고 있었을 것이다.

**하지 않기로 한 것** 화면에 "기준이 낮다·높다", "강화·완화해야 한다", "법적 의무가 있다"를 쓰지 않는다. 자동 보고서 문장도 만들지 않는다. 표와 근거까지가 시스템의 몫이고 판단은 담당 부서와 전문가가 한다. 화면 맨 위에 이 사실을 적어 뒀다 — 숫자가 있는 화면은 결론이 있는 것처럼 읽히기 때문이다.

**실측 확인**
- 세 뷰가 실제 데이터에서 도는 것 확인(사각지대 3상태 188묶음, 대조 2상태 201표준, 시험항목 2상태 524개)
- `anon`·`authenticated` 접근 권한 없음
- `/insights` 빌드 확인 · `typecheck` · `eslint` 오류 0 · 번들 비밀값 검사 통과

**다음에 할 일 — 실측이 정한 순서**
1. 품목 확정률 올리기 (2,225건이 여기서 막힌다)
2. 전기용품 계열 허용치 파싱 고치기 (국내외 비교가 가능해지는 유일한 길)
3. 담당자 검토 기록 쌓기 (채택·반려, 조항 코드 검수)
4. 그 뒤에 안 2(스냅샷) — 지금은 자료가 바뀌면 숫자도 바뀌어 회의 자료로 못 쓴다

## 라운드 38 — 검색 함수 정렬 보장, 그리고 마이그레이션이 서로를 지우던 문제

**한 일**
- 상위 N건을 자르는 `limit` 앞에 정렬을 명시했다 (038)
- 갈라져 있던 검색 함수 두 개를 한 파일에서 함께 만들도록 정리했다 (040)

### 정렬 보장 — "지금 문제없다"를 이유로 두지 않았다

코드 갈래와 키워드 갈래는 등수를 매긴 뒤 상위 60건만 남기는데, 창 함수 안의 `order by` 는 등수를 계산하는 순서일 뿐 바깥 `limit` 이 어느 행을 남길지는 정하지 않는다. SQL 표준은 바깥 `order by` 없는 `limit` 의 대상을 보장하지 않는다. 즉 "상위 60건"이 아니라 "아무 60건"이 될 수 있었다. 세 번째 갈래(vector)에는 명시적 정렬이 있어 두 곳만 빠져 있었다.

지금 틀린 결과가 나오고 있었을 가능성은 낮다 — PostgreSQL 의 WindowAgg 는 창 함수 정렬 순서대로 행을 내보낸다. 그러나 그것은 구현이 그렇다는 것이지 약속이 아니다. 실행계획이 바뀌면 조용히 달라지고, **검색 결과가 조용히 달라지는 것은 이 체계에서 가장 나쁜 종류의 고장**이다 — 아무도 오류를 보지 못한 채 잘못된 조항을 검토하게 된다. 두 줄이면 보장으로 바뀌므로 미룰 이유가 없었다.

### 마이그레이션이 서로를 지우고 있었다

038 을 넣고 확인하다 **032 가 만든 13인자 호환 함수가 사라진 것**을 발견했다. 038 이 지운 것이 아니었다. 원인은 순서다.

030 첫머리에 13인자 `drop` 이 있다. 030 을 쓸 당시에는 옛 함수를 치우는 올바른 문장이었는데, 032 가 같은 시그니처로 **호환 함수**를 새로 만들면서 그 줄은 이제 "호환 함수를 지우는 줄"이 됐다. 파일 번호상 030 이 032 보다 먼저 도니, 전체를 다시 적용하면 결과가 같지만 **일부만 다시 도는 경우** 어긋난다. 실제로 db:push 가 내용이 바뀐 파일만 골라 돌렸을 때 030 은 재적용되고 032 는 건너뛰어져, 호환 함수만 사라졌다.

이것이 조용한 사고인 이유 — 호환 함수가 없어도 새 코드는 14인자로 부르므로 잘 돈다. 옛 코드가 도는 **배포 구간에서만** 죽는다. 평소엔 아무 증상이 없다가 배포하는 순간 분석이 멈춘다. 032 가 막으려던 바로 그 사고가 그대로 되돌아온다.

게다가 038 이 라이브 정의를 뜬 시점의 14인자에는 (재적용된 030 때문에) 기본값이 다시 붙어 있었다. 그래서 039 로 13인자를 되살리자 이번엔 모호성이 났다.

    ERROR: function public.clause_hybrid_search(...) is not unique

**진짜 원인은 한쪽만 고치는 파일을 여러 번 쓴 것이다.** 두 함수는 따로 존재할 수 없는데 따로 고칠 수 있게 두었다. 040 에서 둘을 한 파일에 합치고 규칙을 적어 두었다 — 앞으로 이 함수를 바꿀 때는 040 을 통째로 복사해 새 번호로 만들고 본체만 손본다. 그리고 그 파일이 가장 큰 번호여야 한다.

**실측 확인**
- 시그니처 두 개가 모두 등록되고, 13인자·14인자 호출이 **둘 다 모호성 없이** 동작
- 14인자에 정렬 수정이 들어간 것 확인(`order by rnk`)
- `anon` 실행 권한 없음, `service_role` 있음 — 둘 다
- 사건 4639 실제 분석 → 후보 20건(수정 전과 같다)

## 라운드 37 — 검수 화면·분석 게이트·번들 검사 (권고 1~4)

**한 일**
- `/api/standards/sync` 를 잠갔다 — 토큰 미설정 시 통과에서 차단으로
- 조항 태그 검수 화면(`/standards/review`)과 검수 이력 표를 만들었다 (037)
- 분석 실행 전 자료 상태를 확인하는 게이트를 넣었다 (`readiness.ts`)
- 클라이언트 번들 비밀값 검사를 만들고 CI 에 붙였다
- 화면 조회 시간을 실측했다

### 조항 검수 화면 — 가장 값어치 있는 한 칸이었다

`clause_tag.review_status` 를 **바꾸는 코드가 저장소에 한 줄도 없었다.** 전부 읽기뿐이었고, 그래서 조항 태그 1,855건이 100% `auto_unreviewed` 였다. 그 결과 세 가지가 동시에 죽어 있었다.

1. **근거등급 A** — `evidenceLevelOf()` 는 승인 태그가 있을 때만 A 를 준다. 승인이 0건이므로 A 가 나온 적이 없다. 등급 체계의 최상단이 쓰이지 않는 칸이었다.
2. **030 의 검색 스위치** — 검수 확정분만 쓰도록 만든 인자인데, 켜면 코드 근거가 전부 사라진다. 승인할 방법이 없으니 영원히 그렇다.
3. **3번째 산출물** — "담당자가 검토한 기록"을 재료로 삼는데 그 기록이 안 쌓인다.

설계문서가 §3.3 에서 "사람이 검수 확정한 태깅은 자동 재태깅으로 덮어쓰지 않는다"고 적어 둔 것도, 확정할 길이 없으면 지킬 것이 없는 약속이었다.

화면을 만들면서 지킨 것들 —
- **조항 단위로 받는다.** 코드 하나씩 누르게 하면 같은 본문을 여러 번 읽어야 하고, 그러면 뒤로 갈수록 대충 누르게 된다. 판단의 질이 떨어지는 쪽으로 화면이 유도하면 안 된다.
- **확정을 기본값으로 두지 않는다.** 아무것도 안 누르면 미검수로 남는다. "다음"만 계속 눌러 전부 확정되는 화면은 검수가 아니라 통과 의식이다.
- **근거 문구가 조항 전체에 대한 것임을 밝힌다.** 지금 구조는 조항 하나의 `evidence_span` 을 그 조항의 모든 코드가 함께 쓴다. 코드별 근거로 오해하면 잘못된 확정이 나온다. 코드별로 나누려면 태깅 프롬프트를 고쳐야 해서 이번 범위 밖이다.
- **상태 변경과 이력을 한 트랜잭션으로** 묶었다(DB 함수). 따로 나가면 "바뀌었는데 왜 바뀌었는지 모르는" 행이 남는다.

### 분석 게이트 — 일괄 적용하려다 실측에서 막혔다

원 계획서는 `is_confirmed` 로 분석 가능 여부를 묶자고 했다. 그대로 하려다 세어 보니 **리콜 2,259건이 전부 `is_confirmed = false`** 였고 리콜 화면에는 확인 버튼이 아예 없다. 일괄 게이트를 걸면 리콜 분석이 통째로 막힌다.

두 트랙에서 "확인"의 뜻이 다르기 때문이다. 사고보고서는 PDF 에서 뽑은 글자가 제대로인지 사람이 봐야 하지만(표가 뭉개졌는지는 기계가 모른다), 리콜은 협회가 `approval_status='approved'` 로 승인한 것만 받아 온다. 우리 쪽에 다시 확인할 원문이 없다. **없는 절차를 있는 것처럼 요구하면 담당자는 뜻 없는 버튼을 2,259번 누르거나 게이트를 꺼 버린다.** 그래서 트랙별로 나눴다.

### server-only 는 넣었다가 되돌렸다

React 의 `server-only` 패키지를 넣어 봤더니 **CLI 가 전부 죽었다.** 그 패키지의 기본 진입점은 Node 에서 무조건 예외를 던지고, 빈 모듈은 `react-server` 조건에서만 선택된다. 이 저장소는 명령줄과 화면이 `src/lib` 를 함께 쓰고(CLAUDE.md §9), `db.ts`·`env.ts` 를 import 하는 스크립트가 19개다. 그것들을 다 죽이면서 얻을 보호가 아니었다.

그래서 원 계획서가 원래 적어 둔 방식으로 돌아왔다 — **"빌드 산출물을 검사한다".** import 를 막는 대신 실제로 샜는지를 결과물에서 확인한다(`npm run check:bundle`). 값 대조(실제 비밀값 문자열)와 모양 대조(`postgresql://`·`sb_secret_`·`service_role`) 두 가지로 보고, 찾아도 값 자체는 절대 출력하지 않는다 — 오류 메시지에 남으면 그것이 또 하나의 유출이다.

### 화면 로딩 실측 — 가정이 틀렸다

02 설계서 §4.1 이 "먼저 잰다"고 한 것을 실제로 쟀다.

    사고 요약(스칼라 서브쿼리 4개)     중앙 11ms
    사고 목록 25행(행별 서브쿼리 5개)  중앙 13ms
    리콜 전체 건수                   중앙 11ms
    리콜 목록 25행                   중앙 12ms

**쿼리가 병목이 아니다.** 원 계획서는 "행별 상관 서브쿼리 때문에 페이지당 DB 작업량이 커질 수 있다"며 집계 CTE·뷰·인덱스로 정리하자고 했는데, 25행에 서브쿼리 5개씩 붙여도 13ms다. **이 항목은 닫는다** — 고칠 것이 없다.

순차 대 병렬도 쟀다. 처음엔 병렬이 158ms 로 더 느리게 나와 라운드 33 의 변경이 손해인 줄 알았는데, 그것은 연결을 새로 여는 첫 호출이었다. 풀이 데워진 뒤로는

    순차 중앙 30ms  →  병렬 중앙 11ms

이므로 그 변경은 옳다. 다만 `loading.tsx` 가 여전히 값어치 있는 이유는 따로 있다 — 느린 것은 쿼리가 아니라 서버리스 기동과 네트워크이고, 그것은 뼈대를 먼저 그려야 가려진다.

**실측 확인**
- 검수 기능을 실물로 시험: 조항 4(2-13)의 코드 2건을 확정 → `approved` 로 바뀌고 이력 2행이 판번호(v0.9.7)와 함께 남았다. 같은 것을 또 눌러도 0건(중복 없음). 사유 없는 반려는 막혔다. 시험 뒤 원래대로 되돌렸고 확인했다
- 분석 게이트: 사고보고서(확정됨)·리콜 2건 모두 통과. 사고보고서를 임시로 미확인으로 바꾸자 `SOURCE_UNCONFIRMED` 로 막혔고, 되돌린 뒤 다시 통과했다
- 번들 검사: 일부러 `postgresql://` 문자열을 심으니 잡아냈고 **종료코드 1**, 제거하니 0. CI 가 실제로 막는다는 뜻이다
- `npx eslint .` 오류 0 · 경고 2(기존 코드) · `typecheck` · `build` 통과
- `/standards/review` 가 빌드 산출물에 올라온 것 확인

**남은 것**
- 리콜 증분 수집, 창 함수 `limit` 정렬 보장
- 코드별 근거 분리(태깅 프롬프트 변경 필요)
- 3번째 산출물(별도 문서)

## 라운드 36 — 검사와 복구를 사람 기억에서 떼어 낸다 (Lint 도입 + 자동 재실행)

**담당자 지적** "대기열 확인 및 오류 재실행, Lint 등 통합 로직도 자동으로 돌아가야 하는 것 아닌가"

맞는 지적이다. 라운드 34·35 에서 찾아낸 것들은 전부 **내가 손으로 조사해서** 나왔다. `recalls-fetch` 가 52번 연속 실패하는 동안 자동으로 다시 시도한 적이 한 번도 없었고, 알림은 쌓이기만 해서 아무도 읽지 않게 됐다. 그게 문제의 본질이었다.

**먼저 확인한 것 — 끊긴 자료가 오염을 남겼는가** "중간에 끊기면 잘못된 값이 들어간 것 아닌가"라는 물음에 답하려고 전수 감사를 했다. **13개 항목 전부 0건**이다.

    고아 캐시 0 · 캐시 없는 사건 0 · 코드 없는 사건 0 · 검색문장 빈 사건 0
    한쪽 축만 있는 사건 0 · 주코드 없는 축 0 · external_ref 중복 0
    코드북에 없는 코드(사건·조항) 0 · 근거 빈 태그 0
    사라진 조항을 가리키는 결과 0 · 과거 판 조항을 가리키는 결과 0

이유는 두 가지다. 504 는 Netlify 프록시가 먼저 포기한 것이고 Lambda 는 뒤에서 계속 돌아 작업을 끝냈다. 그리고 쓰기가 전부 upsert 라 다음 순환이 같은 구간을 덮는다. **운이 좋았던 것이지 구조가 지켜 준 것이 아니다** — 라운드 33 에서 트랜잭션으로 묶어 둔 것이 이제야 그 자리를 메운다.

부수적으로 하나 더 나왔다. 고치기 전에는 그 40건에 매 주기 AI 를 다시 불렀는데, LLM 은 비결정적이라 **주기마다 코드가 조금씩 달라져 검색문장과 임베딩이 계속 갈렸다.** 라운드 35 의 재사용 수정이 이것도 같이 잡았다.

**Lint — 돌아간 적이 없었다** `package.json` 에 `next lint` 스크립트는 있었지만 **ESLint 설정 파일이 없었다.** 설정이 없으면 next lint 는 검사 대신 "어떻게 설정할까요?"를 되묻고 멈춘다. 즉 스크립트만 있고 검사는 한 번도 돌지 않았다. 게다가 next lint 는 Next 16 에서 사라진다.

eslint 9 + eslint-config-next 를 넣고 `eslint.config.mjs` 를 만들었다. 규칙을 처음부터 세게 걸지 않은 이유가 있다 — 이미 쓰여 있는 코드에 엄격한 규칙을 한 번에 걸면 경고가 수백 개 나오고, 그러면 아무도 안 보게 된다.

첫 실행 결과: **오류 0건, 경고 3건.** 그중 하나는 이번에 내가 만든 것이라 고쳤고, 나머지 둘(`postcss.config.mjs` 익명 기본 내보내기, `layout.tsx` 커스텀 폰트)은 기존 코드라 손대지 않았다(CLAUDE.md §3).

**어디서 자동으로 도는가** GitHub Actions(`.github/workflows/ci.yml`)에서 푸시·PR 마다 lint → typecheck → build 를 돈다. Netlify 빌드와 분리한 이유는, lint 경고 때문에 배포가 멈추면 급할 때 사람들이 검사를 끄는 쪽을 택하기 때문이다. 배포를 막을지는 경고가 0 이 된 뒤에 정한다.

**자동 재실행 — 알림 다음이 비어 있었다** 지금까지 자동으로 도는 것은 세 가지였다: `embed_tick`(1분, 5회까지 재시도), `ops_watch`(5분, 실패를 세어 알림), 정기 작업(2~5분). 빠진 것은 **"알린 다음"**이다.

    035  실패한 정기 작업을 같은 구간으로 다시 보낸다 (5분마다, 구간당 최대 3회)
         세워 둔 임베딩과 태깅 조항을 하루 한 번 다시 대상에 넣는다 (새벽 4시 10분)
    036  인자를 모르는 옛 기록은 다시 보내지 않는다

036 이 필요했던 이유가 이번 라운드에서 가장 조심스러운 지점이다. 035 가 `job_run.query` 컬럼을 새로 만들었으므로 그전 기록 257건에는 인자가 없다. 그런 행을 재시도하면 `run_job_at(job, null)` 이 되어 URL 에서 인자가 통째로 빠지고, 라우트가 **기본값 limit=20** 으로 돈다. 034 에서 3건으로 낮춘 이유가 30초 제한이었는데 재시도가 20건을 요청하면 반드시 시간 초과가 난다 — **실패를 고치려고 만든 장치가 더 큰 실패를 만드는 셈**이다. 인자를 아는 실행만 다시 보내도록 막았다.

**왜 즉시 재시도와 하루 한 번을 나눴는가** 정기 작업 실패는 대개 일시적(시간 초과·순간 장애)이라 곧바로 다시 하면 풀린다. 반면 임베딩·태깅 실패는 원인이 자료 자체일 가능성이 높아, 곧바로 반복하면 같은 실패를 분 단위로 되풀이하며 돈만 쓴다. 하루 한 번이면 "그 사이 사람이 고쳤을 수도 있는" 간격이 되고, 안 고쳐졌으면 하루 한 번의 비용만 든다(태깅 40건 기준 약 $0.09).

담당자가 "전부 자동(태깅 포함)"을 선택했으므로 태깅도 자동으로 되돌린다. 다만 **같은 건수가 사흘 내리 반복되면 알림을 보낸다** — 자동 재시도로 풀리지 않는 문제는 자동화가 아니라 사람이 볼 일이기 때문이다. 되돌린 이력은 `recovery_log` 에 쌓고 운영 화면 「확인이 필요한 것」 맨 위에 보여 준다. 자동으로 도는 것일수록 무엇이 몇 번 되풀이되는지 보여야 한다 — 안 그러면 "문제 없음"과 "매일 같은 문제를 덮고 있음"이 화면에서 똑같아 보인다.

**실측 확인**
- `npx eslint .` → 오류 0 · 경고 2(둘 다 기존 코드)
- `npm run typecheck` · `npx next build` · `git diff --check` 통과
- 자동 재시도를 실물로 시험했다. `limit=20` 으로 일부러 실패시키자(30초 초과) 504 가 기록되고, 인자(`offset=300&limit=20`)가 보존된 채 자동으로 다시 발송됐다. **같은 것을 또 불러도 중복 발송하지 않았다**(0건)
- **무한 재시도가 막히는지 확인했다** — 시도 1 → 2 → 3 에서 정확히 멈췄고 4회차는 생기지 않았다. 이 장치의 가장 위험한 실패 모드라 반드시 확인해야 했다
- `auto_recover()` 실행 정상. `anon` 이 부를 수 없는 것 확인 (`retry_failed_jobs`·`auto_recover`·`run_job_at`·`reset_tag_failures` 전부 false)
- `embed_tick` 은 이미 잘 돌고 있었다. 앞서 "막힌 3건"으로 보였던 것은 발송하고 응답을 기다리는 중간 상태였고, 실제로 곧 임베딩이 채워졌다(956·957·958)

## 라운드 35 — 리콜 수집 꼬리 제거: 이미 해 둔 AI 보정을 매번 다시 하고 있었다

**경보** `recalls-fetch` 504 가 한 건 왔다(2026-09-04 18:00). 034 이후로는 75건 중 74건이 성공하고 있었으므로 52/52 실패였던 그전과는 성격이 다르다 — 꼬리만 남은 것이다.

**무엇이 꼬리를 만들었나** 034 이후 성공한 74건의 분포를 봤다.

    중앙 13,912ms · p90 20,052ms · 최대 28,182ms (제한 30,000ms)
    25초를 넘긴 2건, 그리고 504 로 죽은 1건

느린 실행이 **전부** `llmReclassified: 1` 이었다. AI 재분류 한 번이 약 6초라, 그것이 섞인 구간만 제한 근처로 간다.

**그런데 그 AI 호출은 처음부터 필요 없는 것이었다** 코드북 v0.9.7 에 없는 `HF.S.STD` 를 가진 리콜이 40건인데, 세어 보니 **그 40건 전부가 이미 `ADMIN-recall_hub-v0.9.7+LLMFIX` 태그를 갖고 있었다.** 보정은 끝나 있었다.

판단 근거가 원본의 `hazard_factor_code` 하나뿐이었던 것이 원인이다. 그 값은 협회 관리자 시스템의 것이라 우리가 고칠 수 없고, 다음 주기에도 그대로 무효다. 그래서 같은 사건을 볼 때마다 AI 를 다시 불렀다 — 26.7시간마다 40번씩, 영원히.

돈이 새는 것보다 나쁜 것은 그것이 시간 제한을 밀어 올렸다는 점이다. 배치 크기를 아무리 재서 맞춰도, 매 주기 되풀이되는 불필요한 6초가 남아 있는 한 꼬리는 사라지지 않는다. 034 에서 3건으로 낮춘 것은 증상을 눌렀을 뿐 원인을 건드리지 못했다.

**어떻게 고쳤나** AI 를 부르기 전에 "이 사건에 이미 보정해 둔 것이 있는가"를 먼저 본다. 코드북 판번호가 지금과 같을 때만 재사용하므로, 판이 올라가면 자연히 다시 부른다.

재사용한 축은 태그를 다시 넣지 않는다. 이 갈래가 없으면 `llmFix` 가 null 이라 관리자 승인 행(`review_status='approved'`, 판번호에 LLMFIX 없음)으로 들어가서, **AI 가 고른 코드가 사람이 승인한 것처럼 기록된다.** 근거등급 A 의 뜻이 무너지는 일이라 반드시 막아야 한다.

검색용 문장 갱신은 태그를 새로 넣지 않는 경우에도 한다 — 원본의 제목·서술이 바뀌면 검색용 문장도 따라 바뀌어야 하기 때문이다.

**실측 확인**
- 느렸던 세 구간(783·735·1400)을 고친 코드로 실행: `llmReclassified=0`, `tagged=3` 유지. 코드는 그대로 붙고 AI 호출만 사라졌다
- 실행 뒤 태그 행 수가 그대로다(LLMFIX 82 / 관리자 승인 75). 새로 만들어진 행 없음
- "LLM 이 고른 코드가 approved 로 들어간 행" 0건
- 리콜 2,259건 중 임베딩 없는 것 3건 — 불필요한 재생성이 일어나지 않았다
- `npm run typecheck` 통과

**배포해야 듣는다** 이 수정은 코드 쪽이다. 배포 전까지는 약 1.3%(75건 중 1건) 확률로 504 가 계속 난다. 급하면 SQL 만으로 구간을 3건에서 2건으로 낮춰 임시로 막을 수 있으나, 배포하면 불필요해지는 조치다.

## 라운드 34 — 정기 작업 실패 경보 대응 (배포 순서 사고 + 리콜 배치 크기)

**경보 내용** `recalls-fetch` 가 502·504 로 실패한다는 알림이 쌓였다(2026-09-04 15:55).

**둘로 갈렸다. 하나는 이번 라운드가 만든 사고다.**

### 사고 1 — 스키마만 앞서 나가고 코드가 따라오지 않았다 (내가 만든 것)

라운드 33 에서 마이그레이션 030 을 프로덕션 DB 에 적용하면서 옛 13인자 `clause_hybrid_search` 를 지우고 14인자짜리로 바꿨다. 그런데 그 시점에 배포된 사이트는 아직 13인자로 부르는 옛 코드였다. **배포 사이트의 분석 실행이 "function does not exist" 로 죽고 있었다.** 화면은 멀쩡해 보이고 버튼을 눌러야 드러나므로 경보에도 안 잡혔다 — 리콜 경보를 조사하다 발견했다.

배운 것: 스키마를 먼저 올리고 코드를 나중에 올리는 것은 예외가 아니라 정상 순서다. 두 시점 사이에는 **늘** 옛 코드와 새 스키마가 함께 도는 구간이 있다. 그 구간에서 기능이 죽지 않으려면 옛 호출 방식이 살아 있어야 한다.

마이그레이션 032 로 13인자 호환 함수를 되살렸다. 만들면서 실측한 것:
- 14인자에 기본값이 남은 채 13인자 함수를 함께 두면 13인자 호출이 모호해진다. 실제로 확인했다 — `function public._tov(integer) is not unique`
- 마지막 인자의 기본값만 뗄 수는 없다. PostgreSQL 은 기본값 있는 인자 뒤에 기본값 없는 인자를 두지 못하게 한다
- `create or replace` 로는 기본값을 제거할 수 없다 — `cannot remove parameter defaults from existing function`

그래서 14인자 본체는 지웠다 다시 만들되 기본값을 전부 없애고, 짧은 호출은 13인자 래퍼가 받게 나눴다. 본체는 손으로 옮겨 적지 않고 `pg_get_functiondef()` 로 DB 에서 그대로 뽑았다 — 140줄을 옮겨 적다 한 글자만 틀려도 검색 결과가 조용히 달라진다.

### 사고 2 — 리콜 배치 10건이 시간 제한 절벽 바로 너머에 있었다 (기존 문제)

`recalls-fetch` 는 **200 을 한 번도 받은 적이 없다**(48시간 52번 전부 실패, `마지막 성공 = null`). 같은 시간대에 `standards-sync` 는 50/52 성공했으므로 인증·배포·DB 는 멀쩡했다. 즉 내 변경과 무관한, 처음부터 있던 고장이다.

배포 환경에 직접 요청을 보내 쟀다.

| limit | 소요 |
|---|---|
| 1 | 6,983ms |
| 2 | 10,229ms |
| 3 | 12,663ms |
| 4 | 15,609ms |

깨끗한 직선이다 — 고정비 약 4,100ms + 건당 약 2,875ms. **028 이 정한 10건을 넣으면 32,850ms 로 30초 제한을 넘긴다.** 자료가 있는 구간은 예외 없이 죽는다. 자료가 없는 구간(오프셋 2,260 이상)은 1,098ms 로 잘 끝나지만, 최근 52번은 오프셋이 순차로 도는 구간이라 전부 자료가 있는 쪽에 몰려 있었다.

028 의 10건은 재 보고 정한 값이 아니었다. "20건은 위험하니 10건" 정도의 짐작이었고 그 짐작이 한 번도 맞은 적이 없다.

**평균으로 고르면 안 된다는 것을 두 번째 측정에서 배웠다.** 033 에서 5건으로 낮췄는데, 여러 구간에서 다시 재 보니 흩어짐이 컸다.

| offset | 5건 소요 |
|---|---|
| 200 | 20,851ms |
| 700 | 20,052ms |
| **1400** | **27,186ms** |
| 1900 | 19,930ms |

최악이 27,186ms 로 여유가 2.8초뿐이고, AI 재분류가 섞이면(전체의 1.8%, 약 6초 추가) 33,186ms 로 넘긴다. 5건은 "대체로 되지만 가끔 죽는" 값이었다. 이 작업은 실패해도 화면에 표시가 없고 알림만 쌓이므로, 가끔 죽는 설정은 오래 방치된다 — 실제로 52번 연속 실패하는 동안 아무도 몰랐다.

그래서 034 에서 최악값(건당 약 4,617ms)으로 다시 계산해 **3건**으로 정했다. 한 바퀴가 느려지는 것은 주기를 5분에서 2분으로 줄여 벌충했다 — 800구간 × 2분 = 약 26.7시간으로, 문서에 적어 두었던 42시간보다 오히려 빠르다.

**건수뿐 아니라 시간으로도 끊게 했다.** `loadRecalls` 에 `timeBudgetMs` 를 넣어 15초를 넘기면 새 건을 시작하지 않는다(tag-chunk 가 이미 쓰는 방식). 건당 비용은 앞으로 바뀐다 — 코드북에 없는 코드가 늘면 AI 재분류가 늘고, 외부 조회가 느려지면 그만큼 는다. 그때 다시 절벽을 넘지 않게 하려는 것이다. **이 부분은 배포해야 듣는다.**

**실측 확인**
- 032 적용 후 13인자·14인자 호출을 각각 실행해 둘 다 모호함 없이 동작하는 것 확인. `anon` 실행 권한 없음, `service_role` 있음
- 사건 4639 로 실제 분석 실행 → 후보 20건 정상 복구(마이그레이션 전과 같은 건수)
- 034 적용 후 최악이었던 구간(1398·1401·1404)을 3건으로 실행 → 최대 20,869ms, 여유 9.1초. AI 1건이 섞여도 26.9초로 제한 안쪽
- **실제 cron 이 돌린 7건이 모두 200.** 직전 52건은 모두 실패였다
- `npm run typecheck` · `npx next build` · `git diff --check` 통과

**아직 배포되지 않은 것** 라운드 33·34 의 코드 변경(리콜 트랜잭션, 시간 예산, 검색 스위치, 태깅 실패 처리, `loading.tsx` 등)은 전부 커밋·배포 전이다. DB 는 마이그레이션 029~034 까지 적용돼 있다. 032 덕분에 배포 전에도 옛 코드가 정상 동작하지만, 나머지 개선은 배포해야 효과가 난다.


## 라운드 33 — 02 설계서의 P0·P1 구현과 실측 판정

**한 일**
- 마이그레이션 029·030·031을 추가하고 적용했다
- 태깅 실패 조항이 대기줄을 막던 문제를 고쳤다 (P0-1)
- 검색에 검수 확정 태그만 쓰는 스위치를 만들었다 (P0-2, 기본은 꺼짐)
- 원본 보관에 실패하면 사건을 만들지 않도록 바꿨다 (P0-3)
- 네 화면에 `loading.tsx`를 붙이고, 서로 무관한 조회를 동시에 보내도록 바꿨다
- 리콜 한 건의 DB 쓰기를 한 트랜잭션으로 묶었다
- 분석 실행에 질의 원문·검색어·재채점 상태·프롬프트 판번호를 남기게 했다
- 환경설정 숫자에 범위 검증을, 작업 URL의 `limit`·`offset`에 상한을 뒀다
- 임베딩 차원을 코드 상수로 고정했다
- 안전기준 화면의 상단 집계를 현재 판 기준으로 통일했다

**왜 했는가 — 계획서의 등급이 실제 피해와 달랐다**

원 계획서(`docs/코드베이스_성능_로직_개선계획_2026-09-04.md`)는 P0이 8개였다. 코드를 열어 보니 관찰은 대부분 사실이었지만 **가장 비싼 문제가 곁가지에 있었다.**

태깅에 실패한 조항은 `clause_tag`에 아무것도 남기지 않는다. 그런데 대상 조회 조건이 "태그가 없는 조항"이라 실패분이 대기줄 맨 앞에 영원히 남는다. 순서가 `(standard_id, order_index)`로 고정이라 자리가 밀리지도 않는다. 그래서

- `offset=0` 요청이 1분마다 같은 조항을 다시 AI로 부른다 (돈이 계속 나간다)
- `countTaggable()`과 `run_tag_chunk()`의 `v_left`가 0에 닿지 못한다
- 자동 실행이 스스로 꺼지지 않고 완료 알림도 오지 않는다

반대로 원 계획서가 P0으로 올린 "고정 오프셋 때문에 조항을 건너뛴다"는 **자기회복된다.** 앞의 것들이 처리될수록 밀린 조항이 목록 앞으로 당겨져 결국 `offset=0` 범위에 들어오고, 종료 조건이 매번 새로 세는 `v_left = 0`이라 미처리분을 남기고 꺼질 수 없다. 중복 과금도 구조적으로 불가능하다 — 예산 12초·동시 6건이면 한 요청의 실처리가 10건 안팎인데 구간 간격이 40이다. 그래서 예약 장부(`FOR UPDATE SKIP LOCKED`)는 만들지 않았다.

**확인한 것 — 숫자로 판정한 것**

DB에서 직접 세어 원 계획서의 남은 두 P0을 판정했다.

| 잰 것 | 결과 | 판정 |
|---|---|---|
| 조항별 태깅 판 개수 | **730개 조항 전부 1판.** 2판 이상 0건 | 태그 버전 중복은 **존재하지 않는다**. P0으로 볼 근거가 없다 |
| 조항 태그 검수 분포 | **auto_unreviewed 1,855건 (100%), approved 0건** | 검수 확정 태그가 하나도 없다 |
| 분석 결과의 코드 근거 | 160건 전부 `match_path='CODE'`, 그 **160건 전부가 미검수 태그만으로** 받은 등급 | 스위치를 켜면 코드 근거가 전부 사라진다 |
| 태깅 실패 조항 | 0건 (029 적용 시점) | 아직 쌓이지 않았다. 지금 막는 것이 맞다 |
| 남은 태깅 대상 | 5,670건 | 코드 부여는 아직 초반이다 |
| `review_log` | **0건** | 담당자 검토 기록이 아직 없다 |

마지막 줄이 중요하다. 3번째 산출물(기준 개선 요인)의 원자료로 `review_log`를 꼽아 두었는데, 실제로는 **비어 있다.** 집계를 만들어도 지금은 아무것도 나오지 않는다. 표를 먼저 만들 일이 아니라 검토 기록이 쌓이는 것이 먼저다.

**왜 검색 스위치의 기본값을 끄로 뒀는가**

미검수 비율이 100%였기 때문이다. 켜면 지금 담당자가 보는 코드 근거 160건이 전부 `HYBRID`로 내려간다. 그 변화를 예고 없이 주면 "어제까지 근거가 있던 조항이 오늘 사라진" 것으로 보인다. 그래서 `p_require_approved_tags`를 인자로만 만들고 기본은 기존 동작 그대로 뒀다. 어떤 설정으로 돌렸는지는 `match_run.require_approved_tags`에 남으므로 나중에 두 결과를 나란히 비교할 수 있다.

화면이 완전히 무방비였던 것은 아니다. `evidenceLevelOf()`가 승인 태그가 있을 때만 근거등급 A를 주므로 지금 모든 결과는 B로 표시되고 있다. 다만 그것은 "표시를 달리하는 것"이고, 이번에 만든 스위치는 "후보에서 빼는 것"이다. 층이 다르다.

**왜 원본 보관 실패에서 멈추게 했는가**

전에는 보관에 실패해도 진행했다. 주석에 이유가 적혀 있었다 — "추출 결과까지 버리면 담당자가 다시 올려야 하기 때문". 그런데 그렇게 하면 사건이 만들어지고 분석까지 이어지는데 원본 PDF는 어디에도 없다. 사고보고서는 개인정보 때문에 저장소에 두지 않으므로 Storage가 유일한 보관처다. 즉 조항 결과에서 원문 페이지로 되짚을 길이 영영 사라진다 — 이 체계가 지키기로 한 것이 바로 그 되짚기다.

다시 올려야 하는 부담은 그대로 두되 추출 결과는 버리지 않는다. `status='error'`로 기록을 남겨 담당자가 무엇이 왜 막혔는지 볼 수 있게 했고, 버킷을 고친 뒤 같은 파일을 다시 올리면 그 기록을 이어받아 보관만 다시 시도한다. 이 되살리기 경로가 없으면 중복 파일로 걸러져 고친 뒤에도 손쓸 방법이 없다.

**덤으로 찾은 것 — 리콜 임베딩이 주기마다 통째로 버려지고 있었다**

리콜 적재를 트랜잭션으로 묶으면서 발견했다. `case_event`의 검색용 문장을 갱신할 때 `embedding = null`을 **무조건** 실행하고 있었다. 리콜 수집은 5분마다 구간을 돌며 전체 2,252건을 약 42시간 주기로 다시 훑으므로, 바뀐 것이 없어도 모든 리콜의 임베딩이 주기마다 날아가고 다시 만들어졌다. 이유 없는 반복 과금이다.

검색용 문장이 실제로 달라졌을 때만 버리도록 고쳤다(`is distinct from`). 원 계획서에는 없던 항목이다.

**같이 고친 것 — 사건 서술이 갱신되지 않던 문제**

`case_event` 삽입이 `on conflict do nothing`이라, 원본에서 제목이나 위해 설명이 고쳐져도 사건 쪽은 옛 문장 그대로였다. 이제 원본에서 온 필드는 갱신한다. 다만 `is_confirmed`·`product_scope_id`처럼 담당자가 손댄 값은 건드리지 않는다 — 원본을 다시 받았다는 이유로 사람의 판단을 지울 수는 없다.

**재채점 실패를 성공과 구분한 이유**

리랭커가 죽으면 `rerank_score`가 전부 null인 채로 저장됐다. 그런데 화면에서 그것은 "AI가 후보를 다 낮게 봤다"와 구별되지 않는다. 담당자가 낮은 점수를 근거로 조항을 반려하면, 아무도 채점하지 않은 결과가 반려 근거로 기록에 남는다. `rerank_status`를 `skipped`/`ok`/`failed` 세 갈래로 나누고, 실패한 실행에는 화면에 그 사실을 적었다.

**환경설정을 기본값으로 되돌리지 않고 멈추게 한 이유**

`TAGGING_CONCURRENCY=1000`은 요청 한도에 걸려 실패만 만들고, `RRF_K=-60`은 `1/(k+등수)`의 분모를 0이나 음수로 만들어 점수 순서를 뒤집는다. 둘 다 오류로 죽지 않고 "결과가 좀 이상한" 상태가 되므로 원인을 찾기가 가장 어려운 종류의 고장이다. 조용히 기본값으로 바꾸면 담당자는 자기가 넣은 값으로 돈다고 믿게 되고, 실측 결과를 잘못된 설정과 짝지어 기록하게 된다. 그래서 시작하는 자리에서 멈춘다.

주소줄로 오는 `limit`·`offset`은 반대다. 아무나 보낼 수 있으므로 멈추지 않고 범위 안으로 자르되, 잘랐다는 사실을 응답의 `clamped`에 실어 준다.

**실측 확인**
- `npm run typecheck`, `npx next build` 통과. `git diff --check` 통과
- 마이그레이션 029·030·031 적용 완료. `clause_hybrid_search`가 인자 14개짜리 하나만 남은 것 확인(옛 13개짜리는 정상 삭제됨)
- `run_tag_chunk()` 본문에 `tag_fail_count < 3` 조건이 들어간 것 확인
- `reset_tag_failures()`에 `anon` 실행 권한이 없는 것 확인
- 사건 2237로 분석을 실제 실행 → 정상 동작. `match_run` 13번에 질의 원문이 기록되고 `rerank_status='skipped'`(`--no-rerank`로 돌렸으므로 옳다), `require_approved_tags=false`로 남은 것 확인
- 지난 실행 11·12번은 마이그레이션이 `rerank_status='ok'`로 되짚어 채웠고, `query_text`는 null로 남았다 — 없는 것과 "빈 질의로 돌렸다"를 섞지 않기 위해서다
- 실패 조항 배제를 실물로 검증했다: 대상 목록 맨 앞 조항(5.3.4.2) 하나를 `tag_fail_count=3`으로 만들자 남은 대상이 5,670 → 5,669로 줄고 포기 건수가 0 → 1로 잡혔다. SQL 쪽 `run_tag_chunk()`도 같은 5,669를 봤다 — TypeScript의 `taggableWhere()`와 SQL 함수의 대상 정의가 실제로 일치한다는 뜻이다. `reset_tag_failures()`로 되돌리자 5,670으로 복구됐다
- 이 검증에서 `run_tag_chunk()`를 직접 부른 탓에 실제 태깅 요청 세 건이 나갔다. 조항 30건이 처리됐고(실패 0건) 비용은 약 $0.07이다. 의도한 일은 아니지만, 결과적으로 이번 변경 뒤에도 태깅 경로 전체가 정상 동작한다는 것이 확인됐다 — 남은 대상 5,670 → 5,640, `tag_fail_count`는 0을 유지했다

**하지 않은 것과 그 이유**
- `/api/standards/sync` 삭제 — 미들웨어가 이미 막고 있고 `maxDuration=300`인데 Netlify는 30초에 끊어 동작하지도 않는다. 지우는 것이 맞다고 보지만 협회 측에서 이 경로를 알고 있을 수 있어 담당자 확인 전까지 두었다
- 태깅 예약 장부 — 위에 적은 대로 자기회복되므로 만들지 않았다
- 태그 버전 단일화 — 실측 결과 중복이 0건이라 이 항목은 닫는다
- `007`의 창 함수 `limit` 정렬 보장 — 상위 60건 반환이 SQL 표준상 보장되지 않지만 Postgres WindowAgg가 정렬 순서로 내보내므로 현재 오동작 가능성이 낮다. 140줄짜리 함수를 또 통째로 다시 만들 값어치가 없다고 보고 P2로 남겼다
- 리콜 증분 수집, 서버 전용 import 경계 — 이번 범위 밖

## 라운드 32 — 개선계획서 검증과 우선순위 재조정 (`02_1차 보완 및 구현 설계서.md`)

**한 일**
- `docs/코드베이스_성능_로직_개선계획_2026-09-04.md`가 지목한 항목을 실제 파일에서 하나씩 재현 검증했다
- 검증 결과에 따라 우선순위를 다시 매기고 P0의 구현 방법을 확정한 `02_1차 보완 및 구현 설계서.md`를 루트에 만들었다
- 원 계획서는 고치지 않고 `docs/`에 그대로 뒀다 (CLAUDE.md §6 — 버전 공존)
- 코드는 변경하지 않았다. 이번 라운드의 산출물은 문서뿐이다

**왜 했는가**
- 원 계획서는 P0이 8개였다. 그중 5개가 3번째 산출물 하나에 붙어 있었다. 전부 P0이면 어디부터 손대야 할지 알 수 없다
- 더 중요한 것은, 지적된 관찰은 대부분 사실인데 **등급이 실제 피해와 맞지 않았다**는 점이다. 문서만 읽으면 알 수 없고 코드를 열어야 드러난다
- 이 저장소는 "구현 전 설계 문서의 가정을 실물로 검증하는" 성격이 크다(CLAUDE.md §5). 계획서 자체에도 같은 잣대를 댔다

**확인한 것 — 사실로 재현된 것**
- 고정 오프셋 0/40/80 (`027:116`), 동기화 토큰 미설정 시 통과 (`sync/route.ts:25`), 검색이 미검수 태그를 모두 사용 (`007:103`)
- `src/app` 전체에 `loading.tsx` 0개, `Suspense` 사용처 0개
- 사고보고서 목록은 행마다 상관 서브쿼리 5개 (`accidents/page.tsx:120-129`)
- 리콜 적재가 건별 트랜잭션이 아님 (`recall/load.ts` 103·145·164·171·177행이 각각 별도 호출)
- Storage 보관 실패 후에도 사건 생성 진행 (`accidents/actions.ts:54-59`)
- `insight` 문자열이 `src`·`supabase`·`codebook` 어디에도 없음 → 3번째 산출물 미구현 확인

**확인한 것 — 등급이 실제 피해와 어긋난 것**
- **태깅 고정 오프셋은 누락이 아니다.** 조회 순서가 `(standard_id, order_index)`로 고정이고 대상 집합이 줄기만 하므로, 밀린 조항은 다음 차례에 목록 앞으로 당겨져 회수된다. 자동 종료 조건도 매번 새로 세는 `v_left = 0`이라 미처리분을 남기고 꺼질 수 없다. 중복 과금도 구조적으로 불가능하다 — 예산 12초·동시 6건이면 한 요청의 실처리가 10건 안팎인데 구간 간격이 40이다. **P0 → P2**
- **태그 버전 중복의 영향은 서술의 1/30이다.** `007:183`의 코드 가산점이 `hit_full > 0` 불리언이라 태그가 몇 개든 가산폭은 `p_w_code = 0.5`로 같다. 영향받는 것은 `code_ranked` 등수뿐이고 그 몫은 최대 `1/61 ≈ 0.016`이다. 게다가 자동 경로는 `not exists clause_tag` 조건이라 재태깅을 하지 않으므로 현재 발생 건수가 0일 수 있다. **P0 → P2, 세어 보고 정한다**
- **동기화 API에는 이중 방어가 있다.** `src/middleware.ts:13`이 `/api/jobs/` 외 전 경로에 Basic 인증을 건다. 실제로 열리는 것은 `STANDARDS_SYNC_TOKEN`과 `SITE_AUTH_PASSWORD`가 둘 다 빈 경우뿐이다. 게다가 `maxDuration = 300`인데 Netlify는 30초에 끊으므로 이 라우트는 완주하지도 못한다. **P0 → P2, 그리고 방어를 더하기보다 라우트 삭제를 권고**

**확인한 것 — 원 계획서가 곁가지로 둔 진짜 P0**
- 태깅 실패 조항은 `clause_tag`에 아무것도 남기지 않으므로(`tag-run.ts:220`, `283`) 대상 목록 맨 앞에 영구히 남는다
- `offset=0` 요청이 매 차례 그 조항부터 다시 시도하고, `countTaggable()`과 `run_tag_chunk()`의 `v_left`가 0에 도달하지 못한다
- 결과적으로 자동 실행이 스스로 꺼지지 않고 완료 알림도 오지 않으며, 같은 조항에 1분마다 계속 과금한다
- 누락은 다음 차례에 회수되지만 이쪽은 회수되지 않는다. **이것을 P0-1로 올렸다**

**원 계획서에 없던 관찰**
- `007`의 `code_ranked`·`keyword_ranked`가 `row_number() over (order by ...)` 뒤에 바깥 `ORDER BY` 없이 `limit`을 건다(`007:118`, `149`). 상위 60건 반환이 SQL 표준상 보장되지 않는다. `vector_ranked`에는 명시적 `order by`가 있다. 실무상 Postgres WindowAgg가 정렬 순서로 내보내므로 현재 오동작 가능성은 낮아 P2로 뒀다

**같이 정리한 것**
- 3번째 산출물(KC안전기준 개선 요인)은 개선이 아니라 신규 기능이므로 후속 문서로 분리했다. 다만 원자료가 이미 준비돼 있음은 확인했다 — `match_run.queried_hf_codes`·`result_count`(0건 run도 보존), `review_log.decision`·`reject_reason`, `clause_link`, `recall_cache.cited_standards`, `case_event.product_scope_id`
- 안전기준 JSON의 Storage 보관은 이번 범위에서 뺐다. 사고보고서와 달리 `KC안전기준/` 폴더가 저장소에 있고 git 이력으로 되짚을 수 있어 원본이 사라지는 구조가 아니다. 협회가 폴더 밖에서 갱신하는 운영이 생기면 그때 다시 본다
- 원 계획서는 2장 등급과 3장 실행순서가 어긋나 있었고(P0 메인화면이 2단계, P1 안전기준 집계가 1단계), P1 리랭킹 provenance와 P1 사고보고서 상태 표시는 어느 단계에도 없었다. 새 문서는 둘을 일치시켰다
- 측정 목표에 숫자를 넣었다(제목 표시 200ms 이내 등). 원 계획서는 "수치 확보"라고만 적혀 있었다

## 라운드 31 — `humanize-korean` 스킬 프로젝트 규칙 반영

**한 일**
- `skills/humanize-korean/references/project-terms.md`에 KC 프로젝트 화면 권장 표현과 보호 용어를 추가했다
- `skills/humanize-korean/references/project-preservation.md`에 문서 장르별 처리와 수치·코드·책임 주체 보존 규칙을 추가했다
- `SKILL.md`에 UI·운영알림·설계문서 모드와 프로젝트 보존 검사 단계를 연결했다
- `metrics.py`에 숫자·코드·상태값·URL·명령어 등 보호 토큰 전후 비교와 경고를 추가했다
- 스킬 `README.md`에 새 참고 파일과 프로젝트 사용법을 반영했다

**왜 중요한가**
- 화면 표현은 쉽게 바꿀 수 있지만, `담당자가 확인한다`를 `시스템이 확인한다`로 바꾸거나 `검토 신호`를 `개선 필요`로 바꾸면 업무 책임과 판단 수준이 달라진다
- 00·01 설계문서, 법령, 안전기준 원문은 자연스러운 문장보다 수치·코드·근거 보존이 우선이다
- 일반 번역투 검사는 이런 의미 변화를 자동으로 잡지 못하므로, 프로젝트 전용 보존 목록과 정량 경고를 별도로 두었다

**확인한 것**
- 보호 토큰이 같은 윤문 예문에서 `preservation.ok=true` 확인
- 숫자·코드·상태값·URL이 바뀐 예문에서 `preservation.ok=false`와 사용자 확인 경고 확인
- Python 실행 오류 없음
- 이번 라운드에서는 앱 화면이나 데이터 처리 로직을 변경하지 않고 재사용 가능한 스킬 파일만 보완함

## 라운드 30 — `humanize-korean` 스킬 보완 계획

**한 일**
- 저장소의 `skills/humanize-korean` 스킬과 최근 화면 문구 개선 결과를 대조했다
- 일반 한국어 윤문 규칙과 KC안전기준 프로젝트의 기술·정책 의미 보존 규칙을 분리하는 계획을 작성했다
- 화면 문구, 운영 알림, README, 설계문서, 법령·안전기준 원문을 장르별로 다르게 처리하는 방안을 정리했다
- 프로젝트 표준 용어, 보호해야 할 수치·코드·법령·상태값, 의미 보존 검사와 회귀 검증 절차를 계획에 추가했다

**왜 중요한가**
- `태깅`, `임베딩`, `HF/DT`, `시험방법` 같은 말은 화면에서 쉽게 풀어야 하지만, 무조건 다른 말로 바꾸면 업무 의미가 달라질 수 있다
- 화면 문구는 짧고 쉬워야 하는 반면, 00·01 설계문서와 법령·안전기준 원문은 사실·근거·수치 보존이 더 중요하다
- 기존 `metrics.py`는 번역투 패턴과 변경률은 확인하지만, 수치·코드·책임 주체·상태값이 보존됐는지는 확인하지 않는다
- 따라서 스킬을 재사용하려면 “자연스러운 문장”뿐 아니라 “업무 의미와 정책 경계가 그대로인지”를 함께 확인해야 한다

**확인한 것**
- `SKILL.md`에 Fast·Strict 모드, 정부 문서 K 패턴, 변경률 경고, 자체검증 절차가 있음
- `references/quick-rules.md`와 `gov-patterns.md`가 일반 번역투·행정 문체를 구분함
- `src/lib/terms.ts`에 화면 표준 용어가 모여 있으나 스킬과 자동 연동되지는 않음
- 이번 라운드에서는 스킬 파일과 화면 문구를 수정하지 않고 보완 계획서만 작성함

## 라운드 29 — 3번째 산출물 구현 대안 보완

**한 일**
- `KC안전기준 개선 요인`을 구현하는 세 가지 대안을 계획서에 추가했다
- 기존 원자료를 바로 집계하는 읽기 전용 방식, 실행 결과를 고정하는 스냅샷 방식, 담당자 검토·후속 이력까지 관리하는 개선 후보 방식으로 나눴다
- 정확성 우선 MVP에는 읽기 전용 집계부터 시작하고, 수치와 근거가 안정된 뒤 스냅샷과 업무 검토 기능을 추가하는 단계적 방식을 권고했다
- 기준 사각지대·국내외 기준 비교·저활용 시험항목에 공통으로 적용할 기간·품목·코드·검토 상태·표본 부족 규칙을 정리했다

**왜 중요한가**
- 3번째 산출물은 단순히 사고·리콜 건수를 세는 화면이 아니라, 대응 조항 부재·기준 수준 비교·시험 후보 활용을 근거와 함께 검토하는 정책 지원 기능이다
- 처음부터 정책 검토 업무까지 만들면 집계 규칙의 오류와 업무 절차의 오류를 구분하기 어렵다. 먼저 기존 `match_result`와 `review_log`를 수작업 계산과 대조하는 것이 정확성 확보에 유리하다
- 집계 결과를 기준 강화·완화로 자동 해석하지 않고, 자료 부족·비교 불가·미검토 상태를 분리해야 설계문서의 전문가 검토 원칙을 지킬 수 있다

**확인한 것**
- 현재 `match_result`, `review_log`, `case_tag`, `clause_tag`, `clause_link`는 건별 근거로 재사용할 수 있음
- 현재 세 가지 개선 산출물을 계산하는 SQL/RPC, 스냅샷 테이블, `/insights` 화면은 없음
- 이번 라운드에서는 코드·DB 동작을 변경하지 않고 구현 대안과 세부 개발 순서만 보완함

## 라운드 28 — 00·01 설계문서 기준 정확성 재검토

**한 일**
- 컨셉 문서와 구현설계 v0.6을 현재 코드·DB 구조와 다시 대조했다
- 속도보다 정확성이 중요한 MVP라는 기준으로, 설계 약속·현재 구현·차이를 표로 정리했다
- 자동 미검수 태그 사용, 원본 보관 실패 허용, 사고보고서 후속 코드화 분리, 태그별 근거 부족, 시험 연결 근거 부족을 우선 개선 항목으로 계획서에 추가했다
- 3번째 산출물은 아직 집계·화면이 없는 상태임을 다시 확인하고, 향후 집계 전에 원자료의 품질·검수 상태를 구분해야 한다는 원칙을 명시했다

**왜 중요한가**
- 이 프로젝트의 MVP 성공 기준은 빠른 응답이 아니라 담당자가 시험 후보의 근거를 원문까지 되짚고 판단할 수 있는지에 있다
- 현재 검색은 반려되지 않은 태그를 사용하므로 자동 미검수 태그도 후보 순위에 영향을 줄 수 있다. 이는 화면에 B 수준으로 표시하는 것만으로 해결되지 않는다
- 사고보고서의 업로드 완료와 코드화·임베딩 완료가 분리되어 있어, 화면 상태를 잘못 읽으면 분석 준비가 끝난 것으로 오해할 수 있다
- 3번째 산출물은 단순 건수 집계가 아니라 사각지대·국내외 기준 차이·저활용 시험항목을 근거와 함께 해석하는 기능이므로, 표본 부족·비교 불가·미검토를 별도로 보존해야 한다

**확인한 것**
- 조항 파싱·시험조건·시험방법 연결·HF/DT 구조·하이브리드 검색·리랭킹 결과 보존은 설계와 대체로 일치
- 검수 확정 태그만 정식 근거로 사용한다는 설계 설명과 `007_clause_hybrid_search.sql`의 실제 조건이 다름
- 사고 PDF 업로드 액션은 추출·사건 생성까지이며 코드화는 별도 명령으로 실행됨
- 원본 보존, 태그별 근거, 시험방법 연결 근거, 리랭킹 입력 매니페스트, 3번째 산출물 화면은 보강이 필요함
- 이번 라운드에서는 코드와 DB 동작을 변경하지 않고 정확성 중심 계획만 보완함

## 라운드 27 — 코드베이스 계획 보완: 화면 로딩과 기준 개선 산출물

**한 일**
- 메인 화면의 로딩 지연 원인을 확인하기 위해 목록 화면의 순차 DB 조회와 로딩 경계 부재를 점검했다
- 컨셉·구현설계 문서의 3번째 산출물인 `KC안전기준 개선 요인`이 현재 구현되어 있는지 코드·SQL·화면에서 대조했다
- 기준 사각지대, 국내외 기준 수준 차이, 저활용 시험항목을 실제 집계해야 하는 후속 기능으로 구체화했다
- 개선 신호를 자동 결론으로 사용하지 않도록 표본 부족·비교 불가·미검토 상태를 별도로 보존하는 계획을 추가했다

**왜 중요한가**
- 현재 사고·리콜 화면은 여러 집계와 목록 조회가 끝난 뒤 화면을 표시하므로, 데이터가 많아질수록 첫 화면을 기다리는 시간이 길어질 수 있다
- 현재 `review_log`, `match_result`, `clause_link`, 사고·리콜 코드 데이터는 건별 분석과 검토를 위한 재료이지만, 이를 모아 기준 개선 신호를 만드는 집계 함수·테이블·화면은 아직 없다
- 사고·리콜이 없다는 사실만으로 시험을 줄이거나, 해외 표준 번호가 다르다는 이유만으로 수준 차이를 단정하면 설계 문서의 “전문가 검토 전제”를 어기므로, 비교 가능한 자료와 부족한 자료를 구분해야 한다

**확인한 것**
- `src/app/accidents/page.tsx`, `src/app/recalls/page.tsx`, `src/app/ops/page.tsx`에 로딩 경계가 없고 여러 서버 조회를 순차 실행하는 구조 확인
- `insight`, `gap`, `underused` 집계 코드와 `/insights` 화면이 없는 것 확인
- 기존 건별 결과·검토 데이터는 3번째 산출물의 원자료로 재사용 가능하지만, 정책용 집계 정의는 새로 확정해야 함
- 이번 라운드에서는 처리 로직과 DB를 변경하지 않고 계획서만 보완함

## 라운드 26 — 코드베이스·성능·로직 개선 계획 점검

**한 일**
- 안전기준 동기화, 리콜 수집, 태깅, 임베딩, 검색, 자동 작업, 서버 액션의 현재 코드를 다시 점검했다
- 처리 누락·검색 점수 왜곡·인증 우회 가능성이 있는 항목과 처리량·유지보수 개선 항목을 우선순위로 나눴다
- 각 개선 항목에 재현 방법과 완료 기준을 붙인 계획서를 `docs/코드베이스_성능_로직_개선계획_2026-09-04.md`로 작성했다

**왜 중요한가**
- 최근 504 대응으로 작업을 짧은 구간으로 나눈 것은 실행시간 문제를 줄였지만, 병렬 요청이 같은 대상을 정확히 나누어 맡는지와 실패 후 재시도가 안전한지는 별도로 확인해야 한다
- 태깅·검색은 과거 버전 데이터가 남는 것이 감사에는 필요하지만, 현재 검색 결과에 과거 코드가 섞이면 관련성 점수와 검수 근거가 실제보다 크게 보일 수 있다
- 자동 동기화는 환경변수 누락이 곧 공개 쓰기 권한으로 이어지지 않도록 운영 환경의 인증 실패를 기본값으로 삼아야 한다

**확인한 것**
- `src/lib/standards/tag-run.ts`, `supabase/migrations/027_tag_chunk_parallel.sql`에서 고정 오프셋 병렬 처리 구조 확인
- `src/app/api/standards/sync/route.ts`에서 토큰 미설정 시 통과하는 분기 확인
- `supabase/migrations/007_clause_hybrid_search.sql`에서 반려되지 않은 과거 태그를 모두 매칭할 가능성 확인
- 리콜 적재의 건별 트랜잭션 경계, 리콜 전체 순회 방식, 안전기준 화면 집계의 현재 판 조건을 점검
- 이번 라운드에서는 데이터·코드 동작을 변경하지 않고 계획서만 작성했다 version: description: |- 이 문서는 코드를 몰라도 "지금까지 무엇을 왜 만들었는지" 알 수 있도록 쓴다. 작업 한 라운드가 끝날 때마다 아래에 새 항목을 추가한다. 오래된 내용을 지우지 않고 쌓아 간다 — 나중에 "그때 왜 이렇게 정했지"를 되짚을 수 있어야 하기 때문이다.
  > (자세한 기술적 근거는 각 코드 파일 맨 위 주석에 있다. 이 문서는 그 요약이다.)
created: 2026-09-01 update: 2026-09-04T00:00:00 url: update description: 라운드 20 — 코드 부여를 눌러 두면 끝까지(29시간→2.8시간, 반복 호출 병렬화로 병목 해소), 목록을 게시판으로, 화면 문구 66곳 순화, 결과를 누른 자리에서 알리도록 고쳐 "안 먹힘·깜빡임" 해결, 좌측 메뉴 고정
---

## 라운드 25 — 메인 화면 흐름과 표현 간소화

**한 일**
- 공통 화면 제목 아래에 실제 업무 순서를 짧은 화살표 흐름으로 표시했다
- 개요·안전기준·사고보고서·리콜·코드북·운영·분석 상세 화면의 설명을 담당자가 바로 읽을 수 있는 문장으로 줄였다
- 현황 숫자 아래의 긴 설명은 기본으로 접고, 필요한 경우 `설명`을 눌러 보도록 바꿨다

**왜 중요한가**
- 기존 화면은 처리 원리와 주의사항을 한 문단에 함께 적어, 담당자가 지금 무엇을 해야 하는지 찾기 어려웠다
- 업무 흐름을 `등록 → 확인 → 분석 → 검토`처럼 표시하면 화면을 처음 열어도 현재 업무의 순서를 바로 파악할 수 있다
- 상태 설명을 모두 펼쳐 두면 숫자와 목록이 아래로 밀리므로, 평소에는 핵심 상태만 보이고 세부 근거는 필요할 때만 열리게 했다
- 데이터 조회, PDF 처리, 안전기준 파싱, 코드 부여, 임베딩, 검색 로직은 변경하지 않았다

**확인한 것**
- 변경 파일 진단 오류 없음
- `npm run typecheck` 통과
- 기존과 동일한 서버 페이지 구조에서 공통 흐름 컴포넌트가 정상적으로 타입 검사됨
- 실제 브라우저의 여러 화면 크기와 긴 목록 스크롤은 별도 브라우저 검증이 필요하다

**바뀐 파일**
- `src/components/Panel.tsx`: 공통 업무 흐름 표시
- `src/components/StatusBar.tsx`: 상태 설명 접기
- `src/app/page.tsx`, `src/app/ops/page.tsx`, `src/app/standards/page.tsx`, `src/app/accidents/page.tsx`, `src/app/recalls/page.tsx`, `src/app/codebook/page.tsx`, `src/app/analysis/[caseId]/page.tsx`: 흐름과 쉬운 문장 적용

## 라운드 24 — 정기 작업 504 타임아웃 완화

**한 일**
- 리콜 수집과 안전기준 동기화가 전체 자료를 한 번의 웹 요청에서 처리하지 않도록 구간 처리 기능을 추가했다
- 리콜은 승인 목록 10건, 안전기준은 JSON 파일 1개씩 별도 요청으로 실행하도록 정기 작업을 바꿨다
- 5분마다 다음 구간을 순환 호출하고, 기존 `job_run` 장부에 각 요청의 HTTP 결과를 남기도록 새 마이그레이션을 추가했다

**왜 중요한가**
- 기존 작업은 Netlify 앞단의 약 30초 요청 제한보다 오래 실행될 수 있어, 실제 일부 처리가 끝났더라도 전체 요청이 HTTP 504로 기록됐다
- 작은 구간으로 나누면 한 구간 실패가 전체 수집 실패가 되지 않고, 다음 주기에 같은 구간을 다시 확인할 수 있다
- 리콜 적재와 기준 적재에는 이미 해시 비교와 유니크 제약이 있어 구간을 반복해도 같은 자료가 중복 생성되지 않는다

**확인한 것**
- 현재 `KC안전기준/` 폴더의 JSON 파일은 76개로 확인했다
- 구간 인자를 연결한 뒤 `npm run typecheck` 통과
- 새 마이그레이션은 아직 운영 DB에 적용하지 않았다. 배포 후 `npm run db:push`와 운영 화면의 작업 결과를 확인해야 한다

**바뀐 파일**
- `src/lib/recall/source.ts`, `src/lib/recall/load.ts`: 리콜 조회 구간
- `src/lib/standards/sync.ts`: 기준 파일 구간
- `src/app/api/jobs/[job]/route.ts`: 배치 인자 전달
- `supabase/migrations/028_bounded_sync_jobs.sql`: 5분 주기 구간 실행
---

## 라운드 23 — 라운드 21 UI 개선 계획 구현

**한 일**
- 계획서 `docs/UI_개선_분석_및_수정계획_2026-09-03.md`의 1단계부터 3단계까지 구현했다.
- 좌측 메뉴와 본문을 데스크톱 grid로 분리해 긴 페이지를 내려도 메뉴가 본문 높이에 끌려가지 않도록 바꿨다. 작은 화면에서는 기존 가로 메뉴를 유지했다.
- 공통 `PageToc` 컴포넌트를 만들어 데스크톱 우측 고정 목차, 현재 섹션 강조, 맨 위로 이동, 모바일 접기 목차를 제공했다.
- 운영·분석 상세·안전기준·사고보고서·리콜·위해요인 코드북 화면에 실제 섹션 ID를 연결했다. 화면의 데이터 조회나 업무 기능은 바꾸지 않았다.
- 계획서가 정한 운영 순서(상태 → 최근 처리 → 확인 필요 → 실행 → 상세 → 알림 → 접속 관리)가 목차에서 바로 보이도록 했다.

**왜 중요한가**
- 긴 목록에서 다른 업무 화면으로 이동하거나 위쪽 상태를 다시 확인하려고 페이지 맨 위까지 올라가야 하는 불편을 줄였다.
- 운영 화면은 예외·실행·설정이 길게 이어져 있어 필요한 위치를 찾는 시간이 걸렸는데, 현재 위치와 주요 업무 영역을 바로 이동할 수 있게 했다.
- 목차는 서버 데이터 조회를 방해하지 않도록 클라이언트에서는 화면에 존재하는 제목의 위치 추적만 담당한다. 모바일에서는 화면 폭을 차지하지 않도록 접힌 상태로 제공한다.

**확인한 것**
- 변경 파일 진단: 오류 없음
- `npm run typecheck`: 통과
- `$env:NODE_OPTIONS=''; npx next build`: 통과. 전체 라우트와 Middleware 번들 생성 확인
- `http://localhost:3000`: HTTP 200 응답 확인
- 최초 빌드는 개발 서버와 `.next` 폴더를 동시에 사용해 산출물이 충돌했으나, 개발 서버를 종료하고 `.next`를 재생성한 뒤 깨끗한 빌드로 다시 통과시켰다.
- 현재 Node 버전에서는 기존 `npm run build`의 `--use-system-ca` 옵션이 허용되지 않아, 옵션을 비운 호환 실행 방식으로 검증했다.

**바뀐 파일**
- `src/app/layout.tsx`, `src/app/globals.css`: 독립 좌측 레일과 목차 반응형 레이아웃
- `src/components/PageToc.tsx`: 공통 목차·현재 위치 추적
- `src/app/ops/page.tsx`, `src/app/analysis/[caseId]/page.tsx`: 우선 적용 화면 목차
- `src/app/standards/page.tsx`, `src/app/accidents/page.tsx`, `src/app/recalls/page.tsx`, `src/app/codebook/page.tsx`: 주요 구역 목차

**남은 범위**
- 실제 브라우저의 1280px·1024px·768px·375px 스크롤과 키보드 조작은 브라우저 자동화 도구 연결 후 별도 확인이 필요하다.
- 계획서의 운영 화면 정보 구조 재배치, Suspense 기반 성능 측정·개선은 데이터와 기능 범위를 유지하는 현재 작업 이후 별도 라운드로 남겼다.


## 라운드 22 — 서비스 중심 README 작성

**한 일**
- 관리자 콘솔, 사고보고서, 리콜, KC안전기준, 코드북, 운영 화면을 기능·서비스 흐름 중심으로 설명하는 README를 작성했다.
- 사고보고서의 PDF 추출·개인정보 점검·담당자 확인 절차와 리콜의 Recall Hub 승인 자료 수집·국내 유통 확인 절차를 분리해 기록했다.
- HF·DT 2차원 위해요인 코드와 GPC 품목분류의 역할을 구분해 설명하고, 코드 검색·키워드 검색·임베딩 의미 검색을 RRF로 결합하는 과정을 도식화했다.
- 핵심 테이블과 외부 Recall Hub, 관리자 콘솔의 관계를 Mermaid 연계도로 정리하고, 실제 폴더·주요 코드·기술 스택·초기 실행 명령을 추가했다.
- 관리자 콘솔 주소 `https://kc-regulation-product-hazard-analysis.netlify.app`와 Recall Hub 주소 `https://recall-hub-admin-dev.vercel.app/recalls`를 확인해 README에 넣었다.

**왜 중요한가**
- 기존 README가 제목만 있어 담당자가 이 시스템으로 무엇을 처리하고, 어느 단계에서 사람의 확인이 필요한지 알 수 없었다. 기능별 입력·처리·검토 결과를 연결해 서비스의 사용 목적을 한 문서에서 파악할 수 있게 했다.
- Recall Hub는 별도 Supabase 프로젝트이므로 내부 DB와 직접 JOIN하는 것처럼 설명하면 운영 구조를 오해하게 된다. 승인 자료를 서버에서 읽어 내부 `recall_cache`로 적재하는 실제 경계를 명시했다.
- 문서의 사실관계는 화면 코드, 검색 SQL, DB 마이그레이션, 환경 설정을 대조해 확인했고 `git diff --check -- README.md`에서 형식 오류가 없음을 확인했다.


## 라운드 21 — 화면 사용성 점검 및 수정 계획

**한 일**
- 좌측 메뉴, 긴 상세 화면, 운영 화면의 구조를 코드 기준으로 점검하고 수정 계획서를 작성했다
- 좌측 메뉴의 고정이 넓은 화면(`lg`)에서만 켜져 창 폭·확대 비율에 따라 풀릴 수 있음을 확인했다
- 긴 화면에는 우측 목차와 현재 위치 표시가 전혀 없음을 확인했다
- 운영 화면은 상태·문제·실행·이력·설정이 한 줄로 길게 이어져 우선순위가 늦게 드러나는 구조임을 정리했다
- 속도, 말투, 정보 밀도, 접근성도 함께 점검해 성능 측정·짧은 상태 문구·페이지별 화살표 업무 흐름을 계획에 추가했다
- UI에 앞서 KC 기준·사고보고서·리콜·태깅·임베딩·분석·DB를 원문부터 검토 기록까지 잇는 별도 감사 계획을 작성했다

**왜 중요한가**
- 담당자가 긴 목록이나 운영 기록을 읽을 때 맨 위까지 되돌아가지 않고 현재 위치에서 바로 이동할 수 있어야 실제 업무 흐름이 끊기지 않는다
- 운영 화면은 평소에는 정상 여부를 빨리 확인하고, 문제가 생겼을 때에는 조치 대상을 먼저 보여야 한다. 기능을 더하기 전에 읽는 순서를 바로잡는 것이 필요하다


---

## 이 문서를 쓰는 방법

- frontmatter 버전과 updated 날짜를 업데이트
- 코드를 크게 바꾸는 작업 한 라운드가 끝나면, 위쪽에 새 항목을 추가한다.
- 라운드에 종료 시간과 날짜를 작성한다.(즉 이 문서 업데이트)
- "무엇을 했는지"보다 "왜 했는지, 뭘 확인했는지"를 더 자세히 쓴다. (무엇을 했는지는 git 커밋 메시지를 보면 되지만, 왜 했는지는 그때 가서야 알 수 있다.)
- 전문용어가 나오면 괄호로 짧게 풀어 쓴다. 이 문서는 코드를 안 보는 사람도 읽을 수 있어야 한다.
	- 블릿 활용 및 개조식 명사형 종결(~분류하였다 -> 분류)하고 마지막은 `.`없이 문장 종료
	- 다만, 설명은 문장형태로 쉽게 풀어 쓴다.


---

## 라운드 1 — 설계 검토와 첫 골격

**한 일**
- 설계문서 3종(컨셉 v0.4, 구현설계 v0.6, 코드북 PDR v0.9.7)을 읽고 실제로 구현  가능한지 확인했다.
- `credentail.md`에 평문으로 있던 국내·해외 리콜 API 키를 `.env.local`로 옮기고 원본 파일은 삭제했다. (저장소가 비공개라 즉시 위험은 낮았지만, 평문 키를 깃에 올려 두는 것 자체가 좋지 않은 습관이라 바로 정리했다.)
- 위해요인 코드북(2차원 분류 — 원인 HF / 결과 DT)을 자동으로 읽어 들이는 프로그램을 만들고, 실제 파일로 돌려서 문서에 적힌 숫자(HF 64개, DT 16개)와 정확히 일치하는지 확인했다.
- KC안전기준 JSON 파일 76개를 열어 봐서, "조항이 어떤 시험방법과 연결되는지"가 파일 안에 이미 들어 있다는 것을 확인했다. (예: "의자가 넘어지지 않아야 한다"는 조항이 "150N의 힘을 가한다"는 시험방법과 서로 짝지어져 있었다.)

**왜 중요한가**
- 이 시스템의 핵심은 "사고 원인 코드"와 "안전기준 조항"을 같은 코드로 이어 붙이는 것이다. 코드북이 정확히 안 읽히면 그 뒤 모든 게 흔들리므로, 제일 먼저 검증했다.

---

## 라운드 2 — 데이터베이스(Supabase) 만들고 실제로 돌려 보기

**한 일**
- Supabase에 새 프로젝트(`kc-hazard-analysis`)를 만들었다.
- 설계에 필요한 표(테이블) 18개와 검색 함수들을 실제로 만들었다.
- 한국어 검색이 되는지 실측했다. PostgreSQL 기본 검색 기능은 "전도가", "전도를"처럼 조사가 붙으면 다른 단어로 착각한다 — 이게 진짜 문제인지 실제로 테스트해 봤더니 정말 그랬다. 그래서 한국어를 제대로 검색하는 별도 기능(PGroonga)을 설치했고, 이번엔 조사가 붙어도 잘 찾는 것을 확인했다.
- 가짜 사고 1건을 넣고 실제로 검색을 돌려 봤다. "의자가 옆으로 넘어져 아이가 떨어졌다"는 사고를 코드화(설계결함 + 낙상)한 뒤 검색했더니, 정확히 그 원인·결과 코드가 붙어 있던 조항("4.3.3 안정성")을 찾아냈고, 그 조항이 가리키는 시험방법("5.9.2")까지 자동으로 이어졌다. 설계문서에 적혀 있던 예시가 실제로 작동한 것을 확인한 셈이다.

**발견해서 고친 문제**
- Supabase는 새로 만든 함수를 기본적으로 "누구나 실행 가능"하게 열어 둔다는 사실을 발견했다. 데이터를 볼 수 있는 건 아니지만, 굳이 열어 둘 이유가 없어서 전부 잠그고 서버만 쓸 수 있게 고쳤다.

---

## 라운드 3 — 사람이 쓸 화면 만들기

**한 일**
- 화면 4개를 만들었다: 전체 현황, 안전기준 목록, 위해요인 코드 목록, 사고보고서 등록, 사건 분석 결과.
- "이 시스템은 위반 여부를 판정하지 않는다"는 원칙을 화면에도 그대로  옮겼다. 예를 들어 위험하다는 뜻으로 빨간색을 쓰지 않는다. 빨간색은 오직 "시스템이 진짜로 고장 났을 때"(연결 실패 등)에만 쓴다. 후보 조항 목록에는 빨간 경고색을 절대 쓰지 않는다 — 시스템이 위험을 판정하는 것처럼 보이면 안 되기 때문이다.
- 사고보고서 PDF를 업로드하면 자동으로 글자를 뽑아내고, 그 안에 주민등록번호·전화번호·이메일 같은 개인정보가 있는지 확인한다. 걸리면 그 파일은 분석 대상으로 넘기지 않는다(외부 AI에게 보내지 않는다는 뜻).

**중간 점검에서 반영한 것**
- 처음 문서(v0.6)를 검토한 보완본(v0.7)이 도중에 도착했다. v0.7은 "품목을 먼저 확정하지 않고 검색부터 하면 다른 제품의 시험이 섞일 수 있다"거나 "검색 결과가 0건이라고 해서 무조건 '이 기준엔 조항이 없다'로 단정하면 안 된다(아직 준비가 덜 된 것일 수도 있다)" 같은 지적을 했다. 두 버전(v0.6, v0.7)이 서로 다르게 말하는 부분은 어느 한쪽만 고르지 않고, 나중에 실제 데이터로 비교해 볼 수 있게 "선택 가능한 방식"으로 만들어 두었다.

---

## 라운드 4 — 실제 데이터 채우기 · 신규 자료 감지 기능 · 정리

**한 일**

1. **환경설정 값 채움** — 승호님이 새로 발급받은 Supabase 접속 정보와 국내·해외 리콜 API 주소를 `.env.local`에 반영했다.

2. **깃(git) 이력 정리** — 이 작업들을 하는 동안, 원격 저장소(GitHub)에 다른 경로로 커밋 3개가 먼저 올라와 있어서 로컬 작업과 갈라져 있었다. 서로 다른 파일을 건드린 커밋들이라 충돌 없이 합쳤다.

3. **실제 데이터 적재** — 지금까지는 프로그램이 "이론적으로 맞다"는 것만 확인했는데, 이번에 처음으로 실제 데이터를 전부 넣어 봤다.
   - 위해요인 코드북 1건 (HF 64개, DT 16개)
   - 안전기준 76개 문서 전체, 조항 13,501개, 시험조건 333개, "이 조항은 이 시험과 연결된다"는 관계 644개
   - 넣은 뒤 개수를 다시 세어 봤더니 프로그램이 미리 계산했던 숫자와 한 자리도 틀리지 않고 똑같았다.

4. **새 자료가 들어왔을 때 자동으로 알아채는 기능** — 협회가 나중에 기준을 개정해서 새 JSON 파일을 주면, 그게 "완전히 새로운 기준"인지  "이미 있는 기준의 개정판"인지 자동으로 구분하는 기능을 만들었다.
   - 완전히 같은 내용이면 → 아무 것도 안 함
   - 이름은 같은데 내용이 바뀌었으면 → 옛날 버전은 지우지 않고 "지금은 안 씀"으로 표시만 하고, 새 버전을 새로 넣는다. (옛 버전에 달려 있던 사람의 판단 기록은 절대 사라지면 안 되기 때문에 지우지 않는다.)
   - 완전히 새로운 이름이면 → 새로 넣는다.
   - 나중에 이 기능을 웹 주소(API)로도 호출할 수 있게 만들어 뒀다. 지금 당장 누가 자동으로 호출하는 건 아니지만, 나중에 "자동 감시" 기능을 붙일 때 바로 쓸 수 있도록 미리 만들어 둔 것이다.

4. **사고조사보고서 5건 처리** — `사고조사보고서` 폴더에 있는 실제 PDF 5건(보조배터리 2건, LED등기구, 스팀다리미, 가습기)을 한 번에 처리하는 기능을 만들어 돌렸다.
   - **처리 중 실제 문제를 하나 발견했다.** 개인정보 탐지 기능이 "계좌번호"로 착각한 게 있었는데, 확인해 보니 진짜 계좌번호가 아니라 보고서에 적힌 접수일자("25-11-24" 같은 형식)였다. 날짜와 계좌번호가 둘 다 숫자에 하이픈이 섞인 모양이라 헷갈린 것이다. 실제 계좌번호는 자릿수가 훨씬 많다는 점을 이용해서 구분하도록 고쳤다. 고친 뒤에는 날짜는 더 이상 걸리지 않으면서, 진짜 계좌번호처럼 생긴 것은 여전히 잡아내는 것을 확인했다.
   - 5건 모두 정상 처리됐다. (다만 이 상태로는 "등록"만 된 것이고, 담당자가 화면에서 원문을 확인하고 "확정" 버튼을 눌러야 실제 분석 대상이 된다 — 이것도 의도한 설계다. 사람이 확인하기 전에는 시스템이 멋대로 넘어가지 않는다.)

---

## 라운드 5 — AI 코드화 실제 가동 · 품목 확정 · 해외 리콜 연결

**한 일 (크게 넷)**

#### **① AI에게 코드를 붙이게 하되, 두 번 물어보게 했다**

앞 라운드까지는 "AI로 코드를 붙인다"고 만들어만 두고 실제로는 못 돌렸다. API 키가 채워져서 이번에 처음 돌렸다.

그냥 돌리지 않고 방식을 하나 정했다. **싼 모델에게 세 번 물어보고, 세 번 답이 갈리면 그때만 비싼 모델에게 다시 묻는다.**

왜 이렇게 했는가. 값을 재 봤더니 싼 모델(Luna) 세 번이 비싼 모델(Terra) 한 번보다 **3.3배 싸다.** 그런데 세 번 물어서 답이 같으면 그 답은 믿을 만하다. 답이 갈리는 건은 원래도 어려운 건이니 비싼 모델로 올린다.

여기서 한 가지를 일부러 피했다. AI에게 "네 답이 얼마나 확실하냐"고 물어 그 숫자로 판단하는 방법이 있는데, **설계문서가 그 숫자를 믿지 말라고 명시** 했다(§5.2.3). 실제로 틀린 답에도 0.9를 매긴다. 대신 **같은 질문을 세 번 해서 답이 얼마나 일치했는지**를 기준으로 삼았다. 이건 AI의 자기 평가가 아니라 관측된 사실이다.

돌려 본 결과 — 안전기준 조항 53개 중 **16개(30%)가 비싼 모델로 올라갔고, 그중 10개는 비싼 모델이 다른 답을 냈다.** 이 10개가 사람이 제일 먼저 봐야 할 목록이다.

> 정직하게 덧붙인다. 비싼 모델이 답을 **바꿨다**는 것과 **맞혔다**는 것은 다르다. 
> 어느 쪽이 옳은지는 전문가가 만든 정답지가 있어야 알 수 있고, 그건 아직 없다. 지금은 "여기가 흔들리는 구간"까지만 말할 수 있다.

#### **② 사고 사건을 실제 흐름에 연결했다 — 품목부터 정한다**

사고보고서를 넣으면 이제 이 순서로 처리된다.

```
사고보고서 → ① 품목이 무엇인가 → ② 그 품목에 적용되는 기준은 무엇인가
          → ③ 원인·결과 코드 붙이기 → ④ 관련 조항 찾기
```

**품목을 제일 먼저 정하는 게 핵심이다.** 설계문서가 한 문장으로 정리해 뒀다 — "위해요인 코드가 같아도 제품이 다르면 관련 시험이 다르다." 코드부터 찾으면 유아용 의자 사고에 전기다리미 시험이 섞여 나온다.

품목을 못 찾으면 **전체 검색을 자동으로 돌리지 않고 멈춘다.** 억지로 아무거나 보여 주는 것보다 "못 찾았습니다"가 낫다.

품목 찾는 길은 두 갈래다. 어린이제품 33종은 이름으로 바로 찾고, 전기용품은 파일명에 품목이 없어서 **각 기준의 '적용범위' 원문을 뒤진다.** 예를 들어 "가습기"는 KC 60335-2-98의 적용범위 문장에 적혀 있어서 그렇게 찾았다.

#### **③ 실제로 돌려 보니 AI가 옳았던 일**

사고보고서 5건을 모두 코드화했더니 **전부 원인이 "알 수 없음"으로 나왔다.** 처음엔 AI가 일을 못한 줄 알고 한 건을 직접 열어 봤다.

가습기 화재 사건이었는데, AI는 "물 부족 시 자동 전원 차단 기능 미작동으로 화재 발생"이라고 상황은 정확히 읽어 냈다. 그런데 원인 칸은 비웠다. 보고서 본문에 이렇게 적혀 있었기 때문이다 — **"제품 시험 결과 모두 안전기준에 적합", "특이사항을 식별하지 못함".**

즉 **조사에서 결함을 못 찾은 사건**이었고, AI는 없는 원인을 지어내지 않았다.

맞는 판단이었다.

그래서 이런 사건은 화면에 이렇게 알리도록 했다.

> ※ 이 사건은 원인이 확정되지 않았습니다. 아래 후보는 피해유형과 어휘만으로 넓게 건진 것이므로 코드 근거가 없습니다. 특정 시험을 단정하지 마십시오.

원인을 모르는데 결과(화재)만 가지고 "이 시험을 하십시오"라고 하면 그건 근거 없는 지목이 된다.

#### **④ 해외 리콜(트랙 B)을 붙였다**

해외 리콜 2,239건이 있는 협회 시스템(Recall Hub)에 실제로 연결했다. 그러면서 **설계 때의 전제 두 개가 실물과 다른 것을 발견했다.**

*전제 1 — "해외 리콜은 이미 코드가 붙어 있다"* → **아니었다.** 실제 응답에는 코드가 없고 "질식 위험", "화학적 위험", "전기적 위해 (Electrical Hazard)" 같은 자유 서술만 온다. 표기가 제각각이라 분류 기준으로 쓸 수 없다. 그래서 **해외 리콜도 우리가 코드를 붙이기로** 했다. 조항·사고·리콜 셋이 같은 코드 위에 있어야 대조가 되고, 그게 이 체계의 존재 이유다.

*전제 2 — 주소 오타* → `.env.local`에 적혀 있던 `recalhub.kr`(L 하나)은 존재하지 않는 주소였다. `recallhub.kr`(L 둘)이 맞는 주소다. 고쳤다.

**그리고 설계문서가 "먼저 확인하라"고 남겨 둔 항목 하나를 실측해서 답했다.**

컨셉 문서는 "다른 나라와 우리 기준의 수준 차이를 비교하려면, 그 나라가 어떤 기준을 근거로 리콜했는지 알아야 한다. 확보 가능한 범위를 먼저 확인해야 한다"고 적어 두고 미뤄 놨었다. 2,239건 전수를 훑어서 셌다.

| 출처 | 건수 | 위반 표준 명시 | 비율 |
|---|---:|---:|---:|
| EU | 864 | 450 | **52%** |
| 중국 | 172 | 106 | **62%** |
| 영국 | 266 | 57 | 21% |
| 프랑스 | 171 | 9 | 5% |
| 미국·호주·캐나다·일본 등 | 766 | 3 | 0~1% |
| **합계** | **2,239** | **625** | **27.9%** |

**결론: 기준 수준 비교는 전체의 28%, 사실상 EU·중국·영국 건에서만 가능하다.** 나머지 72%는 사유만 적혀 있어 비교할 근거가 없다. 이걸 알고 시작하는 것과 모르고 시작하는 것은 다르다.

한 가지 더 확인했다. EU 공고의 "EN 60598-2-12"와 우리 "KC 60598-2-12"는 같은 국제표준(IEC)에서 온 같은 문서라 **번호로 바로 대조된다.** 반면 완구 표준(EN 71)이나 중국 가전 표준(GB 4706)은 우리 어린이제품 기준과 번호 체계가 아예 달라 번호로는 안 이어진다 — 그건 사람이 판단할 몫으로 남겼다.

#### **⑤ 심각한 누락 하나를 발견해서 고쳤다**

해외 리콜 분석을 돌려 보다가 결과가 이상하게 얄팍한 것을 발견했다. 조항 본문이 대부분 **"제1부의 이 항목을 적용한다"** 한 줄이었다.

이유를 찾아보니 이랬다. 전기용품 국제표준은 **"제1부(공통) + 제2-x부(품목별)"** 두 권으로 되어 있고, **제2-x부는 제1부를 고치는 개정문**이다. 실제 요구사항의 대부분은 제1부에 있다.

```
KC 60335-2-98 (가습기)   요구사항  54개  ← 우리가 보고 있던 것
KC 60335-1    (공통)     요구사항 395개  ← 통째로 빠져 있던 것
```

**가습기 사고를 분석하면서 요구사항의 88%를 못 보고 있었다.** 품목을 찾을 때 제1부를 자동으로 함께 넣도록 고쳤다. 어린이제품에서 부속서에 공통안전기준을 같이 묶는 것과 같은 구조인데, 전기용품은 그 묶음이 없어서 만들어 준 것이다.

#### **⑥ 정확도 측정표를 만들었다**

설계문서 §5.8이 요구한 비교표(코드만 / 코드+어휘 / 코드+어휘+의미 / +리랭킹을 하나씩 켜면서 재현율·오탐률을 재는 표)를 만들었다.

정답지를 어디서 가져오느냐가 갈리는 지점인데, **운영 기록을 정답지로 쓰지 않기로** 했다. 시스템이 보여 준 후보 중에서 담당자가 고른 기록은, 시스템이 애초에 안 보여 준 조항을 정답에 넣을 수 없다. 그러면 점수가 늘 부풀려진다. 전문가가 **시스템 결과를 보기 전에** 작성한 정답지를 기본으로 삼는다.

> 지금은 정답지가 없어서 실제 수치는 아직 못 낸다. 표를 찍는 도구까지만 준비된 상태다.

#### **⑦ 순위 재조정(리랭킹)이 한 번도 작동한 적이 없었다는 것을 발견했다**

검색 결과의 순서를 AI가 다시 매기는 기능이 있는데, 그동안 계속 꺼 놓고 돌려서 아무도 몰랐다. 켜 봤더니 바로 거절당했다.

원인은 데이터베이스가 큰 정수(조항 번호표 등)를 **숫자가 아니라 글자로** 돌려주기 때문이었다. `6070`이 아니라 `"6070"`으로 온다. 프로그램 안에서는 숫자라고 적혀 있어서 자동 검사에도 안 걸린다. 조용히 틀리는 자리다.

같은 문제가 네 군데에 있었고, 그중 하나는 **정확도 측정표를 항상 0%로 만들고 있었다** — 정답 조항 번호는 글자, 시스템이 찾은 조항 번호는 숫자라 서로 대조가 안 됐던 것이다.

고친 뒤 실제로 돌려 보니 이렇게 나왔다 (가습기 화재 사건).

```
[1] 19.4     이상운전 시험 조건            재채점 0.82
[2] 24.101   온도과승방지장치는 비자동복귀형   재채점 0.76
[3] 2-98     (기준 표지)                  재채점 0.35
```

**2순위 24.101이 이 사건의 핵심이다.** 사고 보고서가 "물 부족 시 자동 전원 차단 기능 미작동"이라고 적은 그 장치를 규정한 조항이다. AI가 붙인 이유도 정확했다 — "물 부족 시 보호차단이 작동하지 않았다는 사고 경위와 밀접하다."

그리고 순위 재조정은 기준 표지 조항(2-98)을 1위에서 3위로 내렸다. 이유도 적어 놨다 — "가습기 기준 전체에는 해당하지만 구체적 시험 조건을 규정한 조항은 아니다." 이게 이 기능이 해야 할 일이다.

#### **⑧ 표시 조항이 기준마다 다르게 분류되고 있었다**

같은 성격의 절인데 제목 표기에 따라 갈렸다.

```
"표시사항"           → 표시 조항으로 분류  28건
"표시 및 사용설명서"   → 성능요건으로 분류   14건
```

국제표준(IEC 60335) 제7절은 "표시 및 사용설명서"라는 묶인 제목을 쓰는데, 분류 규칙이 그 형태를 몰랐다. 고쳤더니 성능요건 7,766 → 7,472, 표시 조항 879 → 1,173으로 정리됐다.

여기서 한 가지를 **일부러 바꾸지 않았다.** 표시 조항은 지금 검색 후보에서 빠져 있는데, 설계문서가 "사고 원인이 경고·표시와 관련될 때만 켠다"고 정해 놨기 때문이다. 다만 **그 '켜는' 장치는 아직 안 만들었다.** 화상·질식 사고에서 경고 표시는 실제로 확인하는 항목이므로, 조건부로 켜는 기능이 다음 과제로 남아 있다. 지금은 표시 조항 1,173건이 어떤 사건에서도 후보가 되지 않는다.


**왜 이 라운드가 중요한가**

- 이제 사고보고서 한 건을 넣으면 **품목 확정 → 코드화 → 조항 검색 → 시험방법**  까지 끊기지 않고 이어진다. 그전까지는 조각들만 있었다.
- 해외 리콜도 같은 엔진을 타되, **국내 유통 확인·기준 수준 비교·안전조치 검토는 별도로** 다룬다. 리콜을 사고와 똑같이 취급하면 트랙 B의 고유한 가치(제품안전기본법 13조 3항 사업자 보고의무 판단 등)가 사라진다.
- **시스템이 판단하지 않는 선을 그었다.** 국내에 같은 제품이 유통됐는지는통관·유통 자료로 확인할 일이지 이 시스템이 추측할 일이 아니다. 담당자가 확인한 결과를 받아 적기만 한다.

---

## 지금 시점에서 되어 있는 것 / 안 되어 있는 것

*(라운드 5 종료 시점 기준)*

**되어 있는 것**
- 위해요인 코드북, 안전기준 76개 문서(조항 13,501개), 사고조사보고서 5건이 모두 데이터베이스에 들어가 있다. 보고서 PDF 원본도 보관함(Storage)에 있다.
- 사고보고서 한 건을 넣으면 **품목 확정 → 원인·결과 코드화 → 관련 조항 검색 → 시험방법 연결**까지 끊기지 않고 이어진다. 가습기 화재 사건으로 끝까지 돌려서 확인했다.
- AI 코드화가 실제로 돌아간다. 싼 모델 3회 → 답이 갈리면 비싼 모델로 승격하는 2단 방식이고, 흔들린 건을 검수 목록으로 따로 뽑아 준다.
- 해외 리콜(2,239건)에 실제로 연결된다. 검색해서 가져오고, 코드를 붙이고, 국내 기준 조항과 대조하고, 상대국이 지목한 표준과 우리 기준을 견줄 수 있다.
- 화면에서 현황을 볼 수 있고, 사고보고서를 올릴 수 있다.

**아직 안 되어 있는 것 (다음에 할 일)**
- **전체 조항 태깅**: 요구사항 조항 7,766개 중 지금까지 코드를 붙인 것은 가습기 관련 기준뿐이다. 나머지는 아직이다. 한 번에 다 돌리기 전에 담당자가 지금까지 붙은 코드를 검수해 보는 게 순서다.
- **정확도 수치**: 재는 도구(§5.8 비교표)는 만들었지만, **전문가가 만든 정답지가 없어서 실제 숫자는 아직 못 낸다.** "이 사고면 어느 조항의 시험을 의뢰하겠는가"를 담당자가 시스템 결과를 보기 전에 적어 주면 바로 잴 수 있다.
- **국내 리콜 연동**: 해외 리콜은 연결했지만 국내 리콜(주소는 받아 둠)은 아직이다. 국내 리콜은 코드가 안 붙어 있어 우리가 붙여야 한다.
- **전기용품 품목 등록**: LED등기구, 스팀다리미 등 일부 품목은 적용범위 원문 검색으로도 안 잡힌다. 담당자가 확인해서 등록해 줘야 한다.
- **화면 반영**: 이번 라운드에 새로 생긴 것들(조항 역할 구분, 품목 확정 근거, 원인 미확정 안내, 해외 리콜 분석)이 아직 화면에는 안 붙었다. 명령줄에서만 돌아간다.

---

## 라운드 6 — 리랭킹 "되돌아감" 오인 확인 · 해외 리콜 리치 데이터 갭 분석

**한 일 (코드 변경 없음, 조사만)**

**① 리랭킹(rerank.ts)·매칭(match.ts)이 되돌아갔다는 우려 확인**

라운드 5에서 고친 bigint→Number() 정규화, effort 기반 LLM 호출이 이후 머지 과정에서 사라진 것처럼 보인다는 걱정이 있어서 실제로 확인했다.

`git diff`로 그 커밋(fb1ddc0)과 지금 최신 상태를 직접 비교해 봤더니 **두 파일 모두 차이가 하나도 없었다** — 즉 되돌아간 게 아니다. 그 뒤에 있었던 병합(머지) 커밋 두 개도 이 두 파일에는 아무 변경을 남기지 않은 "깨끗한" 병합이었다. 만약 지금도 예전처럼 동작하는 것처럼 보인다면, 코드 문제가 아니라 (1) 오래된 빌드를 보고 있거나, (2) 실행 환경에 `OPENAI_RERANK_EFFORT` 값이 실제로 전달 안 되고 있거나, (3) 다른 작업 폴더(워크트리)를 보고 있는 경우일 가능성이 크다 — 코드 자체는 정상이다.

**② 해외 리콜 원본 테이블(Recall Hub)과 지금 쓰는 API 사이의 격차 분석**

지금 이 시스템이 해외 리콜을 가져오는 창구(`src/lib/recall/hub.ts`)는 협회 Recall Hub 관리자 시스템이 공개한 **얇은 API 하나**뿐이다. 그런데 그 관리자 시스템 뒤에는 담당자가 직접 검토·승인한 **훨씬 두꺼운 원본 표(90개 이상 컬럼)**가 따로 있다는 제보가 있어서, 그 원본 표 스키마 문서(`docs/해외리콜수집/recalls table schema.txt`, `recalls_rows.sql` 실제 표본 데이터)를 읽고 지금 API가 주는 정보와 정확히 뭐가 다른지 대조했다.

- 지금 API가 주는 건 16개 필드뿐이다(제품명, 브랜드, 위험유형, 국가 등 기본 정보).
- 원본 표에는 있지만 API에는 없는 것 중 가장 중요한 두 가지:
  - **원인-결과 2차원 분류(HF-DT) 코드**: 이 시스템이 국내 사고에 쓰는 것과 **똑같은 코드 체계**(`HF.*`/`DT.*`)로 이미 담당자가 분류해 둔 값이 원본 표에 있다. 그런데 API는 이걸 안 준다. 그래서 지금 이 시스템은 해외 리콜마다 **AI로 다시 분류하는 중복 작업**(`recalls:code`)을 하고 있는 셈이다 — 담당자가 이미 검토해서 붙여 놓은 값을 안 쓰고 있었다.
  - **승인 상태(`approval_status`)**: 담당자 표본 100건을 세어 보니 승인(approved) 53건, 반려(rejected) 46건, 검토대기(pending) 1건이었다. 즉 원본 표에서 승인된 것만 골라 오면 담당자가 이미 거른, 신뢰도 높은 건만 받을 수 있는데 지금 API로는 이 구분을 알 방법이 없다.
  - 그 밖에도 부상자 수, 원문·번역문 양쪽, 이미지·PDF 여러 장, 관리자가 고친 이력 전체(`update_history`) 등이 원본 표에는 있지만 API에는 없다.
- 원본 표에 직접 연결하려면 그 표가 있는 Supabase 프로젝트(`xicivtmjfkpuehgsieex`)의 DB 접속 정보가 필요한데, **지금 연결된 관리 도구 계정에는 이 프로젝트가 없었다.** 같은 조직에 이름이 비슷한 프로젝트(`recall-management`)가 있었지만 주소(ref)가 다르고 사용 중지 상태였다 — 확인해 보길 잘했다, 헷갈렸으면 엉뚱한 곳에 연결할 뻔했다.
- 처음엔 `해외리콜정보_recall_table_schema.txt` 파일을 못 찾아서 "저장소에 없다"고 적었는데, 다시 확인해 보니 **없어진 게 아니라 이번 조사 직전 커밋에서 `해외리콜수집/recalls table schema.txt`로 이름이 바뀐 것**이었다(파일을 옮기면서 이름도 정리한 것으로 보인다 — 잘못된 게 아니다). `recall-hub-admin화면.png`도 실제로 있었고, 다만 아직 git에 올라가 있지 않은 상태(작업 폴더에만 있음)였다.

**왜 이 라운드가 중요한가**
- 리랭킹 문제는 실제로는 없었다는 걸 diff로 직접 확인해서, 애먼 코드를 건드리지 않을 수 있었다.
- 해외 리콜 쪽은 "API가 부실하다"는 느낌이 실제로 어디가 얼마나 부실한지(16개 vs 90개 이상 컬럼, 특히 사람이 검토한 HF-DT 분류와 승인 여부)를 숫자로 확인했다. 이걸 알아야 다음에 뭘 연결할지 우선순위를 정할 수 있다.

**다음 라운드에 필요한 것 (담당자 확인 필요)**
1. `xicivtmjfkpuehgsieex` 프로젝트의 DB 접속 문자열(DATABASE_URL) — `.env.local`에 추가해야 원본 표를 직접 읽을 수 있다.
2. 지금 하고 있는 자체 AI 분류(`recalls:code`)를 원본 표의 담당자 승인 분류로 완전히 바꿀지, 아니면 두 방식을 한동안 같이 두고 비교해 볼지 결정 필요.
3. 원본 표를 끌어올 때 필요한 컬럼만 고를지, 통째로 다 가져와 둘지(`raw_data` 원본 포함) 범위 결정 필요.

---

## 라운드 7 — 해외 리콜, 원본 표 직접 연결로 전환 (AI 재태깅 폐지)

**결정 사항 (담당자 확인)**
- `recall-management`(ref 다름, INACTIVE)는 이번 건과 무관한 별도 프로젝트로 확인됨.
- 자체 AI 태깅(`recalls:code`)을 원본 표의 담당자 승인 분류로 **완전 대체**.
- 원본 표는 컬럼을 고르지 않고 `raw_data` 포함 통째로 가져온다.

**한 일**
- 원본 표(`xicivtmjfkpuehgsieex` 프로젝트, `recalls` 표)에 Supabase REST 키(`sb_secret_*`)로 직접 붙는 조회 모듈(`src/lib/recall/source.ts`)을 만들었다. 얇은 API(`src/lib/recall/hub.ts`, 16개 필드)는 이제 아무 데서도 안 써서 지웠다.
- 적재 스크립트(`scripts/load-recalls.ts`)를 다시 짰다. 예전엔 "캐시(`recalls:fetch`) → AI 코드화(`recalls:code`)" 두 단계였는데, 원본 표에 이미 담당자가 승인한 위해요인 코드가 있으므로 **한 번에 끝난다** — AI를 한 번도 안 부른다.
- `recall_cache` 표에 컬럼을 더했다(승인 상태, 심각도, 부상 건수 등 — 014 마이그레이션).

**실제로 돌려서 확인한 것 (전수, 2026-09-02)**
- 원본 표 전체 4,828건 중 승인(approved) 2,253건. 이 승인 건만 가져온다.
- 2,253건 캐시 저장 → 사건 행 신규 2,206 + 기존 47(먼저 소량으로 시험 삼아 넣어 둔 건들과 겹침, 정상) → 품목 확정 1,511건(67%) → **코드화 2,213건(98.2%, AI 호출 0회)**.
- KC 보유 기준과 번호가 같은 위반 표준이 명시된 건 78건.
- 재실행해서 중복이 안 생기는지도 확인했다 — `case_event` 2,253건, 중복 `external_ref` 0건. `case_tag` 7,848행이 정확히 2,213개 사건에 물려 있다(전부 일치).

**막혔다가 뚫은 문제 — 코드가 다 안 맞았다**
- 승인 건의 위해요인 코드를 우리 코드북(v0.9.7, 80개)과 전수 대조했더니 두 코드가 안 맞았다.
  - `HF.S.STD`(주 40건·부 155건, 승인 건의 8.7%) — PDR 부록 J 변경이력을 보니 **우리 코드북에서 v0.9 때 이미 폐기하고 8개로 세분화한 코드**였다(§5.2). v0.9.5에서는 실제 사례를 하나씩 봐 가며 `HF.S.QC`/`HF.S.PROC`/`HF.S.NATL.DEF` 등으로 손으로 바꿨다는 기록까지 있다 — 즉 자동으로 1:1 치환할 수 있는 코드가 아니다. 관리자 시스템(Recall Hub)이 이 폐기 이전의 옛 코드북 기준으로 분류를 돌리고 있다는 뜻이라, 두 시스템의 코드북이 언제부턴가 어긋나 있었다는 얘기다(거버넌스 동기화 문제 — Recall Hub 담당자에게 알려야 한다).
  - `HF.M.REG.VIOL`(1건) — 애초에 없던 코드로 보인다. 우리 코드북엔 `HF.M.REG.ILLEGAL`· `HF.M.REG.UNMANAGED`(REG 하위)와 별도로 `HF.M.VIOL`(내부규정 위반, REG와 나란한 최상위)이 있는데, 이 둘을 합쳐서 잘못 만든 코드로 추정된다.
  - 임의로 짐작해서 매핑하지 않았다. 코드북에 없는 코드는 그 건에서 버리고, **주 위해요인 코드 자체가 안 맞는 40건은 아예 코드화하지 않고 미분류로 남겼다**(부코드만 안 맞는 경우는 그 코드만 빼고 나머지로 정상 코드화했다). 실행하면 미분류 건 목록(recall_cache id·출처·guid)이 그대로 찍힌다 — 담당자가 건별로 봐야 한다.

**부수적으로 고친 버그**
- 이 참에 손대다가 발견한 것: `case_event`에 `external_ref` 유니크 제약이 원래 없어서, 적재 스크립트를 재실행할 때마다 같은 리콜이 새 행으로 또 쌓이는 상태였다(막았다고 주석엔 적혀 있었는데 실제로는 안 막혔다). `source_type + external_ref` 부분 유니크 인덱스를 추가해서 고쳤고, 재실행 테스트로 실제로 중복이 안 생기는 것까지 확인했다(위 실측 참고).

**일부러 안 바꾼 것**
- 이번에도 `chunk_summary`(AI 한 줄 요약)는 안 만든다. 원본 표의 서술 필드 (`product_description`, `hazard_description`, `recall_cause` 등)가 얇은 API 시절보다 훨씬 풍부해서, 검색용 텍스트(`search_text`)에 그대로 넣는 것만으로 충분하다고 보고 AI 요약 단계를 아예 건너뛰었다 — 비용도 안 들고, `buildCaseSearchText()`가 요약 없이도 정상 동작하도록 이미 짜여 있었다(변형 A에서 "요약:" 줄만 비워짐).
- `recalls:code`(AI 코드화) npm 스크립트 자체는 지우지 않았다. 사고보고서·국내 리콜은 여전히 이 경로로 코드화해야 하고, 해외 리콜에 대해서만 이제 할 일이 없어질 뿐이다(이미 `case_tag`가 있으면 건너뛰는 조건이 있어 자동으로 그렇게 된다).

**다음에 할 일**
- 미분류 40건(`HF.S.STD`) — Recall Hub 담당자와 협의해서 원본 쪽 코드북을 올리거나, 우리 쪽에서 건별로 세분 코드를 골라 줘야 한다.
- 사고보고서 쪽에도 `HF.S.STD` 처럼 옛 코드가 남아 있는지는 아직 확인 안 했다.

---

## 라운드 8 — HF.S.STD 자동 재분류 · GPC 코드 조회 · 사고사진 비전 분석

세 가지를 한 라운드에 붙였다. 앞의 두 가지(HF.S.STD, GPC)는 라운드 6·7의 뒷정리이고, 사진 분석은 완전히 새 기능이다.

### ① HF.S.STD 40건, LLM이 세부 코드로 재분류 — 이제 "미분류"가 생기면 자동으로 이렇게 처리된다

라운드 7에서 미뤄 둔 40건(관리자 쪽이 폐기된 코드 `HF.S.STD`를 붙인 건)을, 우리 쪽 코드북에서 유효한 세부 코드로 LLM이 다시 분류하도록 적재 스크립트를 고쳤다. 새 프롬프트를 만들지 않았다 — 기존 `tagCase()`(사고·리콜 코드화에 원래 쓰던 함수)는 애초에 "지금 유효한 코드 목록"만 답으로 허용하도록 짜여 있어서, 이 함수를 그대로 다시 부르기만 하면 폐기된 코드가 나올 수 자체가 없다. 그래서 앞으로 관리자 쪽에서 또 다른 옛 코드를 보내와도(코드북이 계속 바뀌므로 또 생길 수 있다) 사람이 매번 손대지 않아도 똑같이 처리된다.

관리자 값을 그대로 쓴 코드는 `review_status='approved'`, LLM이 대신 채운 축만 `'auto_unreviewed'`(검수 대상)로 남긴다 — 같은 사건 안에서도 축마다 출처를 구분해 남겨서, 나중에 담당자가 뭘 다시 봐야 하는지 바로 걸러낼 수 있게 했다.

실제로 40건 전부 돌려 봤다. 대부분(32건)은 `HF.H.ELEC.BAT`, `HF.S.NATL.DEF`처럼 구체적인 세부 코드로 갔고, 일부(8건)는 `HF.H`, `HF.S`처럼 큰 갈래 이름만 나왔다. 이건 이번에 새로 생긴 문제가 아니라 **이 시스템의 코드화 기능 전체가 원래 갖고 있던 특성**이라는 것도 확인했다 — 코드북 조회 옵션을 어떻게 주든 그 큰 갈래 이름 자체가 항상 고를 수 있는 목록에 들어 있었다(사고보고서 코드화에도 똑같이 적용된다). 다만 그런 애매한 답이 나오면 검수 대상으로 표시되게 만들어 뒀으니, 이번 경우엔 딱 설계한 대로 걸러진 셈이다.

### ② GPC(GS1 국제 품목분류) 코드 조회 — 처음 생각과 다르게 방식을 바꿨다

담당자가 "이미 있는 edge function을 부르자"고 했는데, 실제로 만들어 보니 두 가지 문제가 나와서 방식을 바꿨다.

1. 그 edge function은 후보를 1개(top-1)만 주도록 고정돼 있었다. 담당자가 원한 "후보 3~5개"를 애초에 못 낸다.
2. 그 edge function 안에 박혀 있는 OpenAI 키가 크레딧이 다 떨어진 상태였다 (`429 no credits remaining` 실측). 우리가 고칠 수 있는 부분이 아니다.

그런데 담당자가 이어서 보내 준 n8n 워크플로(`(GS1) [한국리콜__OECD] OECD GPC RAG.json`)를 보니, 그 edge function이 쓰는 것과 **같은 프로젝트 안에 있는 원본 표·검색 함수**(`oecd_gpc_202405` 표, `match_documents_202405` 함수)를 직접 참조하고 있었다. 실제로 그 함수를 우리 쪽 anon 키로 직접 불러 봤더니(edge function을 거치지 않고) **바로 실행이 됐다** — 즉 협회가 처음부터 이 함수를 외부에서 부를 수 있게 열어 둔 상태였다.

그래서 edge function은 아예 안 쓰기로 했다. 대신:
- 상품명·설명 문장을 **우리 OpenAI 키**로 임베딩한다(그쪽 크레딧 문제와 무관해진다).
- 그 벡터로 `match_documents_202405`를 직접 불러 후보를 원하는 개수(3~5개)만큼 받는다.

실제로 "가습기"로 검색해 봤다. 1위가 정확히 "가습기 - 이동형" 브릭이었다. 그런데 유사도 값 자체는 0.25 안팎으로 낮았다 — 원래 edge function의 기본 임계값(0.75)을 그대로 썼다면 **정답인 1위조차 "일치 없음"으로 걸러졌을 것**이다. 그래서 임계값으로 거르지 않고 순위(상위 몇 개)로만 판단하기로 했다. 또 하나, 사진에서 뽑은 좀 더 상세한 설명 문장으로 다시 검색했더니 1위가 "제습기"(가습기의 반대 기능)로 바뀌고 "가습기"가 2위로 밀린 적도 있었다 — 이게 정확히 담당자가 "후보 여러 개를 보고 싶다"고 한 이유였다는 걸 실측으로 확인한 셈이다. 1개만 믿었으면 틀렸을 사례다.

### ③ 사고조사보고서 첨부 사진 분석 — 새 기능, 실제로 텍스트가 놓친 단서를 찾았다

지금까지 사고보고서는 텍스트만 읽었다. PDF 안에 삽입된 사진은 있어도 그냥 파일째로 보관만 하고 아무도 들여다보지 않았다. 그런데 라운드 4·5에서 사고 5건 중 다수가 "시험 결과 안전기준 적합"이라는 문장만 남기고 원인 미확인(HF.UNKNOWN)으로 끝났던 걸 다시 보니, 사진에 텍스트에 없는 단서가 있을 수 있겠다 싶어 이번에 붙였다.

**한 일**
- PDF에서 삽입 사진만 뽑아낸다(페이지를 통째로 찍는 게 아니라 실제 이미지 오브젝트만 — 표·글자는 안 섞인다). 가로세로 200px 미만인 것은 뺀다(로고·직인 같은 서식 이미지가 여러 보고서에서 정확히 같은 크기로 반복되는 걸 실측으로 확인했다). 걸러지지 않고 통과한 것 중에서도 진짜 증거 사진인지는 비전 LLM에게 최종 판단을 맡긴다 — 놓치는 쪽보다 더 보내는 쪽이 안전하다는 이 프로젝트의 원칙을 사진에도 그대로 적용했다.
- 뽑은 사진은 Supabase Storage(기존 원본 보관 버킷)에 저장하고, 새 표 `source_file_image`에 사진마다 분석 결과를 남긴다.
- 비전 LLM(담당자 요청대로 최상위 모델은 아니고 이 시스템의 "품질 우선" 등급 모델을 씀 — 연 50건 안팎이라 비용보다 품질이 우선)이 사진마다 "실제 증거 사진인가/장식 이미지인가"부터 판단하고, 증거 사진이면 뭐가 보이는지·위해요인 관련 관찰이 있는지 적는다. 근거 없는 원인 추정은 하지 않는다는 이 시스템의 기존 원칙(§5.2.2)을 사진 쪽에도 그대로 옮겼다.
- 증거 사진들을 종합한 "제품 서술 한 문장"을 만들어서, 그걸 그대로 위 GPC 조회의 입력으로 재사용한다 — 담당자가 말한 "이것도 GPC 분류에 쓸 수 있겠다"는 생각이 실제로 맞았다.

**실제로 돌려서 확인한 것 (가습기 사고, 2026-09-02)**
- 11쪽짜리 보고서에서 사진 4장을 뽑았고, 비전 LLM이 4장 전부 증거 사진으로 판단했다.
- 그중 4쪽 사진 2장에서 **"바닥면 내부 부품 주변에 갈색 변색 또는 얼룩이 보인다"**는 관찰을 냈다. 이 보고서의 텍스트만으로는(원래 결론이 "시험 결과 적합") 전혀 나오지 않았던 단서다. 텍스트 코드화 결과는 여전히 HF.UNKNOWN(원인 미확인)으로 남아 있는데, 이 갈변 관찰이 담당자가 재검토할 때 실제로 쓸모 있을 만한 신호다.

**작업 중 실수하고 바로 고친 것**
- 이 기능을 가습기 사건으로 테스트하면서 `--force` 옵션을 썼는데, 이게 그 사건의 **기존 확정 상태·품목·코드화 결과(라운드 5의 리랭킹 시연 데이터)를 통째로 지워 버렸다.** 바로 알아채고 코드화를 다시 돌리고(`HF.UNKNOWN` / `DT.THERMAL.FIRE`로 원래와 같은 결과 확인) 확정 상태를 되돌리고 임베딩을 다시 만들어 복구했다. 앞으로 이런 시연·데모 데이터가 있는 사건에는 `--force`를 함부로 쓰면 안 된다는 걸 기록해 둔다.
- GPC 조회를 처음엔 edge function 기준으로 구현하고 사고 스크립트에 연결까지 했는데, 위 ②의 이유로 도중에 직접 RPC 호출 방식으로 다시 짰다. 함수 자체(`findGpcCandidates`)는 실제 호출로 검증했지만, 새 방식으로 사고 적재 스크립트 전체를 다시 돌려서 확인하지는 못했다 — 위 `--force` 실수를 반복하지 않으려고 기존 5건에는 재실행을 안 했다. **다음에 새 사고보고서가 들어오면 그때 사진→비전→GPC 전체 경로가 실제로 이어지는지 처음으로 확인하게 된다.**

**바뀐 파일 요약**
- `src/lib/cases/extract-pdf.ts`: `extractPhotos()` 추가(사진 추출).
- `src/lib/llm/client.ts`: `structuredVisionCall()` 추가(이미지 포함 구조화 호출).
- `src/lib/llm/vision.ts`(신규): 사진 분석 프롬프트·스키마.
- `src/lib/gpc/lookup.ts`(신규): GPC 후보 조회(직접 RPC 방식).
- `src/lib/cases/item-name.ts`(신규): 품목명 추출 로직을 `tag-cases.ts`에서 뽑아내 `load-cases.ts`와 같이 쓰게 함(중복 방지).
- `scripts/load-cases.ts`: 사진 추출·보관·분석·GPC 조회를 적재 파이프라인에 연결.
- `scripts/load-recalls.ts`: HF.S.STD 등 무효 코드 자동 LLM 재분류.
- `supabase/migrations/015_case_photos.sql`: `source_file_image` 표, Storage 버킷에 `image/jpeg` 허용 추가.
- `package.json`: `sharp`(이미지 인코딩) 추가.

**다음에 할 일**
- 새 사고보고서가 들어올 때 사진→비전→GPC 경로 실사용 확인.
- GPC 후보를 화면에 아직 안 보여 준다 — `case_event.raw_fields.gpc_candidates`에는 있지만 UI 연결은 다음 과제.
- HF.H/HF.S 처럼 큰 갈래 이름만 나오는 문제는 이번 라운드 범위 밖이지만, 사고보고서 코드화에도 있는 만큼 프롬프트를 더 다듬을지는 별도로 검토해야 한다.

---

## 라운드 9 — 사진→비전→GPC 첫 실사용 확인 · GPC 화면 표시 · KC기준 GPC 착수 · 큰 갈래 이름 문제 실태 조사

라운드 8이 "다음에 할 일"로 남긴 네 가지를 그대로 이어받았다. 새 기능을 만들지 않고, 지난 라운드가 만든 것이 실제로 작동하는지 확인하고, 확인하는 과정에서 발견한 것들을 정리하는 라운드다.

### ① 사진→비전→GPC 전체 경로, 실제 사건으로 처음 끝까지 돌려 확인

DB를 열어 보니 가습기 사건의 사진 분석(4장, 증거사진 4장)은 이미 되어 있었는데 GPC 쪽(`gpc_brick_code`, `raw_fields.gpc_candidates`)은 비어 있었다. 라운드 8이 "GPC 조회 함수 자체는 검증했지만 사고 적재 스크립트 전체를 다시 돌려서 확인하지는 못했다"고 남긴 바로 그 지점이다.

GPC 조회 함수를 단독으로 먼저 불러 봤더니 `self-signed certificate in certificate chain` 오류로 실패했다 — 그런데 이건 진짜 버그가 아니라, 이 프로젝트의 네트워크 호출 스크립트가 전부 쓰는 `NODE_OPTIONS=--use-system-ca` 없이 직접 실행해서 생긴 내 테스트 환경 문제였다. 그 옵션을 붙이자 정상적으로 후보 5개가 나왔다(2위가 "가습기 - 이동형" 브릭 — 타당한 결과).

그래서 실제 파이프라인(`npm run cases:load -- --only 가습기 --force`)을 그대로 다시 돌렸다. 사진 4장 추출 · 비전 분석 · GPC 후보 5개 조회 · 1위 승격까지 **한 번에 이어졌다.** 다만 1위로 뽑힌 건 "가습기"가 아니라 "제습기 - 이동형 (전기식)"이었다(2위가 정답 "가습기 - 이동형", 3위가 "가습기 - 고정형") — 이건 라운드 8이 이미 실측한 것과 같은 현상(절대 유사도가 낮아 순위가 흔들릴 수 있음)이고, 그래서 화면에도 1위만 보여 주지 않고 순위 전체를 보여 주기로 했다(② 참고).

**실수하고 바로 복구한 것** — `--force`를 다시 썼다가, 라운드 8이 이미 한 번 겪었던 바로 그 사고를 또 냈다. 가습기 사건의 확정 상태·품목(가습기)·코드 (HF.UNKNOWN/DT.THERMAL.FIRE)·검색 결과(match_run, 라운드 5의 리랭킹 시연 데이터)가 전부 지워졌다. 확인 직후 `npm run cases:code -- --case <id> --all` → `npm run embed -- --cases` → `npm run search -- --case <id>` 순서로 다시 돌려서, 라운드 8이 기록해 둔 것과 정확히 같은 결과(HF.UNKNOWN/DT.THERMAL.FIRE, 확정 상태, match_run 20건)로 복구했다. **이 사건에는 절대 `--force`를 쓰면 안 된다** — 라운드 5·8 이후로 이 사건이 시연용 골든 데이터가 됐다는 걸 이번에 한 번 더 확인한 셈이니, 다음에 또 잊지 않도록 이번에도 다시 적어 둔다.

### ② GPC 후보를 사건 분석 화면(화면 D)에 표시

`case_event.raw_fields.gpc_candidates`에는 있었지만 화면에 없던 것을 `src/app/analysis/[caseId]/page.tsx`의 "품목·적용기준" 섹션 바로 아래에 추가했다. 1위만 확정처럼 보여 주지 않고 순위 전체 + 유사도 값을 그대로 보여 주고, 1위가 확실한 답이 아닐 수 있다는 문구를 함께 넣었다 — ①에서 실측한 "제습기가 1위로 나온" 사례가 바로 이 판단의 근거다. GPC 후보가 없는 사건(사진이 없거나 아직 분석 전인 경우)에는 이 섹션 자체를 숨긴다.

### ③ KC안전기준(standard)에 GPC 코드 부여 — 착수만, 전량 배치는 안 돌림

`public.standard`에 `gpc_brick_code`·`gpc_candidates` 컬럼만 추가했다 (`016_standard_gpc.sql`). 대상을 `standard`로 잡은 이유는 GPC가 "제품이 무엇인가"를 분류하는 것이고 `standard.item_name`이 문서 1건당 그 제품명을 이미 갖고 있기 때문이다 — `clause`는 개별 요건 행이라 제품 단위가 아니다.

`--limit`을 필수로 받는 시험용 스크립트(`scripts/tag-standards-gpc.ts`, `npm run standards:gpc`)를 만들어 5건만 돌려 봤다. **기술적으로는 문제없이 동작했지만(호출·저장 다 됨), 정확도는 사고사진 GPC보다 눈에 띄게 나빴다:**

| 표준 | item_name | GPC 1위 결과 |
|---|---|---|
| 안전인증 부속서 1 | 어린이용 물놀이기구 | 어린이용 요실금 제품 — 오답 |
| 안전인증 부속서 3 | 가정용압력냄비및압력솥 | 자기기압계(비전기식) — 오답 |
| 안전확인 부속서 16 | 유아용 캐리어 | 기타 유아 안전/보안/감시 — 애매 |
| 안전확인 부속서 70 | 휴대용 예초기의 날 및 보호덮개 | 개인용 안전장비 세트 — 애매 |
| 공급자적합성 부속서 1 | 어린이용 가죽제품 | 기타 유아 급식용품 — 오답 |

**왜 이런 차이가 나는지 짐작 가는 이유** — 사고사진 GPC는 비전 LLM이 사진을 보고 뽑아낸 상세한 서술 문장("투명 물 수위창과 하단 조작부가 있는 흰색 원통형 전기 가습기…")을 조회 입력으로 쓰는데, 기준 쪽은 지금 `item_name` + `display_name` (예: "가정용압력냄비및압력솥" + "안전인증 부속서 3(가정용압력냄비및압력솥)")처럼 짧고 건조한 행정 분류명만 넣고 있다. 임베딩 유사도 검색은 입력 문장이 구체적일수록 잘 맞는데, 지금 입력은 그 반대다. **이건 다음 라운드에서 담당자가 판단할 문제다** — 조회 입력에 조항 본문 일부(적용범위 문장 등)를 더 넣을지, 아니면 기준 76건 규모에서는 GPC가 애초에 잘 안 맞는 방식인지.

전체 76건(`is_current`) 배치는 이번 라운드에서 돌리지 않았다 — 비용도 들고, 위 표처럼 정확도가 검증 안 된 상태로 76건 다 채우면 나중에 되돌리기 번거롭다. `item_name`이 null인 표준(KC 60335-2-xx류 베이스표준, 조회할 품목명 자체가 없음)도 이 방식 밖이라 별도 검토가 필요하다.

### ④ HF.H/HF.S처럼 큰 갈래 이름만 나오는 문제 — 실태만 조사(프롬프트는 안 고침)

라운드 8이 사고코드화(case_tag)에서 "이 시스템 전체가 원래 갖고 있던 특성"이라고 확인한 것과 같은 문제를, 이번엔 조항 코드화(clause_tag)에서도 실측했다.

| 대상 | 전체 코드화 건수 | HF.H 맨몸(하위 없이) | HF.S 맨몸 | 합계 비율 |
|---|---|---|---|---|
| 조항(clause_tag) | 373건 | 17건 | 30건 | 47건 (12.6%) |
| 사건(case_tag) | 2,263건 | 19건 | 13건 | 32건 (1.4%) |

조항 쪽이 사건 쪽보다 9배 가까이 높다. 왜 그런지 보려고 `HF.S` 맨몸으로 붙은 조항 8건의 본문을 직접 읽었는데, **거의 다 KC 60335-1(전기용품 공통기준)의 순수 시험절차·측정값 조항**이었다(예: "누설 전류는 시험 전압을 인가한 후 5초 이내에 측정한다", "온도 상승 값은 표3에서 정한 값을 초과하여서는 안된다"). 이런 조항은 애초에 특정 위해요인을 서술하지 않는다 — 회사가 국내표준을 안 지켰다거나(HF.S.NATL) 내부규정이 없다는(HF.S.CORP) 얘기가 전혀 아니다. 그런데도 모델은 (아마 뭐라도 골라야 해서) `HF.S`라는 큰 갈래 이름을 골랐다.

leaf 코드가 코드북에 없어서 생기는 문제가 아니라는 것도 확인했다 — `HF.H.ELEC.INS`(94건), `HF.H.PHY.SMALL`(246건) 같은 세부 코드가 실제로 가장 많이 쓰이고 있다. 즉 모델이 leaf를 고를 수 없어서가 아니라, **순수 시험절차 조항처럼 위해요인 자체가 애매한 경우에 `HF.UNKNOWN` 대신 `HF.S` 맨몸을 고르는 경향**으로 보인다. 이번 라운드에서는 프롬프트를 고치지 않았다 — 근거만 정리해 뒀으니, 프롬프트를 바꿀지(예: "순수 시험절차만 있고 위해요인 서술이 없으면 HF.UNKNOWN을 쓴다"는 규칙 추가) 담당자가 다음 라운드에서 판단하면 된다.

**바뀐 파일 요약**
- `src/app/analysis/[caseId]/page.tsx`: GPC 후보 순위 표시 섹션 추가.
- `supabase/migrations/016_standard_gpc.sql`(신규): `standard.gpc_brick_code`, `standard.gpc_candidates` 컬럼.
- `scripts/tag-standards-gpc.ts`(신규): 기준 GPC 조회 시험용 스크립트(`--limit` 필수).
- `package.json`: `standards:gpc` 스크립트 추가.
- 조항 코드화(`clause_tag`)·사건 코드화(`case_tag`)·기준 GPC 시험(`standard`) 5건은 실제 DB 값을 바꿨다(모두 위 표에 실측치로 남김). `scripts/tag-clauses.ts`, `scripts/tag-cases.ts` 등 코드 자체는 고치지 않았다.
- `docs/구현이력.md` → 저장소 루트 `구현이력.md`로 위치 이동(같은 라운드 안에서 다른 요청으로 처리 — 문서를 찾기 더 쉬운 위치로 옮기고 `CLAUDE.md`· `src/lib/env.ts`의 경로 참조도 함께 갱신했다).

**다음에 할 일**
- 기준(standard) GPC — 조회 입력을 더 구체적으로 만들지(적용범위 문장 등 추가), 아니면 이 규모에서 GPC 방식 자체를 재검토할지 판단 필요.
- 기준(standard) 전량 배치(76건) — 위 판단이 끝난 뒤에.
- `HF.H`/`HF.S` 맨몸 문제 — "순수 시험절차 조항은 HF.UNKNOWN" 같은 프롬프트 규칙을 추가할지 판단 필요. 사건 쪽(1.4%)보다 조항 쪽(12.6%)이 훨씬 크므로, 손댄다면 조항 프롬프트(`SYSTEM_CLAUSE`)부터가 우선순위가 높아 보인다.
- `item_name`이 null인 베이스표준(KC 60335-2-xx류)은 GPC 조회 대상 자체가 아니다 — 이 표준들에 GPC를 붙이려면 다른 입력(예: 연결된 부속서의 item_name)을 빌려와야 한다.

---

## 라운드 10 — 기준(standard) GPC 조회 입력 보강 · HF.UNKNOWN 프롬프트 규칙 추가 · Netlify 배포 완료

라운드 9가 "판단 필요"로 남긴 두 가지에 대해 담당자가 방향을 정해 주어서 바로 반영했다. 새 기능은 아니고, 이미 있는 것을 고치는 라운드다.

### ① 기준 GPC 조회 입력 — item_name 단독에서 item_name + scope_text 로

담당자 판단: "명칭과 적용범위 부분 등을 복합적으로 전달해서 받으면 될 듯합니다." DB를 열어 실제로 쓸 만한 컬럼이 있는지부터 확인했다. `public.standard.scope_text` (012_product_scope.sql이 "적용범위(SCOPE) 조항 본문을 모은 것"이라 정의해 둔 컬럼)가 있었고, GPC 대상인 item_name 있는 표준 33건 전량에 이미 채워져 있는 것을 실측으로 확인했다(예: KC 60335-2-13 "요리할 때 식용유를 사용하는 가정용 전기 튀김기, 전기 프라이팬"). `scripts/tag-standards-gpc.ts`가 조회 입력으로 쓰던 `display_name`("안전인증 부속서 3(가정용압력냄비및압력솥)" 같은 행정 분류명)을 이 `scope_text`로 바꿨다(없으면 `display_name`으로 폴백).

라운드 9에서 오답이 났던 바로 그 5건을 GPC 컬럼만 초기화하고 다시 돌려 비교했다.

| 표준 | item_name | 이전(display_name) | 이번(scope_text) |
|---|---|---|---|
| 안전인증 부속서 1 | 어린이용 물놀이기구 | 어린이 요실금 제품 — 오답 | 어린이 요실금 - 속옷(일회용) — 오답 |
| 안전인증 부속서 3 | 가정용압력냄비및압력솥 | 자기기압계(비전기식) — 오답 | **압력솥 — 정답** |
| 안전확인 부속서 16 | 유아용 캐리어 | 기타 유아 안전/보안/감시 — 애매 | **유아용 캐리어 — 정답** |
| 안전확인 부속서 70 | 휴대용 예초기의 날 및 보호덮개 | 개인용 안전장비 세트 — 애매 | 온상/냉상/모종상자/육묘용보호덮개 — 오답 |
| 공급자적합성 부속서 1 | 어린이용 가죽제품 | 기타 유아 급식용품 — 오답 | 유아 목욕안전 제품 — 오답 |

5건 중 0건 정답 → 2건 정답으로 개선됐지만 완전히 해결되지는 않았다. 실제 scope_text 원문을 읽어 보니 이유가 보인다 — 이 문장들은 "이 기준은 ~에 대하여 규정한다" 형식의 법령 문체라서, 물놀이기구 건은 "착용형/비착용형 수영보조용품" 이라고만 쓰여 있고 "물놀이기구"라는 단어 자체가 없다. 가죽제품 건도 "만 13세 이하의 유아 및 아동이 사용하는 제품…"이라는 일반 문구만 있고 "가죽"이라는 단어가 아예 없다. item_name으로 그 단어를 보충해도 scope_text 쪽 문장이 길어서 희석되는 것으로 보인다. 사고사진 GPC(비전 LLM이 뽑은 구체적 서술문)만큼 좋은 입력을 기준 쪽에서 구하기는 근본적으로 더 어렵다는 뜻이라, 이 방식의 한계로 일단 기록해 둔다. 전량 배치(76건)는 이번에도 안 돌렸다 — 정확도가 완전히 검증되지 않았다.

### ② HF.H/HF.S 맨몸 문제 — "순수 시험절차면 HF.UNKNOWN" 규칙 추가

담당자 판단: "순수 시험절차 조항에 HF.S를 고르는 건 잘못됐다." 라운드 9가 추정한 원인(모델이 뭐라도 골라야 해서 HF.S 맨몸을 고르는 경향)이 맞다고 확인된 것으로 보고, `src/lib/llm/tagging.ts`의 `SYSTEM_CLAUSE`(조항 태깅 프롬프트)에 규칙을 추가했다: 시험절차·측정방법·합격판정값만 서술하고 위해요인 자체가 없으면 hf_primary를 HF.UNKNOWN으로 두고, 목록에 마땅한 코드가 안 보인다고 HF.S를 대신 고르지 말라는 것과, HF.S는 조항이 국내표준 미준수(HF.S.NATL)나 내부규정 부재(HF.S.CORP)를 실제로 서술할 때만 쓰라는 것을 명시했다.

이번 라운드에서는 프롬프트만 고쳤다 — 기존에 이미 태깅된 373건(그중 47건이 HF.H/HF.S 맨몸)을 새 규칙으로 다시 태깅하지는 않았다. `npm run tag -- --retag`는 클로즈(clause) 단위 필터가 없어 47건만 골라 다시 돌릴 수 없고, 검수 승인 안 된 조항 전체(373건)를 다시 부르게 된다 — 비용은 크지 않지만(라운드 9 실측 기준 조항 1건당 약 $0.0022, 373건이면 1달러 안팎) 재태깅은 임베딩도 같이 무효화시키므로(`npm run embed` 재실행 필요) 담당자 승인 없이 진행하지 않았다. 다음 라운드에서 재태깅 여부를 정하면 된다.

### ③ Netlify 배포 완료 (담당자 확인)

라운드 9와 같은 요청에서 병렬로 진행하던 Netlify 배포(`netlify.toml` 생성, CLI 설치까지는 이 세션이 하고 로그인·프로젝트 연결·환경변수 등록은 담당자가 직접 진행하기로 남겨 둔 것)를 담당자가 완료했다. 프로젝트명 `kc-regulation-product-hazard-analysis`, houuya 팀 아래 GitHub 연동, 환경변수 전부(시크릿 24개 + `NEXT_PUBLIC_*` 2개) 등록 확인(스크린샷). 배포 진행 중인 상태를 확인했다 — 실제 기능(DB 조회 등)이 프로덕션에서 되는지는 다음 라운드에서 확인이 필요하다.

**바뀐 파일 요약**
- `scripts/tag-standards-gpc.ts`: 조회 입력을 `display_name`에서 `scope_text`로 변경.
- `src/lib/llm/tagging.ts`: `SYSTEM_CLAUSE`에 "순수 시험절차면 HF.UNKNOWN" 규칙 추가.
- `standard` 테이블: 라운드 9 시험 5건의 `gpc_brick_code`/`gpc_candidates`를 새 결과로 재기록(실제 DB 값 변경).
- `netlify.toml`, `.gitignore`(`.netlify/` 제외): 라운드 9에서 이어진 Netlify 설정.

**다음에 할 일**
- ~~조항 코드화 47건(HF.H/HF.S 맨몁) 재태깅 여부~~ — 담당자 승인 받아 진행함. 결과는 라운드 11 "④ HF.S 맨몸 문제" 절 참고(도중에 범위를 초과한 사고가 있었고, 최종 47→10건).
- ~~기준(standard) GPC — scope_text로도 못 잡는 5건 중 3건 문제~~ — 라운드 11에서 LLM 검증 레이어로 이어서 다룸(같은 5건 재검증 결과 오답 0건).
- Netlify 프로덕션에서 DB/OpenAI 호출이 실제로 되는지 확인. (미확인 — 다음 라운드)

---

## 라운드 11 — 기준 GPC에 LLM 검증 레이어 추가 · HF.S 맨몸 재태깅(별도 진행)

라운드 10이 "scope_text로도 못 잡는 5건 중 3건" 문제를 남겼는데, 담당자가 방향을 "LLM을 GPC 매칭에 추가로 붙이되, 입력 컬럼을 정확성 우선으로 다시 검토해서 최대한 제공"으로 정해 줘서 그대로 반영했다.

### 왜 순위 1위만 보는 방식으로는 한계가 있었는지 (분석)

3건을 후보 top-20까지 넓혀서 직접 봤더니 서로 다른 두 문제가 섞여 있었다.

1. **순위만 밀린 경우** — 정답이 후보 안에 있는데 임베딩 1위가 아니다. "어린이용 물놀이기구"는 후보 11위에 "목욕/수영용 물놀이완구"가 있었는데 1~3위는 전부 무관한 "요실금 제품"이었다. 절대 유사도 자체가 낮아서(0.25~0.4대) 순위를 그대로 믿을 수 없다는 뜻이다.
2. **후보 안에 정답이 아예 없는 경우** — "휴대용 예초기의 날 및 보호덮개"는 top-20 안에 예초기·정원공구 관련 항목이 하나도 없었다(정원용 기압계·습도계· 개인보호구류만 나왔다). "어린이용 가죽제품"의 "용어의 정의" 조항을 직접 읽어봤더니 "36개월 이상 만 13세 이하의 어린이가 사용하는 가죽제품"이라고만 되어 있고 "가죽"이 구체적으로 무엇인지(가방·신발·벨트 등) 서술이 없었다 — KC기준 자체가 여러 GPC 브릭에 걸친 우산 카테고리라 애초에 "정답 브릭 하나"가 존재하지 않을 수 있다는 뜻이다.

1번은 후보를 다시 읽고 고르는 LLM 판정으로 고칠 수 있지만, 2번은 후보에 없는 것을 골라낼 수 없다 — 이 경우엔 "맞는 게 없다"고 정직하게 답하는 것이 맞고, 지금까지처럼 임베딩 1위를 억지로 확정해 저장하는 게 오히려 더 나쁜 방식이었다.

### ① 조회 입력 재검토 — scope_text 외에 어떤 컬럼이 있는지 다시 봤다

`clause` 테이블에 `clause_role='DEFINITION'`(용어의 정의, 전체 817건)과 `level_name='서문'`(전문) 조항이 있다는 걸 확인했다. 특히 "어린이용 물놀이기구"의 서문에는 "에어매트리스, 에어베게, 펀치볼, 비치볼과 같이 물에서 사용할 우려가 있는 제품도 검사대상에 포함"처럼 scope_text보다 훨씬 구체적인 제품 예시가 있었다(서문은 clause_role이 SCOPE가 아니라 OTHER라서 지금까지 scope_text 집계에는 안 잡혔던 것도 확인). 정의 조항은 표준마다 148~818자 정도로 짧아 비용 부담 없이 전량 포함할 수 있는 것도 확인했다.

그래서 `scripts/tag-standards-gpc.ts`의 조회·검증 입력을 **품목명 + 기준명 + 인증구분(cert_scheme) + 적용범위(scope_text) + 서문 + 용어의 정의(DEFINITION 조항 전체)**를 조합한 문장으로 바꿨다(`buildProductContext()`). 가죽제품처럼 정의 조항 자체에 서술이 없는 경우는 이렇게 입력을 넓혀도 못 잡는다는 것을 예상하고 진행했다 — 아래 검증 결과가 그걸 확인해 준다.

### ② LLM 검증 레이어 추가 (`src/lib/gpc/verify.ts`, 신규)

`findGpcCandidates()`가 주는 후보를 5개가 아니라 **15개**까지 넓히고 (`CANDIDATE_COUNT`), `verifyGpcMatch()`가 그 후보 목록을 구조화 출력(JSON Schema, `tagging.ts`와 같은 방식)으로 검토해 실제로 맞는 것을 고르거나 `'NONE'`을 고르게 했다. brick_code를 후보 목록의 enum으로 고정해 목록에 없는 코드를 지어내지 못하게 막은 것도 `tagging.ts`와 같은 원리다. 기존 `gpc_brick_code`(임베딩 1위, 미검증) 컬럼은 의미를 안 바꾸고 그대로 뒀다 — `case_event.gpc_brick_code`와 같은 뜻을 유지해야 두 테이블의 같은 이름 컬럼이 안 헷갈린다. 검증된 답은 새 컬럼 `gpc_verified_brick_code` + 근거가 담긴 `gpc_verification`(jsonb)에 남긴다(`016_standard_gpc.sql`을 건드리지 않고 `017_standard_gpc_verification.sql`로 추가).

### ③ 같은 5건으로 재검증한 결과

| 표준 | 라운드10(top-1, scope_text만) | 라운드11(후보15 + LLM검증) |
|---|---|---|
| 어린이용 물놀이기구 | 오답(요실금 속옷) | **정답 확정** — 수영 훈련 보조기구 (확신 0.93) |
| 가정용압력냄비및압력솥 | 정답(압력솥) | **정답 유지** — 압력솥 (확신 0.99, 임베딩1위는 이번에 자기기압계로 바뀌었지만 LLM이 정정) |
| 유아용 캐리어 | 정답 | **정답 유지** (확신 0.99) |
| 휴대용 예초기의 날 및 보호덮개 | 오답(애매) | **NONE**(확신 0.99) — "후보에는 예초기·절단날·보호덮개에 해당하는 브릭이 없다"고 LLM이 정확히 판단 |
| 어린이용 가죽제품 | 오답 | **NONE**(확신 0.98) — "후보들은 유아 식기·기저귀·위생용품에 한정되어 있고 가죽제품에 해당하는 브릭이 없다"고 LLM이 정확히 판단 |

5건 중 **오답 0건**: 정답 확정 3건(확신 0.93~0.99), 정직한 미확정(NONE) 2건. 라운드 10까지는 5건 모두 뭔가 채워졌고 그중 3건이 오답이었는데, 이번엔 애초에 GPC로 못 잡는 2건을 LLM이 스스로 걸러냈다는 게 핵심 차이다. NONE으로 나온 2건은 여전히 "GPC로는 답이 없다"는 뜻이지 실패가 아니다 — 담당자가 수동으로 판단해야 하는 항목으로 남는다.

전량 배치(76건)는 이번에도 안 돌렸다 — 5건 검증만으로 방식을 확정하기엔 표본이 작고, LLM 검증 호출이 하나 늘어난 만큼 비용도 더 든다(추정: 후보 15개 목록
+ 조합 텍스트가 들어가는 호출 1회당 수 센트 수준, 76건이면 수 달러대로 여전히 작지만 표본을 좀 더 늘려 확인한 뒤 결정하는 게 안전하다고 판단).

### ④ HF.S 맨몸 문제 — 프롬프트 규칙을 기존 데이터에도 적용 (별도 subwork로 진행)

라운드 10에서 추가한 "순수 시험절차면 HF.UNKNOWN" 규칙(`SYSTEM_CLAUSE`)을 기존에 태깅된 373건에도 적용해 달라는 담당자 승인을 받아, 별도 백그라운드 작업(subwork)으로 진행했다.

**사고: `--retag`가 373건이 아니라 6,392건을 대상으로 잡았다**

담당자 승인은 "이미 태깅된 373건을 새 규칙으로 다시"였는데, `npm run tag -- --retag`를 그대로 돌렸더니 실제로는 승인(`review_status='approved'`) 안 된 `clause_role='REQUIREMENT'` 조항 **전체(6,392건)**를 대상으로 잡았다 — 스크립트가 "이미 태깅됐는지"가 아니라 "승인됐는지"만 걸렀기 때문이다(그때는 승인된 행이 0건이었으므로 사실상 전량이 걸린 것). 이 사실을 뒤늦게 `tagging_run` 표에서 `clause_count = 6392`로 확인하고 즉시 중단시켰다.

그런데 첫 중단 시도(`TaskStop`)는 실제로는 실패했다 — Windows에서 `npm run tag` 가 npm → cross-env → tsx → node 4단 자식 프로세스로 실행되는데, `TaskStop`은 최상위 셸만 종료하고 그 아래 자식 프로세스 트리는 계속 살아 있었다. 그 사실을 모른 채 "중단됐다"고 보고했다가, 이후 DB를 다시 조회해서 같은 프로세스가 15분 넘게 계속 조항을 태깅하고 있었다는 것을 발견했다 (`clause_tag.created_at`이 조회 시점 바로 몇 초 전까지 계속 찍히고 있었다). `tasklist`/`Get-CimInstance Win32_Process`로 살아있는 node 프로세스 4개의 실제 커맨드라인을 확인해 `taskkill //F`로 프로세스 트리 전체를 강제 종료하고 나서야 완전히 멈췄다.

이 사고로 원래 계획에 없던 **125개 조항**이 새 프롬프트로 먼저 태깅됐다 (주로 조항 ID 4~276번대 — 원래 목표였던 47건은 4252~11373번대라 겹치지 않았다). **이 125건은 틀린 데이터가 아니다** — 새 규칙이 적용된 정상적인 태깅 결과이고, 이전에는 아예 태깅이 안 돼 있던 조항들이라 오히려 코드화 범위가 늘어난 것뿐이다. 다만 담당자가 승인한 범위(47건 교정) 밖의 일이고 예상 못 한 LLM 호출 비용이 발생했으므로, 지우지 않고 그대로 남긴 채 이렇게 기록해 둔다. clause_id 목록은 커밋에 포함되지 않는 DB 값 변경이라 여기 텍스트로만 남긴다: 4, 8, 9, 10, 11, 15, 19–83(연속 아님, 다수), 118–164, 206–211, 243–276 범위에 흩어진 125건(정확한 전체 목록은 `clause_tag` 표에서 `created_at >= '2026-09-02T06:30:02'`이고 `tagging_version`에 `round10fix`가 안 붙은 행으로 재현 가능).

**정작 대상이었던 47건 교정**

`npm run tag -- --retag`로는 47건만 정확히 겨냥할 수 없어서(개별 clause_id 필터가 없음), 라운드 9가 특정한 47개 clause_id를 그대로 받아 별도의 임시 스크립트로 처리했다. 주의한 점 하나 — `tagging_version` 문자열이 모델·코드북 버전만으로 정해져서 재실행해도 이전과 완전히 같은 문자열이 나온다. 그 상태로 그냥 insert만 하면 `on conflict (clause_id, axis, code, tagging_version)`가 코드가 다르면 충돌로 안 잡아서, 옛 `HF.S` 행과 새 `HF.UNKNOWN` 행이 같은 조항에 `is_primary=true`로 동시에 남는 사고가 날 수 있었다. 그래서 이 47건에 한해 기존 `clause_tag` 행을 먼저 지우고 새로 넣는 방식으로 짰다(버전 문자열 끝에 `-round10fix`를 붙여 구분도 남겼다). 47건 전부 성공(실패 0)했고, 이어서 `npm run embed`로 재태깅·사고로 함께 무효화된 임베딩(총 172건)을 전부 다시 채웠다.

**최종 확인 수치** (모든 백그라운드 프로세스 완전 종료 확인 후 재측정)

| 항목 | 사고 전(라운드 9) | 지금 |
|---|---|---|
| HF.H/HF.S 맨몸(최신 태깅 기준) | 47건 | **10건** |
| `review_status='approved'` 행 | 0건 | 0건 — 그대로 보존 확인 |
| 태깅된 조항 총수(distinct clause_id) | 373건 | 498건(373 + 사고 125) |
| 임베딩 없는 태깅 조항 | — | 0건(전부 재생성 확인) |

47건 중 37건이 이번 교정으로 코드가 바뀌었다(대부분 `HF.UNKNOWN`으로, 일부는 `evidence_span`이 실제로 있는 조항이라 세부 leaf 코드로 바뀜 — 예: "4.1.1"은 `HF.H.PHY.FRAC,HF.M.DES`로). 남은 10건은 재태깅해도 여전히 `HF.H`/`HF.S` 맨몸으로 나온 것들인데, 이건 오류가 아니라 모델이 "정말 애매해서 대분류까지만 판단 가능"으로 다시 판단한 경우로 보인다 — 47 → 10은 "완전히 0건"이 목표가 아니라 "순수 시험절차 조항이 더는 HF.S로 잘못 붙지 않는 것"이 목표였다는 점에서 실측상 목표를 달성한 결과로 본다. 다만 10건이 왜 여전히 맨몸인지는 다음 라운드에서 개별 확인이 필요하면 할 수 있다.

**이번 사고에서 얻은 교훈** (다음에 비슷한 배치를 돌릴 때 참고)
- `npm run tag -- --retag`는 "승인 안 된 것 전체"를 대상으로 하지 "이미 태깅된 것만"이 아니다. 특정 조항 몇 건만 교정하고 싶으면 이 플래그를 그대로 쓰면 안 되고, clause_id를 직접 지정하는 별도 스크립트가 필요하다 (이번에 쓴 방식).
- Windows에서 `npm run <script>`로 띄운 프로세스는 다단 자식 프로세스 트리라, 상위 셸만 종료해서는 실제로 안 멈출 수 있다. 정말 멈췄는지는 DB의 `created_at`처럼 실시간으로 갱신되는 값으로 재확인해야 하고, 안 멈췄으면 `Get-CimInstance Win32_Process`로 실제 자식 PID를 찾아 `taskkill //F`로 트리 전체를 종료해야 한다.

### ⑤ "가죽제품은 GPC에 대응 브릭이 없다"는 판단이 틀렸다는 걸 담당자가 잡아냄

③에서 "가죽제품·예초기는 GPC 카탈로그에 대응 브릭이 없는 우산 카테고리"라고 정리했는데, 담당자가 "가죽제품이 없다는 게 이상하다"며 원본 자료(로컬 GPC 참고 JSON, n8n 워크플로 2개, `oecd_gpc_202405`/`recalls_oecd_staging` 실제 테이블)를 짚어 줬고, 다시 파보니 그 판단이 틀렸다는 게 드러났다.

**로컬 JSON은 샘플이었다** — `docs/OECD리콜등록/supabase/gpc table/ oecd_gpc_202405_rows.json`을 파싱해 보니 100건뿐이었다. 원격 테이블을 직접 조회하니(anon 키로 unrestricted 접근 가능) **5,281건**이었고, "가방"(10건), "벨트"(10건), "지갑"(1건) 브릭이 실제로 있었다("가죽" 자체는 0건이지만 가방·지갑·벨트가 실제 지칭 대상이다). 표준 9의 6.2조(표시사항, MARKING 역할이라 지금까지 조회 입력에 안 들어갔던 조항)에도 "지갑류, 가방류, 벨트류, 신발류"라고 정확히 나열돼 있었다.

**그런데 이걸 조회 입력에 넣어도(top-50까지 넓혀도) 여전히 못 찾았다** — "가방" 단어 하나만 조회해도 "일회용 식품포장 랩"이 1위로 나올 만큼, 이 임베딩 인덱스(`oecd_gpc_202405`, text-embedding-3-small) 자체가 짧은 한국어 명사 질의에 근본적으로 약하다는 걸 확인했다(반면 "벨트"는 단독 조회로도 브릭이 잡혔다 — 일관되게 약한 게 아니라 항목별로 들쭉날쭉하다). 별도 RPC(`match_gpc_vector`, 더 풍부한 정의·속성을 담은 `gpc_bricks` 테이블을 대상으로 하는 것으로 보임)도 비교해 봤는데 "벨트"는 더 잘 찾았지만 "압력솥"·"유아용 캐리어"는 오히려 못 찾아서, 어느 한쪽이 우월한 게 아니라 서로 다른 곳에서 실패하는 구조였다.

**담당자 판단: 새 RPC를 섞지 말고, 이 협회가 이미 실제로 운영 중인 파이프라인을 그대로 재활용하라.** 해외리콜 OECD 일일 처리 워크플로 (`docs/OECD리콜등록/(GS1) [한국리콜__OECD] OECD GPC RAG.json`, `recalls_oecd_staging.assignedGpc{Brick,Class,Family,Segment}Code`에 저장)를 분석해 보니, 우리와 같은 `oecd_gpc_202405`/`match_documents_202405`를 쓰지만 **Brick이 후보와 명확히 안 맞으면 NONE으로 포기하지 않고 Class→Family→ Segment로 단계적으로 내려가며 답하는 설계**였다. 지금까지 우리 방식은 "Brick 확정 아니면 NONE" 이분법이라 이 완충 구간이 없었다.

**반영**: `src/lib/gpc/verify.ts`를 계층적 하향 구조로 다시 짰다 — Brick/Class/ Family/Segment 코드를 전부 하나의 enum으로 모아 LLM이 고르게 하고, 실제로 어느 계층인지·그 상위 계층 전체(Segment~Class)가 무엇인지는 모델의 자기보고를 믿지 않고 후보 목록과 대조해 서버가 직접 판정한다(`resolveHierarchy()`, `tagging.ts`의 `validateCodes()`와 같은 원리). 담당자가 "Brick이 소속된 Class/Family/Segment도 알려줘야 한다"고 지적해서 — Brick이 확정돼도 그 상위 계층 전체를 같은 후보 행에서 그대로 채워 저장하도록 만들었다 (`recalls_oecd_staging`이 네 컬럼을 항상 같이 채우는 것과 동일). 017의 `gpc_verified_brick_code`/`gpc_verification`(이분법 전용) 대신 `018_standard_gpc_hierarchical.sql`로 `gpc_verified_level` + `gpc_verified_{segment,family,class,brick}_{code,title}` 8개 컬럼을 새로 만들었다(017은 이미 적용된 마이그레이션이라 고치지 않고 새 파일로 이었다).

**같은 5건으로 다시 검증한 결과**: 3건은 그대로 BRICK 확정 유지(물놀이기구· 압력솥·유아용 캐리어), **예초기 날/보호덮개는 NONE 대신 SEGMENT "잔디/정원 용품"(확신 0.62)로 하향 확정** — 정확한 Brick은 아니지만 완전히 빈 답보다 훨씬 유용하다. 가죽제품은 이번에도 NONE이었는데, 후보 15건 전체가 여전히 "유아 급식/기저귀/위생/식품" 계열뿐이라 Segment 단계까지 내려가도 의류· 액세서리 쪽 후보 자체가 없었다 — 이건 이제 "설계의 한계"가 아니라 "이 임베딩 인덱스가 이 사례에서 진짜로 후보를 못 찾는다"는 정직한 결과로 볼 수 있다.

**바뀐 파일 요약**
- `src/lib/gpc/verify.ts`(신규 → 라운드 11 안에서 계층적 하향으로 재작성): LLM GPC 후보 검증(`verifyGpcMatch`), Brick→Class→Family→Segment 단계적 하향.
- `scripts/tag-standards-gpc.ts`: 조회 입력에 서문·정의 조항 추가, 후보 5→15개, 검증 단계 추가, 대상 필터를 `gpc_brick_code is null`에서 `gpc_verification is null`로 변경, 계층 전체 컬럼 저장으로 갱신.
- `supabase/migrations/017_standard_gpc_verification.sql`(신규, 라운드 11 안에서 018로 대체): `standard.gpc_verified_brick_code`, `standard.gpc_verification` 컬럼 추가.
- `supabase/migrations/018_standard_gpc_hierarchical.sql`(신규): `gpc_verified_brick_code` 단일 컬럼을 걷어내고 `gpc_verified_level` + `gpc_verified_{segment,family,class,brick}_{code,title}` 로 계층 전체를 담도록 확장.
- `standard` 테이블: 시험 5건의 GPC 관련 컬럼 전체를 새 결과로 재기록(실제 DB 값 변경).
- `clause_tag`/`clause` 테이블: 조항 코드화 데이터 변경(실제 DB 값 변경, 파일 아님) — 사고로 125건 신규 태깅, 목표였던 47건 교정(37건 코드 변경), 관련 임베딩 172건 재생성. 이 라운드에서 새로 남은 코드 파일은 없다(교정용 임시 스크립트는 작업 후 삭제).

**다음에 할 일**
- 기준(standard) GPC 전량 배치(76건) — 계층적 하향까지 반영해 방식은 이번 라운드로 확정된 것으로 보이지만, 표본을 좀 더 늘려(예: 15~20건) 확인한 뒤 담당자 승인으로 전량 진행할지 결정.
- 화면(analysis 페이지)의 GPC 후보 표시를 `gpc_verified_level`/`gpc_verified_*` 계층 정보도 함께 보여주도록 업데이트할지 검토 — 지금은 라운드 9에서 만든 화면이 `gpc_candidates` 순위만 보여주고 검증 결과는 아직 화면에 없다.
- ~~조항 코드화 47건(HF.H/HF.S 맨몸) 재태깅~~ — 완료(47 → 10건). 남은 10건이 왜 여전히 맨몸인지 개별 확인이 필요하면 다음 라운드에서.
- 사고로 함께 태깅된 125건이 실제로 품질 문제가 없는지 — 급하지 않지만, 검수 단계에서 이 125건도 다른 조항과 동일하게 눈에 띄면 좋겠다(별도 표시는 안 해 뒀다).
- "가방"/"지갑"류를 짧은 한국어 명사만으로 못 찾는 이 임베딩 인덱스의 한계 — 이 라운드에서는 KC기준 GPC 매칭만 계층적 하향으로 완충했는데, 해외리콜 OECD 파이프라인도 같은 인덱스를 쓰므로 같은 한계를 안고 있을 수 있다. 급한 건 아니지만 인지해 둘 필요가 있다.

---

## 라운드 12 — case_event(사고조사) GPC를 standard와 완전히 같은 구조로 통합

담당자가 "라운드 11의 GPC 검증 로직이 사고조사 GPC 코드 부여에도 쓰이는 것이지요?"라고 물어서 확인했더니 **아니었다** — `scripts/load-cases.ts`의 `processPhotos()`는 여전히 라운드 9 이전 방식(후보 5개 중 임베딩 1위를 검증 없이 그대로 `case_event.gpc_brick_code`에 확정)이었다. "최대한 공유하도록 리팩터링해 달라"는 요청을 받아, 실행 전에 계획 모드로 탐색· 설계·질문 확인을 거친 뒤 반영했다.

### 결정 사항 (질문으로 확인)

1. **저장 구조를 완전히 동일화** — `case_event`에도 `standard`와 똑같은 12개 전용 컬럼(계층 전체)을 만들기로 했다. "가볍게(jsonb 하나만)" 안도 있었지만, `standard` 자신이 라운드 11에서 017(단일 컬럼)→018(계층 전체)로 이미 한 번 버린 방식이라 같은 실수를 반복할 이유가 없다는 게 이유였다.
2. **후보 개수도 5→15로 통일** — 라운드 10~11에서 실측한 "후보를 좁게 뽑으면 정답이 밀려난다"는 근거가 사고사진 GPC에도 똑같이 적용된다고 보고, 임베딩 RPC 호출만 늘리고(LLM 검증 호출은 그대로 1회) 통일했다.
3. **분석 화면(case_event 패널)도 이번에 함께 갱신** — `standard`의 GPC를 화면에 새로 노출하는 건 범위 밖으로 남겼다(라운드 9~11 동안 만든 `standard` 쪽 GPC는 지금도 화면 어디에도 안 보인다 — 별개 결정 필요).

### 무엇을 공유하게 했는가

`findGpcCandidates()`(조회)와 `verifyGpcMatch()`(검증, 계층적 하향)는 이미 `standard`·`case_event` 어느 쪽에도 쓸 수 있게 범용으로 짜여 있었는데, "후보 조회 → 없으면 NONE 처리 → 있으면 검증"이라는 오케스트 레이션 자체가 각자 따로였다(그리고 `case_event` 쪽엔 검증 단계 자체가 없었다). 이 오케스트레이션을 `src/lib/gpc/assign.ts`의 `findAndVerifyGpc()` 하나로 뽑아 두 스크립트(`scripts/tag-standards-gpc.ts`, `scripts/load-cases.ts`)가 똑같이 부르게 했다(CLAUDE.md §9 — 여러 진입점이 같은 로직을 쓰면 `src/lib/`로 뽑는다). 후보 개수 상수 (`DEFAULT_GPC_CANDIDATE_COUNT = 15`)도 `src/lib/gpc/lookup.ts`에 하나로 모았다.

두 파이프라인이 여전히 다른 건 "조회 입력을 무엇으로 조합하는가"뿐이다 — `standard`는 적용범위·서문·정의 조항을 모으고, `case_event`는 사고사진 비전 분석 서술(`vision.productDescription`)을 쓴다. 이건 억지로 공유 함수로 묶지 않았다(CLAUDE.md §2 — 입력 스키마 자체가 다른데 공유 함수를 만들면 오히려 부자연스럽다).

### 새 마이그레이션과 백필

`019_case_event_gpc_hierarchical.sql`로 `018_standard_gpc_hierarchical.sql` 과 같은 컬럼명 세트(`gpc_verified_level` + `gpc_verified_{segment,family, class,brick}_{code,title}` + `gpc_candidates`/`gpc_verification` 전용 컬럼)를 `case_event`에 추가했다. 기존에 `raw_fields.gpc_candidates`에 들어 있던 값(실측: 5건 중 1건 — 아래 참고)을 새 컬럼으로 1회 백필했고, `raw_fields`의 그 키는 지우지 않고 흔적으로 남겼다. 검증 컬럼은 과거 데이터에 채울 근거가 없어 NULL로 뒀다(재처리해야 채워진다).

### 실측 검증 — 골든 데이터를 건드리지 않고 확인한 방법

`사고조사보고서/`에 실제로 적재된 사건 5건 중 사진·GPC 데이터가 있는 건 `(26)27★ 가습기 사고조사 보고서`(case_event id 4639) 1건뿐이었는데, 이건 **절대 `--force`로 재처리하면 안 되는 시연용 골든 데이터**다(라운드 9·11 에서 두 번이나 실수로 지웠다가 복구한 바로 그 사건). 그래서 이번엔 `npm run cases:load --force`로 실물 재처리하는 대신:

1. `findAndVerifyGpc()`를 이 사건의 실제 입력(품목명 "가습기", 라운드 9 기록에 남은 실제 비전 서술 "투명 물 수위창과 하단 조작부가 있는 흰색 원통형 전기 가습기")으로 **DB에 쓰지 않고** 직접 호출해 새 코드 경로만 확인했다. 결과: **BRICK "가습기 - 이동형"(10005331)을 확신 0.99로 정확히 찾았다** — 기존에 이 사건에 저장돼 있던 값(임베딩 1위를 검증 없이 확정한 "제습기 - 이동형(전기식)", 오답)과 다르게, 새 파이프라인은 같은 카테고리 문제(가습기/제습기 헷갈림)를 실제로 고친다는 걸 확인했다.
2. `npm run db:push`로 019 적용 후 `select count(*) filter (where raw_fields ? 'gpc_candidates')` vs `count(*) filter (where gpc_candidates is not null)`를 비교해 백필이 정확히 1건→1건으로 됐는지 확인했다.
3. `npm run dev`로 로컬 서버를 띄우고(포트 3000이 이전 실수로 남은 좀비 프로세스에 물려 있어 3001로 뜬 것도 확인 후 정리) 실제 `/analysis/4639` 페이지를 그대로 열어 새 쿼리·화면 코드가 에러 없이 렌더링되는지 확인했다 — 이 사건은 아직 재처리 전이라 `gpc_verified_level`이 NULL인 "검증 전" 상태이고, 화면에 "LLM 검증 전 데이터입니다"라는 새 문구와 기존 후보 5건 목록이 정상적으로 나오는 것을 확인했다.

BRICK 이외의 분기(CLASS/FAMILY/SEGMENT/NONE 확정, 검증됨 배지)는 실제 사건 데이터로는 라이브 확인하지 못했다 — 같은 검증 함수(`verifyGpcMatch`) 자체는 이번 라운드 전체를 통해 `standard` 쪽에서 이미 여러 번(BRICK· SEGMENT·NONE 각각) 실측 검증됐으므로 로직 자체의 신뢰도는 확보됐다고 보지만, 화면 렌더링까지 실물로 본 건 "검증 전" 분기뿐이라는 점은 투명하게 남긴다. 다음에 새 사고조사보고서가 들어와 사진이 있으면 자연스럽게 검증되는 분기를 실물로 볼 수 있다.

**바뀐 파일 요약**
- `src/lib/gpc/assign.ts`(신규): `findAndVerifyGpc()` — 조회+검증 공유 오케스트레이션.
- `src/lib/gpc/lookup.ts`: `DEFAULT_GPC_CANDIDATE_COUNT = 15` 상수 추가(공유).
- `scripts/tag-standards-gpc.ts`: `findAndVerifyGpc()` 사용으로 단순화(후보 없음 분기 제거), 헤더 주석에서 case_event와의 대비 문구 정정.
- `scripts/load-cases.ts`: `processPhotos()`가 `findAndVerifyGpc()`를 쓰도록 교체, `case_event` 12개 컬럼 전체 기록, 콘솔 로그·주석 갱신.
- `supabase/migrations/019_case_event_gpc_hierarchical.sql`(신규): `case_event`에 계층 컬럼 9개 + `gpc_candidates`/`gpc_verification` 전용 컬럼 추가, `raw_fields.gpc_candidates` 1건 백필.
- `src/app/analysis/[caseId]/page.tsx`: 로컬 `GpcCandidate` 재선언 제거하고 `src/lib/gpc/lookup.ts`에서 import, 쿼리에 계층 컬럼 전체 추가, GPC 패널을 검증됨(BRICK/CLASS/FAMILY/SEGMENT)·NONE·검증 전 세 상태 모두 그리도록 재작성.
- 재처리 실물 테스트는 하지 않았다(골든 데이터 보호) — `case_event` 테이블 값 변경 없음, DB는 마이그레이션(019)과 백필 1건만 바뀌었다.

**다음에 할 일**
- 다음에 실제 새 사고조사보고서(사진 포함)가 들어오면 그걸로 BRICK 이외 분기(CLASS/FAMILY/SEGMENT/NONE)의 화면 렌더링을 실물로 확인.
- `cases:load`에 `--limit` 안전장치가 없는 기존 격차 — 이번 리팩터링과는 무관하지만 여전히 남아 있다.
- `load-recalls.ts`(RECALL_DOMESTIC/RECALL_OVERSEAS)에는 아직 GPC 부여 로직이 연결돼 있지 않다 — 필요하면 `findAndVerifyGpc()`를 그대로 가져다 쓸 수 있다.

---


## 라운드 13 — Netlify 배포 실패 수정 (참고자료 폴더를 타입검사에서 제외)

라운드 12를 푸시한 뒤 Netlify 자동 배포가 실패했다. 로컬에서는 `npm run build`가 잘 되던 것이 배포 서버에서만 깨진 경우라, 원인부터 정확히 짚고 최소한만 고쳤다.

### 무엇이 깨졌는가

배포 로그의 마지막 에러는 한 줄이었다.

```
./docs/OECD리콜등록/supabase/gpc edge_function/index.ts:9:23
Type error: Cannot find module 'https://deno.land/std@0.168.0/http/server.ts'
```

`docs/` 안에 참고용으로 넣어 둔 **Supabase Edge Function 코드**(GPC 조회 서버, 우리 앱이 아니라 Supabase가 따로 돌리는 프로그램)를 빌드가 자기 소스코드로 착각해 문법·타입 검사를 시도한 것이다. 이 코드는 Deno라는 다른 실행 환경에서 도는 것이라 `https://...` 주소로 남의 코드를 바로 불러 쓰는데, 우리 앱(Node.js/Next.js) 환경에서는 그 주소를 해석할 방법이 없어 "그런 모듈 없다"고 멈춘다.

### 왜 로컬에서는 안 걸렸는가 (이 라운드의 핵심)

`tsconfig.json`(타입검사 설정)의 검사 대상이 `**/*.ts` — **저장소 안의 모든 TypeScript 파일**로 되어 있었다. 즉 이 문제는 처음부터 있었는데, 로컬에는 이전 빌드 결과 캐시(`.next`)가 남아 있어 바뀌지 않은 파일을 다시 검사하지 않고 넘어가 드러나지 않았다. Netlify는 매번 저장소를 새로 받아 캐시 없이 전부 검사하므로("No cached dependencies found. Cloning fresh repo") 그제서야 나타났다.

배포 실패 자체보다 이 사실이 더 중요해서 기록한다 — **로컬 빌드 성공이 배포 성공을 보장하지 않는다.** 캐시가 가려 주는 문제가 있다.

### 어떻게 고쳤는가

`tsconfig.json`의 제외 목록에 `docs`를 한 단어 추가했다.

```
"exclude": ["node_modules", "docs"]
```

- `docs/`는 설계문서·표 스키마·남의 시스템 코드 사본을 모아 두는 **자료 폴더**이지 우리 앱이 실행하는 코드가 아니다. 실제로 이 폴더에는 위 Edge Function 말고도 `docs/next.config.ts`(설정 파일 사본)처럼 앱과 무관한 파일이 더 있어, 앞으로 자료를 더 넣어도 같은 사고가 안 나도록 폴더째 제외하는 쪽을 택했다
- 우리 앱 코드는 전부 `src/`·`scripts/`·`codebook/`에 있어서 검사 범위가 줄어드는 부작용은 없다(추적 중인 `.ts` 파일 목록으로 확인)
- 문제의 파일을 지우거나 확장자를 바꾸는 방법도 있었지만, 그 파일은 "지금 돌고 있는 GPC 조회 서버가 실제로 어떻게 생겼는지" 남겨 둔 참고 자료라 원본 그대로 두는 게 맞다고 봤다

### 확인한 것

- 로컬에서 `npm run build` 재실행 → 성공. 화면 8개 경로(`/`, `/analysis`, `/analysis/[caseId]`, `/cases`, `/codebook`, `/standards`, `/api/standards/sync`, `/_not-found`)가 모두 정상 생성되는 것 확인
- 앱 동작에는 영향이 없는 설정 한 줄 변경이라 별도 기능 테스트는 하지 않았다

**바뀐 파일**
- `tsconfig.json`: `exclude`에 `docs` 추가(1줄)

**다음에 할 일**
- 라운드 12에서 남긴 과제들은 그대로 남아 있다(새 사고조사보고서로 BRICK 이외 분기 화면 확인, `cases:load --limit` 안전장치, 리콜 적재에 GPC 부여 연결)

---

## 라운드 14 — Netlify 배포 실패 2차 수정 (시크릿 스캔 오탐)

라운드 13(타입검사 제외)을 푸시하니 배포가 한 단계 더 진행됐지만 다른 곳에서 또 실패했다. 이번엔 코드 문제가 아니라 **Netlify가 자체적으로 돌리는 "시크릿 스캐너"**가 걸린 것이다 — Netlify에 등록해 둔 환경변수 값과 똑같은 문자열이 저장소 어디엔가 있으면 "비밀이 새어 나갔다"고 보고 빌드를 통째로 막는 기능이다. 라운드 13에서 고친 타입 오류가 이 단계보다 먼저 있어서, 지금까지는 시크릿 스캐너까지 도달한 적조차 없었다 — 즉 이 문제도 원래부터 있었는데 이번에 처음 드러난 것이다.

### 걸린 값이 진짜 비밀인지부터 확인

걸렸다고 나온 7개 환경변수를 하나씩 실제로 확인했다.

- `OPENAI_BULK_MODEL`, `OPENAI_VISION_MODEL`, `OPENAI_VISION_EFFORT`, `OPENAI_EMBEDDING_MODEL`, `OPENAI_EMBEDDING_DIM` — 이건 애초에 "비밀"이 아니라 **모델 이름·응답 강도·임베딩 차원수 같은 설정값**이다 (`gpt-5.6-terra`, `medium`, `1536` 같은 문자열). `.env.example`에도 이미 실제 값 그대로 적혀 있고 코드 기본값(`src/lib/env.ts`)에도 똑같이 박혀 있어서, Netlify가 "환경변수로 등록된 값은 전부 비밀"이라고 기계적으로 판단하는 바람에 코드 자기 자신과 설계문서(`01_사전분석...md` 의 실측 기록, `KC안전기준/*.json`)를 "유출"로 오인한 것이다.
- `GPC_LOOKUP_URL` — `.env.example`에 이미 실제 주소를 그대로 커밋해 둔 값이다(접근에 필요한 `GPC_LOOKUP_ANON_KEY`는 비워 둠). 담당자가 처음부터 공개해도 되는 값으로 다뤄 온 것으로 보고 그대로 뒀다.
- `RECALL_SOURCE_SUPABASE_URL` — `docs/해외리콜수집/recalls_rows.sql` (해외 리콜 원본 표를 통째로 내보낸 옛 백업 파일) 안에서 80곳 넘게 걸렸는데, 실제로 열어 보니 전부 `.../storage/v1/object/public/ recall-images/...` 형태의 **공개(public) 이미지 스토리지 주소**였다. 리콜 사진 URL이 우연히 이 프로젝트 호스트명을 포함하고 있을 뿐, 접근 권한이 있는 키(`RECALL_SOURCE_SUPABASE_SECRET_KEY`)와는 무관하다.
- 반대로 **진짜 자격증명**(`OPENAI_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`, 각종 `*_SECRET_KEY`/`*_ANON_KEY`)은 스캔 결과에 단 한 건도 나오지 않았다 — 전부 `.env.example`엔 빈칸으로 남겨 뒀기 때문에 대조할 값 자체가 없어서다. CLAUDE.md §7 원칙(자격증명은 `.env.local` 에만)이 그대로 지켜지고 있었다는 뜻이라, 안심하고 다음 단계로 진행했다.

### 어떻게 고쳤는가

`netlify.toml`에 `SECRETS_SCAN_OMIT_KEYS`를 추가해, 위에서 확인한 "진짜 비밀이 아닌" 7개 값 + 같은 성격(설정 파라미터)인 나머지 `OPENAI_*_MODEL`/`*_EFFORT`, `TAGGING_*`, `SEARCH_TEXT_VARIANT`, `RRF_*` 까지 함께 스캔 대상에서 뺐다. 실제 키·비밀번호·DB 연결 문자열은 이 목록에 넣지 않았다 — 그런 값이 실제로 코드나 문서에 등장하는 사고가 나면 이번처럼 "그냥 오탐이다"로 넘기지 않고 그 자리에서 멈추고 확인해야 한다.

### 확인한 것

- 스캔에 걸린 7개 값 전부를 실제로 하나씩 열어 확인(파일 내용 대조)한 뒤에야 제외 목록에 넣었다 — "일단 빌드가 통과하게 다 빼자"로 접근하지 않았다
- 실제 키 계열 환경변수는 이번에도, 앞으로도 스캔 대상에 그대로 남아 있다

**바뀐 파일**
- `netlify.toml`: `[build.environment]` 블록 추가, `SECRETS_SCAN_OMIT_KEYS` 로 설정값 성격의 환경변수 18개 제외

---

## 라운드 15 — 배포 사이트 외부 차단 (ID/PW 로그인 화면)

배포된 사이트(`https://kc-regulation-product-hazard-analysis.netlify.app/`)에 아무나 URL만 알면 들어올 수 있는 상태였는데, 외부 차단을 위해 첫 화면에 ID/PW를 입력해야 들어오게 해 달라는 요청을 받았다.

### 먼저 확인한 것 — Netlify 기본 기능으로 되는지

Netlify에는 사이트 전체에 비밀번호를 거는 기능이 원래 있어서, 그것부터 API로 켜 보려고 했다(`update-visitor-access-controls`). 이 방식은 코드를 한 줄도 안 건드리고, 브라우저가 띄우는 표준 로그인 팝업(사용자이름+비밀번호 두 칸)으로 뜨기 때문에 요청하신 "ID/PW 입력해서 들어가기" 화면과 겉보기엔 똑같다. 담당자께 "사용자이름 칸은 검증 안 되고 아무 값이나 통과되는데 괜찮은지"부터 먼저 확인했고, 외부 차단이 목적이라 괜찮다는 답을 들었다.

그런데 실제로 API를 호출하니 422 오류로 거부됐다. 팀 플랜을 확인해 보니 **Free 플랜**이었다 — 이 사이트 비밀번호 보호 기능은 Pro 이상 유료 플랜에만 있는 기능이라, Free 플랜에서는 API로도 켤 수 없다는 걸 이번에 실측으로 확인했다.

### 그래서 택한 방법 — Next.js middleware로 직접 구현

Netlify 기본 기능이 막혀 있으므로, 처음에 두 번째 대안으로 안내드렸던 방식(코드로 직접 구현)으로 갔다. Next.js의 `middleware.ts`는 Netlify의 Next.js 런타임이 자동으로 엣지(요청이 서버에 닿기 전 가장 앞단)에서 돌려 주므로, Netlify 전용 Edge Function을 따로 만들 필요가 없었다.

`src/middleware.ts`를 새로 만들어 모든 요청에 대해 브라우저의 HTTP Basic 인증(요청 헤더 `Authorization: Basic ...`)을 확인하고, 비밀번호가 맞지 않으면 401과 함께 `WWW-Authenticate` 헤더를 돌려준다 — 이 헤더가 바로 브라우저가 로그인 팝업을 띄우게 만드는 신호다. 사용자이름은 환경변수(`SITE_AUTH_USERNAME`)가 채워져 있을 때만 검증하고, 비어 있으면 아무 값이나 통과시킨다(현재는 `kc`로 채워서 실제로 검증되게 했다 — 어차피 코드로 만드는 김에 공짜라 사용자이름도 같이 확인하게 했다).

**로컬 개발이 막히지 않도록** `SITE_AUTH_PASSWORD` 환경변수가 아예 비어 있으면(로컬 `.env.local`에 안 넣으면) 그냥 통과시키게 했다 — 이 프로젝트에서 이미 쓰고 있는 "필수 값 없으면 멈춤, 선택 값 없으면 기본 동작"(`src/lib/env.ts`의 `optional()`) 관례를 그대로 따른 것이다.

### 자격증명 보관

비밀번호는 담당자가 원하는 방식(직접 입력 vs 자동 생성) 중 자동 생성을 택해 `Kite-Delta-8924` 형태(단어-단어-숫자, 기억하기 쉬운 조합)로 만들었다. **코드나 `.env.example`에는 절대 넣지 않고**(CLAUDE.md §7), Netlify MCP 도구(`manage-env-vars`)로 배포 환경에만 직접 등록했다 (`SITE_AUTH_USERNAME=kc`, `SITE_AUTH_PASSWORD`는 "secret"으로 표시해 Netlify 대시보드에서도 값이 가려지게 함). `.env.example`엔 두 변수 이름과 빈 칸, 그리고 동작 설명 주석만 추가했다.

### 확인한 것

- `npm run build` — 새 미들웨어가 있어도 빌드가 정상적으로 되고, 결과에 `ƒ Middleware 34.4 kB`로 등록된 것을 확인
- 실제 배포 후 로그인 팝업이 뜨는지, 맞는 비밀번호로 통과되는지는 이 라운드 안에서 배포 완료 후 담당자가 직접 확인하기로 함(브라우저 네이티브 팝업이라 이쪽에서 자동 테스트하기 애매함)

**바뀐 파일**
- `src/middleware.ts`(신규): 전체 경로에 HTTP Basic 인증 적용 (`_next/static`, `_next/image`, `favicon.ico`는 제외 — 정적 자산이라 막을 실익이 없고 막으면 오히려 매 요청마다 엣지 호출만 늘어난다)
- `.env.example`: `SITE_AUTH_USERNAME`/`SITE_AUTH_PASSWORD` 템플릿 추가
- Netlify 배포 환경변수(코드 아님, 대시보드/API로만 등록): `SITE_AUTH_USERNAME=kc`, `SITE_AUTH_PASSWORD=(비밀·별도 전달)`

**다음에 할 일**
- 비밀번호를 나중에 바꾸고 싶으면 Netlify 대시보드에서 `SITE_AUTH_PASSWORD` 값만 바꾸고 재배포하면 된다(엣지 함수는 배포 시점 값을 그대로 굳혀서 쓰므로 값을 바꾼 뒤엔 재배포가 필요하다)
- Free 플랜을 나중에 Pro로 올리면 Netlify 기본 비밀번호 보호로 옮기고 이 미들웨어는 걷어내도 된다 — 지금 굳이 미리 그렇게 안 한 이유는 Free 플랜에서 당장 되는 방법이 이것뿐이었기 때문

---

## 라운드 20 — 눌러 두면 끝까지 도는 코드 부여 · 게시판 · 말 다듬기

2026-09-03 17:00 종료

담당자가 화면을 써 보며 준 지적을 차례로 반영한 라운드다. 지적 하나하나가 실측으로 이어졌고, 그 과정에서 **성능 병목 하나와 UI 설계 실수 하나**를 잡았다.

### 1) "npm run tag 도 웹페이지에서 클릭하면 자동으로 돌리게 해줘"

라운드 19의 「조금 실행하기」는 자동이 아니었다. 18초어치만 하고 멈추니 남은 5,884건을 끝내려면 수천 번을 눌러야 했다 — 자동이 아니라 **수동을 잘게 쪼갠 것**이었다.

**진짜 병목은 조항 한 건 자체였다.**

`tagClause()` 는 싼 모델을 3번 불러 답이 흔들리는지 본다(일치도). 그런데 그 세 번을 `for` 문으로 차례로 기다리고 있었다. 세 호출은 **같은 입력을 각각 묻는 것**이라 서로의 결과를 재료로 쓰지 않는데도 그랬다.

```
for (let i = 0; i < 3; i++) {
  attempts.push(await call(...));   ← 하나씩 순서대로
}
```

| | 전 | 후 |
|---|---|---|
| 조항 1건 처리 시간 | 4.75초 | **2.5초** |
| 조항 1건 실제 대기 | 약 18초 | **약 7초** |

두 번째 수치가 더 중요하다. 배포 환경은 요청 하나가 30초를 넘으면 끊는데, 1건이 18초면 예산을 아무리 낮춰도 "이미 시작한 건은 끝까지 기다린다"는 규칙 때문에 30초를 넘겼다. 실제로 **504 Inactivity Timeout** 을 반복해서 맞았다.

**호출 수도 비용도 그대로다. 기다리는 방식만 바꿨다.**

여기에 세 가지를 더 얹었다.

- **1분마다 스스로 이어서 하는 작업**(026). 기본은 꺼짐 — 돈이 들기 때문에 켜는 것은 사람이고, 켤 때 화면이 남은 건수와 비용을 보여 준다. 다 끝나면 저절로 꺼지고 텔레그램으로 알린다.
- **한 tick 에 요청 세 개**(027). 서버리스는 요청마다 새로 뜨므로 준비 시간(기동·연결· 코드북 적재)이 매번 든다. 없앨 수 없으니 나눠 갖는다 — 세 개가 각자 뜨면서 준비도 동시에 하므로 준비 시간이 세 배로 늘지 않고 작업 시간만 세 배가 된다. 같은 조항을 두 번 처리하지 않도록 요청마다 건너뛸 개수를 준다(0/40/80).
- 상태를 따로 저장하지 않고 **cron 작업 자체를 켜고 끈다**. 상태가 한 군데에만 있어야 "화면은 켜졌다는데 실제로는 안 도는" 어긋남이 생기지 않는다.

**실측 결과 (배포 환경, 4분 관찰)**

| | |
|---|---|
| 처리 속도 | **137건 / 4분 = 시간당 약 2,055건** |
| 남은 5,670건 | **약 2.8시간** |
| 요청 실패 | 0건 |

**29시간 → 2.8시간.** 퇴근 전에 켜면 아침에 끝나 있다.

### 2) "게시판 형태로 1페이지에 보여주고 페이지 넘김, 날짜 정렬, 찾기"

`components/Board.tsx` 신설. 사고보고서·리콜이 함께 쓴다. 리콜만 2,259건이고 앞으로 더 늘어 전부 쏟으면 찾을 수가 없었다.

**클라이언트 자바스크립트를 쓰지 않는다.** 조건을 주소줄에 두고 서버가 그대로 조회한다. 그래서 주소를 복사해 동료에게 보낼 수 있고, 뒤로 가기가 제대로 돌고, 자바스크립트가 없어도 검색·정렬이 된다. 정렬 열은 화이트리스트로만 받는다.

해외·국내 구분은 드롭다운 대신 **단추**로 뒀다 — 드롭다운은 열고·고르고·찾기까지 세 번을 눌러야 해서 자주 쓰는 구분에는 맞지 않는다.

### 3) "한글 표현들이 이상합니다. 영어를 그냥 한글로 표현했습니다"

humanize-korean 스킬 룰북으로 점검해 **66곳**을 고쳤다. 걸린 것은 주로 명사형 종결·번역투·직역 명사였다.

| 전 | 후 | 걸린 패턴 |
|---|---|---|
| `~한 건` | `~한 사고 / ~한 리콜` | 개조식 종결(K) |
| `찾을 수 있는 상태` | `~까지 찾아냅니다` | state 직역(A) |
| `보고의무 판단의 전제입니다` | `이걸 알아야 보고 의무가 있는지 따질 수 있습니다` | 명사 직역 |
| `이 화면은 조회 전용입니다` | `여기서는 보기만 하고 고치지 않습니다` | read-only 직역 |
| `분모는 …입니다` | `오른쪽 수는 …만 센 것입니다` | 수학 용어를 화면 위치로 |
| `상대국 근거` | `리콜한 나라가 든 근거` | |
| `국내 유통 동일성` | `국내에도 풀렸는가` | |
| `이 체계는 유통 여부를 추정하지 않습니다` | `이 시스템은 국내에 풀렸는지를 스스로 짐작하지 않습니다` | |
| `적재` | `들여오기 / 넣기` | 전문어 |

법조문(「제품안전기본법」 제13조 제3항)·수치·코드명은 그대로 뒀다.

### 4) "분석 실행이 안먹히네요. 그리고 화면이 깜빡입니다"

**실제로는 잘 돌고 있었다.** DB 를 보니 버튼을 누른 시각에 분석이 만들어져 있었다. 문제는 **알려 주는 방식**이었고, 그 설계가 라운드 19에서 내가 만든 것이다.

```
결과 문장 → 주소줄(?done=) → 화면 맨 위에 띠
```

이 방식이 두 가지를 동시에 망가뜨렸다.

- **목록을 내려간 상태에서는 그 띠가 안 보인다.** 특히 스팀다리미·LED등기구처럼 "적용할 기준을 못 찾아 분석하지 않았습니다"인 경우 화면에 아무 변화가 없어서, 담당자에게는 **버튼이 죽은 것과 똑같아 보인다.** 동작은 설계대로였지만 (v0.7 §3.2 — 품목 미확정이면 전 기준을 뒤지지 않는다) 그것을 말해 주지 않았다.
- **주소가 바뀌면 화면 전체를 서버에서 다시 그린다.** 이 화면들은 조회가 무거워서 (현황 7종 + 목록) 그때마다 눈에 띄게 깜빡였다.

**주소를 아예 바꾸지 않는 방식으로 바꿨다**(`components/ActionForm.tsx`). 결과 문장이 버튼 옆에 바로 붙고, 숫자만 조용히 새로 읽는다(`router.refresh`).

실측으로 확인한 증거: 버튼을 누른 뒤에도 **스크롤 위치가 그대로였고, 펼쳐 둔 다른 항목도 펼친 채 남아 있었다.** 화면을 다시 그리지 않았다는 뜻이다.

> 이 프로젝트는 그동안 클라이언트 자바스크립트 없이 서버에서만 그려 왔다. 그런데
> "지금 보내는 중"과 "방금 무슨 일이 있었는지"는 서버가 알 수 없는 상태다 —
> 브라우저 안에서만 일어나는 일이기 때문이다. 그래서 이 두 컴포넌트만 예외로 뒀다.

### 5) "좌측메뉴가 페이지 스크롤 다운하면 올라가 버리네요"

목록이 수천 건이라 한참 내려가면 메뉴가 화면 밖으로 사라져, 다른 화면으로 가려면 맨 위까지 되올라와야 했다. 넓은 화면에서 레일을 붙여 뒀다.

- `self-start` 가 필요했다. 부모가 flex 라 기본값(stretch)으로 늘어나면 sticky 가 붙을 여백이 없어 아무 효과가 없다. 높이를 내용만큼만 잡아야 비로소 붙는다.

### 6) "통계 자동 업데이트, 시간이 많이 걸리는 작업에 대해 현황 잘 나오도록"

`components/AutoRefresh.tsx` 신설. 진행 중인 일이 있을 때만 20초마다 숫자를 새로 읽는다. 할 일이 없는데 계속 서버를 두드리면 그것대로 낭비다.

- 코드 부여 자동 실행 중 → 남은 건수가 저절로 준다
- 의미 검색 준비가 밀려 있음 → 준비된 건수가 저절로 는다
- 개별 자료에도 「의미 검색 준비 중…」 표시(대기열 조회)

몇 시간짜리 일을 지켜보면서 계속 새로고침을 누르는 것은 화면이 할 일을 사람에게 떠넘기는 것이다.

### 그 밖에 고친 것

- 사건번호(사건 2197) 삭제 — 내부 식별자일 뿐 담당자가 쓸 일이 없다
- 「이 리콜/사건 분석 보기」 → **분석 상세보기**
- 날짜 `26. 09. 01.` → `26.09.01.`
- 위해요인 코드 카드를 맨 뒤로 — 앞줄 끝에 혼자 남아 아랫줄이 비어 보이던 자리 정리
- 사고보고서 원문(수천 자)과 시험방법 본문을 접었다. 조항번호는 항상 보인다 — 이 화면에서 얻어야 할 결론이 "몇 조 시험을 의뢰할 것인가"이고 그 답이 번호다
- 버튼을 누르면 「분석하는 중…」으로 바뀌고 잠긴다. 아무 표시가 없으면 눌린 줄 모르고 또 누르게 되고, 돈 드는 작업이면 돈도 두 번 나간다

### 작업 중 스스로 만든 실수 둘

- **`\s` 가 셸에서 먹혀** `replace(/s/g,'')` 가 됐다. 날짜에서 문자 s 를 지우는 버그였는데 배포 전에 잡았다. 셸을 거쳐 정규식을 쓰는 것은 그만두는 게 맞다
- **`redirect()` 를 `try` 안에서 불렀다.** redirect 는 흐름을 끊으려고 특별한 오류를 던지는 방식인데 `catch` 가 그것을 잡아 버려 버튼이 먹통이 됐다. 결과 문장만 만들고 `try` 밖에서 보내도록 고쳤다 — 이번에 `redirect` 자체를 안 쓰게 되면서 이 함정도 함께 사라졌다

**바뀐 파일**
- `src/lib/llm/tagging.ts`: 반복 호출을 동시에 (병목 해소)
- `src/lib/standards/tag-run.ts`: 동시 처리·구간 나누기(offset)
- `supabase/migrations/026`(자동 실행 켜고 끄기)·`027`(병렬 요청)
- `src/components/Board.tsx`(신규): 찾기·정렬·쪽 넘김·갈래 단추
- `src/components/ActionForm.tsx`(신규): 누른 자리에서 결과를 말하는 버튼
- `src/components/AutoRefresh.tsx`(신규): 진행 중일 때 숫자 자동 갱신
- `src/lib/terms.ts`·각 화면: 문구 66곳
- `src/app/layout.tsx`: 좌측 레일 고정
- `src/app/{accidents,recalls,ops,analysis/[caseId]}/`: 위 내용 반영

---

## 라운드 19 — 화면을 담당자의 말로 다시 쓰다

2026-09-03 14:10 종료

### 왜 했는가 — 화면이 만든 사람의 말로 쓰여 있었다

담당자가 화면을 실제로 써 보고 준 지적이 출발점이다. 요약하면 하나다. **화면이 코드를 아는 사람만 읽을 수 있는 상태였다.**

> "시험연결: 시험항목이란 뜻인가?" / "태깅: 2차원 위해요인 태깅인가?" /
> "코드 갈래가 작동하지 않는다가 무슨 뜻인지…" / "확정됨은 무슨 뜻인지?" /
> "DB 금고는 무슨 말이며" / "npm run search -- --case 4639 등은 무슨 의미인지"

전부 설계문서와 코드 주석의 말이다. 만든 사람에게는 정확하지만 이 화면을 쓸 사람(협회 담당자)에게는 뜻을 짐작할 수 없는 암호다. 여기에 더해 세 가지가 겹쳤다 — 현황 요약이 없어 스크롤로 세어야 했고, 사고보고서와 리콜이 한 통에 섞여 있었고, 기준 이름이 `KC 60335-1`처럼 번호만 나왔다.

### 1) 말을 바꿨다

`src/lib/terms.ts` 한 곳에 모았다. 같은 말이 여러 화면에 흩어져 있으면 한쪽만 고치게 되고, 그러면 같은 것을 두 이름으로 부르는 상태가 되어 처음보다 나빠진다.

| 전 | 후 | 실제 뜻 |
|---|---|---|
| 태깅 | **위해요인 코드 부여** | 원인(HF)·피해유형(DT) 두 축 코드를 붙이는 일 |
| 시험연결 | **시험방법 연결** | "이 요건은 몇 조 시험으로 확인한다"는 조항↔조항 연결 |
| 시험조건 | **시험 항목·허용치** | 표에서 뽑은 수치 (예: 납 90 mg/kg 이하) |
| 미해결 참조 | **다른 기준을 가리키는 참조** | 대상이 이 문서 밖에 있음 |
| 임베딩 | **의미 검색 준비** | 문장을 숫자로 바꿔 뜻으로 찾게 하는 것 |
| 임베딩 좌표계 | **의미 검색 기준** | 모델이 섞이면 같은 잣대가 아니게 됨 |
| 배치 | **자동 작업** | |
| DB 금고 | **비밀값 보관함** | |
| 확정됨 | **원문 확인함** | 추출된 글자를 눈으로 확인했다는 표시 |
| 폴백·미태깅 품목 | **코드 없이 찾은 결과** | |

문장도 고쳤다.

- 전: "태깅되지 않아 코드 갈래가 작동하지 않습니다."
- 후: "위해요인 코드가 없어 **코드로 찾는 방식**이 작동하지 않습니다. 뜻이 비슷한 문장을 찾는 방식만 남습니다."

- 전: "시험방법 연결이 없어 후보에서 시험으로 잇지 못합니다."
- 후: "관련 조항은 찾을 수 있지만 **어떤 시험을 의뢰해야 하는지까지는 알려 드리지 못합니다.**"

그래도 남는 개념(위해요인 코드, 재채점 등)은 각 화면 아래 **「이 화면에 나오는 말 풀이」**를 접어 두었다. 본문에 설명을 늘어놓으면 화면이 설명서가 되기 때문이다.

### 2) 메뉴를 업무 둘로 다시 짰다

```
전                          후
1 안전기준                  1 안전기준     조항·시험 적재
2 위해요인 코드             2 사고보고서   등록·현황·분석
3 사건                      3 리콜         수집·현황·분석
4 분석                      ─────────────
                            운영           상태·알림·접속 관리
                            위해요인 코드   참고 문서
```

- **위해요인 코드를 아래로 내렸다** — 담당자 지적대로 업무 단계가 아니라 참고 문서다. 조회만 하는 대상이고 그 화면에서 할 일이 없다.
- **사건·분석을 사고보고서와 리콜로 나눴다** — 둘은 들어오는 경로가 다르다. 사고보고서는 사람이 PDF 를 올려 원문까지 확인해야 하고, 리콜은 외부 표에서 자동으로 들어오며 코드까지 붙어 온다. 한 통에 담으면 어느 쪽이 밀렸는지 알 수 없다.
- 등록과 분석을 따로 두지 않고 한 화면에 합쳤다 — 담당자가 "사고보고서 + KC안전기준 연계 분석"을 하나의 일로 인식하기 때문이다.
- 옛 주소(`/cases`, `/analysis`)는 새 주소로 넘겨준다. 북마크가 죽지 않는다.

### 3) 세 화면 위에 현황을 붙였다

목록만 있으면 "지금 몇 건이 어디까지 됐는가"를 스크롤로 세어야 한다. 숫자 하나하나가 다음에 할 일을 가리키게 했다.

**분모를 조심해서 골랐다.** 라운드 18에서 한 번 고친 자리인데, 이번에 **또 틀린 것을 발견했다.**

```
라운드 17 : 임베딩 498 / 13,501   ← 96%가 밀린 것처럼 보임 (사실은 재료가 없음)
라운드 18 : 임베딩 498 / 498      ← 고침
라운드 19 : 코드 부여 498 / 13,501 ← 이것도 같은 오류였다
            코드 부여 498 / 6,392  ← 고침
```

코드를 붙이는 대상은 **요건 조항뿐**이다(v0.7 §5.3). 정의·적용범위·시험방법에 코드를 붙이면 아무것도 요구하지 않는 문장이 진짜 요건과 같은 자격으로 검색에 걸린다.

### 4) 기준의 한글 명칭을 되살렸다

담당자 지적: "KC 60335-1라고만 쓰지 말고, 기준 명칭도 같이 작성"

**원인을 찾았다.** `display_name` 은 파일명에서 만들어진다. `안전확인 부속서 16(유아용 캐리어)` 처럼 파일명에 품목이 있으면 괄호에서 뽑아 쓸 수 있었지만, `1. KC 60335-2-13_result.json` 에는 번호밖에 없다. 76건 중 전기용품 계열이 전부 이 상태였다.

**그런데 문서 안에는 이름이 있었다.** 파일명만 보느라 못 썼을 뿐이다.

```
KC 60335-1     → 제1부: 일반 요구사항
KC 60335-2-34  → 제2-34부: 전동 압축기의 개별요구사항
KC 60335-2-67  → 제2-67부: 상업용 및 산업용 바닥처리 청소기의 개별 요구사항
```

**40건**을 채웠다. 계열명("가정용 및 이와 유사한 전기기기의 안전성")은 일부러 뗐다 — 40건이 전부 같아서 표에 나란히 놓으면 앞부분이 똑같은 줄이 40개 늘어서고, 구분되는 것은 부(部) 표제 쪽이기 때문이다.

- **부속서 문서는 뽑지 않기로 했다.** 해 보고 접었다: `안전기준준수 부속서 5(가구)` 가 본문 소제목 "4. 안전요구사항"을 집었고, `가정용 섬유제품`은 표지 큰 글씨를 OCR 이 글자마다 띄어 놔서 되붙이니 "가정용섬유제품"이 됐다. 둘 다 파일명에서 뽑은 품목명보다 나쁘다. **더 나은 값이 이미 있는데 위험한 추정을 얹을 이유가 없다.**
- OCR 오탈자("전기 튜김기")는 고치지 않았다. 우리가 문서에 없는 말을 지어내기 시작하면 화면의 어떤 글자가 원문인지 알 수 없게 된다.

### 5) 분석을 화면에서 돌릴 수 있게 했다

조사에서 드러난 사실: **분석 실행은 CLI 전용이었다.** `searchCandidates`/`persistRun` 을 부르는 곳이 스크립트 세 개뿐이고 화면에는 실행 버튼이 아예 없었다. 화면에 `npm run search -- --case 4639` 가 안내로 박혀 있던 이유가 이것이다.

`src/lib/search/run.ts` 로 실행부를 뽑아 명령줄과 화면이 같은 함수를 쓰게 하고, 「분석 실행」 버튼을 사고보고서·리콜 목록과 분석 상세에 붙였다. 명령어 안내는 지웠다.

### 6) 자동 실행 — "npm run 을 꼭 로컬에서 해야 하나"

스크립트마다 답이 달랐다. 코드를 직접 뒤져 판정했다.

| 작업 | 판정 | 근거 |
|---|---|---|
| 리콜 수집 | **자동 (매일 03:40)** | 로컬 파일 없음, 외부 표만 읽음, 재실행 안전 |
| 기준 폴더 동기화 | **자동 (매일 03:50)** | `KC안전기준/` 76개가 저장소에 커밋돼 있음 |
| 분석 실행 | **화면 버튼** | 1건에 몇 초 |
| 조항 코드 부여 | **화면 버튼 (비용 표시)** | 돈이 든다 — 사람이 결정해야 한다 |
| 사고보고서 적재 | **로컬만** | 원본 PDF 가 개인정보라 저장소에 없다 |
| 정확도 평가 | **로컬만** | 사람이 손으로 채운 정답지가 있어야 한다 |

방식은 라운드 17에서 검증된 것을 그대로 썼다 — `pg_cron` + `pg_net`. 다만 임베딩과 달리 이 작업들은 TypeScript 로직이 두꺼워 SQL 로 옮겨 적을 수 없다. 그래서 DB 가 우리 사이트의 `/api/jobs/*` 를 부르고, 로직은 `src/lib` 한 벌로 둔다(CLAUDE.md §9).

### 7) 알림을 넓혔다 · 코드북 버전 관리

- **직접 보내기**: 운영 화면에 입력칸을 두어 담당자가 쓴 말을 그대로 텔레그램으로 보낸다. 메모를 남기거나 알림이 잘 오는지 확인할 때 쓴다.
- **좋은 소식도 알린다**: 새 리콜 도착, 새 기준 감지, 코드 부여 완료.
- **알릴 문제를 셋에서 다섯으로**: 정기 작업 실패, 원문 확인이 7일 넘게 밀린 사고보고서를 추가.
- **코드북 버전**: DB 에 이미 `diff_versions()` 함수와 태깅별 `codebook_version` 기록이 있었다. 새로 만든 것 없이 화면으로 꺼내기만 했다 — 판별 태깅 건수를 보여 주고, 옛 판으로 붙은 것이 있으면 **무엇이 달라졌는지**까지 표시한다.

---

### 실측으로 발견한 것 (이번 라운드의 값어치는 여기 있다)

**① 태깅 잔량이 13,003건이 아니라 5,902건이었다 — 비용도 $29 가 아니라 약 $13**

라운드 17·18에서 "남은 13,003건 · 약 $29"라고 두 번 적었는데 **틀렸다.** `npm run tag --dry` 를 돌려 보니 5,902건이었다. 역할별로 세어 보고 이유를 알았다.

| 조항 역할 | 건수 | 코드 부여 대상인가 |
|---|---|---|
| REQUIREMENT (요건) | 7,472 | **예** (본문 15자 미만 1,080건 제외 → 6,392) |
| TEST_METHOD (시험방법) | 2,259 | 아니오 — 관계로 도달한다 |
| ANNEX (부록) | 1,384 | 아니오 |
| MARKING (표시) | 1,173 | 아니오 |
| DEFINITION (정의) | 817 | 아니오 |
| 그 밖 (OTHER·SCOPE·REFERENCE) | 396 | 아니오 |
| 합계 | 13,501 | |

**6,029건은 애초에 코드를 붙이지 않는 조항이다.** "13,003건이 밀렸다"는 말은 "밀린 것"과 "대상이 아닌 것"을 합쳐 센 것이었다. 담당자가 이 숫자를 보고 자동화 범위를 결정했으므로 정정해 둔다 — 실제로는 **$13 안팎**이다.

**② 성공 신호를 catch 가 삼키고 있었다**

운영 화면의 「코드 부여 실행」을 눌러도 아무 일도 일어나지 않았다. 원인은 코드였다.

```ts
try {
  ...
  back('완료');     // ← redirect() 는 "특별한 오류"를 던져서 흐름을 끊는다
} catch (e) {
  back('실패');     // ← 그 신호를 여기서 잡아 버렸다
}
```

`redirect()` 를 `try` 안에서 부르면 안 된다. 결과 문장만 만들어 두고 `try` 밖에서 보내도록 고쳤다. 라운드 18의 다른 버튼들은 우연히 이 형태를 피해 있어 드러나지 않았다.

**③ 버튼을 눌렀는데 화면이 침묵하던 자리**

「분석 실행」을 눌렀는데 아무 변화가 없었다. 버그인 줄 알고 CLI 로 같은 사건을 돌려 보니 `SCOPE_UNRESOLVED` — 품목에 맞는 기준을 못 찾아 **설계대로** 분석을 만들지 않은 것이었다. 동작은 옳았지만 화면이 그 사실을 말해 주지 않으니 담당자는 버튼이 고장 난 줄 안다. 어떤 경우든 무슨 일이 있었는지 문장으로 돌려보내게 고쳤다.

> "이 제품에 어떤 안전기준을 적용할지 정하지 못해 분석하지 않았습니다.
>  품목을 먼저 확정해야 합니다 — 전 기준을 뒤지면 다른 제품의 시험이 섞입니다."

**④ 번들 안에서 폴더 경로가 터졌다**

`/api/jobs/standards-sync` 가 `path argument must be of type string` 으로 실패했다. `import.meta.dirname` 이 웹 서버 번들에서 `undefined` 가 되는 문제로, `src/lib/env.ts` 가 이미 같은 이유로 조심하고 있던 자리다. 번들에서는 작업 디렉터리를 기준으로 삼도록 고치고, `next.config.ts` 에 `KC안전기준/` 을 배포판에 포함하도록 명시했다(안 하면 배포 환경에서 "폴더가 비었다"로 조용히 끝난다).

**⑤ `.env.example` 에 있던 옛 오타**

`$0.00222` 가 `/usr/bin/bash.00222` 로 치환돼 있었다. `dbdc513` 커밋부터 있던 것으로 이번 작업과 무관하지만, 같은 파일을 고치는 중이라 함께 바로잡았다.

### 검증

- `npm run typecheck` · `npm run build` 통과, 새 라우트 전부 생성
- 브라우저로 전 화면 확인 — 머리글 고정, 한글 명칭, 현황 숫자, 용어
- `/api/jobs/*` 인증: 토큰 없음 401 · 틀린 토큰 401 · 모르는 작업 404 · 정상 200
- `standards-sync` 실제 실행: 76건 확인, 신규 0 · 개정 0 (모두 적재돼 있으므로 정상)
- `run_job()` SQL 배관 확인 후 시험 기록 삭제(오탐 알림 방지)
- 「코드 부여 실행」 버튼: **1건 처리, 남은 5,901건** — 18초에 1건이다. 대량은 여전히 `npm run tag` 가 맞다
- 「분석 실행」 버튼: 결과 문장이 화면에 뜨는 것까지 확인

### 담당자가 해야 할 한 가지

**`JOBS_TOKEN` 을 Netlify 환경변수에 넣어야** 정기 실행이 배포 환경에서 동작한다. 값은 `.env.local` 에 이미 만들어 뒀다(48자). 같은 값을 Netlify 에 넣어 주세요.

- 넣지 않아도 화면과 알림은 정상 동작한다. 리콜 수집·기준 동기화만 401 로 막힌다.
- 막히면 「정기 작업 실패」 알림이 오므로 조용히 실패하지는 않는다.

**바뀐 파일**
- `src/lib/terms.ts`(신규): 화면 용어 한곳에 모음
- `src/lib/search/run.ts`(신규): 분석 실행부 — 명령줄·화면 공용
- `src/lib/recall/load.ts`·`src/lib/standards/tag-run.ts`(신규): 스크립트에서 뽑은 실행부. `scripts/load-recalls.ts`·`tag-clauses.ts`·`search.ts` 는 얇아졌다
- `src/lib/standards/title.ts`(신규)·`scripts/backfill-standard-title.ts`(신규)
- `src/app/accidents/`·`src/app/recalls/`(신규), `src/app/cases`·`analysis` → 넘겨주기
- `src/app/api/jobs/[job]/route.ts`(신규), `src/middleware.ts`: 그 경로만 예외
- `src/components/StatusBar.tsx`(신규), `Panel.tsx`: 용어 풀이·결과 띠 추가
- `supabase/migrations/024`(기준 명칭·현황 뷰)·`025`(정기 실행·알림 확대)
- `next.config.ts`(신규): 기준 폴더를 배포판에 포함
- `src/app/{page,standards,codebook,ops}/…`: 현황·용어·버전 관리

---

## 라운드 18 — 운영 화면 신설 · 문제 알림 · 프런트엔드 점검

2026-09-03 10:30 종료

### 왜 했는가 — 관리에 필요한 것이 다 흩어져 있었다

라운드 17에서 임베딩이 자동으로 돌게 만들었는데, 그 뒤에 문제가 하나 남았다. **자동으로 도는 것은 잘 돌 때 아무 표시가 안 난다.** 그리고 잘 돌지 않을 때도 표시가 안 난다. 담당자가 확인하려면 터미널에서 `npm run embed:status`를 쳐야 했는데, 이건 코드를 다루지 않는 사람에게는 없는 기능이나 마찬가지다.

같은 문제가 다른 것들에도 있었다.

| 관리에 필요한 것 | 그동안 어디 있었나 |
|---|---|
| 자동 배치가 도는지 | 터미널 명령 |
| 접속 ID/비밀번호 바꾸는 법 | 아무 데도 없음(구현이력 라운드 15·16 본문에만) |
| 오류가 났는지 | 아무 데도 없음 |
| 오류 연락 | 없음 |

그래서 **화면 하나에 모으고**, 오류는 **찾아가지 않아도 오게** 만들었다.

### 1) cron 실행 기록 자동 정리

라운드 17 비용 계산에서 "연 64 MB씩 쌓인다"고 적어 둔 항목을 처리했다. 30일이 지난 기록을 매일 새벽에 지운다(`cron-log-prune`, 매일 03:20 KST).

- 30일로 정한 이유는, 이 기록의 쓸모가 "최근에 배치가 제대로 돌았나" 하나뿐이기 때문이다. 한 달 전 실패를 들여다볼 일은 없다 — 그때 실패했다면 그 결과가 `embed_queue`에 남아 있고 그쪽이 진짜 장부다
- 30일이면 4만 3천 건·5.5 MB 안팎에서 더 늘지 않고 평형을 이룬다

### 2) 문제 알림 (텔레그램)

**DB에서 직접 보낸다.** 알림을 웹사이트 쪽에 두면 "아무도 화면을 안 보면 알림도 안 간다"가 되어 알림의 존재 이유가 사라진다. 문제는 DB 안의 배치에서 생기고, 그건 사람이 자고 있는 밤에도 돈다. 라운드 17에서 이미 `pg_net`(DB가 외부를 호출하는 기능)을 깔아 뒀으므로 보낼 길도 이미 있었다.

메시지 형식은 담당자가 이미 쓰던 n8n Error Trigger를 그대로 따랐다 (`docs/n8nErrorTrigger/Error Trigger.json` — 같은 텔레그램 대화, "무엇이 / 무슨 오류 / 언제" 세 줄). 이미 익숙한 형식을 바꿀 이유가 없다.

- 다만 **HTML 서식(parse_mode)은 쓰지 않았다.** 오류 메시지에 `<`나 `&`가 섞이면 텔레그램의 HTML 파싱이 깨져 **알림 자체가 유실된다.** 알림은 못생겨도 도착하는 쪽이 낫다

**알리는 것은 딱 세 가지다.**

| 무엇 | 왜 이것만 |
|---|---|
| 임베딩 보류 | 재시도로 안 풀린다 = 사람이 봐야 한다 |
| 배치 실행 실패 | 배치가 죽으면 아무것도 갱신되지 않는다. 가장 위험 |
| 임베딩 모델 혼재 | 오류 없이 검색 품질만 조용히 망가지는 유일한 경우 |

대기 건수나 개별 실패 1건은 알리지 않는다 — 스스로 회복되는 것들이라 화면으로 충분하다. **알림이 시끄러우면 아무도 안 읽는다**는 게 이 목록이 짧은 이유다. 같은 종류는 6시간(배치 실패는 1시간) 안에 다시 보내지 않는다.

### 3) 운영 화면 `/ops`

좌측 레일의 1~4는 **자료가 흐르는 순서**(적재 → 코드 → 사건 → 분석)다. 운영은 그 흐름 위에 있지 않고 "흐름이 지금 돌고 있는가"를 보는 자리라서, 번호를 주지 않고 선 하나를 사이에 두고 아래에 놓았다. 아무 데나 5를 붙이면 번호가 담고 있던 의미가 깨진다.

화면이 답하는 것은 넷이다.

1. **지금 상태** — 네 개의 신호등(자동 배치 / 배치 실행 / 임베딩 좌표계 / 알림 설정)
2. **확인이 필요한 것** — 보류된 건과 실패한 배치를 사유까지 펼쳐 보여 준다
3. **지금 하기** — 배치 즉시 실행 · 시험 알림 발송 · 보류 건 재시도(세 개 다 여러 번 눌러도 안전하다. 자동으로 일어날 일을 앞당길 뿐이다)
4. **접속 관리 · 알림 설정 · 명령어 모음**

**접속 관리(ID/PW)를 화면에 적어 둔 이유**가 따로 있다. 라운드 15·16에서 실제로 겪은 함정 두 가지가 구현이력 본문에만 적혀 있었는데, 그건 다음에 비밀번호를 바꿀 사람이 읽을 곳이 아니다. 그래서 바꾸는 자리 바로 옆에 옮겨 적었다.

- 「secret」으로 표시해 등록하면 **저장됐다는 응답만 오고 실제로는 저장되지 않는다** (그 결과 사이트가 열린 채로 있었다)
- **환경변수만 바꾸면 반영되지 않는다.** 값이 배포 시점에 고정되므로 빈 커밋으로 재배포를 한 번 일으켜야 한다

비밀번호 값 자체는 화면에 표시하지 않는다. 설정돼 있는지 여부만 본다.

### 4) 프런트엔드 전면 점검 — 고친 것 넷

**(가) 배포 화면이 DB 오류 원문을 그대로 노출하고 있었다.** 연결이 실패하면 postgres 오류 문자열을 `<pre>`로 화면에 뿌렸는데, 그 안에는 접속 호스트·사용자· 포트가 들어 있다. 이 사이트는 ID/PW 한 겹 뒤에 있을 뿐이라 화면에 뿌릴 값이 아니다. 개발 중에만 보여 주고 배포에서는 서버 기록으로만 남기게 고쳤다.

- 안내 문구도 틀려 있었다 — 배포 사이트에는 `.env.local`이 없는데 "그 파일을 채우세요"라고 안내하고 있었다. 없는 파일로 담당자를 보내는 셈이었다

**(나) 첫 화면의 임베딩 숫자가 사람을 오해하게 만들고 있었다.**

```
고치기 전 : 임베딩  498 / 13,501   ← 96%가 망가진 것처럼 보인다
고친 뒤   : 임베딩  498 / 498      ← 임베딩할 수 있는 것은 다 됐다
```

13,003건은 아직 태깅 전이라 **임베딩할 재료(검색용 텍스트)가 아예 없다.** 분모를 전체 조항으로 두면 "임베딩이 밀렸다"로 읽히는데 사실은 "태깅이 안 됐다"가 맞다. 둘은 해야 할 일이 완전히 다르다(하나는 무료·자동, 하나는 $29·수동). 분모를 "검색용 텍스트가 있는 조항"으로 바꿨다.

**(다) 예상 못 한 오류가 나면 아무 설명 없는 기본 화면이 떴다.** 담당자 입장에서 "사이트가 죽었다"와 "이 화면 하나가 죽었다"가 구분되지 않았다. 오류 화면과 없는 주소 화면을 만들어 다음 행동(다시 시도 / 운영 화면으로)을 주게 했다.

**(라) 휴대전화에서 좌측 레일이 본문을 아래로 밀어냈다.** 단계 4개가 세로로 쌓여 화면 위쪽 250px가량을 먹었다. 이번에 텔레그램 알림을 받고 **휴대전화로 운영 화면을 여는 경로가 실제로 생겼으므로** 그냥 둘 수 없어, 좁은 화면에서는 가로로 눕히고 넓은 화면에서는 그대로 세로 레일이 되게 했다.

### 실측으로 확인한 것

- 세 작업이 모두 등록·활성 상태다 — `embed-tick`(1분), `ops-watch`(5분), `cron-log-prune`(매일 03:20 KST)
- 운영 화면의 버튼 두 개를 실제로 눌러 동작을 확인했다
  - 「임베딩 배치 지금 실행」 → `배치를 실행했습니다 — 수거 0건 / 발송 0건`
  - 「시험 알림 보내기」 → `보내지 못했습니다. 텔레그램 봇 토큰이 금고에 없습니다` (아직 토큰을 안 넣었으므로 이게 정상 응답이다)
- `ops_watch()` 직접 실행 → `보류 0 / 배치실패 0 / 모델혼재 1 → 알림 0건 발송`
- **토큰이 없어도 배치가 죽지 않는다**를 확인했다. `ops_notify()`가 예외를 던지지 않고 `false`를 돌려준다 — 알림을 못 보내는 것이 본 작업을 멈출 이유는 아니다
- `npm run build` 통과, `/ops` 라우트 생성 확인

**021의 권한 수정이 실제로 효과가 있었다.** 이번에 만든 새 함수 3개 (`ops_notify`·`ops_watch`·`cron_log_prune`)의 권한을 확인해 보니, **아무 조치도 하지 않았는데** 처음부터 닫힌 채로 만들어졌다.

```
acl={postgres=X/postgres, service_role=X/postgres}   ← anon 없음
```

라운드 17에서 `alter default privileges ... from public`을 걸어 둔 결과다. 같은 실수가 반복되지 않는 것을 이번에 실물로 확인했다.

### 아직 안 된 것 — 담당자가 해야 할 한 가지

**텔레그램 봇 토큰**이 필요하다. 이것만 넣으면 알림이 살아난다.

1. `.env.local`에 `TELEGRAM_BOT_TOKEN=` 값을 넣는다(n8n에서 쓰는 봇과 같은 토큰이면 그대로 쓰면 된다)
2. `npm run ops:secret` 한 번 실행
3. `npm run ops:test` 또는 운영 화면의 「시험 알림 보내기」로 확인

대화 번호는 n8n 파일에 있던 값(`5185533472`)을 기본값으로 넣어 뒀다. 다른 곳으로 받고 싶으면 `.env.local`에 `TELEGRAM_CHAT_ID`를 넣으면 된다.

- 토큰은 저장소에 올라가지 않는다 — DB 금고(Supabase Vault)에 들어간다

**바뀐 파일**
- `supabase/migrations/022_cron_log_retention.sql`(신규): 30일 지난 cron 기록 정리 함수와 매일 1회 작업
- `supabase/migrations/023_ops_alert.sql`(신규): 알림 장부 `ops_alert`, 발송 함수 `ops_notify`, 감시 함수 `ops_watch`, 요약 뷰 `ops_status`, 5분 주기 작업
- `src/app/ops/page.tsx`·`actions.ts`(신규): 운영 화면과 버튼 3개
- `src/app/error.tsx`·`not-found.tsx`(신규): 오류·없는 주소 화면
- `src/app/layout.tsx`: 레일에 운영 항목 추가, 좁은 화면에서 가로 배치
- `src/components/Panel.tsx`: 배포 환경에서 DB 오류 원문을 감춤
- `src/app/page.tsx`: 임베딩 지표 분모 수정, 오류를 서버 기록에 남김
- `scripts/set-db-secret.ts`: 텔레그램 값도 넣도록 확장, 시험 발송 기능 추가
- `package.json`·`.env.example`: `ops:secret`·`ops:test` 추가, 텔레그램 변수 항목

---

## 라운드 17 — 임베딩 자동 생성 (DB가 1분마다 스스로 채운다)

2026-09-03 09:20 종료

### 왜 했는가 — "새 자료는 반쪽만 검색되는" 상태였다

지금까지 임베딩(문장을 숫자 벡터로 바꿔 뜻이 비슷한 것을 찾게 하는 것)은 사람이 `npm run embed` 명령을 쳐야만 생겼다. 그래서 기준을 새로 적재하거나 해외 리콜을 새로 받아 오면, 누군가 그 명령을 칠 때까지 새 자료는 **의미검색에 잡히지 않았다**.

이게 눈에 잘 안 띄는 이유가 있다. 우리 검색은 두 갈래를 합쳐 쓰는데(§5.4),
- 키워드 갈래(pgroonga)는 `search_text`만 있으면 **바로** 잡히고
- 의미 갈래(벡터)는 임베딩이 있어야 잡힌다

즉 명령을 안 쳐도 검색은 "되긴 된다". 다만 결과가 반쪽이다. 아무 오류도 안 나고 조용히 품질만 떨어지는 종류의 결함이라, 사람이 잊는 순간 알아채기 어렵다. 그래서 사람 손을 아예 빼기로 했다.

### 원래 계획에서 무엇이 바뀌었나 — Edge Function을 안 쓰기로 했다

착수 시점의 계획은 `pg_cron`(DB 안의 알람시계) → **Edge Function**(Supabase가 돌려 주는 작은 서버 프로그램) → OpenAI 순이었다. 그런데 Edge Function을 올리려면 Supabase CLI를 설치하고 로그인해야 하고, 이 세션에 연결된 Supabase 자동화 도구는 이 프로젝트가 속한 조직 계정이 아니라 접근이 안 된다. 즉 그 단계에서 담당자 손을 한 번 빌려야 진행되는 구조였다.

그래서 먼저 **실제로 되는지부터 재 봤다**. 우리가 이미 쓰고 있는 DB 접속 경로(`DATABASE_URL`)로 두 확장을 설치할 수 있는지 트랜잭션 안에서 시험하고 되돌려 봤더니, 둘 다 설치 가능했다.

| 확장 | 하는 일 | 설치 가능 여부 |
|---|---|---|
| `pg_cron` 1.6.4 | DB 안에서 정해진 주기로 명령 실행 | 가능 |
| `pg_net` 0.20.4 | DB가 직접 외부 API를 호출 | 가능 |

그러면 중간에 Edge Function을 둘 이유가 없다. DB가 알람을 울리고 DB가 직접 OpenAI를 부른다. 담당자가 해야 할 일이 다섯 가지에서 **0**이 되고, 고장 날 수 있는 지점도 하나 줄어든다.

- 대신 잃는 것도 적어 둔다 — 재시도·오류 로그가 TypeScript로 짤 때보다 거칠고, 로직이 SQL이라 읽기 어렵다
- 그럼에도 이 선택이 맞다고 본 이유는, 여기서 필요한 로직이 "임베딩이 빈 행을 골라 API에 보내고 결과를 써 넣는다"가 전부이기 때문이다. 정말 어려운 부분인 검색용 텍스트 조립(`SEARCH_TEXT_VARIANT`, §5.2.4)은 이미 적재 단계에서 끝나 DB 컬럼에 저장돼 있다. 즉 TypeScript 로직을 SQL로 베껴 적는 중복이 사실상 생기지 않는다

### 어떻게 도는가

`pg_net`은 비동기다 — 요청을 보내면 접수번호만 즉시 주고, 답은 잠시 뒤 따로 도착한다. 그래서 1분마다 도는 작업이 두 단계로 나뉜다.

```
매분 00초  ┌ 1단계 수거 : 지난번에 보낸 요청의 답이 왔는지 보고, 왔으면 벡터로 저장
           └ 2단계 발송 : 임베딩이 빈 행을 골라 OpenAI에 새로 보냄(한 번에 최대 40건)
```

**한 요청에 한 행만 보낸다**(여러 건을 묶지 않는다). OpenAI 임베딩은 글자 수 단위로 돈을 받기 때문에 묶어도 비용이 줄지 않는다. 반면 묶으면 (1) 응답 배열의 순서를 행에 되짚어 맞추는 코드가 필요하고 (2) 한 건이 실패하면 묶음 전체가 실패하며 (3) 실패 횟수를 행 단위로 셀 수 없다. 한 행 = 한 요청으로 두면 발송 장부(`embed_queue` 표) 자체가 그대로 행 단위 재시도 장부가 되어 이 셋이 전부 사라진다.

**모델 이름을 환경변수가 아니라 "이미 쌓인 벡터"에서 가져온다.** 이 배치는 `.env.local`을 읽을 수 없다. 모델명을 SQL에 적어 두면, 누군가 `OPENAI_EMBEDDING_MODEL`을 바꿨을 때 명령줄 배치와 자동 배치가 서로 다른 모델로 벡터를 만들어 한 표에 섞인다 — 같은 지도인 줄 알았는데 하나는 위경도, 하나는 도로명 주소인 상황이 되어 검색이 조용히 망가진다(§2.2). 이미 저장된 벡터의 모델을 그대로 따라가게 해서, 이 배치는 새 좌표계를 **만들 수 없게** 했다. 두 종류가 섞여 있으면 아예 실행을 거부한다.

### 실측으로 확인한 것

**1) 정상 경로 — 실제로 채워지는가**

조항 1건(#4)의 임베딩을 일부러 지우고(원본 벡터는 파일로 백업) 기다렸다.

| 시점 | 상태 |
|---|---|
| +40초 | 장부에 `sent`(발송됨) 기록 |
| +101초 | 임베딩 채워짐, 장부에서 삭제 |

- 모델 `text-embedding-3-large`, 1536차원 — 원본과 동일
- 원본 벡터와의 코사인 유사도 **0.999966** (1.0이면 완전히 같음)
- 즉 명령줄로 만든 것과 자동 배치가 만든 것이 사실상 같은 벡터다

최악의 지연은 약 100초다(발송이 다음 tick, 수거가 그다음 tick).

**2) 실패 경로 — 조용히 멈추지 않는가** (이쪽이 더 중요하다)

일부러 틀린 API 키로 요청을 하나 띄워서, 그 실패한 답이 왔을 때 어떻게 되는지 확인했다.

| 확인 항목 | 결과 |
|---|---|
| 401 오류가 장부에 남는가 | `failed(1회)` + 사유 `HTTP 401 Incorrect API key provided...` |
| 실패 직후 곧바로 또 보내지 않는가 | 발송 0건 (10분 대기 규칙 작동) |
| 5회를 채우면 그만 보내는가 | 발송 0건, `embed_status.parked=1`로 표시 |
| 대기가 지나고 횟수가 남으면 다시 보내는가 | 발송 1건, 장부 `sent(2회)` |
| 정리 후 장부가 비는가 | 잔여 0건 |

5회 제한을 둔 이유는 **보이게 하려는 것**이다. 글자가 너무 길어 모델이 받지 못하는 행처럼 재시도로 절대 풀리지 않는 행이 있으면, 제한이 없을 때 그 행은 영영 실패-재시도만 반복하면서 아무에게도 알려지지 않는다. 5회에서 세워 두고 `parked`로 표시하면 `npm run embed:status`에 숫자로 뜬다.

> 처음 이 문서를 쓸 때 "5회 제한을 둔 이유는 돈"이라고 적었는데, 뒤이어 비용을
> 실제로 계산해 보니 과장이었다. 실패한 요청(401·400)은 과금되지 않고, 10분
> 대기 규칙 때문에 최악이라도 하루 144회가 상한이라 조항 1건이 영원히 막혀 봐야
> 연 1달러 안팎이다. 돈이 아니라 "조용히 망가진 채로 남지 않게" 하는 장치로
> 이해하는 것이 맞다.

**3) 권한 사고 — 새 함수가 외부에 열려 있었다**

만들어 놓고 권한을 조회해 보니, 새로 만든 함수 3개를 `anon`(로그인 안 한 외부 키)이 전부 실행할 수 있었다. 020에서 분명히 이렇게 막았는데도 그랬다.

```sql
revoke execute on function public.embed_tick() from anon, authenticated;
```

권한 목록을 직접 들여다보고서야 원인을 알았다.

```
embed_tick    acl={=X/postgres, postgres=X/postgres, service_role=X/postgres}
get_codes     acl={          postgres=X/postgres, service_role=X/postgres}
                ↑ 맨 앞의 =X 가 범인
```

수령자 이름이 비어 있는 `=X` 항목은 **PUBLIC**(누구나)을 뜻하고, 이건 PostgreSQL이 새 함수마다 기본으로 주는 권한이다. `anon`은 PUBLIC을 통해 실행 권한을 물려받으므로, `anon`이라는 이름만 지워 봐야 통로는 그대로 열려 있다. `from public`으로 지워야 한다.

더 큰 문제는 이게 이번 한 번의 실수가 아니라는 점이다. 라운드 2에서 "앞으로 만드는 함수도 기본으로 열리지 않게 한다"며 걸어 둔 방어선(009의 `alter default privileges`)도 `anon`·`authenticated`만 막고 PUBLIC은 놓치고 있었다. **009 이후에 만드는 함수는 전부 이 구멍에 빠지는 상태였고, 020이 첫 사례다.** (007에서 만든 `clause_hybrid_search`는 우연히 `from public`으로 지워서 무사했다 — 두 파일이 서로 다른 방식을 쓰고 있었고 그 차이가 여태 드러나지 않았을 뿐이다.)

021에서 세 함수를 `from public`으로 다시 닫고, 방어선도 PUBLIC까지 막도록 보강했다. 최종 확인:

```
embed_collect / embed_dispatch / embed_tick
  anon=false  authenticated=false  service_role=true
  acl={postgres=X/postgres, service_role=X/postgres}   ← 기존 함수와 동일
```

- CLAUDE.md §8이 "새 함수를 만들면 실행 권한이 자동으로 열린다"고 경고해 뒀는데도 또 걸렸다. 이번에 배운 것은 **`revoke ... from anon, authenticated` 로는 부족하고 `from public`이어야 한다**는 것이다

**4) 덤으로 확인된 것 — 조항 13,501건 중 임베딩은 498건뿐**

상태 조회를 붙이고 나서야 드러난 숫자다.

| 표 | 전체 | 임베딩 완료 | 대기 |
|---|---|---|---|
| `clause` (KC기준 조항) | 13,501 | **498** | 0 |
| `case_event` (사고·리콜) | 2,264 | 2,264 | 0 |

"대기 0"인데 완료가 498건뿐인 이유는, 나머지 13,003건이 **아직 AI 코드화 (태깅)를 안 거쳐서 `search_text`가 비어 있기** 때문이다. 임베딩할 재료가 없으니 자동 배치의 대상도 아니다.

- 즉 이 자동 배치를 켰다고 해서 나머지 13,003건이 저절로 검색되지는 않는다
- 그 13,003건을 검색 대상으로 만들려면 먼저 **태깅**(`npm run tag`)을 돌려야 하고, 그건 LLM 호출 비용이 드는 별개의 결정이다
- 태깅을 돌리면 그때부터는 임베딩이 자동으로 따라붙는다 — 이 라운드의 성과가 거기서 나온다

### 비용 — 실측

담당자 질문에 답하기 위해 추정 대신 실제 숫자로 계산했다. 기준이 되는 두 값은 실측이다.

- **글자당 0.494 토큰** — 테스트로 나간 실제 요청이 504자를 249토큰으로 셌다 (한글이 섞인 우리 문체 기준. 영어보다 토큰을 많이 먹는다)
- **`text-embedding-3-large` 100만 토큰당 $0.13** — OpenAI 공식 단가. 다만 OpenAI 문서 페이지끼리 $0.13과 $0.065로 값이 엇갈린다는 보고가 있어, 비싼 쪽을 잡았다. 실제로는 절반일 수도 있다

| 무엇을 | 글자 수 | 토큰 | 비용 |
|---|---|---|---|
| 지금 재료가 있는 것 전량 재생성 (조항 498 + 사건 2,264) | 136만 자 | 67만 | **$0.09** |
| 남은 조항 13,003건 (태깅 후 임베딩) | 280만 자 (추정) | 138만 | **$0.18** |
| **합계 — 자료 전부를 맨바닥에서 다시 만들 때** | | 205만 | **약 $0.27 (400원 안팎)** |

- 즉 **임베딩은 전량 다시 만들어도 커피 한 잔이 안 된다.** 자동 배치를 켜 두는 상시 비용은 이보다 훨씬 작다 — 새로 들어오는 자료에만 비례하기 때문이다. 해외 리콜이 월 100건씩 들어온다고 해도 월 2.6만 토큰, **월 5원 수준**이다
- Supabase 쪽에 추가 요금은 없다. 유휴 상태의 1분 tick은 확인 질의 한 번으로 끝나고(서버 측 수 밀리초), 늘어나는 건 실행 기록뿐이다 — 1건당 128바이트 × 하루 1,440건 = **64 MB/년**

**진짜 돈이 드는 것은 임베딩이 아니라 태깅이다.** 라운드 9 실측 기준 조항 1건당 약 $0.0022이므로, 남은 13,003건을 태깅하면 **약 $29**다. 임베딩(약 $0.18)의 160배다. `scripts/embed-clauses.ts` 머리말이 "임베딩은 세 산출물 중 가장 싸다"고 적어 둔 것이 숫자로 확인된 셈이다.

- 그래서 "13,003건을 살릴 것인가"는 임베딩 결정이 아니라 **태깅 결정**이다. 임베딩 쪽은 얼마가 되든 반올림 오차에 가깝다

### 담당자가 알아야 할 것

- **평소에 할 일은 없다.** 자료를 적재하면 1분 안에 임베딩이 붙는다
- 상태가 궁금하면 `npm run embed:status` — 대기·발송중·실패·보류 건수와 최근 cron 실행 5회가 나온다
- OpenAI 키를 바꾸면 `npm run embed:secret`을 한 번 돌려야 한다(자동 배치는 `.env.local`이 아니라 DB 금고(Vault)에 든 키를 쓴다). 키 자체는 저장소에 올라가지 않는다
- `parked`(보류) 건수가 0이 아니면 사람이 봐야 한다 — 재시도로 안 풀리는 건이다
- 대량 최초 생성은 여전히 `npm run embed`가 훨씬 빠르다. 이 배치는 증분용이다

### 주의로 남기는 것

- 021이 public 스키마의 새 함수 기본 권한을 닫았다. 나중에 외부 이용 시스템(Recall Hub 등)에 조회 함수를 열어 줄 때는 그 함수에만 명시적으로 `grant`해야 한다
- pg_cron 실행 기록은 `cron.job_run_details`에 쌓인다. 1분 주기라 하루 1,440건이다. 지금 규모에서는 문제없지만, 언젠가 정리가 필요할 수 있다

**바뀐 파일**
- `supabase/migrations/020_embed_background.sql`(신규): `pg_net`·`pg_cron` 설치, 발송 장부 `embed_queue` 표, 수거·발송·진입점 함수 3개, 상태 조회 뷰 `embed_status`, 1분 주기 등록
- `supabase/migrations/021_embed_function_grants.sql`(신규): 020의 함수 실행 권한을 PUBLIC으로부터 회수, 009의 기본 권한 방어선을 PUBLIC까지 보강
- `scripts/set-db-secret.ts`(신규): `.env.local`의 OpenAI 키를 DB 금고에 넣는다(`npm run embed:secret`)
- `scripts/embed-status.ts`(신규): 자동 배치 상태 확인(`npm run embed:status`)
- `package.json`: 위 두 명령 추가

---

## 라운드 16 — 로그인 미들웨어 두 가지 버그 수정 (환경변수 미저장, 헤더 한글)

라운드 15를 배포한 뒤 실제로 접속해서 확인해 보니 두 가지가 차례로 걸렸다. "빌드만 통과하면 끝"이 아니라는 걸 다시 확인한 라운드다.

### 버그 1 — 비밀번호 환경변수가 저장되지 않았다

배포 후 curl로 직접 테스트해 보니(정상 접속·틀린 비밀번호·틀린 사용자이름 세 가지 경우 모두) **전부 200**이 나왔다 — 로그인 화면 자체가 안 뜨는 상태였다. Netlify에 등록해 둔 환경변수 전체 목록을 다시 조회해 보니 `SITE_AUTH_USERNAME`은 있는데 `SITE_AUTH_PASSWORD`는 아예 없었다 — MCP 도구가 "저장했다"는 응답을 줬는데도 실제로는 저장되지 않은 것이다("secret"으로 표시해서 등록하려 한 호출만 이렇게 조용히 실패했다 — 원인까지는 못 밝혔다). "secret" 표시 없이 다시 등록하니 정상적으로 값이 들어갔다.

### 버그 2 — 헤더에 한글을 넣어서 브라우저가 아예 거부

환경변수를 고치고 재배포했더니 이번엔 담당자가 "Value is not a valid ByteString"이라는 브라우저 오류를 보고했다. 401 응답에 붙인 `WWW-Authenticate: Basic realm="KC 제품위해 분석"` 헤더에 한글을 넣은 게 원인이었다 — HTTP 헤더 값은 아스키(정확히는 Latin-1) 문자만 허용되는 규격이라, 한글처럼 그 범위를 벗어나는 문자가 들어가면 브라우저가 헤더 자체를 못 만들고 그 자리에서 멈춘다. `realm`을 영문("KC Hazard Analysis")으로 바꿔서 해결했다. 화면에 보이는 텍스트가 아니라 브라우저가 내부적으로 해석하는 HTTP 헤더라는 걸 놓쳐서 생긴 실수였다.

### 왜 로컬 빌드로는 못 잡았는가

두 문제 다 `npm run build`(정적 분석·번들링)로는 못 잡는 종류다. 환경변수 저장 실패는 배포 인프라 쪽 문제라 로컬에 아예 재현할 방법이 없었고, ByteString 오류는 실제로 401 응답이 브라우저에 도달해서 헤더를 파싱하는 순간에만 터지는 런타임 오류라 빌드 타임에는 조용히 통과한다. 이번에도(라운드 13·14와 마찬가지로) "로컬에서 되면 배포도 된다"는 가정이 또 한 번 깨졌다.

### 확인한 것

- 재배포는 커밋을 새로 만들어야만 트리거된다는 것도 이번에 확인했다 — 환경변수만 바꾸는 걸로는 이미 떠 있는 엣지 함수가 자동으로 갱신되지 않는다("Values are frozen at deploy time"). 그래서 `--allow-empty` 빈 커밋으로 재배포를 한 번 강제했다
- 배포된 사이트에 실제로 `curl`로 인증 없이/틀린 값으로/올바른 값으로 접속해 401·200이 정확히 갈리는지 확인하는 절차를 남겨 둔다 — 다음에 이 미들웨어를 건드릴 때도 같은 방식으로 확인할 것

**바뀐 파일**
- `src/middleware.ts`: `WWW-Authenticate` 헤더의 `realm` 값을 한글에서 영문으로 교체
- Netlify 배포 환경변수: `SITE_AUTH_PASSWORD`를 "secret" 아닌 일반 변수로 재등록(값은 라운드 15와 동일)

---
