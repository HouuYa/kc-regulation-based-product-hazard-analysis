# 04-1. 결과에서 원인으로 가는 다리 — 구현 계획

`04_2차 보완을 위한 분석.md` §7.1 하나만 떼어 상세 설계로 옮긴 문서다. 작성일 2026-09-05. 수치는 모두 그날 DB에서 직접 센 것이다.

---

## 0. 이 문서가 푸는 문제

담당자는 사고보고서를 읽고 **원인을 짐작한 뒤** 그 원인을 확인할 시험을 고른다. 시스템은 **피해 결과**로만 조항을 찾는다. 그래서 둘이 갈라진다.

사건 5553(전기머리인두 화재)이 그 예다.

```
사람   화재가 났다 → "습기가 들어가 누전됐을 수 있다" → 내습성(15)·누설전류(16) 시험
시스템 화재가 났다 → 화재 코드가 붙은 조항        → 온도상승(11)·이상운전(19)
```

시스템이 틀린 것이 아니다. **결과에서 원인으로 건너가는 다리가 없을 뿐이다.** 이 문서는 그 다리를 놓는다.

---

## 1. 왜 원인으로 검색하지 못하나

원인(HF) 코드로 검색하면 될 일인데 그럴 수가 없다.

| 자료 | 건수 | 원인 미상(HF.UNKNOWN) |
|---|---|---|
| 사고보고서 | 70건 | **54건 (77%)** |
| 해외 리콜 | 2,299건 | 227건 (10%) |

사고조사보고서가 원인을 특정하지 않는다. *"제품 시험 결과 모두 안전기준에 적합"*, *"특이사항을 식별하지 못함"* 같은 서술이 실제로 들어 있다. AI가 못 뽑은 것이 아니라 문서에 없다.

**그런데 리콜에는 원인이 적혀 있다.** 2,299건 중 2,072건에 원인 코드가 붙어 있다. 여기서 "어떤 피해에 어떤 원인이 따라오는가"를 세면 사고보고서에 쓸 수 있다.

---

## 2. 다리의 정체 — 표 하나다

지금 코드북에는 원인(HF) 목록과 피해(DT) 목록이 **따로** 있고 둘을 잇는 줄이 없다. 그 줄을 표로 만드는 것이 전부다.

```
피해유형        원인 후보          근거 건수
화재      →    과열               243건
화재      →    절연 불량           160건
화재      →    보호회로 부재         15건
감전      →    절연 불량           428건
감전      →    방수 결함            44건
```

손으로 적지 않는다. 리콜 자료를 세면 나온다. 위 숫자가 실제로 센 결과다.

---

## 3. 순서를 어떻게 매기나 — 리프트

빈도순으로 줄 세우면 안 된다. 실측이 그 이유를 보여 준다.

「설계결함」은 화재와 116건 함께 나온다. 적지 않은 숫자다. 그런데 이 코드는 **어느 사고에나** 붙는다(전체 629건). 화재 사고에서 설계결함이 보인다는 사실은 아무것도 알려 주지 않는다.

「과열」은 전체 309건 중 243건이 화재와 함께 나온다. 이건 화재의 표지다.

리프트는 그 차이를 재는 값이다.

```
리프트 = 이 피해일 때 그 원인이 나오는 비율 ÷ 평소 그 원인이 나오는 비율

1 근처   그 피해와 상관없이 원래 흔한 원인이다
크다     그 피해에 유난히 몰린다 = 원인 후보로 값어치가 있다
```

화재를 리프트순으로 세운 결과다(근거 10건 이상만).

| 원인 후보 | 축 | 근거 | 신뢰도 | 리프트 |
|---|---|---|---|---|
| 비화재 열적사고 | H | 32건 | 7.1% | 5.23 |
| 과열 설계결함 | H | 243건 | 53.6% | 4.11 |
| 보호회로 부재 | H | 15건 | 3.3% | 3.92 |
| 전기적 위해 | H | 106건 | 23.4% | 3.30 |
| 법적 인증 위반 | M | 84건 | 18.5% | 1.84 |
| 절연 불량 | H | 160건 | 35.3% | 1.62 |
| 안전정보 부족 | S | 74건 | 16.3% | 1.57 |
| 국내표준 미흡 | S | 40건 | 8.8% | 1.02 |
| 설계결함 | M | 116건 | 25.6% | **0.96** |

빈도 3위였던 설계결함이 리프트 0.96으로 저절로 바닥에 깔렸다. 의도한 대로다.

**근거 건수 하한을 둔다.** 없으면 1건짜리 우연이 리프트 최상위를 차지한다. 기본 5건.

### 그런데 리프트로 줄을 세우면 안 된다 (구현 중 실측으로 고침)

위 표대로 리프트순 상위 5를 뽑아 돌렸더니 **사건 5553을 여전히 못 찾았다.** 뽑힌 다섯이 비화재 열적사고(31건)·과열(240건)·보호회로 부재(14건)·배터리(77건)·하드웨어(9건)였고, 정작 **절연 불량이 7위로 밀렸다.**

리프트는 드문 코드일수록 커진다. 근거 14건짜리가 158건짜리를 밀어낸다.

그래서 역할을 나눴다.

```
리프트    문지기.  "이 피해와 무관하게 원래 흔한 원인"을 쳐낸다 (설계결함 0.96 탈락)
신뢰도    줄세우기. 이 피해에서 실제로 자주 나온 순서로 보여 준다
```

신뢰도순으로 바꾸니 화재의 후보가 과열(58.7%) → **절연 불량(38.6%)** → 전기적 위해(24.7%) 순이 됐다. 담당자가 화재를 보고 떠올리는 순서에 가깝다. 이 상태에서 사건 5553이 처음으로 적중했다(다른 모든 구성 0건, 원인 다리만 2건).

---

## 4. 불량과 불법 — 이 구분이 이 설계의 핵심이다

리프트만으로는 부족하다. 위 표 5위 「법적 인증 위반」은 통계적으로 유의미하게 올라오지만 **시험으로 확인할 수 있는 원인이 아니다.** 불법 제조 여부를 판정하는 KC 시험 조항은 없다.

`docs/제품안전법제도/★★★★업무이해 20230405.xlsx` 「불법제품 단속」 시트가 이 구분을 한 줄로 적어 놓았다.

> *"불법은 기준이 있어 판단이 가능하나, 리콜은 안전성조사가 선행되어야 함"*

| | 불량 | 불법 |
|---|---|---|
| 무엇인가 | 안전기준의 기능·성능에 불합격 | 법령이 정한 의무를 지키지 않음 |
| 예 | 과열, 절연 불량, 방수 결함 | 인증 미획득, 표시사항 위반, 주요 부품 변경 후 재인증 누락 |
| 어떻게 확인하나 | **시험을 해 봐야 안다** | **법령을 보면 판정된다** |
| 이 체계의 산출물 | 시험항목 | 인증·표시 확인 항목 |

### 코드북에 「확인 경로」 칸을 더한다

거르는 규칙을 검색 로직에 흩어 놓지 않고 코드북에 둔다. 원인 코드마다 "이것은 무엇으로 확인하는가"를 한 칸에 적는다.

| 확인 경로 | 뜻 | 해당 코드(예) | 내보낼 곳 |
|---|---|---|---|
| `TEST` 시험 | 불량. 안전기준 시험으로 확인 | 과열, 절연불량, 방수결함, 설계결함 | **시험항목** |
| `LEGAL` 법령 | 불법. 인증·표시·신고 위반 | 법적 인증 위반, 안전정보 부족 | 인증·표시 확인항목 |
| `GAP` 기준공백 | 기준 자체가 없거나 미흡 | 국내표준 미흡, 국제표준 미흡 | 사각지대(3번째 산출물) |
| `OTHER` 기타 | 관리·공정·사람 요인 | 품질관리 결함, 어린이 오용 | 시험 도출 대상 아님 |

**불법을 버리지 않는다.** 화재에서 「법적 인증 위반」이 리프트 1.84로 올라온 것은 잡음이 아니라 *"이 제품군은 시험보다 인증·표시를 먼저 확인해야 한다"* 는 신호다. 다만 시험항목 목록에 섞이면 안 되므로 다른 출구로 내보낸다.

`OTHER` 를 관리·사람으로 더 쪼개지 않은 이유는, 코드북에 이미 M-SHELL 대분류가 있어 필요할 때 그것으로 나눌 수 있기 때문이다. 칸을 늘려 같은 정보를 두 번 두지 않는다.

### 표시사항은 안전기준 안에 있어도 불법이다

세부 표시사항이 KC안전기준 문서에 실려 있더라도, 그것을 지키지 않은 것은 법령이 정한 표시 의무 위반이므로 불법으로 본다. 시험을 의뢰할 항목이 아니다.

실제 사례가 있다. 열냉각시트 사고보고서(`(25)6`)의 결론이 이렇다.

```
2.2 안전기준 표시요구사항 확인
    「어린이제품 공통안전기준」 5. 표시사항
    → 제조연월·공통 주의문구 미표시 → 경결함
```

「어린이제품 공통안전기준」이라는 **안전기준 문서 안의 조항**이지만 성격은 불법이다.

다행히 이 구분은 DB에 이미 있다. `clause.clause_role` 이 `REQUIREMENT`(성능요건)와 `MARKING`(표시·주의사항)을 나눠 두고 있다(마이그레이션 011). 시험항목을 제시할 때 `REQUIREMENT` 만 쓰면 표시사항이 섞이지 않는다.

---

## 5. 다리가 실제로 작동하는지 미리 확인했다

설계를 쓰기 전에 5553 사건으로 확인했다. 담당자가 지목했으나 시스템이 못 찾은 조항의 원인 코드를 조회했다.

```
절 15.1   (내습성)   → HF.E.MOIST(습기), HF.H.ELEC.INS(절연불량), HF.M.DES
절 15.1.1            → HF.H.ELEC.INS, HF.H.PHY.WATER(방수)
절 16.1~3 (누설전류)  → HF.H.ELEC.INS
```

화재에서 절연불량을 원인 후보로 추정하면 **절 15·16이 코드 매칭으로 걸린다.** 담당자의 "습기가 들어갔을 수 있다"는 가설에 해당하는 `HF.E.MOIST` 코드도 실재한다.

다리를 놓을 자리가 맞다는 뜻이다.

---

## 6. 스키마

두 표를 코드북 스키마에 만든다. 코드끼리의 관계이므로 코드 정의 옆에 두는 것이 맞다.

### `codebook.hf_route` — 원인 코드의 확인 경로

| 칸 | 뜻 |
|---|---|
| `code` | 원인 코드 (PK) |
| `route` | `TEST` / `LEGAL` / `GAP` / `OTHER` |
| `note` | 왜 이 경로인지 |
| `updated_at` | 고친 시각 |

코드 문자열을 키로 쓰고 코드북 판번호를 걸지 않았다. 확인 경로는 판이 바뀌어도 잘 변하지 않는 성질이고, 판마다 64줄을 복제하면 고칠 때 어느 판을 고쳐야 하는지가 새 문제가 된다.

### `codebook.cause_bridge` — 결과에서 원인으로 가는 다리

| 칸 | 뜻 |
|---|---|
| `dt_code` | 피해유형 (PK) |
| `hf_code` | 원인 후보 (PK) |
| `source` | 어느 자료에서 셌나 (PK) — `RECALL` |
| `support` | 함께 나온 사건 수 |
| `confidence` | 이 피해일 때 이 원인이 나온 비율 |
| `lift` | 특이성 |
| `sample_size` | 이 피해를 가진 사건 수 (분모) |
| `computed_at` | 계산 시각 |

**뷰가 아니라 표로 굳힌다.** 리콜은 계속 쌓이므로 같은 질의라도 시점마다 답이 달라진다. 나중에 "그때 왜 이 후보를 냈나"를 되짚으려면 그 시점의 값이 남아 있어야 한다. `computed_at` 과 `sample_size` 가 그 근거다.

**확인 경로로 걸러서 저장하지 않는다.** `LEGAL`·`GAP` 도 그대로 담는다. 걸러내면 "불법 신호"라는 다른 산출물을 만들 수 없다. 거르기는 쓸 때 한다.

**품목별로 나누지 않는다.** 리콜의 GPC가 0건이라(라운드 43) 나눌 키가 없다. 전역 사전으로 시작하고, GPC가 채워지면 그때 나눈다.

---

## 7. 담당자가 보는 흐름

원인이 미상인 사건에서만 작동한다. 사고보고서의 77%가 여기 해당한다.

```
사건 분석 실행
  → 원인 미상 감지
  → 원인 후보 제시   "화재의 원인 후보 — 과열 54%(243건) · 절연불량 35%(160건) …"
  → 담당자가 고른다
  → 고른 원인으로 다시 검색 → 시험항목 제시
  → match_run 에 "다리 사용 · 담당자가 고른 것: 절연불량" 을 남긴다
```

자동으로 반영하지 않고 담당자가 고르게 하는 이유가 있다. 5553에서 "머리인두에 습기가 들어갔을 수 있다"고 판단한 것은 **품목을 아는 사람**이다. 통계는 후보를 좁혀 주는 데까지가 몫이다.

### 지키는 선 두 개

- 추정한 원인은 `case_tag`(확정 태그)에 **쓰지 않는다.** 검색 입력에만 산다. 통계적 추측이 조사 결과로 굳으면 안 된다.
- 추정 근거로 걸린 조항은 **증거수준 A를 받지 못한다.** 확정 태그 일치와 같은 자격을 줄 수 없다.

---

## 8. 구현 순서와 판정 기준

### 1단계 — 표를 만들고 효과를 잰다

| 할 일 | 결과물 |
|---|---|
| 스키마 | `codebook/sql/003_cause_bridge.sql` |
| 확인 경로 초기값 | 코드 규칙으로 자동 채움 (`003` 안에서) |
| 표 채우기 | `scripts/build-cause-bridge.ts` → `npm run cause:build` |
| 추정 함수 | `src/lib/search/cause-bridge.ts` |
| 채점 | `scripts/eval.ts` 에 「+ 원인 다리」 구성 추가 |

**판정 기준.** 아래 세 숫자가 오르지 않으면 다리는 틀린 것이고, 그렇게 적는다.

오탐률도 함께 본다. 재현율이 올라도 오탐이 급증하면 담당자가 목록을 신뢰하지 않는다.

### 실측 결과 (2026-09-05 · 정답셋 47건)

```
구성                   재현율    오탐률   상위5포함률
①+②+③ 하이브리드        16.2%    89.5%      14.5%     ← 기준선
+ 원인 다리(대체)         13.1%    90.5%      15.0%
+ 원인 다리(병합)         14.6%    89.3%      16.5%
+ 리랭킹                16.2%    89.5%      29.9%
```

**자동으로 켜면 안 된다.** 어느 형태든 평균 재현율이 떨어졌다. 판정 기준대로 이 형태는 채택하지 않는다.

### 그런데 건별로 보면 이야기가 다르다

```
47건 중 23건(절반)은 하이브리드가 한 건도 못 찾는다
  그 23건 중 대체 방식이 5건을 살렸다   5553 · 5556 · 5557 · 5561 · 5577
  병합 방식은 3건을 살렸다              5557 · 5561 · 5577

이미 찾고 있던 24건 중 병합이 나쁘게 만든 것 8건
```

평균이 떨어진 이유가 여기 있다. 다리는 **이미 잘 찾고 있는 사건의 순위를 흔들면서** 값을 깎았다. 사건 5560은 19건에서 14건으로 줄었다 — 결과 기반으로 잘 찾던 것을 원인 기반 후보가 밀어냈다.

특히 5553은 **다른 어느 구성도 한 건도 못 찾는데 대체 방식만 2건을 찾았다.** 담당자가 지목한 내습성·누설전류가 그것이다.

### 그래서 결론이 바뀌었다

다리는 **기본 순위를 고치는 장치가 아니라, 아무것도 못 주던 자리에 줄 것을 만드는 장치다.**

| | 채택 여부 |
|---|---|
| 기본 검색에 자동 반영 | **하지 않는다.** 재현율이 떨어진다 |
| 담당자가 원인 후보를 고르면 그것으로 다시 검색 | **한다.** 0건이던 23건 중 5건에 답이 생긴다 |

§7이 "자동으로 반영하지 않고 담당자가 고르게 한다"고 적은 것이 실측으로 확인됐다. 그때는 "품목을 아는 사람만 후보를 걸러낼 수 있다"는 이유였는데, 숫자로 보니 이유가 하나 더 있었다 — 자동으로 섞으면 잘 되던 것까지 망가진다.

### 함께 드러난 것

**정답셋 47건 중 23건(49%)에서 시스템이 한 건도 맞히지 못한다.** 평균 재현율 16.2%라는 숫자보다 이쪽이 더 중요한 사실이다. 절반은 성능이 낮은 것이 아니라 아예 답을 못 내고 있다. 다리는 그중 5건을 건드렸을 뿐이고, 나머지 18건은 다른 원인을 봐야 한다.

### 2단계 — 화면

1단계 숫자를 보고 착수 여부를 정하기로 했었다. 숫자가 말한 것은 "만들지 마라"가 아니라 **"자동으로 켜지 말고 담당자 손에 쥐여 줘라"** 였다. 그래서 화면은 만들되, 기본 검색 결과를 건드리지 않는 형태로 만든다.

| 화면 | 하는 일 |
|---|---|
| `/codebook` | 「결과 → 원인 다리」 목차 추가. 표 전체를 근거 건수·신뢰도·리프트와 함께 조회 |
| 사건 분석 화면 | 원인 미상일 때 후보를 **따로** 제시. 담당자가 고르면 그 원인으로 다시 검색해 **두 번째 목록**으로 보여 준다 |

기본 목록을 덮어쓰지 않는 것이 핵심이다. 덮어쓰면 잘 찾던 8건이 나빠진다.

담당자에게는 두 목록의 성격을 그대로 적어 준다.

```
결과로 찾은 조항    이 피해유형을 다루는 조항
원인으로 찾은 조항   추정한 원인을 확인할 시험 · 무엇을 근거로 추정했는지 함께
```

---

## 9. 이번에 하지 않는 것

| 안 하는 것 | 왜 |
|---|---|
| 조항 HF 태깅 보강 | 조항 6,400건 중 원인 코드가 있는 것이 3,001건(47%)뿐이라 절반은 다리로도 못 건넌다. 다만 이번 채점에서 "못 찾은 조항 중 몇 %가 미태깅 탓인지"를 세어 다음 판단의 근거로 남긴다 |
| 확인 경로 검수 화면 | 규칙으로 채운 초기값이 얼마나 맞는지 먼저 보고 정한다. 틀린 줄이 몇 개인지 모르는 채로 화면부터 만들지 않는다 |
| 품목별 사전 | 리콜에 GPC가 없다 |
| 사고보고서로 사전 만들기 | 77%가 원인 미상이라 셀 것이 없다 |

---

## 10. 이 작업에서 나온 다음 판단 자료

- `docs/제품안전법제도/안전기준목록조사 취합(전기 생활 어린이).xlsx` 에 품목→기준 대응이 **558행**(전기 446 · 생활 75 · 어린이 37) 있다. §4.2의 "필요한 기준 8종이 없다"와 §5의 품목 확정(70건 중 58건, 83%)을 직접 푸는 자료다.
- 현재 안전기준 **76종 적재, 조항 13,501건**. 위 목록의 나머지는 향후 적재 대상이다.
